---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-08-17T06:46:53.355Z
---

<current_state>
Between phases. Session was triage + two quick-fix releases (v3.5.1, v3.5.2) + upstream librespot PR.
v3.5.2 is the current release with patched binary (spoton v3.0.2).
STATE.md is stale — says Phase 58 "planning" but Phase 58 has been complete since 2026-07-23.
</current_state>

<completed_work>

This session (2026-08-17):
- Closed #147 (davison confirmed v3.5.0 fix) and #148 (duplicate)
- Labeled + commented #149 (health check idle guard) and #150 (audio key timeout)
- Quick task 260817-ana: idle guard on Signal 1 + classifyAudioKeyError 'timeout' (commit 4ba5247)
- Released v3.5.1 (Perl-only: idle guard + classifier, binary reuse)
- Upstream librespot PR #1743: audio key retry (2× on timeout) + clean track failure (ErrorKind-based)
- Fable 5 review: 2 Critical + 4 Warnings all fixed (ErrorKind import, pending map leak, fmt, CHANGELOG)
- Fork pin to stiefenm/librespot branch fix/audio-key-retry-v2 (based on commit 9c7d756)
- discovery-patched also pinned to fork (type mismatch fix)
- Released v3.5.2 (binary rebuild, spoton v3.0.2)
- Local deploy + regression test on dev LMS (Connect + metadata OK, 429 window transient)
</completed_work>

<remaining_work>

No active phase work. Next actions:
1. Monitor librespot#1743 for upstream merge → revert fork pin
2. Monitor #149/#150 for Rouzax confirmation
3. Pick next phase: 59-02 (Connect review findings), 62 (Browse + Queue), or librespot dev HEAD upgrade
</remaining_work>

<decisions_made>

- v3.5.1 = Perl-only patch (idle guard + classifier), v3.5.2 = binary rebuild (fork pin)
- Fork based on 9c7d756 (our pinned commit), NOT dev HEAD — dev HEAD has breaking API changes (infallible to_base62/to_uri) causing 19 compile errors
- librespot-discovery-patched must share same fork source as librespot-core (type mismatch otherwise)
- Librespot dev HEAD API upgrade deferred to own phase (memory: project_librespot_dev_upgrade.md)
</decisions_made>

<blockers>
None blocking. Two waiting-user items (#149, #150).
</blockers>

## Required Reading (in order)
1. Memory `project_librespot_fork_pin.md` — check PR #1743 status at every resume
2. Memory `project_librespot_dev_upgrade.md` — Rust API upgrade needed before reverting to upstream

## Infrastructure State
- LMS running on dev machine (lyrionmusicserver.service), binary deployed at /usr/share/squeezeboxserver/Plugins/SpotOn/Bin/x86_64-linux/spoton (v3.0.2)
- squeezelite running in normal user daemon mode (not test_sink)
- Forum monitor: all runs successful

<context>
Rouzax delivered two excellent bug reports (#149, #150) documenting AP drop resilience issues. Both fixed end-to-end: Perl-side detection (v3.5.1) and librespot-side root cause (v3.5.2). Upstream PR submitted. Fork pin is temporary — track merge.
</context>

<next_action>
Start with: check librespot#1743 PR status, check #149/#150 for Rouzax responses, then decide next phase.
</next_action>
