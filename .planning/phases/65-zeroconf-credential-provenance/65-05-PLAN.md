---
phase: 65-zeroconf-credential-provenance
plan: 05
type: execute
wave: 5
depends_on: ["65-01", "65-02", "65-03", "65-04"]
files_modified:
  - librespot-spoton/src/main.rs
autonomous: false
requirements: ["GH #147"]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The spoton binary again supports --get-token: given a --cache dir containing a credentials.json, it connects to the AP, fetches a Mercury Keymaster token (hm://keymaster/token/authenticated), and prints {\"accessToken\":...,\"expiresIn\":...} JSON to stdout (approach A from the plan brief; amends CONTEXT D-03 per orchestrator direction — the mechanical revert of d7fd566 is explicitly permitted)"
    - "The --check capability manifest advertises \"get-token\": true so Perl can gate on Helper->getCapability('get-token')"
    - "HYPOTHESIS VERIFIED against live Spotify: a Keymaster Mercury token obtained from ZeroConf-paired (correct-provenance) credentials is accepted by api.spotify.com for /me, /search, /me/tracks, and /me/player/devices"
    - "If the hypothesis fails, the phase STOPS here — plans 65-06 through 65-08 must not execute (hard constraint from the plan brief)"
  artifacts:
    - "librespot-spoton/src/main.rs (Mode::GetToken, run_get_token, --get-token/--scope/--client-id parsing, SPOTON_TOKEN_FILE Windows fallback, get-token capability entry)"
  key_links:
    - "credentials.json (auth_type 1, zeroconf provenance) -> Session::connect (Shannon AP auth) -> session.token_provider().get_token(scopes) -> Mercury hm://keymaster/token/authenticated -> accessToken JSON on stdout -> curl api.spotify.com returns 200"
---

<objective>
Restore the `--get-token` binary mode (removed in phase 53-01, commit d7fd566) and verify the core hypothesis of the ZeroConf-only architecture: Keymaster Mercury tokens obtained from a session authenticated with ZeroConf-paired credentials ARE valid for api.spotify.com.

Purpose: This is the load-bearing assumption of plans 65-06..65-08 (PKCE elimination). Spotty works this way with zero PKCE; our previous Keymaster 403s came from sessions with wrong-provenance (PKCE-derived) credentials. The hypothesis is UNVERIFIED until this plan passes — everything downstream is gated on it.
Output: A locally built spoton binary with working --get-token, and a documented live-API verification result (STOP gate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-zeroconf-credential-provenance/65-CONTEXT.md
@.planning/phases/65-zeroconf-credential-provenance/65-04-SUMMARY.md
@librespot-spoton/src/main.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Restore --get-token in main.rs (reverse-apply d7fd566) + capability manifest entry</name>
  <files>librespot-spoton/src/main.rs</files>
  <action>
    Reverse-apply commit d7fd566 ("remove Mode::GetToken and Keymaster CLI flags from main.rs") onto the current librespot-spoton/src/main.rs. Preferred mechanics: run git show d7fd566 -- librespot-spoton/src/main.rs, pipe through git apply -R --3way; resolve conflicts manually if later commits (e.g. 54775a8 DISCOVERY_CLIENT_ID rename) shifted context. The restored pieces are: (1) the Mode::GetToken enum variant, (2) the "--get-token", "--scope", "--client-id" argument-parsing arms plus the scope/client_id variable declarations, (3) the Mode::GetToken match arm requiring --cache, (4) the full run_get_token async fn — Cache::new load of credentials.json, Session::new + session.connect(credentials, false), default scope list (the 16 SpotOn Web API scopes), session.token_provider().get_token(scopes) or get_token_with_client_id when --client-id given, JSON print of accessToken/expiresIn, and the SPOTON_TOKEN_FILE env-var file-write fallback (Windows services have broken stdout piping in Proc::Background — Windows compatibility constraint, main::ISWINDOWS precedent), (5) the doc-comment contract block for --get-token.
    If any librespot-core API drifted since the removal (token_provider/get_token_with_client_id were confirmed present in the locked rev 9c7d756 at core/src/token.rs lines 65-73), mirror the Session construction patterns the current run_token_login uses rather than the historical ones.
    Then add "get-token": true to the JSON capability manifest printed by Mode::Check (alphabetical position after "discover-once") so Plugins::SpotOn::Helper->getCapability('get-token') can gate on it in plan 65-06, and add the restored --get-token line back to the mode list in the file-header doc comment.
    Build with cargo build --release from librespot-spoton/. No new dependencies are added (Cargo.toml and Cargo.lock unchanged — verify with git diff --stat).
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo build --release 2>&1 | tail -3 && ./target/release/spoton -n SpotOn --check | tail -1 | grep -c '"get-token": *true' && git diff --stat -- Cargo.toml Cargo.lock | wc -l</automated>
  </verify>
  <done>cargo build succeeds; --check output line 2 contains "get-token": true; Cargo.toml/Cargo.lock untouched (dependency diff empty); running ./target/release/spoton --get-token without --cache exits non-zero with a usage error.</done>
</task>

<task type="auto">
  <name>Task 2: Live hypothesis verification — Keymaster Mercury token from ZeroConf credentials vs api.spotify.com (STOP GATE)</name>
  <files></files>
  <precondition>Dev LMS account 7716df85 has a ZeroConf-paired credentials.json at /var/lib/squeezeboxserver/cache/spoton/7716df85/credentials.json (prefs show playbackCredSource: zeroconf, verified 2026-08-13); sudo -n read access to that path works; outbound network to Spotify is available.</precondition>
  <action>
    Verify the hypothesis end-to-end against live Spotify. Work in a fresh mktemp -d scratch directory — NEVER in the live account dir.
    1. sudo -n cp /var/lib/squeezeboxserver/cache/spoton/7716df85/credentials.json into the scratch dir, sudo -n chown to the current user, chmod 600. Do not modify the live file (per Pi/deploy memory discipline: only read from LMS dirs).
    2. Run ./target/release/spoton -n SpotOn --get-token --cache SCRATCHDIR and capture stdout+stderr to a scratch file. Extract accessToken from the last JSON line (perl -MJSON::PP one-liner or grep -o; jq if available).
    3. With the token as a Bearer header, curl -s -o /dev/null -w '%{http_code}' each of: https://api.spotify.com/v1/me ; https://api.spotify.com/v1/search?q=test&type=track&limit=1 ; https://api.spotify.com/v1/me/tracks?limit=1 ; https://api.spotify.com/v1/me/player/devices — record the four HTTP codes.
    4. Also record whether --get-token itself succeeded (exit 0, JSON present) vs a Mercury keymaster 403 (stderr contains status_code: 403 — the pre-fix signature).
    5. NEVER print, log, or commit the token value anywhere outside the scratch dir; report only HTTP status codes and masked evidence. Delete the scratch dir afterwards.
    STOP-GATE SEMANTICS (hard constraint from the plan brief): success means --get-token exits 0 AND all four endpoints return 200. On ANY other outcome, mark this task failed, do NOT attempt workarounds (no client-id experiments beyond one optional retry with --client-id left empty vs the account's absence of a custom pref), and report "HYPOTHESIS FAILED" with the observed codes/stderr signatures so the orchestrator halts plans 65-06..65-08.
  </action>
  <verify>
    <automated>test -f /tmp/spoton-65-05-hypothesis.txt && grep -c 'RESULT: PASS (me=200 search=200 tracks=200 devices=200)' /tmp/spoton-65-05-hypothesis.txt</automated>
  </verify>
  <done>/tmp/spoton-65-05-hypothesis.txt exists containing the literal line "RESULT: PASS (me=200 search=200 tracks=200 devices=200)" plus the get-token exit status — written only after genuinely observing those live responses. On failure the file contains "RESULT: FAIL" with details and the task is reported as failed (phase stops).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: STOP gate — confirm the hypothesis result before PKCE elimination begins</name>
  <what-built>Restored --get-token binary mode and ran the live Keymaster-Mercury-token hypothesis test against api.spotify.com using the dev machine's ZeroConf-paired credentials.</what-built>
  <how-to-verify>
    Review the reported HTTP codes from Task 2 (also in /tmp/spoton-65-05-hypothesis.txt). Expected: --get-token exit 0, and 200 for /v1/me, /v1/search, /v1/me/tracks, /v1/me/player/devices.
    Decision: approving means plans 65-06..65-08 will REPLACE PKCE entirely with daemon-sourced Keymaster tokens (Web API + playback from the single ZeroConf pairing step). If any code was non-200, do NOT approve — the phase stops here and we rethink.
  </how-to-verify>
  <resume-signal>Type "approved" to unlock the PKCE-elimination waves, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS host -> Spotify AP/Mercury | stored credentials + token requests cross here |
| scratch dir on dev machine | live account credentials + a live access token exist here temporarily |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-21 | Information Disclosure | Task 2 scratch dir (credentials.json copy + token) | high | mitigate | mktemp -d with 0700 default, chmod 600 on the copy, token never echoed into logs/commits/summary, scratch dir deleted at task end; hypothesis file contains status codes only |
| T-65-22 | Information Disclosure | --get-token stdout in future daemon logs | medium | mitigate | Task 1 restores the historical contract (token only on stdout/SPOTON_TOKEN_FILE, never stderr); Perl-side masking discipline enforced in plan 65-06 |
| T-65-23 | Tampering | Reverse-applied historical Rust code vs current librespot rev | medium | mitigate | cargo build gate + --check manifest gate; API drift resolved by mirroring current run_token_login patterns; Cargo.lock unchanged (no new/updated dependencies) |
| T-65-SC | Tampering | cargo dependency supply chain | low | accept | No package installs: Cargo.toml/Cargo.lock are not modified (verified in Task 1); build uses already-vendored locked revisions only |
</threat_model>

<verification>
- cargo build --release green; --check manifest lists get-token.
- Hypothesis file records PASS with four 200s, or the phase stops.
- git diff shows only librespot-spoton/src/main.rs changed.
</verification>

<success_criteria>
- --get-token restored byte-faithfully to the d7fd566~1 contract (adapted only where librespot APIs drifted), capability advertised.
- Live proof (or disproof) of the ZeroConf-credentials -> Mercury Keymaster token -> Web API chain, with an explicit human STOP gate before any PKCE removal begins.
</success_criteria>

<output>
Create `.planning/phases/65-zeroconf-credential-provenance/65-05-SUMMARY.md` when done. Record the exact HTTP codes and the get-token stderr classification (success vs 403 signature) — plans 65-06..65-08 cite this as their foundation. Note: shipping the rebuilt binary to users is a RELEASE step (CI builds, repo.xml SHA — see release procedure), not part of this phase; the plan 65-06 capability gate keeps old binaries failing gracefully.
</output>
