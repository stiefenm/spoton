---
phase: 65-zeroconf-credential-provenance
plan: 07
type: execute
wave: 7
depends_on: ["65-06"]
status: superseded
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - t/09_settings.t
  - t/02_strings.t
autonomous: true
requirements: ["GH #147"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Settings page has exactly ONE auth action (plan-brief scope item 4): the ZeroConf pairing section — always visible, retitled as the Spotify Account section — with three states: no account yet (primary connect CTA), authorization required (orange banner + re-auth), connected (green). The PKCE OAuth account flow (browser popup, callback, manual paste) is GONE"
    - "The Custom Spotify App section (Client ID input, wizard, quota warnings, save handling) is GONE (plan-brief out-of-scope note: Custom Developer App ID support is being removed)"
    - "A fresh install can complete setup start-to-finish from this page: Start pairing in setup mode -> account auto-created from the paired username (65-06 engine) -> page reload shows the connected account (this reverts the 65-04 decision that made Keymaster-PKCE the primary path — plan-brief scope item 1)"
    - "The 65-03 Keymaster browser fallback (Docker/VLAN) survives inside the auth section and now also works with zero accounts configured (CONTEXT D-02: browser fallback stays secondary)"
    - "Add Another Account starts a setup-mode pairing (mode=new), not a PKCE flow"
    - "Every new or reworded user-facing string has real translations in all 11 languages (CS DA DE EN ES FR IT NL NO PL SV); no string value contains the word keymaster or the client-id hex"
  artifacts:
    - "Plugins/SpotOn/Settings.pm (PKCE account flow removed, playerauth setup-mode endpoints, simplified handler params)"
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (auth-first layout, PKCE/Custom-App JS and sections removed)"
    - "Plugins/SpotOn/strings.txt (4 new + 5 reworded keys, 11 languages)"
  key_links:
    - "basic.html Connect button -> POST playerauth/start (mode=new when adding/no accounts) -> Credentials->startPairing(undef) -> 2s status polling -> state success -> reload -> handler() playbackAuthState 'ok' + account row rendered"
    - "playerauth/browser/start with no accounts -> verifier cached with account_id undef -> browser/manual -> exchangeCode(KEYMASTER_CLIENT_ID) -> deriveCredentialsFromToken(undef, ...) -> setup-mode install"
---

<objective>
Make ZeroConf pairing the one and only auth surface in Settings: remove the PKCE OAuth account flow and the Custom App section, promote the Authorize Playback section to the primary "Spotify Account" section with setup-mode support, and align strings in 11 languages.

Purpose: 65-06 gave the engine (tokens from credentials, accounts from pairing). This plan removes the now-redundant PKCE UI so users see exactly one auth step — the Spotty model.
Output: A Settings page where pairing is the single auth action, working for fresh installs, re-auth, and multi-account.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-zeroconf-credential-provenance/65-CONTEXT.md
@.planning/phases/65-zeroconf-credential-provenance/65-06-SUMMARY.md
@Plugins/SpotOn/Settings.pm
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Settings.pm — one auth surface, setup-mode endpoints</name>
  <files>Plugins/SpotOn/Settings.pm, t/09_settings.t</files>
  <behavior>
    - Exactly 7 addRawFunction registrations remain: diagnosticBundle, clearLogs, and the five playerauth endpoints (start/status/cancel/browser-start/browser-manual)
    - POST playerauth/start with mode=new -> startPairing invoked with undef accountId (setup mode); with zero accounts configured and no params -> also setup mode; with a valid 8-hex existing accountId -> that account (unchanged WR-03 validation); with a malformed/unknown accountId -> unknown_account error
    - playerauth/browser/start + browser/manual accept the no-accounts/setup case: verifier entry carries account_id undef, manual completion calls deriveCredentialsFromToken with undef
    - handler() exposes playbackAuthState: 'none' (zero accounts), 'required' (active account flagged needsPlaybackAuth OR its credentials.json missing), 'ok' — the old PKCE-tokens condition is gone
    - Saving bitrate/normalization/streamingMode/diagnosticMode/spDc/pathfinderHash and account switch/remove all still work (regression)
  </behavior>
  <action>
    Remove the PKCE OAuth account flow from Settings.pm (this completes plan-brief scope items 1 and 4 — the 65-04 Keymaster-PKCE-primary changes disappear with the flow itself):
    1. In new(): delete the three PKCE OAuth endpoint registrations (the auth-start AJAX endpoint, the browser-redirect callback endpoint, the manual paste endpoint) and the extra loopback route registered under the bare path 'login'. Keep diagnosticBundle, clearLogs, and all five playerauth registrations.
    2. Delete the handler subs and helpers that exist solely for that flow: the auth-start handler, the callback handler, the manual-paste handler, the shared finish + store-account tail (both subs), the client-id resolver helper, the configured-check helper, the HTML result-page renderer and its escape helper. Keep _postParam, _jsonResponse, _csrfCheck, _pkceLoadVerifierDataFromState (the browser fallback still uses it; renaming is optional and not required), and every playerauth handler.
    3. In handler(): delete the Client ID save block (the pref_clientId sanitization + token-invalidation branch) and the anyAccountNeedsReauth warning-banner assignment (the playbackAuthState banner is the single channel now). Delete these template params: pkceConfigured, needsMigration, redirectUri, customClientId, clientIdMode, quotaState, reauthReason. Rework playbackAuthState per the behavior block ('required' no longer requires PKCE token files to exist — drop that require and condition). Keep accounts/activeAccount/pairingDeviceName/spDc/madeForYou/pathfinder/diag/log-size params unchanged.
    4. _playerAuthStartHandler: read a 'mode' POST param via _postParam; when it equals 'new' OR when the accounts pref hash is empty, call Credentials->startPairing(undef) (65-06 setup mode). Otherwise keep the current validated-accountId-or-activeAccount resolution unchanged. Response shape unchanged ({status, deviceName} or symbolic reason).
    5. _playerAuthBrowserStartHandler: same setup-mode resolution — when mode=new or zero accounts, skip the accountId validation and store the verifier with account_id => undef. _playerAuthBrowserManualHandler already passes the cached account_id through to deriveCredentialsFromToken; undef now flows into the 65-06 setup path — remove any defined() guard that would block it. The purpose tag ('playerauth') requirement and the no-token-persistence discipline (T-65-13) stay exactly as-is.
    6. t/09_settings.t: remove tests covering the deleted flow (auth-start payload tests, client-id resolver tests, the 65-04 auto-derive tail runtime tests, cross-flow purpose-guard tests that exercised the deleted handlers); keep the CSRF structure checks over the 7 remaining registrations; keep the whole-file derivation-ban regex from 65-03 (the negative-lookahead form) and the browser-manual no-persistence region gates; add: mode=new dispatches startPairing with an undef first argument, zero-accounts default dispatches undef, browser/start setup-mode verifier shape (account_id undef, purpose playerauth).
  </action>
  <verify>
    <automated>perl /home/sti/spoton/t/09_settings.t && perl /home/sti/spoton/t/05_perl_syntax.t && test "$(grep -v '^\s*#' /home/sti/spoton/Plugins/SpotOn/Settings.pm | grep -c addRawFunction)" = "7" && test "$(grep -v '^\s*#' /home/sti/spoton/Plugins/SpotOn/Settings.pm | grep -c 'settings/pkce/')" = "0"</automated>
  </verify>
  <done>t/09 green with the new setup-mode assertions; exactly 7 comment-filtered addRawFunction registrations; zero comment-filtered occurrences of the PKCE endpoint path prefix; t/05 14/14.</done>
</task>

<task type="auto">
  <name>Task 2: basic.html — auth-first template</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <!-- planner-discipline-allow: pkce -->
  <action>
    Restructure the template around the single auth action. Concretely:
    1. Setup guide (zero accounts): replace the 4-step PKCE-era guide with a 3-step pairing guide using the NEW keys PLUGIN_SPOTON_SETUP_PAIR_STEP1 / STEP2 / STEP3 (defined in Task 3: open the Spotify app on a device in the same network and play any track; press the connect button below; pick the device shown in the Spotify Connect device list). Keep PLUGIN_SPOTON_SETUP_GUIDE_TITLE and the TROUBLESHOOT hint/link. The template must no longer reference the retired guide keys (SETUP_PKCE_*, SETUP_BROWSE_*, SETUP_PLAYBACK_*, SETUP_DEVAPP_* — key deletion from strings.txt itself happens in 65-08).
    2. Delete: the needsMigration banner block, the whole PKCE auth WRAPPER section (connect button, popup note, redirect note, manual-paste fallback div, revoked-id warning), the Custom Spotify App / Client ID WRAPPER section, and the two PKCE JS functions (the auth-start popup opener with its reload timer, and the manual-paste submitter). After this, the template contains no case-insensitive occurrence of the four letters p-k-c-e — write replacement comments in auth-neutral wording so this gate holds.
    3. Promote the pairing section: always render it (remove the surrounding customClientId/playbackAuthState condition), anchor id stays #spoton-player-auth, title/desc now come from the reworded PLUGIN_SPOTON_PLAYER_AUTH_TITLE / PLAYER_AUTH_DESC. Status line: playbackAuthState 'ok' -> green PLAYER_AUTH_STATUS_OK; 'required' -> orange PLAYER_AUTH_STATUS_REQUIRED; 'none' -> neutral PLUGIN_SPOTON_AUTH_STATUS_NONE (new key). Keep the instructions + code-styled pairingDeviceName, Start/Cancel buttons, status div, polling JS, pagehide keepalive cancel, and the browser-fallback details block byte-compatible (they are 65-02/65-03 tested surface).
    4. JS: extend spotonPlayerAuthStart(mode) to send the body 'mode=new' (Content-Type application/x-www-form-urlencoded) when called with the literal argument 'new'; parameterless calls keep an empty body. The playbackAuthState banner button now calls spotonPlayerAuthStart() (re-auth of the active account) — no popup, no reload timer; the polling success path already reloads.
    5. Account section: the add-another button calls spotonPlayerAuthStart('new') and uses the reworded PLUGIN_SPOTON_ADD_ANOTHER label; when zero accounts, the section shows PLUGIN_SPOTON_ACCOUNT_NONE as today. Account switch/remove markup unchanged.
    Windows note: pure template/JS — no platform concerns. Keep all remaining sections (player-settings link box, bitrate, streaming mode, normalization, Made For You, Pathfinder, diagnostics, credits) untouched.
  </action>
  <verify>
    <automated>test "$(grep -ci 'pkce' /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html)" = "0" && grep -c 'playerauth/status' /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html && grep -c 'pagehide' /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html && grep -c "spotonPlayerAuthStart('new')" /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html && grep -c 'SETUP_PAIR_STEP1' /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</automated>
  </verify>
  <done>Template renders the pairing section unconditionally with three states; zero case-insensitive PKCE references; polling/pagehide/browser-fallback intact; add-another wired to setup-mode pairing.</done>
</task>

<task type="auto">
  <name>Task 3: Strings — 4 new + 5 reworded keys, 11 languages</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t</files>
  <action>
    Add 4 new keys, each with real translations in ALL 11 languages (CS DA DE EN ES FR IT NL NO PL SV — never EN fallback, per i18n house rule), TAB-indented, alphabetical language order:
    - PLUGIN_SPOTON_AUTH_STATUS_NONE — "No Spotify account connected yet" (EN reference)
    - PLUGIN_SPOTON_SETUP_PAIR_STEP1 — "Open the Spotify app on a phone or computer in the same network and play any track"
    - PLUGIN_SPOTON_SETUP_PAIR_STEP2 — "Click 'Connect Spotify Account' below"
    - PLUGIN_SPOTON_SETUP_PAIR_STEP3 — "In the Spotify app, open the device picker and select the device shown below"
    Reword 5 existing keys in all 11 languages (do not rename them — every consumer keeps compiling):
    - PLUGIN_SPOTON_PLAYER_AUTH_TITLE — now "Spotify Account" (it IS the account section)
    - PLUGIN_SPOTON_PLAYER_AUTH_DESC — one-step framing: connecting via the Spotify app authorizes Browse, Search, Library AND playback in a single step
    - PLUGIN_SPOTON_PLAYER_AUTH_BANNER — authorization required: Spotify features are unavailable until you re-connect via the Spotify app (covers Browse too — Web API tokens now come from the same credentials)
    - PLUGIN_SPOTON_PLAYER_AUTH_START_BTN — "Connect Spotify Account"
    - PLUGIN_SPOTON_ADD_ANOTHER — pairing-based wording ("Connect another Spotify account (via the Spotify app)")
    Constraints: no value contains the word keymaster or any 32-hex client id (t/02 permanent guards); values are plain sentences, no HTML.
    t/02_strings.t: append the 4 new keys to the required-keys list. Do NOT remove any keys from strings.txt or the test in this plan — retirement of orphaned keys is 65-08's grep-audited job.
  </action>
  <verify>
    <automated>perl /home/sti/spoton/t/02_strings.t && perl /home/sti/spoton/t/09_settings.t && for t in /home/sti/spoton/t/*.t; do perl "$t" > /dev/null || echo "FAIL $t"; done</automated>
  </verify>
  <done>t/02 green including the 11-language completeness gate for all 4 new keys; full suite has no FAIL lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> playerauth endpoints | setup-mode pairing/browser-fallback can now be triggered with zero accounts configured |
| pasted loopback URL -> browser/manual | attacker-influenced text parsed for code/state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-29 | Elevation of Privilege | setup-mode endpoints creating accounts | medium | mitigate | _csrfCheck on every write endpoint (unchanged); mode param only widens the ALREADY user-initiated pairing to account creation; single pairing slot + timeouts bound exposure (T-65-24 model) |
| T-65-30 | Spoofing | browser/manual setup mode with undef account | medium | mitigate | purpose='playerauth' one-time verifier still required (T-65-11 unchanged); the exchanged token's own username decides the account (65-06 md5 formula) — no caller-chosen identity |
| T-65-31 | Repudiation | removal of the PKCE audit surface | low | accept | Pairing/install paths already log masked one-liners (65-02); no logging is removed in this plan |
| T-65-32 | Information Disclosure | error strings/JSON leaking internals | low | mitigate | Symbolic reason enums only (T-65-07 discipline preserved); deleted handlers take their error-page renderer with them |
| T-65-SC | Tampering | package installs | low | accept | None — no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- Full t/ suite green.
- Settings.pm: 7 registrations, zero PKCE endpoint paths (comment-filtered).
- basic.html: zero case-insensitive PKCE occurrences; pairing/polling/fallback gates green.
- strings: 11-language completeness for all new/reworded keys.
</verification>

<success_criteria>
- One visible auth action; fresh install, re-auth, and add-another all route through ZeroConf pairing (browser fallback for mDNS-blocked networks).
- No Custom App UI, no PKCE UI, no dead template references.
</success_criteria>

<output>
Create `.planning/phases/65-zeroconf-credential-provenance/65-07-SUMMARY.md` when done.
</output>
