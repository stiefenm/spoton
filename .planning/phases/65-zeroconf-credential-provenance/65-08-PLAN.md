---
phase: 65-zeroconf-credential-provenance
plan: 08
type: execute
wave: 8
depends_on: ["65-07"]
status: superseded
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Status.pm
  - Plugins/SpotOn/Unified/DaemonManager.pm
  - Plugins/SpotOn/API/PKCE.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
  - t/07_token_manager.t
autonomous: true
requirements: ["GH #147"]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existing PKCE-authed accounts are migrated exactly once on first start after upgrade (plan-brief scope item 7): every account WITHOUT valid ZeroConf/Keymaster-provenance credentials (verifyCredentials passes AND playbackCredSource marker set) is flagged needsPlaybackAuth reason 'pkce_retired' BEFORE the first daemon start; accounts with valid paired credentials keep working with zero user action (Browse tokens now come from those same credentials)"
    - "The Browse menu shows ONE auth hint for the active account when it needs (re-)authorization — the old PKCE migration hint and PKCE re-auth hint are gone, and the surviving hint's wording covers Browse AND playback (the Web API dies with the credentials now)"
    - "PKCE.pm remains on disk, marked module-wide as retained ONLY for the Keymaster browser fallback, with per-sub guard comments separating the still-used surface (verifier/challenge/state helpers, buildAuthorizationUrl, exchangeCode, storeVerifier/loadAndDeleteVerifier, KEYMASTER_CLIENT_ID, LOOPBACK_REDIRECT_URI) from the retired account-flow surface (token store/load/delete, refresh, scope list, relay + dynamic loopback redirects) — plan-brief scope item 6: keep, do not delete"
    - "No Perl module outside PKCE.pm references the retired PKCE surface anymore; TokenManager's migration-era queries are gone"
    - "strings.txt contains no orphaned keys: every removed key has zero references across Plugins/ (grep-audited), and all remaining keys keep 11-language completeness"
  artifacts:
    - "Plugins/SpotOn/Plugin.pm (zeroconfOnlyMigrated one-shot migration, consolidated OPML auth hint, reworked auth-required menu)"
    - "Plugins/SpotOn/Status.pm (auth health from credentials, not PKCE token files)"
    - "Plugins/SpotOn/strings.txt (retired keys removed, OPML hint reworded, 11 languages)"
  key_links:
    - "_startUnifiedDaemons -> zeroconfOnlyMigrated one-shot (after the 65-03 credProvenanceMigrated block, before DaemonManager->init) -> markNeedsPlaybackAuth('pkce_retired') -> startHelper gate skips flagged accounts (65-01) -> Settings banner + OPML hint point to the ONE pairing action (65-07)"
    - "handleFeed -> Credentials->needsPlaybackAuth || !-f credentialsPathFor -> single PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED textarea"
---

<objective>
Land the upgrade migration and retire the PKCE-era surfaces everywhere outside Settings: one-shot re-auth flagging for PKCE-only accounts, a single consolidated OPML auth hint, credentials-based status health, PKCE.pm marked as fallback-only, and a grep-audited strings cleanup.

Purpose: after 65-06/65-07, PKCE code paths are unreachable but their hints, migration queries, and strings still exist and would show stale or contradictory guidance. This plan makes the ZeroConf-only story coherent end to end, including for upgrading users.
Output: A plugin whose every auth signal points at the one pairing action; PKCE.pm dormant but documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-zeroconf-credential-provenance/65-CONTEXT.md
@.planning/phases/65-zeroconf-credential-provenance/65-06-SUMMARY.md
@.planning/phases/65-zeroconf-credential-provenance/65-07-SUMMARY.md
@Plugins/SpotOn/Plugin.pm
@Plugins/SpotOn/Status.pm
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Plugin.pm — one-shot pkce_retired migration + consolidated OPML hint</name>
  <files>Plugins/SpotOn/Plugin.pm, t/07_token_manager.t</files>
  <behavior>
    - First boot after upgrade: an account whose credentials.json fails verifyCredentials OR whose prefs entry lacks playbackCredSource -> flagged needsPlaybackAuth('pkce_retired') exactly once; an account with valid credentials AND the marker -> untouched; the migration pref flips to 1 so a second boot flags nothing
    - An already-flagged account is not re-flagged (no double log lines)
    - handleFeed for a flagged or credentials-less active account renders exactly ONE auth textarea (PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED) and still renders Home/Search/Library/Podcasts entries when an account exists
    - handleFeed for a healthy paired account renders zero auth hints
  </behavior>
  <action>
    1. prefs init: add zeroconfOnlyMigrated => 0 (plan-brief scope item 7).
    2. _startUnifiedDaemons: directly AFTER the existing credProvenanceMigrated block and BEFORE DaemonManager->init() (same zero-doomed-starts ordering rationale as 65-03), add the one-shot: when !zeroconfOnlyMigrated, iterate the accounts pref; for each id, skip when Credentials->needsPlaybackAuth($id) already true; flag markNeedsPlaybackAuth($id, 'pkce_retired') with one masked info line UNLESS (Credentials->verifyCredentials($id) returns a hashref AND $accounts->{$id}{playbackCredSource} is set); then set zeroconfOnlyMigrated to 1. This catches both PKCE-only accounts (no credentials.json at all — these would silently lose Browse otherwise, since tokens now require credentials) and legacy files that 65-03's marker-only check could not validate structurally.
    3. handleFeed: delete the two PKCE-era hint blocks — the proactive migration hint (the accountNeedsMigration textarea) and the reauth warning textarea beneath it — and rewire the surviving playback-auth hint's condition to: $activeAccountId && (Credentials->needsPlaybackAuth($activeAccountId) || !-f Credentials->credentialsPathFor($activeAccountId)); drop its PKCE-token-file gate and the migration-precedence guard (both concepts are gone). Keep the rate-limit hint and everything below untouched.
    4. Rework the auth-required helper used by feed branches around line 1224 (_authRequiredMenu / _isAuthRequired shape): needsAuth is now Credentials->needsPlaybackAuth($accountId) || !-f Credentials->credentialsPathFor($accountId); remove its accountNeedsMigration/needsReauth/reauthReason calls and any reason-specific message branching that referenced retired reasons; the message key is PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED. Sweep the rest of Plugin.pm for remaining calls into the retired TokenManager queries and PKCE token-file checks (grep for accountNeedsMigration, needsReauth, reauthReason, loadTokens) and convert each to the credentials-based equivalent — the ~3346 comment block about webPlayer-vs-auth errors just needs its wording updated.
    5. TokenManager.pm: now delete accountNeedsMigration and anyAccountNeedsMigration (their last consumers died in steps 3-4 and in 65-07); update t/07 accordingly (remove their tests; add a structure assertion that both symbols are gone from the module source, comment-filtered).
  </action>
  <verify>
    <automated>perl /home/sti/spoton/t/07_token_manager.t && perl /home/sti/spoton/t/05_perl_syntax.t && test "$(grep -v '^\s*#' /home/sti/spoton/Plugins/SpotOn/Plugin.pm | grep -c 'accountNeedsMigration')" = "0" && grep -v '^\s*#' /home/sti/spoton/Plugins/SpotOn/Plugin.pm | grep -c 'zeroconfOnlyMigrated'</automated>
  </verify>
  <done>Migration one-shot present with the exact skip/flag matrix; both migration queries gone from TokenManager and Plugin (comment-filtered); t/07 + t/05 green.</done>
</task>

<task type="auto">
  <name>Task 2: Status.pm, DaemonManager.pm, PKCE.pm — credentials-based health + fallback-only marking</name>
  <files>Plugins/SpotOn/Status.pm, Plugins/SpotOn/Unified/DaemonManager.pm, Plugins/SpotOn/API/PKCE.pm</files>
  <action>
    1. Status.pm: the auth-health hasToken field (line ~241) currently reports PKCE token-file presence; change it to Plugins::SpotOn::API::Credentials->verifyCredentials($accountId) ? 1 : 0 (require Credentials) — the truthful "can this account get tokens" signal now. Sweep Status.pm for other PKCE references (error-message wording mentioning PKCE refresh) and reword to authorization/credentials terms.
    2. DaemonManager.pm startHelper: in the missing-credentials branch, drop the PKCE-token-file condition and its require — when $activeAccountId is set, always log the single playback-authorization INFO hint (Settings -> the account section). No behavioral change to the gate itself (65-01 flag gate and D-08 mismatch delete stay byte-identical). Also grep DaemonManager for any other PKCE mention in log wording/comments and update the words only.
    3. PKCE.pm: add a module-header block stating the module is RETAINED as the Keymaster browser-fallback engine only (plan-brief scope item 6 — kept, not deleted, as documented fallback), with two explicit lists: STILL USED (generateCodeVerifier, generateCodeChallenge, buildState, parseState, buildAuthorizationUrl, exchangeCode, storeVerifier, loadAndDeleteVerifier, KEYMASTER_CLIENT_ID, LOOPBACK_REDIRECT_URI) and RETIRED-UNUSED as of v65-08 (storeTokens, loadTokens, deleteTokens, refreshAccessToken, PKCE_SCOPES, PKCE_TOKEN_FILE, GITHUB_PAGES_REDIRECT_URI, loopbackCallbackRedirectUri) — each retired sub also gets a one-line guard comment forbidding new call sites without a phase decision. Do not delete or alter any code body.
    4. Cross-module audit gate: after 1-3, the only non-comment references to the retired PKCE surface anywhere under Plugins/ must be inside PKCE.pm itself.
  </action>
  <verify>
    <automated>perl /home/sti/spoton/t/05_perl_syntax.t && test "$(grep -rh 'PKCE::loadTokens\|PKCE::storeTokens\|PKCE::deleteTokens\|PKCE::refreshAccessToken' /home/sti/spoton/Plugins/SpotOn --include=*.pm --exclude=PKCE.pm | grep -vc '^\s*#')" = "0"</automated>
  </verify>
  <done>hasToken reflects verifyCredentials; startHelper hint fires regardless of PKCE token files; PKCE.pm carries the used/retired contract header; zero non-comment call sites of the retired package surface (loadTokens/storeTokens/deleteTokens/refreshAccessToken via the PKCE package) outside PKCE.pm.</done>
</task>

<task type="auto">
  <name>Task 3: Strings retirement + OPML hint rewording (11 languages)</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t</files>
  <action>
    1. Build the removal candidate list from the retired UI: the PKCE auth/manual/popup/opening/redirect-note/connect-button family, the success/error result-page pair, the migration hints (OPML + Settings), the reauth-required pair (OPML + Settings), the bundled/custom client-id family (custom-app title, default-auth info, client-id settings/desc/placeholder/wizard/remove, devmode quota warning, bundled-id revoked), the retired setup-guide step keys (the PKCE/browse/playback/devapp title+desc pairs), and the derive-failed key noted unused since 65-01.
    2. For EACH candidate run a reference audit before deleting: grep -rn across Plugins/ (both .pm and HTML trees) — delete the key from strings.txt (all 11 language lines) ONLY when references are zero; keep any key that still has a live consumer and record it in the summary. Never delete a key the audit still finds.
    3. Reword PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED in all 11 languages: it is now the single OPML auth hint — say that the Spotify account needs (re-)authorization via the Spotify app in SpotOn Settings, and that Browse/Search/Library AND playback are unavailable until then (the 65-03 wording claimed browsing still works — no longer true without PKCE).
    4. t/02_strings.t: remove the deleted keys from the required-keys list, keep the new 65-07 keys, keep the 11-language completeness gate and the keymaster/hex guards green. Add the removed-key regression guards t/02 already uses for BUNDLED_MODE_INFO (absent-key assertions) for at least the PKCE connect-button and migration-hint keys.
  </action>
  <verify>
    <automated>perl /home/sti/spoton/t/02_strings.t && for t in /home/sti/spoton/t/*.t; do perl "$t" > /dev/null || echo "FAIL $t"; done && test "$(grep -rc 'PLUGIN_SPOTON_MIGRATION_REQUIRED' /home/sti/spoton/Plugins/SpotOn/strings.txt)" = "0"</automated>
  </verify>
  <done>t/02 green ("all keys 11 languages ok"); full suite has no FAIL lines; every deleted key has zero references under Plugins/; PLAYBACK_AUTH_REQUIRED reworded in 11 languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upgrade path (prefs + cachedir state) | migration reads on-disk credentials and writes persistent auth flags |
| user-facing strings | wording steers users' auth actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-33 | Denial of Service | over-eager migration flagging healthy accounts | high | mitigate | Flag only when verifyCredentials fails OR provenance marker absent — a healthy 65-02/65-03-paired account passes both and is provably untouched (t-covered matrix in Task 1); one-shot pref prevents repeat runs |
| T-65-34 | Denial of Service | under-flagging: PKCE-only account left unflagged loses Browse silently | medium | mitigate | The no-credentials.json case is explicitly in the flag set; OPML hint + Settings banner both key on the same unified condition, so even an unflagged edge shows the hint via the missing-file check |
| T-65-35 | Information Disclosure | migration log lines exposing account identity | low | mitigate | masked-id discipline (substr 4 + asterisks) as in every 65-xx log line |
| T-65-36 | Tampering | dormant PKCE.pm code re-wired later by accident | low | mitigate | Module-header contract + per-sub guard comments; cross-module audit gate proves zero external callers at this commit |
| T-65-SC | Tampering | package installs | low | accept | None — no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- Full t/ suite green (all files).
- Comment-filtered gates: no migration queries in Plugin.pm/TokenManager.pm; no retired-PKCE-surface calls outside PKCE.pm; migration one-shot present.
- Strings audit: zero orphaned keys, zero dangling references.
</verification>

<success_criteria>
- Upgrading users get exactly one clear re-auth prompt (or none, if already paired); fresh state machine has no PKCE branch anywhere.
- Phase-end UAT (manual, not blocking): (1) upgrade simulation — account with pkce_tokens.json only -> flagged, OPML hint + Settings banner shown, pairing restores Browse AND playback; (2) healthy paired dev account 7716df85 -> zero hints, Browse/Search/Library/playback/Connect all work with PKCE fully removed; (3) fresh-install pairing per the 65-07 flow; (4) spoton-uat playback suite.
</success_criteria>

<output>
Create `.planning/phases/65-zeroconf-credential-provenance/65-08-SUMMARY.md` when done. List every removed string key and every kept-because-still-referenced key.
</output>
