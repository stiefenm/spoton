---
phase: 66-settings-auth-reorg
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/Status.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html
  - t/13_status_page.t
autonomous: true
requirements: ["GH #147"]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With no accounts, the Settings page shows a 4-step onboarding guide: 1 Connect Spotify Account (PKCE), 2 Authorize Playback (ZeroConf pairing), 3 Optional sp_dc cookie, 4 Optional Pathfinder hash — the Developer-App step is gone and the troubleshooting link stays (D-07)"
    - "The Settings page has exactly three h2 sections in order — Account, Global Settings, Diagnostics — and every control lives in the right one (D-05)"
    - "The Authorize Playback pairing UI is ALWAYS visible in the Account section (it is the required step 2), with the browser fallback nested inside it as before"
    - "The Custom Developer App control is always rendered as a collapsed details element (open only when a custom ID is set) — never gated behind IF customClientId (D-06)"
    - "When playback auth is required, the banner directs the user to the Authorize Playback section — not to a PKCE re-auth button (re-auth no longer mints playback credentials, D-02)"
    - "The Status page Auth Health card shows Web API token status AND playback credential status as separate rows, including the credential source (Spotify-app pairing vs browser fallback) and a clear next step when either is missing (D-08)"
  artifacts:
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (4-section layout, always-visible pairing, collapsed Custom App)"
    - "Plugins/SpotOn/Settings.pm (playbackCredSource template param)"
    - "Plugins/SpotOn/Status.pm (_collectAuthHealth playback block)"
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html (Web API Token + Playback Credentials rows)"
  key_links:
    - "Settings handler() -> playbackAuthState/playbackCredSource params -> basic.html Account section renders pairing status + source -> banner anchor #spoton-player-auth -> existing playerauth endpoints (unchanged)"
    - "Status _statusDataHandler -> _collectAuthHealth -> authHealth.{id}.playback {hasCredentials, needsAuth, reason, source} -> status.html renderAuthHealth rows"
---

<objective>
Reorganize the organically grown Settings page into four clear sections reflecting the restored two-step auth architecture, and extend the Auth Health dashboard so a user can always see BOTH halves of their auth state (Web API token, playback credentials) and what to do next.

Phase 66 decisions implemented here (defined in 66-01): D-05 (four sections: Onboarding when no accounts, Account, Global Settings, Diagnostics), D-06 (Custom Developer App always visible, collapsed), D-07 (onboarding steps 1 PKCE / 2 ZeroConf / 3 sp_dc optional / 4 Pathfinder optional, Developer-App step removed), D-08 (Auth Health shows PKCE token status and playback credential status separately, with credential source zeroconf/browser and clear remediation), plus the UI consequences of D-02 (pairing is required, so the playback-auth banner leads to pairing, not to re-auth).

New string keys referenced here are DEFINED in plan 66-03 (same phase, next wave — t/02 only checks strings.txt, and templates render missing keys as raw key names until 66-03 lands, which is acceptable intra-phase).

Purpose: A first-time user can self-navigate setup; an existing user can diagnose auth problems at a glance.
Output: Restructured basic.html, playback block in the auth-health JSON, updated status.html rendering.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-settings-auth-reorg/66-01-SUMMARY.md
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
@Plugins/SpotOn/Settings.pm
@Plugins/SpotOn/Status.pm
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html
</context>

<interfaces>
Consumed (unchanged): all existing template params from Settings handler() (accounts, activeAccount, customClientId, clientIdMode, quotaState, playbackAuthState, pairingDeviceName, spDcMasked, madeForYouState, pathfinderHash, diagnosticEnabled, globalStreamingMode, connectLogSize, redirectUri, reauthReason, needsMigration, pkceConfigured); playerauth endpoints + polling JS (65-02); browser-fallback endpoints + JS (65-03); `Credentials->verifyCredentials/needsPlaybackAuth/playbackAuthReason` and the `playbackCredSource` account-pref marker written by `_installPairedCredentials` (values 'zeroconf' | 'keymaster').

Established here:

```perl
# Settings.pm handler(), next to the playbackAuthState block:
#   $paramRef->{playbackCredSource} =
#     $accounts->{$activeId}{playbackCredSource} || '';   # '' when unset/legacy

# Status.pm _collectAuthHealth($accountId) adds a 6th indicator:
#   $health{playback} = {
#     hasCredentials => (verifyCredentials ? 1 : 0),   # passive file read
#     needsAuth      => needsPlaybackAuth($accountId),
#     reason         => playbackAuthReason($accountId),  # symbolic enum only
#     source         => $accounts->{$accountId}{playbackCredSource} // '',
#   };
```

New string keys (defined in 66-03): PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION, PLUGIN_SPOTON_SETUP_SPDC_TITLE, PLUGIN_SPOTON_SETUP_SPDC_DESC, PLUGIN_SPOTON_SETUP_PATHFINDER_TITLE, PLUGIN_SPOTON_SETUP_PATHFINDER_DESC, PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_ZEROCONF, PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_BROWSER.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: basic.html — four-section restructure, always-visible pairing, collapsed Custom App</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/Settings.pm</files>
  <action>
Restructure the template top-to-bottom. Move existing WRAPPER blocks intact wherever possible — the JS functions, endpoint URLs, element IDs, polling logic, pagehide handler and the always-armed reload timer with focus-cancel are NOT to be modified (their t/09 and grep gates must keep passing). Every block stays inside the single settings form the header/footer templates provide.

1. **Onboarding guide (`[% IF NOT accounts.keys.size %]`, D-07)**: keep the box; steps become — 1: SETUP_PKCE_TITLE/SETUP_PKCE_DESC (existing keys), 2: SETUP_PLAYBACK_TITLE/SETUP_PLAYBACK_DESC (existing keys; reworded to required-pairing framing in 66-03), 3: SETUP_SPDC_TITLE/SETUP_SPDC_DESC (new keys, optional Made For You), 4: SETUP_PATHFINDER_TITLE/SETUP_PATHFINDER_DESC (new keys, optional Web-Player playlist access). Remove the SETUP_BROWSE_TITLE/DESC step and the SETUP_DEVAPP_TITLE step entirely. Keep the troubleshooting hint + TROUBLESHOOTING.md link footer unchanged.

2. **Banners**: keep the needsMigration banner as-is. Rework the `playbackAuthState == 'required'` banner (D-02): remove the spotonPkceStart() button (a PKCE re-auth no longer restores playback); the primary action is a prominent link to `#spoton-player-auth` labeled with PLAYER_AUTH_TITLE, shown unconditionally (drop the IF customClientId around it). Banner text key PLUGIN_SPOTON_PLAYER_AUTH_BANNER (reworded in 66-03). Keep the blue player-settings link box after the banners.

3. **Section: Account** — `<h2>` with new key PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION, containing in order:
   a. The PKCE_AUTH wrapper (Connect button, popup note, custom-ID redirect note, pkce-status, pkce-fallback paste block, BUNDLED_ID_REVOKED hint) — moved unchanged.
   b. The Authorize Playback block: keep the `#spoton-player-auth` anchor div, but remove the surrounding `[% IF customClientId OR playbackAuthState == 'required' %]` gate — the PLAYER_AUTH wrapper is ALWAYS rendered (required step 2). Inside the status line, when playbackAuthState is not 'required' and playbackCredSource is set, append the source label: PLAYER_AUTH_SOURCE_ZEROCONF when playbackCredSource == 'zeroconf', PLAYER_AUTH_SOURCE_BROWSER when it equals the browser-fallback marker value (compare against the literal pref value 'keymaster' in a TT conditional — template logic, not user-facing text). The browser-fallback `<details>` stays nested at the end of this wrapper, untouched.
   c. The sp_dc wrapper and the pathfinderHash wrapper — moved unchanged from the old Made For You section; delete the MADE_FOR_YOU_SECTION `<h2>` and its `<hr>`.
   d. The Custom Developer App block (D-06): remove the outer `[% IF customClientId %]`/`[% END %]` gate around the CLIENT_ID_SETTINGS wrapper so it always renders. Inside the wrapper, wrap everything in `<details[% IF customClientId %] open[% END %]>` with `<summary>` = CUSTOM_APP_TITLE (existing key). Contents unchanged: DEVMODE_QUOTA_WARNING branch, pref_clientId input, the setup wizard details (SETUP_DEVAPP_DESC + redirectUri code block), CUSTOM_APP_REMOVE link. Add the DEFAULT_AUTH_INFO line (existing key, reworded in 66-03) above the details, shown when clientIdMode == 'bundled'. Note: the pref_clientId input is now always submitted; the handler's existing `defined $paramRef->{pref_clientId}` save path already sanitizes and handles empty values correctly — verify no behavior change when saving with an empty field (no token invalidation fires because '' eq old '').
   e. The ACCOUNT_SETTINGS wrapper (account table, switch/remove, Add Another Account) — moved unchanged.

4. **Section: Global Settings** — existing `<h2>` SETTINGS_GLOBAL_SECTION, containing exactly: BINARY_STATUS, BITRATE, STREAMING_MODE_GLOBAL, NORMALIZATION wrappers (all unchanged). The Client-ID block leaves this section (it moved to Account).

5. **Section: Diagnostics** — existing `<h2>` SETTINGS_DIAG_SECTION with DIAG_MODE, DIAG_DOWNLOAD (gated on diagnosticEnabled), DIAG_CLEAR_LOGS, STATUS_PAGE wrappers — all unchanged, stays last before the script block and credits.

6. **Settings.pm handler()**: next to the existing playbackAuthState computation, add `$paramRef->{playbackCredSource}` read from the active account's accounts-pref entry (`playbackCredSource` key written by _installPairedCredentials), defaulting to ''. One line plus a short comment; no other handler changes.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/05_perl_syntax.t && perl t/09_settings.t && T=Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html && grep -c '<h2>' $T | grep -qx 3 && grep -c 'PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION' $T | grep -qv '^0$' && grep -c 'PLUGIN_SPOTON_MADE_FOR_YOU_SECTION' $T | grep -qx 0 && grep -c 'PLUGIN_SPOTON_SETUP_BROWSE_TITLE' $T | grep -qx 0 && grep -c 'PLUGIN_SPOTON_SETUP_DEVAPP_TITLE' $T | grep -qx 0 && grep -c 'PLUGIN_SPOTON_SETUP_SPDC_TITLE' $T | grep -qv '^0$' && grep -c 'PLUGIN_SPOTON_SETUP_PATHFINDER_TITLE' $T | grep -qv '^0$' && grep -c 'IF customClientId OR playbackAuthState' $T | grep -qx 0 && grep -c 'playerauth/status' $T | grep -qv '^0$' && grep -c 'pagehide' $T | grep -qv '^0$' && grep -c 'spoton-player-auth' $T | grep -qv '^0$' && grep -v '^[[:space:]]*#' Plugins/SpotOn/Settings.pm | grep -c 'playbackCredSource' | grep -qv '^0$'</automated>
    <human-check>Render the Settings page in three states: (a) no accounts — 4-step guide with sp_dc/Pathfinder as optional steps, no Developer-App step, troubleshooting link present; (b) account without credentials — banner links to the always-visible Authorize Playback section, Custom App collapsed; (c) account with custom ID — Custom App details open, pairing section still visible. Pairing and browser-fallback flows still complete end-to-end.</human-check>
  </verify>
  <done>Three h2 sections in the specified order with every control in its section (D-05); pairing UI unconditionally visible; Custom App always rendered, collapsed by default (D-06); onboarding matches the 4-step two-step-auth model (D-07); banner routes to pairing (D-02); all existing JS/endpoint gates still green.</done>
</task>

<task type="auto">
  <name>Task 2: Auth Health — separate Web API and playback credential indicators with source</name>
  <files>Plugins/SpotOn/Status.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html, t/13_status_page.t</files>
  <action>
1. **Status.pm `_collectAuthHealth` (D-08)**: add the 6th indicator block `playback` per the interfaces section: `require Plugins::SpotOn::API::Credentials;` then hasCredentials from `verifyCredentials($accountId)` (returns the parsed hashref only for a valid auth_type-1 file — treat truthy as 1; this is a passive local file read, keeping the T-54-02/T-54-06 zero-outbound-calls guarantee), needsAuth from `needsPlaybackAuth`, reason from `playbackAuthReason` (symbolic enum only, never stderr), source from the accounts pref entry's `playbackCredSource` (default ''). Update the sub's header comment to list 6 indicators. Never include username or auth_data from the credentials file in the payload.

2. **status.html `renderAuthHealth`**: rename the 'PKCE Status' row label to 'Web API Token' with three states: 'Valid' (hasToken && !needsReauth, dot-alive), 'Re-auth required' (hasToken && needsReauth, dot-dead), 'Missing' (!hasToken, dot-dead). Keep the Last API Call row. Add a 'Playback Credentials' row directly after it, derived from h.playback: needsAuth or !hasCredentials -> 'Missing — authorize playback in SpotOn Settings' (dot-dead; append the symbolic reason in parentheses when present, e.g. credential_error); hasCredentials && source == 'zeroconf' -> 'Paired (Spotify app)' (dot-alive); hasCredentials && source == 'keymaster' -> 'Paired (browser fallback)' (dot-alive); hasCredentials with empty source -> 'Legacy (token-derived) — re-pairing recommended' (dot-warn). This page's labels are EN-only by existing convention (diagnostic page). Keep the sp_dc, Connect, Migration and Audio Keys rows unchanged.

3. **t/13_status_page.t**: extend the auth-health coverage: stub/fixture an account so `_collectAuthHealth` returns the playback block, and assert (a) the block carries the four keys hasCredentials/needsAuth/reason/source, (b) the payload contains no `auth_data` and no raw credential material anywhere in the serialized JSON, (c) an account without credentials reports hasCredentials 0. Follow the existing stub patterns in the file (same fake Credentials/prefs layers t/13 already uses; add recording stubs for the three Credentials queries if not present).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/05_perl_syntax.t && perl t/13_status_page.t && perl -ne 'print if /^sub _collectAuthHealth/ .. /^}/' Plugins/SpotOn/Status.pm | grep -v '^[[:space:]]*#' | grep -c 'needsPlaybackAuth' | grep -qv '^0$' && perl -ne 'print if /^sub _collectAuthHealth/ .. /^}/' Plugins/SpotOn/Status.pm | grep -v '^[[:space:]]*#' | grep -c 'playbackCredSource' | grep -qv '^0$' && grep -c 'Playback Credentials' Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html | grep -qv '^0$' && grep -c 'Web API Token' Plugins/SpotOn/HTML/EN/plugins/SpotOn/status.html | grep -qv '^0$'</automated>
    <human-check>Open /plugins/SpotOn/status.html on the dev LMS: the Auth Health card shows 'Web API Token' and 'Playback Credentials' as separate rows; after ZeroConf pairing the playback row reads 'Paired (Spotify app)'; delete credentials.json (test only) and confirm the row flips to the Missing state with the Settings pointer.</human-check>
  </verify>
  <done>Auth Health reports the two auth halves independently with credential source and remediation text (D-08); collector stays passive (no outbound calls, no secrets in payload); t/13 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Status JSON endpoint -> browser | Read-only, unauthenticated within LMS session; must never carry credential material |
| Settings form POST -> handler | pref_clientId now always submitted; sanitization path must hold |
| Template interpolation | Account names / pref echoes rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-05 | Info Disclosure | authHealth playback block | high | mitigate | Only booleans, symbolic reason enum and the source marker cross the boundary; t/13 asserts no auth_data in serialized payload (Task 2 step 3) |
| T-66-06 | Tampering | always-submitted pref_clientId field | medium | mitigate | Existing T-04.4-01 sanitization (alnum, 32 chars) unchanged; empty-submit no-op verified in Task 1 step 3d |
| T-66-07 | Info Disclosure | template echoes (spDcMasked, customClientId, displayName) | medium | mitigate | All existing `| html` filters preserved when moving blocks; no new unfiltered interpolation introduced |
| T-66-08 | DoS | auth-health collector triggering outbound calls per render | low | mitigate | Collector stays passive: verifyCredentials is a local file read; zero HTTP calls (T-54-02 discipline, asserted in comment + review) |
| T-66-SC | Tampering | supply chain | low | accept | No package-manager installs in this phase |
</threat_model>

<verification>
- `perl t/05_perl_syntax.t`, `perl t/09_settings.t`, `perl t/13_status_page.t` green; full `t/*.t` suite green
- Template gates: 3 h2 sections, no Made-For-You heading, no removed guide steps, pairing UI ungated, Custom App details pattern present, all 65-02/65-03 JS anchors intact
- Manual render check of the three page states (Task 1 human-check) and the status-page rows (Task 2 human-check)
</verification>

<success_criteria>
- A user with zero accounts sees a correct 4-step guide; a user missing playback credentials is routed to pairing; a power user finds the Custom App option without a pref hack (D-05/D-06/D-07)
- Auth Health answers "which half of my auth is broken and what do I do" at a glance (D-08)
- No JS/endpoint regressions in pairing or browser fallback
</success_criteria>

<output>
Create `.planning/phases/66-settings-auth-reorg/66-02-SUMMARY.md` when done
</output>
