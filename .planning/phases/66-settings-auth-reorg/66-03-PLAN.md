---
phase: 66-settings-auth-reorg
plan: 03
type: execute
wave: 3
depends_on: ["66-02"]
files_modified:
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
autonomous: true
requirements: ["GH #147"]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every key the 66-02 template references exists in strings.txt with REAL translations in all 11 languages (CS DA DE EN ES FR IT NL NO PL SV) — never EN filler (D-09)"
    - "All auth-related strings describe the two-step model: step 1 connects the account (Web API), step 2 authorizes playback via pairing — no string claims one authorization enables playback (D-02)"
    - "Keys whose only references were removed by 66-02 are gone from strings.txt (SETUP_BROWSE_*, SETUP_DEVAPP_TITLE, MADE_FOR_YOU_SECTION)"
    - "No translation VALUE contains the word keymaster (t/02 permanent regression guard stays green)"
  artifacts:
    - "Plugins/SpotOn/strings.txt (7 new keys, 7 reworded keys, 4 removed keys, 11 languages each)"
    - "t/02_strings.t (required-keys and removed-keys lists updated)"
  key_links:
    - "basic.html string filters -> strings.txt keys -> t/02 bilingual + 11-language + removed-key gates"
---

<objective>
Land the i18n layer for the phase-66 two-step auth model and Settings reorganization: new section/guide/source keys referenced by 66-02, rewording of every string the 65-04 one-step flow made inaccurate, and removal of keys the reorganized template no longer uses.

Implements D-09 (all 11 languages genuinely translated) and the messaging side of D-02 (ZeroConf pairing is the required playback step), D-06 (default identity messaging), D-07 (guide steps).

Purpose: UI text matches the actual auth architecture in every supported language.
Output: strings.txt with the full key set, updated t/02 gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-settings-auth-reorg/66-02-SUMMARY.md
@Plugins/SpotOn/strings.txt
@t/02_strings.t
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: strings.txt — new + reworded keys for the two-step model, 11 languages</name>
  <files>Plugins/SpotOn/strings.txt</files>
  <action>
All keys with REAL translations in all 11 languages (CS DA DE EN ES FR IT NL NO PL SV — the file's actual language set; never EN filler), TAB-indented, alphabetical language order within each key. No translation value may contain the word "keymaster" or any client-id hex (t/02 guard); write "browser authorization" / "Spotify's app identity" instead.

New keys (D-05/D-07/D-08 support):
- PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION — EN "Account"
- PLUGIN_SPOTON_SETUP_SPDC_TITLE — EN "Optional: Made For You playlists"
- PLUGIN_SPOTON_SETUP_SPDC_DESC — EN "To browse your Daily Mixes and other Made For You playlists, add your sp_dc browser cookie in the Account section below. You can do this anytime later."
- PLUGIN_SPOTON_SETUP_PATHFINDER_TITLE — EN "Optional: Web Player playlist access"
- PLUGIN_SPOTON_SETUP_PATHFINDER_DESC — EN "Some editorial playlists need a Pathfinder query hash from the Spotify Web Player. Only add this if certain playlists fail to load — see the help text next to the field below."
- PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_ZEROCONF — EN "Paired via Spotify app"
- PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_BROWSER — EN "Paired via browser authorization"

Reworded keys (replace the full value block in all 11 languages; two-step framing per D-02):
- PLUGIN_SPOTON_PKCE_AUTH_DESC — step 1 of 2: connects your Spotify account for browsing, search and library access; playback is authorized separately in the next step.
- PLUGIN_SPOTON_PLAYER_AUTH_DESC — required step 2: playback and Spotify Connect need credentials from the Spotify app. Start pairing, then select the shown device in the Spotify app on your phone or computer.
- PLUGIN_SPOTON_PLAYER_AUTH_BANNER — playback authorization required: your account is connected, but playback is not yet authorized. Use "Authorize Playback" below.
- PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED — (OPML hint, rendered by Plugin.pm) playback not yet authorized — open SpotOn Settings and complete "Authorize Playback"; browsing keeps working.
- PLUGIN_SPOTON_SETUP_PKCE_DESC — guide step 1: click "Connect Spotify Account" and approve in the browser window; this enables browsing, search and your library.
- PLUGIN_SPOTON_SETUP_PLAYBACK_DESC — guide step 2: click "Authorize Playback" and pick the shown pairing device in your Spotify app; this enables playback and Spotify Connect. Required once per account.
- PLUGIN_SPOTON_DEFAULT_AUTH_INFO — default mode: SpotOn uses its built-in app identity with its own API quota — no Spotify Developer App needed. Two authorizations complete the setup: connect your account, then authorize playback.

Removed entirely (their only references were deleted in 66-02): PLUGIN_SPOTON_SETUP_BROWSE_TITLE, PLUGIN_SPOTON_SETUP_BROWSE_DESC, PLUGIN_SPOTON_SETUP_DEVAPP_TITLE, PLUGIN_SPOTON_MADE_FOR_YOU_SECTION. Keep PLUGIN_SPOTON_SETUP_DEVAPP_DESC (still used inside the Custom App wizard details) and PLUGIN_SPOTON_CUSTOM_APP_TITLE / PLUGIN_SPOTON_CLIENT_ID_DESC (65-04 wording still accurate). Before deleting, grep Plugins/ for each removed key to confirm zero remaining references.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/05_perl_syntax.t && grep -c 'PLUGIN_SPOTON_SETUP_BROWSE_TITLE\|PLUGIN_SPOTON_SETUP_DEVAPP_TITLE\|PLUGIN_SPOTON_MADE_FOR_YOU_SECTION' Plugins/SpotOn/strings.txt | grep -qx 0 && perl -e 'local $/; open my $fh, "<:encoding(UTF-8)", "Plugins/SpotOn/strings.txt" or die; $_=<$fh>; for my $k (qw(PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION PLUGIN_SPOTON_SETUP_SPDC_TITLE PLUGIN_SPOTON_SETUP_SPDC_DESC PLUGIN_SPOTON_SETUP_PATHFINDER_TITLE PLUGIN_SPOTON_SETUP_PATHFINDER_DESC PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_ZEROCONF PLUGIN_SPOTON_PLAYER_AUTH_SOURCE_BROWSER PLUGIN_SPOTON_PKCE_AUTH_DESC PLUGIN_SPOTON_PLAYER_AUTH_DESC PLUGIN_SPOTON_PLAYER_AUTH_BANNER PLUGIN_SPOTON_PLAYBACK_AUTH_REQUIRED PLUGIN_SPOTON_SETUP_PKCE_DESC PLUGIN_SPOTON_SETUP_PLAYBACK_DESC PLUGIN_SPOTON_DEFAULT_AUTH_INFO)) { my ($b) = /^\Q$k\E\n((?:\t.*\n)+)/m or die "$k missing\n"; my $n = () = $b =~ /^\t(?:CS|DA|DE|EN|ES|FR|IT|NL|NO|PL|SV)\t/mg; die "$k has $n languages\n" unless $n == 11 } print "all keys 11 languages ok\n"'</automated>
  </verify>
  <done>Seven new and seven reworded keys carry real translations in all 11 languages (D-09); four obsolete keys are gone; no value mentions the retired service name.</done>
</task>

<task type="auto">
  <name>Task 2: t/02_strings.t — gate lists updated</name>
  <files>t/02_strings.t</files>
  <action>
1. Append the seven new keys from Task 1 to the @bilingual_keys required list.
2. Remove PLUGIN_SPOTON_SETUP_BROWSE_TITLE / PLUGIN_SPOTON_SETUP_BROWSE_DESC / PLUGIN_SPOTON_SETUP_DEVAPP_TITLE from any required list they appear in (grep the file — only some SETUP_ keys are listed) and add all four removed keys (including PLUGIN_SPOTON_MADE_FOR_YOU_SECTION) to a new phase-66 removed-keys assertion block, mirroring the existing Plan-65-04 BUNDLED_MODE_INFO pattern (assert !$has_en && !$has_de with a message referencing plan 66-03).
3. Leave the tab-indentation gate and the keymaster-value guard untouched — both must pass against the Task 1 result.
4. Run the full strings gate.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/02_strings.t && grep -c 'PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION' t/02_strings.t | grep -qv '^0$' && grep -c 'PLUGIN_SPOTON_MADE_FOR_YOU_SECTION' t/02_strings.t | grep -qv '^0$'</automated>
  </verify>
  <done>t/02 requires every new key, asserts every removed key is absent, and passes green together with the existing guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strings.txt -> rendered UI | Values are template-interpolated via the string filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-09 | Info Disclosure | translation values leaking internal identifiers | low | mitigate | t/02 keymaster-value guard + no client-id hex in any value (Task 1 rule) |
| T-66-SC | Tampering | supply chain | low | accept | No package-manager installs; translations authored in-repo |
</threat_model>

<verification>
- `perl t/02_strings.t`, `perl t/05_perl_syntax.t` green; full `t/*.t` suite green
- 11-language gate over all new + reworded keys; removed keys absent from strings.txt AND from Plugins/ templates/modules (grep)
- Spot-check DE + one non-German language for genuine (non-EN-copy) translations of the reworded auth keys
</verification>

<success_criteria>
- Rendered Settings/OPML text describes the two-step auth truthfully in every language (D-02, D-09)
- No orphaned or missing string keys after the 66-02 template restructure
</success_criteria>

<output>
Create `.planning/phases/66-settings-auth-reorg/66-03-SUMMARY.md` when done
</output>
