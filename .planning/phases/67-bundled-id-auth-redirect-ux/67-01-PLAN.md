---
phase: 67-bundled-id-auth-redirect-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
  - TROUBLESHOOTING.md
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /login returns 400 (handler found) instead of 404 — D-01"
    - "Bundled Client ID skips the 8-request endpoint probe on startup — D-04"
    - "Settings page shows shared-quota hint when using bundled ID, recommends custom — D-02, D-03"
    - "TROUBLESHOOTING.md has a dedicated rate-limit section explaining shared bucket and custom ID recommendation"
    - "Forum reply 267 reflects correct diagnosis (shared bucket) — D-05"
  artifacts:
    - Plugins/SpotOn/Settings.pm
    - Plugins/SpotOn/API/Client.pm
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    - Plugins/SpotOn/strings.txt
    - t/02_strings.t
    - TROUBLESHOOTING.md
    - ~/SynologyDrive/forum-reply-267.txt
  key_links:
    - "Client.pm probeEndpointLimits must check isBundledId and hardcode known limits"
    - "Settings.pm handler() must pass isBundled to template for quota hint rendering"
    - "basic.html template must render shared-quota warning for bundled mode and recommend custom ID"
---

<objective>
Fix the /login redirect handler bug (D-01), overhaul the bundled Client ID UX to communicate shared-bucket degradation (D-02, D-03), skip startup limit probing for the bundled ID (D-04), update TROUBLESHOOTING.md with rate-limit guidance, and update the agriff79 forum reply (D-05).

Purpose: A/B testing confirmed the bundled ncspot Extended Quota Client ID is shared across 138+ installations and produces 429s under load. The UX must clearly recommend creating a personal Client ID and explain the trade-off. The probe skip saves 8 API requests from the shared bucket on every LMS restart across all installations.

Output: Fixed /login handler, reframed Settings Client ID UX, probe skip logic, updated TROUBLESHOOTING.md, updated forum reply.
</objective>

<execution_context>
@/home/sti/.claude/gsd-core/workflows/execute-plan.md
@/home/sti/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/67-bundled-id-auth-redirect-ux/67-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: /login fix (D-01), probe skip (D-04), Settings UX reframing (D-02, D-03)</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/API/Client.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (lines 56-62 for /login handler, lines 310-325 for clientIdMode/quotaState logic, lines 425-485 for _pkceStartHandler, lines 1043-1061 for _pkceClientId)
    - Plugins/SpotOn/API/Client.pm (lines 1-28 for SPOTON_DEFAULT_CLIENT_ID constant, lines 95-110 for reset/defaults, lines 125-245 for probeEndpointLimits)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (lines 244-273 for the current Client ID settings section)
  </read_first>
  <action>
    THREE changes, all end-to-end wired:

    1. D-01 (/login fix): The working tree already has `'login'` replacing `qr{^login$}` on Settings.pm line 60. No code change needed, just verify the diff is present and commit with subsequent tasks.

    2. D-04 (probe skip for bundled ID): In Client.pm, add an early-return guard at the top of `probeEndpointLimits` (after the existing `$_limitsProbed` and `$_probeInflight` guards, before the rate-limit deferral check). The guard checks whether `$prefs->get('clientId')` is falsy (no custom ID configured = bundled mode). When bundled, hardcode the five known limit values: search=50, library=50, artist_albums=50, album_tracks=50, playlist_items=100. Set `$_limitsProbed = 1` and `$_limitsLastProbed = time()` and call `$doneCb->()` immediately. Log at INFOLOG level: "Client: bundled ID — using known limits (probe skipped)". This saves 8 API requests from the shared ncspot bucket on every LMS restart across 138+ installations. The `force => 1` override must still work (bypass the bundled guard when the user explicitly re-probes).

    3. D-02/D-03 (Settings UX reframing): Restructure the Client ID section in basic.html to flip the recommendation. The current UX hides the custom ID input behind a details/summary "Advanced" toggle and shows an info paragraph for bundled mode. The new UX should:
       a. When bundled (clientIdMode == 'bundled'): Show a prominent amber-colored hint using the new string key PLUGIN_SPOTON_BUNDLED_SHARED_QUOTA_HINT (to be created in Task 2). This replaces the current PLUGIN_SPOTON_DEFAULT_AUTH_INFO paragraph. The hint communicates that the bundled ID is shared across all SpotOn installations and rate limiting may occur during peak usage, and recommends creating a personal Client ID.
       b. When custom (clientIdMode != 'bundled'): Show a green-colored confirmation using the new string key PLUGIN_SPOTON_CUSTOM_QUOTA_OK.
       c. The Client ID input and setup wizard remain inside a details/summary, but the summary text changes: when bundled it reads the new string PLUGIN_SPOTON_CUSTOM_APP_SETUP (recommending setup) instead of the old "Advanced: Use your own..." framing. When custom, the summary reads PLUGIN_SPOTON_CUSTOM_APP_TITLE (unchanged, stays as-is for the manage/edit case). The details element opens by default when bundled (add `open` attribute when `clientIdMode == 'bundled'`).
       d. Keep the devmode quota warning, bundled_id_revoked, remove-link, and setup wizard sections intact. No structural changes to those.

    Also update Settings.pm handler(): the existing `$paramRef->{clientIdMode}` and `$paramRef->{quotaState}` assignments are sufficient for the template. No Perl-side logic changes needed beyond D-04 in Client.pm.
  </action>
  <acceptance_criteria>
    - Settings.pm line 60 reads `'login'` (not qr{^login$})
    - Client.pm probeEndpointLimits has a bundled-ID guard that sets all 5 limits to their known values and returns without making any HTTP requests
    - Client.pm bundled guard is bypassed when `force => 1` is passed
    - basic.html shows the shared-quota hint (amber) when clientIdMode == 'bundled' and the private-quota confirmation (green) when custom
    - basic.html details element for Client ID input is open by default when bundled
    - No regression: custom ID users still see their ID in the input field, devmode warning still renders when applicable
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton &amp;&amp; grep -q "'login'" Plugins/SpotOn/Settings.pm &amp;&amp; grep -q 'bundled.*known.*limits\|known.*limits.*bundled\|probe.*skip' Plugins/SpotOn/API/Client.pm &amp;&amp; grep -q 'BUNDLED_SHARED_QUOTA_HINT\|CUSTOM_QUOTA_OK' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    D-01: /login handler uses plain string key. D-04: bundled ID skips probing, hardcodes known limits. D-02/D-03: Settings template shows shared-quota warning for bundled and private-quota confirmation for custom, with Client ID input open by default for bundled users.
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n strings — shared-quota messaging (D-02, D-03) + test gate</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t</files>
  <read_first>
    - Plugins/SpotOn/strings.txt (lines 989-1105 for CLIENT_ID_SETTINGS through DEVMODE_QUOTA_WARNING, lines 1054-1065 for CUSTOM_APP_TITLE, lines 1080-1091 for DEFAULT_AUTH_INFO)
    - t/02_strings.t (lines 117-186 for required-keys list, lines 239-241 for removed-keys gates)
  </read_first>
  <action>
    String changes in strings.txt (all 11 languages: CS DA DE EN ES FR IT NL NO PL SV, real translations):

    1. ADD new key PLUGIN_SPOTON_BUNDLED_SHARED_QUOTA_HINT after the existing PLUGIN_SPOTON_DEFAULT_AUTH_INFO block. English value: "The bundled Client ID is shared across all SpotOn installations. Rate limiting may occur during peak usage. For the best experience, create your own Spotify Developer App below." All 11 translations must be genuine, not English fallback.

    2. ADD new key PLUGIN_SPOTON_CUSTOM_QUOTA_OK after the new BUNDLED hint. English value: "Using your own Client ID — private API quota, no shared rate limits." All 11 genuine translations.

    3. ADD new key PLUGIN_SPOTON_CUSTOM_APP_SETUP after existing PLUGIN_SPOTON_CUSTOM_APP_TITLE. English value: "Recommended: Create your own Spotify Developer App". All 11 genuine translations. This is the details/summary label shown when bundled mode is active, replacing the "Advanced" framing.

    4. REWORD PLUGIN_SPOTON_CLIENT_ID_DESC to remove the "optional advanced setting" framing. New English value: "SpotOn ships a bundled Client ID that works out of the box. Creating your own Spotify Developer App gives you a private API quota with no shared rate limits." All 11 genuine translations.

    5. REWORD PLUGIN_SPOTON_DEFAULT_AUTH_INFO to focus on the two-step setup without the "own API quota" claim (it IS shared). New English value: "SpotOn works out of the box — no Spotify Developer App needed. Two authorizations complete the setup: connect your account, then authorize playback." All 11 genuine translations.

    6. REWORD PLUGIN_SPOTON_CUSTOM_APP_TITLE to read "Manage Custom Spotify App" (shown when a custom ID is already configured). All 11 genuine translations.

    Update t/02_strings.t: add the 3 new keys (BUNDLED_SHARED_QUOTA_HINT, CUSTOM_QUOTA_OK, CUSTOM_APP_SETUP) to the required-keys list. Position them adjacent to existing CUSTOM_APP_TITLE entry.
  </action>
  <acceptance_criteria>
    - strings.txt contains PLUGIN_SPOTON_BUNDLED_SHARED_QUOTA_HINT with all 11 language values
    - strings.txt contains PLUGIN_SPOTON_CUSTOM_QUOTA_OK with all 11 language values
    - strings.txt contains PLUGIN_SPOTON_CUSTOM_APP_SETUP with all 11 language values
    - PLUGIN_SPOTON_CLIENT_ID_DESC EN value no longer contains "optional advanced"
    - PLUGIN_SPOTON_DEFAULT_AUTH_INFO EN value no longer contains "own API quota"
    - PLUGIN_SPOTON_CUSTOM_APP_TITLE EN value reads "Manage Custom Spotify App"
    - No EN value used as fallback for any other language
    - t/02_strings.t lists the 3 new keys in the required-keys array
    - `cd /home/sti/spoton && perl t/02_strings.t` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton &amp;&amp; perl t/02_strings.t 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    3 new i18n keys and 3 reworded keys across all 11 languages with genuine translations. String test passes. UX messaging flipped from "bundled is default, custom is advanced" to "bundled works but is shared, custom is recommended."
  </done>
</task>

<task type="auto">
  <name>Task 3: TROUBLESHOOTING.md rate-limit section + forum reply (D-05)</name>
  <files>TROUBLESHOOTING.md, ~/SynologyDrive/forum-reply-267.txt</files>
  <read_first>
    - TROUBLESHOOTING.md (lines 82-93 for existing HTTP 400/empty results section, lines 120-144 for mDNS section — new section goes between these)
    - ~/SynologyDrive/forum-reply-267.txt (current draft to replace)
    - .planning/phases/67-bundled-id-auth-redirect-ux/67-CONTEXT.md (Evidence section for A/B test data)
  </read_first>
  <action>
    1. TROUBLESHOOTING.md: Add a new section "### Frequent rate limiting (HTTP 429)" between the "Browse/Search return HTTP 400" section and the "Made For You playlists missing" section. Content covers:
       - Symptom: OPML menus show "Spotify requests throttled -- please wait", Status page shows 429 count, playback or browsing interrupted
       - Cause: The bundled Client ID (d420a117...) is shared across all SpotOn installations and all ncspot users worldwide. Under peak usage, the shared API quota can be exhausted, triggering Spotify's 429 rate limiter.
       - Solution 1 (recommended): Create your own Spotify Developer App at developer.spotify.com/dashboard. Enter the Client ID in SpotOn Settings. Your own app has a private rate-limit bucket that is not shared with anyone. The setup wizard in SpotOn Settings walks through the steps.
       - Solution 2: If 429s are intermittent and brief, SpotOn automatically backs off and retries — waiting a few seconds usually resolves it.
       - Note: Since v3.6.0, SpotOn skips the startup limit probe when using the bundled ID, saving 8 API requests from the shared quota on every LMS restart.

    2. D-05 forum reply: Rewrite ~/SynologyDrive/forum-reply-267.txt with the corrected diagnosis. The reply should address @agriff79 directly (@agriff79 as first line per forum convention), explain that the bundled Client ID is shared across all installations and ncspot users, that A/B testing confirmed it produces 429s under load while a private ID does not, and recommend creating a personal Client ID via SpotOn Settings wizard. Mention that the next release will add a prominent hint in Settings about this. Keep the tone helpful and concise. Language: English (per project convention for forum replies).
  </action>
  <acceptance_criteria>
    - TROUBLESHOOTING.md contains a "### Frequent rate limiting (HTTP 429)" section
    - The section mentions the shared ncspot bucket as root cause
    - The section recommends creating a personal Spotify Developer App as the primary solution
    - ~/SynologyDrive/forum-reply-267.txt starts with @agriff79
    - Forum reply explains the shared-bucket cause and recommends a personal Client ID
    - Forum reply is in English
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton &amp;&amp; grep -q "Frequent rate limiting" TROUBLESHOOTING.md &amp;&amp; grep -q "shared" TROUBLESHOOTING.md &amp;&amp; grep -q "@agriff79" ~/SynologyDrive/forum-reply-267.txt &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    TROUBLESHOOTING.md has a dedicated rate-limit section explaining the shared-bucket problem and recommending a personal Client ID. Forum reply 267 updated with correct diagnosis per D-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form → Perl handler | clientId user input sanitized (existing 32-char alnum check) |
| Bundled Client ID → Spotify API | Shared quota, no new trust boundary — existing mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-01 | Denial of Service | probeEndpointLimits bundled skip | low | mitigate | Hardcoded limits match Spotify's documented Extended Quota values; force-probe override preserved for manual re-check |
| T-67-02 | Information Disclosure | forum-reply-267.txt | low | accept | Reply discusses publicly known ncspot Client ID (open-source project, ID is in public repos) — no secret disclosed |
</threat_model>

<verification>
1. `grep "'login'" Plugins/SpotOn/Settings.pm` confirms D-01 fix present
2. `perl t/02_strings.t` passes with new keys
3. `perl t/05_perl_syntax.t` passes (no syntax errors in modified Perl files)
4. `grep "Frequent rate limiting" TROUBLESHOOTING.md` confirms new section
5. `grep "BUNDLED_SHARED_QUOTA_HINT" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` confirms template wired
</verification>

<success_criteria>
- /login handler uses plain string (not regex) — verified by grep
- Bundled Client ID skips probe, hardcodes known limits — verified by grep + test
- Settings page clearly communicates shared vs private quota — verified by template inspection
- 3 new + 3 reworded i18n keys across 11 languages — verified by perl t/02_strings.t
- TROUBLESHOOTING.md has rate-limit guidance — verified by grep
- Forum reply updated — verified by file content check
</success_criteria>

## Artifacts this phase produces

**Symbols / Exports:**
- No new exported symbols

**Modified interfaces:**
- `Client.pm::probeEndpointLimits` — new early-return branch for bundled ID (callers unchanged)
- `basic.html` — Client ID section restructured (no new template variables, uses existing clientIdMode)

**New string keys:**
- `PLUGIN_SPOTON_BUNDLED_SHARED_QUOTA_HINT`
- `PLUGIN_SPOTON_CUSTOM_QUOTA_OK`
- `PLUGIN_SPOTON_CUSTOM_APP_SETUP`

**Reworded string keys:**
- `PLUGIN_SPOTON_CLIENT_ID_DESC`
- `PLUGIN_SPOTON_DEFAULT_AUTH_INFO`
- `PLUGIN_SPOTON_CUSTOM_APP_TITLE`

<output>
Create `.planning/phases/67-bundled-id-auth-redirect-ux/67-01-SUMMARY.md` when done
</output>
