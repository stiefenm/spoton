---
phase: 70-jivelite-pagination-fill-gh-157
plan: 03
type: execute
wave: 3
depends_on: [70-01, 70-02]
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - CHANGELOG.md
autonomous: true
requirements: [JVL-03, JVL-04, JVL-05, JVL-08]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "_searchTypeFeed and _podcastSearchTypeFeed answer a JiveLite-style request (quantity=210) with multi-page fills using the nested type-key total (JVL-03) — up to 21 sequential calls in dev mode (limit 10), 5 with the bundled ID (limit 50)"
    - "R-4 nameless->ignore placeholder mapping and R-5 offset caps (1000; 50 for shows/episodes in dev mode) still hold over the accumulated multi-page result"
    - "SpotOnAddToPlaylist lists up to 210 playlists per JiveLite block instead of 50 (JVL-04)"
    - "_albumFeed at offset 0 with quantity exceeding the embedded getAlbum tracks continues fetching via getAlbumTracks until quantity or album end is reached (JVL-05)"
    - "Play-all, Material Skin, and Classic Web paths through all four functions behave exactly as before (JVL-08)"
  artifacts:
    - "Plugins/SpotOn/Plugin.pm: converted _searchTypeFeed, _podcastSearchTypeFeed, SpotOnAddToPlaylist, _albumFeed"
    - "CHANGELOG.md: [Unreleased] Fixed entry for GH #157"
  key_links:
    - "search feeds -> _fetchPages extractItems/extractTotal closures over $data->{$key}{items}/{total}"
    - "_albumFeed offset-0 browse -> embedded seed + _fetchPages(startOffset => seedCount, maxItems => $qty - seedCount) continuation"
    - "advertised search total -> min(helper safeTotal, R-5 cap)"
---

<objective>
Convert the four non-uniform feed functions — the two search drill-downs (nested response keys), the add-to-playlist picker, and the album tracklist (embedded-seed special case) — completing the 11-function conversion, and record the fix in CHANGELOG.md.

Purpose: finishes GH #157 — after this plan every SpotOn menu JiveLite can open serves complete 200-item blocks.

Output: 4 converted functions + CHANGELOG entry + full-conversion sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-jivelite-pagination-fill-gh-157/70-01-SUMMARY.md
@.planning/phases/70-jivelite-pagination-fill-gh-157/70-02-SUMMARY.md
@Plugins/SpotOn/Plugin.pm
</context>

## Artifacts this phase produces

See 70-01-PLAN.md for the full phase artifact list. This plan produces:

- Converted `_searchTypeFeed`, `_podcastSearchTypeFeed` (with `extractTotal` for nested keys), `SpotOnAddToPlaylist`, `_albumFeed` (embedded-seed + continuation).
- CHANGELOG.md `[Unreleased]` entry for GH #157.

<tasks>

<task type="auto">
  <name>Task 1: Convert _searchTypeFeed + _podcastSearchTypeFeed with extractTotal (JVL-03)</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm: `_searchTypeFeed` (grep `sub _searchTypeFeed`; note %typeToKey, %typeToItem, R-4 ignore mapping, R-5 1000 cap, error shape WITH `offset => $offset, total => 0`) and `_podcastSearchTypeFeed` (grep; note show/episode key map, R-5 per-type maxOffset 50/1000)
    - Plugins/SpotOn/Plugin.pm: `_fetchPages` header comment (extractTotal contract from 70-01)
  </read_first>
  <action>
    Neither sub has a play-all/cache branch — the single `Client->search` call IS the whole body. Convert each to one `_fetchPages` call:

    Shared changes for both subs:
    - Add `my $qty = $args->{quantity} || 200;` next to the existing `$offset` line (these subs currently ignore quantity entirely).
    - Compute `$key` from the existing type map BEFORE the fetch (it is needed by the closures).
    - `_fetchPages({ accountId => $accountId, apiFn => ..., pageLimit => Plugins::SpotOn::API::Client->getLimit('search'), startOffset => $offset, maxItems => $qty, extractItems => sub { ($_[0]->{$key} || {})->{items} || [] }, extractTotal => sub { ($_[0]->{$key} || {})->{total} }, done => ... })`.
    - apiFn closure: `sub { my ($acct, $params, $cb) = @_; Plugins::SpotOn::API::Client->search($acct, { q => $query, type => $type, limit => $params->{limit}, offset => $params->{offset} }, $cb) }`.
    - done receives `($allItems, $err, $total)`. Error-and-empty keeps each sub's EXISTING error shape verbatim: `$callback->({ items => [ _authRequiredItem($client, $accountId, $err) ], offset => $offset, total => 0 })`.
    - Run the existing R-4 mapping (nameless entries -> `{ ignore => 1 }`) over `@$allItems` exactly as it currently runs over the single page's `$resultItems`; keep the `$realCount`/NO_RESULTS-at-offset-0 logic.
    - R-5 cap applied AFTER the helper total: `_searchTypeFeed` caps `$total` at 1000; `_podcastSearchTypeFeed` caps at `($type eq 'show' || $type eq 'episode') ? 50 : 1000`. Keep both R-5 comments. Note the cap composes safely with JVL-06: the helper's safeTotal is clamped low on partial fills, the cap only ever lowers it further.
    - `_searchTypeFeed` keeps its %typeToItem dispatch (`_trackItem`/`_albumItem`/`_artistItem`/`_playlistItem`); `_podcastSearchTypeFeed` keeps its show/episode dispatch to `_showItem`/`_episodeItem($client, $_, undef)`.
    - Update both header comments: bounded multi-page fill for fixed-size block clients (GH #157); dev-mode search limit 10 means up to 21 sequential pages per 210-block — accepted, throttled by Client.pm.
  </action>
  <verify>
    <automated>perl t/05_perl_syntax.t && perl t/23_search_history.t && perl t/25_fetch_pages.t</automated>
  </verify>
  <acceptance_criteria>
    - All three test files pass
    - Both subs: exactly one `Plugins::SpotOn::API::Client->search(` call remains, and it sits inside the apiFn closure passed to `_fetchPages` (inspect via `sed -n '/^sub _searchTypeFeed/,/^}$/p'` and same for `_podcastSearchTypeFeed`)
    - Both subs pass an `extractTotal` closure reading the nested `{$key}` total
    - `_multiTypeSearch` (the overview aggregator) is unchanged — `git diff` shows no hunk touching it
    - R-5 cap lines present in both subs; R-4 ignore-mapping present in both subs
  </acceptance_criteria>
  <done>Both search drill-down feeds page through nested-key responses honoring offset/quantity, with R-4/R-5 semantics preserved over accumulated results.</done>
</task>

<task type="auto">
  <name>Task 2: Convert SpotOnAddToPlaylist (JVL-04) + _albumFeed embedded-seed continuation (JVL-05)</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm lines 981-1018 pre-phase (`SpotOnAddToPlaylist` — NOTE: signature is ($client, $cb, $params, $args); index/quantity come from $params, not $args; error shape is a NO_RESULTS textarea, not _authRequiredItem)
    - Plugins/SpotOn/Plugin.pm: `sub _albumFeed` complete (grep; four branches: play-all with inline seed paginator, cache-slice, offset==0 browse via getAlbum embedded tracks, offset>0 browse via getAlbumTracks)
  </read_first>
  <action>
    **SpotOnAddToPlaylist (JVL-04):** replace the single `getUserPlaylists` call with `_fetchPages({ accountId => $accountId, apiFn => sub { my ($acct, $params2, $cb2) = @_; Plugins::SpotOn::API::Client->getUserPlaylists($acct, $params2, $cb2) }, pageLimit => Plugins::SpotOn::API::Client->getLimit('library'), startOffset => $offset, maxItems => $qty, done => ... })`. Delete the dead `$limit` ternary. done: when `!@$allItems && $err` keep the existing shape `$cb->({ items => [{ name => cstring($client, 'PLUGIN_SPOTON_NO_RESULTS'), type => 'textarea' }] })`; otherwise run the existing per-playlist loop (id guard, `_isMadeForYou` skip, `_doAddToPlaylist` passthrough hash verbatim) over `@$allItems` and fire `$cb->({ items => \@items, offset => $offset, total => $total })` with the helper's `$total`.

    **_albumFeed (JVL-05):** convert ONLY the two browse branches; the play-all branch (inline seed paginator) and the cache-slice `elsif` stay byte-identical (JVL-08 — do not refactor them onto _fetchPages in this phase).

    (a) `elsif ($offset == 0)` initial browse branch: after `getAlbum` succeeds, let `$seedCount = scalar(@$tracks)`. If `$total <= $seedCount` OR `$qty <= $seedCount`: keep the existing behavior exactly (map seed via `_albumTrackItem` with the album's images/artist/name/release_date, NO_RESULTS on empty, `$callback->({ items => \@items, offset => 0, total => $total })`). Otherwise (JiveLite case — quantity 210 exceeds the <=50 embedded tracks and the album has more): continue with `_fetchPages({ accountId, apiFn => sub wrapping getAlbumTracks($acct, $albumId, $params, $cb), pageLimit => Plugins::SpotOn::API::Client->getLimit('album_tracks'), startOffset => $seedCount, maxItems => $qty - $seedCount, done => ... })`. In that done, build the combined list `(@$tracks, @$fetched)`, map ALL of it via `_albumTrackItem($client, $_, $images, $artist0, $album->{name}, { defer_cache => \@deferredMeta, albumReleaseDate => $album->{release_date} // '' })`, push NO_RESULTS if empty, and fire `$callback->({ items => \@items, offset => 0, total => $combinedTotal })` followed by the deferred-meta flush (FIX-01 — up to 210 items). `$combinedTotal`: the helper's 3rd arg is already in absolute track coordinates (startOffset = seedCount), so on error it equals seedCount + fetchedCount (JVL-06 clamp holds) and on success it is the album's true total — use it directly.

    (b) final `else` (offset>0 browse pages): replace the single `getAlbumTracks` call with `_fetchPages({ startOffset => $offset, maxItems => $qty, pageLimit => Plugins::SpotOn::API::Client->getLimit('album_tracks'), apiFn => the same hoisted getAlbumTracks closure, done => ... })`. Hoist that closure once above the branch structure so branches (a) and (b) share it. done: error-and-empty -> existing `_authRequiredItem` shape; map via `_albumTrackItem($client, $_, $albumImages, $albumArtist, $albumName, { albumReleaseDate => $albumReleaseDate })` (passthrough context, exactly as the current else does); keep the empty-success NO_RESULTS push; callback with `offset => $offset, total => $total`.

    Delete `_albumFeed`'s dead `$limit` ternary. Update both subs' header comments (GH #157 bounded fill; for _albumFeed document the seed+continuation rule).
  </action>
  <verify>
    <automated>perl t/05_perl_syntax.t && perl t/25_fetch_pages.t</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass
    - `SpotOnAddToPlaylist`: `_fetchPages({` present; `_isMadeForYou` skip and `_doAddToPlaylist` passthrough hash unchanged; error path still a NO_RESULTS textarea (not _authRequiredItem)
    - `_albumFeed`: `getAlbumTracks` appears in the sub only inside the play-all branch's inline paginator (unchanged) and the single hoisted browse closure; the offset==0 branch contains a `_fetchPages({` call with `startOffset => $seedCount`-style seeding; the play-all branch and cache-slice `elsif` show no diff in `git diff`
    - `_albumFeed` offset==0 short-circuit preserved: when the embedded tracks already cover the request, zero getAlbumTracks calls are made (source inspection of the `$total <= $seedCount || $qty <= $seedCount` guard)
  </acceptance_criteria>
  <done>Playlist picker and album tracklist serve full JiveLite blocks; embedded-seed shortcut still avoids redundant API calls; play-all album path untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full-conversion sweep + CHANGELOG entry</name>
  <files>Plugins/SpotOn/Plugin.pm, CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-15 (current `[Unreleased]` section — empty as of planning)
    - Plugins/SpotOn/Plugin.pm (converted state after Tasks 1-2)
  </read_first>
  <action>
    **Sweep (JVL-02/03/04/05 completeness):** verify all 11 target functions are converted by listing every direct `Plugins::SpotOn::API::Client->` data-fetch invocation inside the 11 subs (`_savedTracksFeed`, `_savedAlbumsFeed`, `_savedShowsFeed`, `_showFeed`, `_userPlaylistsFeed`, `_playlistFeed`, `_artistAlbumsFeed`, `_albumFeed`, `_searchTypeFeed`, `_podcastSearchTypeFeed`, `SpotOnAddToPlaylist`) and confirming each sits inside an apiFn closure, the _albumFeed play-all inline paginator, or a getAlbum metadata fetch. Fix any straggler found. Then run the full local test suite subset that exercises Plugin.pm: t/05, t/23, t/25.

    **CHANGELOG:** under `## [Unreleased]` add a `### Fixed` heading (currently absent) with one entry: JiveLite / piCorePlayer lists no longer show empty rows past the first API page — all Browse, Library, Search, and Add-to-Playlist menus now fill JiveLite's fixed 200-item blocks with bounded multi-page fetches, and partial failures report a truncated-but-consistent item count instead of unfillable positions. Reference `([#157](https://github.com/stiefenm/spoton/issues/157))`. English, style matching the 3.5.7 entries. Note: plan 70-04 (wave 1) already added an icon entry under `[Unreleased]` `### Fixed` — if that heading now exists, append to it rather than duplicating it.
  </action>
  <verify>
    <automated>perl t/05_perl_syntax.t && perl t/23_search_history.t && perl t/25_fetch_pages.t && grep -c "#157" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - All three tests pass; `grep -c "#157" CHANGELOG.md` returns >= 1
    - `grep -c '_fetchPages({' Plugins/SpotOn/Plugin.pm` returns >= 11 (10 converted else/browse sites + _albumFeed continuation; wrapper delegation adds one more)
    - CHANGELOG.md has exactly one `### Fixed` heading under `[Unreleased]`
    - Sweep documented in the SUMMARY: per-function conversion status table for all 11 functions
  </acceptance_criteria>
  <done>All 11 functions verified converted; user-facing CHANGELOG entry for GH #157 recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search query -> Spotify API | Query strings pass through unchanged (existing URI encoding in Client.pm) |
| LMS plugin -> Spotify Web API | Untrusted nested-key JSON drives search pagination |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-05 | DoS | search multi-page fill (dev mode limit 10 -> 21 calls/block) | medium | mitigate | maxItems bound + R-5 offset caps + Client.pm inflight=1 sequencing + 300s search cache; 429s handled by existing deferral (#155) |
| T-70-06 | Tampering | nested search response shape | low | mitigate | extractItems/extractTotal closures null-guard every level (`($_[0]->{$key} || {})`) |
</threat_model>

<verification>
- t/05, t/23, t/25 pass
- Manual (dev LMS, post-phase UAT): JiveLite or `spottyDebug`-style CLI request with `index 0, quantity 210` against a >50-item Liked Songs list returns 210 items; Material Skin browse + play-all and Classic Web paging still work (JVL-08)
</verification>

<success_criteria>
- Search, podcast search, add-to-playlist, and album feeds fill JiveLite blocks (JVL-03/04/05)
- R-4/R-5, MFY filter, embedded-seed shortcut, play-all paths preserved (JVL-08)
- CHANGELOG documents GH #157
</success_criteria>

<output>
Create `.planning/phases/70-jivelite-pagination-fill-gh-157/70-03-SUMMARY.md` when done, including the 11-function conversion status table.
</output>
