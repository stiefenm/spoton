---
phase: 70-jivelite-pagination-fill-gh-157
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png
  - CHANGELOG.md
autonomous: true
requirements: [JVL-09]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "recently.png and madeforyou.png are grey-gradient glyphs legible on Material Skin's light theme, byte-identical to the verified assets from commit 30f8b7b (JVL-09)"
    - "CHANGELOG documents the icon fix under [Unreleased] referencing GH #124"
  artifacts:
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png (9957 bytes, from 30f8b7b)"
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png (18561 bytes, from 30f8b7b)"
    - "CHANGELOG.md [Unreleased] Fixed entry for GH #124"
  key_links:
    - "git cherry-pick -n 30f8b7b -> two staged PNGs, no code changes"
---

<objective>
Recover the icon contrast fix from the reverted Phase 69 branch: the grey-gradient regenerations of the Recently Played and Made For You home icons (GH #124) live in commit `30f8b7b` on branch `phase-69-attempt-1` and were lost when Phase 69 was reverted. Cherry-pick the binary asset changes and record them in the CHANGELOG.

Purpose: both icons are currently white silhouettes — invisible on the Material Skin light theme. The regenerated assets were already PIL-contract-verified on the branch (white_frac=0.000, max_lum<=229, 512x512).

Output: two regenerated PNGs + CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CHANGELOG.md
</context>

## Artifacts this phase produces

See 70-01-PLAN.md for the full phase artifact list. This plan produces:

- Regenerated `Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png` and `.../madeforyou.png` (binary, cherry-picked from `30f8b7b`).
- CHANGELOG.md `[Unreleased]` `### Fixed` entry for GH #124.

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick icon assets from phase-69-attempt-1</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png, Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png</files>
  <precondition>Commit 30f8b7b exists locally (`git cat-file -e 30f8b7b` succeeds; it lives on branch phase-69-attempt-1)</precondition>
  <read_first>
    - Output of `git show --stat 30f8b7b` (confirms the commit touches exactly the two PNGs: madeforyou.png 8436 -> 9957 bytes, recently.png 14896 -> 18561 bytes)
  </read_first>
  <action>
    Implements JVL-09. Run `git cherry-pick -n 30f8b7b` (no-commit: stages the two binary changes without importing the Phase-69 commit message). Verify the working tree now matches the branch assets: `git diff phase-69-attempt-1 -- Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png` must produce empty output, and the two files must be 9957 and 18561 bytes respectively. Commit with this phase's own message (fix scope `70-04`, reference GH #124), per the standard per-task commit flow. No code changes — the commit must contain exactly the two PNG paths.
  </action>
  <verify>
    <automated>git diff phase-69-attempt-1 -- Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png | wc -l | grep -qx 0 && stat -c%s Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/madeforyou.png Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/recently.png</automated>
  </verify>
  <acceptance_criteria>
    - `git diff phase-69-attempt-1 -- <both PNG paths>` is empty (byte-identical to the verified branch assets)
    - File sizes: madeforyou.png == 9957 bytes, recently.png == 18561 bytes
    - `git show --stat HEAD` (after commit) lists exactly the two PNG paths, no other files
  </acceptance_criteria>
  <done>Both home icons in the working tree are the verified grey-gradient assets from 30f8b7b, committed with a Phase-70 message.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry for the icon fix</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-15 (`[Unreleased]` currently has no subsections)
    - Reference wording: `git show 964f18c --format= -- CHANGELOG.md` (the Phase-69 branch's icon CHANGELOG entry — reuse its Fixed line, drop the #131 sync-group line which belongs to the reverted Phase 69 work)
  </read_first>
  <action>
    Under `## [Unreleased]` add a `### Fixed` heading with the single icon entry, adapted from 964f18c: "Recently Played" and "Made For You" home icons legible on light theme — both icons are now grey-gradient glyphs matching the Top Tracks icon, fixing their invisibility as white silhouettes on the Material Skin light theme, referencing `([#124](https://github.com/stiefenm/spoton/issues/124))`. English, Keep-a-Changelog style matching existing entries. Do NOT include the #131 catch-up-window line from the branch commit — that feature was reverted and is not part of this phase.
  </action>
  <verify>
    <automated>grep -q "#124" CHANGELOG.md && ! sed -n '/## \[Unreleased\]/,/^## \[3/p' CHANGELOG.md | grep -q "#131"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#124" CHANGELOG.md` >= 1 with the new entry under `[Unreleased]` / `### Fixed`
    - The `[Unreleased]` section contains no reference to issue #131 (reverted Phase-69 scope)
    - Existing released sections (3.5.7 and older) unchanged
  </acceptance_criteria>
  <done>Icon fix documented under [Unreleased]; no reverted-scope leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local git branch -> main working tree | Binary assets imported from own-repo commit 30f8b7b (author: project owner, PIL-contract verified on branch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-07 | Tampering | cherry-picked PNGs | low | mitigate | byte-identity check against the reviewed branch commit (`git diff phase-69-attempt-1 -- <paths>` empty) pins content to the verified assets |
</threat_model>

<verification>
- Both PNGs byte-identical to 30f8b7b state; sizes 9957/18561 bytes
- CHANGELOG entry present, no #131 leakage
- Optional human check (post-phase UAT): open Material Skin light theme on dev LMS — both icons render as grey glyphs
</verification>

<success_criteria>
- JVL-09 delivered: verified grey-gradient icons restored on main-line work, documented in CHANGELOG
</success_criteria>

<output>
Create `.planning/phases/70-jivelite-pagination-fill-gh-157/70-04-SUMMARY.md` when done.
</output>
