---
phase: 70-jivelite-pagination-fill-gh-157
plan: 05
type: execute
wave: 1
depends_on: [70-03]
files_modified:
  - Plugins/SpotOn/Plugin.pm
autonomous: true
gap_closure: true
requirements: [JVL-05, JVL-06, JVL-08]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A mid-fill getAlbumTracks failure in _albumFeed's seed-continuation branch surfaces _authRequiredItem when the continuation delivered nothing, instead of silently reporting a truncated album as a successful total (closes VERIFICATION gap 1 / 70-REVIEW WR-01, JVL-06)"
    - "_albumFeed's offset>0 bounded fill defers metadata cache writes via the FIX-01 protocol, so paging into the second block of a large album never runs up to 210 synchronous Cache->set() calls inline before the LMS callback fires (closes gap 2 / WR-02, JVL-08 phase pattern)"
    - "_albumFeed's embedded-seed short-circuit delivers at most $qty items when the client requests fewer than the seeded track count, honoring the bounded-fill contract every other branch in the phase honors (closes gap 3 / WR-03, JVL-05)"
    - "All other _albumFeed branches (play-all inline paginator, cache-slice elsif) and every other converted feed remain byte-for-byte unchanged (JVL-08)"
  artifacts:
    - "Plugins/SpotOn/Plugin.pm: sub _albumFeed with all three branch defects fixed (~lines 3279-3337)"
  key_links:
    - "seed-continuation done callback -> _authRequiredItem on (!@$fetched && $err)"
    - "offset>0 done callback -> _albumTrackItem({ defer_cache => \\@deferredMeta }) -> _flushDeferredMeta after $callback"
    - "embedded-seed branch -> @{$tracks} sliced to min($qty, $seedCount) before mapping"
---

<objective>
Close the three verified defects in `_albumFeed` (the only feed of the 11 GH #157 conversions with gaps), applying the exact patterns this phase already established and applied in every sibling feed: the `_authRequiredItem` error guard, the FIX-01 deferred-metadata-cache protocol, and the bounded-fill quantity contract.

Purpose: 70-VERIFICATION.md scored 7/9 must-haves; truths #2 and #10 fail solely because of these three `_albumFeed` defects, all pre-diagnosed with concrete fixes in 70-REVIEW.md (WR-01, WR-02, WR-03) and never remediated. This plan applies those fixes verbatim so JVL-05/JVL-06/JVL-08 are fully satisfied and the phase can pass re-verification.

Output: Fixed `_albumFeed` in Plugins/SpotOn/Plugin.pm, all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-jivelite-pagination-fill-gh-157/70-VERIFICATION.md
@.planning/phases/70-jivelite-pagination-fill-gh-157/70-REVIEW.md
@Plugins/SpotOn/Plugin.pm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _albumFeed's three branch defects (WR-01 error guard, WR-02 defer_cache, WR-03 qty slice)</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm lines 3147-3339 (complete `sub _albumFeed` — all three defects live in the offset==0 browse path and the offset>0 else-branch; the play-all branch and cache-slice elsif at the top are reference-only and must not change)
    - Plugins/SpotOn/Plugin.pm lines 3314-3337 (the offset>0 branch — note it ALREADY has the correct `_authRequiredItem` guard; it is the in-function template for Fix 1, and Fix 2 adds the defer_cache protocol to it)
    - 70-REVIEW.md sections WR-01, WR-02, WR-03 (the exact fix specs, including the `!@$fetched` predicate rationale for WR-01)
  </read_first>
  <action>
    Three surgical edits inside `sub _albumFeed`, all within ~60 lines of each other. Every pattern being added already exists elsewhere in this same function or file — replicate, do not invent.

    **Fix 1 (WR-01 / gap 1) — seed-continuation error guard, ~line 3300:**
    In the `done => sub {...}` callback of the seed-continuation `_fetchPages` call (the one with `startOffset => $seedCount`), insert an error guard as the FIRST statements of the callback, before the `map`:
    guard on `if (!@$fetched && $err)`, fire `$callback->({ items => [ _authRequiredItem($client, $accountId, $err) ] })` and `return`.
    Predicate note from WR-01: `@$tracks` (the embedded seed) is guaranteed non-empty in this branch, so the guard must test `!@$fetched` — "the continuation call itself failed and delivered nothing" — NOT `!@items` (which would never be true here). When `@$fetched` is non-empty but `$err` is set, fall through: partial delivery with the JVL-06-clamped `$combinedTotal` is the correct behavior, matching `_fetchPages`'s contract. Everything after the guard (the existing `@deferredMeta` map, NO_RESULTS fallback, callback, flush) stays unchanged.

    **Fix 2 (WR-02 / gap 2) — FIX-01 deferred cache in the offset>0 branch, ~lines 3322-3336:**
    In the `else` branch's `done => sub {...}` callback (the one already carrying the `_authRequiredItem` guard):
    - Declare `my @deferredMeta;` after the guard.
    - Add `defer_cache => \@deferredMeta` to the opts hashref passed to `_albumTrackItem` (alongside the existing `albumReleaseDate` key).
    - Append `_flushDeferredMeta(undef, \@deferredMeta, 0) if @deferredMeta;` AFTER the `$callback->(...)` line — flush ordering matters: the LMS callback must fire before the deferred SQLite writes, exactly as the sibling seed-continuation branch (~line 3310) and `_savedTracksFeed`/`_playlistFeed` already do.
    - Optionally add the same one-line `# FIX-01: defer metadata cache writes.` comment used at the other call sites for consistency.

    **Fix 3 (WR-03 / gap 3) — slice embedded-seed short-circuit to $qty, ~line 3281:**
    In the `if ($total <= $seedCount || $qty <= $seedCount)` branch, deliver at most `$qty` items instead of all `$seedCount`. Compute `my $deliver = ($qty < $seedCount) ? $qty : $seedCount;` and map over the array slice `@{$tracks}[0 .. $deliver - 1]` instead of `@{$tracks}`. (`$qty` is always defined here — it defaults to 200 at the top of the sub.) Keep `total => $total` in the callback unchanged: advertising the real album total is what lets JiveLite request the next block; only the delivered item count is bounded.

    **Do NOT touch:** the play-all branch (inline `$fetchPage` paginator, lines ~3172-3252), the cache-slice `elsif` branch, the hoisted `$apiFn`, the `_fetchPages` helper itself, or any other feed function. 70-VERIFICATION.md confirmed all of those correct; this plan's diff must be confined to the three edits above.
  </action>
  <verify>
    <automated>perl t/05_perl_syntax.t && perl t/23_search_history.t && perl t/25_fetch_pages.t</automated>
  </verify>
  <acceptance_criteria>
    - `perl t/05_perl_syntax.t` passes (Plugin.pm compiles)
    - `perl t/23_search_history.t` passes (no behavior regression in loaded-plugin tests)
    - `perl t/25_fetch_pages.t` passes (the `_fetchPages` contract itself is untouched)
    - Fix 1: `sed -n '/^sub _albumFeed/,/^}/p' Plugins/SpotOn/Plugin.pm | grep -vE '^\s*#' | grep -c '_authRequiredItem'` returns 3 (was 2: play-all fallback + offset>0 guard; the new seed-continuation guard makes 3)
    - Fix 2: `sed -n '/^sub _albumFeed/,/^}/p' Plugins/SpotOn/Plugin.pm | grep -vE '^\s*#' | grep -c 'defer_cache'` returns 5 (was 4) and same pipeline with `_flushDeferredMeta` returns 5 (was 4)
    - Fix 3: `sed -n '/^sub _albumFeed/,/^}/p' Plugins/SpotOn/Plugin.pm | grep -vE '^\s*#' | grep -c 'deliver'` returns >= 2 (the `$deliver` computation and its use in the slice)
    - `git diff --stat` shows Plugins/SpotOn/Plugin.pm as the only modified source file, and `git diff` contains no hunks outside `sub _albumFeed`
    - Full suite regression: `prove -q t/` reports PASS (985+ tests)
  </acceptance_criteria>
  <done>All three 70-VERIFICATION.md gaps are closed: the seed-continuation branch surfaces auth errors like every sibling feed, the offset>0 branch defers metadata cache writes per FIX-01, the embedded-seed branch never delivers more than $qty items — with zero diff outside _albumFeed and the full test suite green.</done>
</task>

</tasks>

<verification>
- `perl t/05_perl_syntax.t`, `perl t/23_search_history.t`, `perl t/25_fetch_pages.t` all pass
- `prove -q t/` full-suite regression passes
- All three grep gates in the acceptance criteria hold (comment-filtered counts: 3/5/5, `$deliver` present)
- `git diff` confined to `sub _albumFeed` in Plugins/SpotOn/Plugin.pm
</verification>

<success_criteria>
- 70-VERIFICATION.md gaps 1-3 (WR-01/WR-02/WR-03) each have a corresponding source change matching 70-REVIEW.md's specified fix
- JVL-05 fully satisfied: embedded-seed + continuation fill with the same error-safety, cache-deferral, and bounded-fill guarantees as the other 10 converted feeds
- Re-verification of Phase 70 can score 9/9 must-haves
</success_criteria>

<output>
Create `.planning/phases/70-jivelite-pagination-fill-gh-157/70-05-SUMMARY.md` when done. Note explicitly which review finding each edit closed (WR-01/WR-02/WR-03) so re-verification can map gaps to commits. WR-04 (search-feed offset-ceiling clamp) remains intentionally out of scope — it is a non-blocking efficiency defect per 70-VERIFICATION.md, not a gap.
</output>
