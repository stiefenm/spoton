---
phase: 71-soloist-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Soloist.pm
  - t/05_perl_syntax.t
  - t/26_soloist_check.t
  - t/27_soloist_key.t
autonomous: false
requirements: [SOLO-BIN]
user_setup:
  - service: soloist-binary
    why: "Erste Implementierungsaufgabe braucht ein reales Soloist-Binary auf einer Linux-Dev-Maschine, um Download-Struktur, --version-Flag und Verhalten empirisch zu bestätigen (RESEARCH Pitfall 5 / A2 / A3)."
estimate:
  tokens: 70000
  raw_tokens: 44000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Soloist->get() liefert den gecachten Binary-Pfad wenn vorhanden, sonst undef — ohne Crash (D-03/D-04)"
    - "Der Arch-Map löst x86_64/aarch64/arm auf die korrekten download- und bindir-Strings auf (RESEARCH Pitfall 4)"
    - "Soloist->storeKey() schreibt cachedir/spoton/soloist/spak.key mit mode 0600 atomar (staging+rename+chmod, D-10/D-11)"
    - "Soloist->hasKey() liefert true genau dann wenn die Key-Datei existiert"
    - "SOLOIST_VERSION-Konstante beschreibt die validierte Version; ein nach Download geprüftes Binary mit abweichender Version wird nicht aktiviert (D-05, Pitfall 1)"
    - "perl -c Plugins/SpotOn/Soloist.pm besteht (in t/05_perl_syntax.t registriert)"
  artifacts:
    - Plugins/SpotOn/Soloist.pm
    - t/26_soloist_check.t
    - t/27_soloist_key.t
  key_links:
    - "DaemonManager (Plan 02) ruft Soloist->get() und Soloist->hasKey() auf"
    - "Settings.pm (Plan 03) ruft Soloist->storeKey(), Soloist->hasKey(), Soloist->get() auf"
    - "Soloist->libPath() liefert Bin/<bindir>/ für LD_LIBRARY_PATH (D-06, konsumiert in Phase 72)"
---

<objective>
Neues Modul `Plugins/SpotOn/Soloist.pm` als struktureller Zwilling von `Helper.pm` (D-02), aber mit cachedir-basierter Binary-Auflösung statt findbin (D-03/D-04). Dies ist der TRACER-Plan der Phase: er beweist die neue, tragende Architektur end-to-end (Arch-Erkennung → Download-URL → Cache-Pfad → Version-Check → Key-Datei) auf den frühen Kontext-Tokens des Executors, bevor die dünnen Wiring-Schichten (DaemonManager, Settings) in Plan 02/03 daran andocken.

Umgesetzte Decisions: D-01 (Feature-Branch soloist, Precondition), D-02 (eigenes Soloist.pm), D-03 (Auto-Download bei erster Aktivierung), D-04 (Cache-Dir Speicherort), D-05 (Versions-Pin-Konstante), D-10 (spak.key mode 0600), D-11 (Key-Datei-Schreiben durch Soloist.pm).

Purpose: Ohne ein funktionierendes Soloist.pm kann kein anderer Teil der Phase (Dispatch, Settings) etwas dispatchen oder anzeigen. Der Backend-Kern muss zuerst stehen und getestet sein.
Output: Soloist.pm mit get/download/version-check/storeKey/hasKey/keyPath/libPath, plus Wave-0 Test-Scaffolds.
</objective>

<precondition>
Aktueller git-Branch ist `soloist` (D-01: Soloist-Entwicklung auf Feature-Branch, Merge zu main erst bei v4.0 Release/Phase 75). `.planning/config.json` hat `git.branching_strategy: "none"` (RESEARCH Pitfall 6) — GSD legt den Branch NICHT automatisch an. Falls nicht auf `soloist`: mit `git checkout -b soloist` anlegen, dann fortfahren. Diese Vorbedingung steht in Konflikt zur Memory-Notiz "nur noch main Branch" — D-01 (neuere gelockte Decision) hat Vorrang.
</precondition>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-soloist-foundation/71-CONTEXT.md
@.planning/phases/71-soloist-foundation/71-RESEARCH.md
@.planning/phases/71-soloist-foundation/71-PATTERNS.md
@Plugins/SpotOn/Helper.pm
@Plugins/SpotOn/API/Credentials.pm
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <reversibility rating="one-way">D-05: Der Versions-Pin bestimmt alle künftigen Patch-Offsets (Lifetime, 24-Bit) und die Fake-libpulse-ABI-Annahmen; ein Wechsel erfordert Revalidierung aller Patches. Deshalb Human-Decision vor dem Festschreiben der Konstante.</reversibility>
  <decision>Welcher Soloist-Versions-String wird als validierte Version in `use constant SOLOIST_VERSION` gepinnt (D-05, one-way)?</decision>
  <context>
    Der Executor lädt einmalig ein reales Soloist-Binary auf einer Linux-Dev-Maschine (Download-URL aus RESEARCH: https://soloist-builds.spotifycdn.com/soloist_release_&lt;arch&gt;.tar.gz — [ASSUMED], per curl -I / tar tzf verifizieren, A2/A3), führt das tatsächliche Version-Flag aus (--version bzw. das per --help bestätigte Flag ohne --api-key, A5) und ermittelt den Versions-String. Die Konstante ist ein Datensatz "das haben wir validiert", KEIN URL-Parameter (Pitfall 1 — die Download-URL ist unversioniert). Der Pin bestimmt alle künftigen Patch-Offsets; ein späterer Wechsel erzwingt Revalidierung.
  </context>
  <options>
    <option id="pin-current">
      <name>Aktuell heruntergeladene Version pinnen (vom Executor ermittelter String)</name>
      <pros>Entspricht dem real getesteten Binary; Fake-libpulse-ABI gegen genau diese Version validierbar</pros>
      <cons>Muss bei künftigen Soloist-Builds manuell revalidiert werden (kein Auto-Update, D-05)</cons>
    </option>
    <option id="defer">
      <name>Pin verschieben bis manuelle Dev-Maschinen-Validierung abgeschlossen ist</name>
      <pros>Kein Festschreiben einer unbestätigten Version</pros>
      <cons>Blockiert Plan 01 Task 1 (Konstante wird dort benötigt)</cons>
    </option>
  </options>
  <resume-signal>Nenne den zu pinnenden SOLOIST_VERSION-String (z.B. "1.3.7.295") oder "defer"</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Soloist.pm Skeleton — Arch-Map, get(), Version-Check, libPath (end-to-end Kern)</name>
  <files>Plugins/SpotOn/Soloist.pm, t/05_perl_syntax.t, t/26_soloist_check.t</files>
  <read_first>
    - Plugins/SpotOn/Helper.pm (get/helperCheck/_versionCompare/init/Arch-Regex — 1:1 adaptieren, aber KEIN _findBin/findbin für Soloist)
    - .planning/phases/71-soloist-foundation/71-PATTERNS.md (Abschnitt Soloist.pm — Imports, Arch-Map, get()-Divergenz, Version-Compare verbatim)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Pattern 1, Pitfall 4/5, Code Examples Arch-Map; Anti-Pattern Shell-String)
    - t/05_perl_syntax.t (Struktur der @pm_files-Liste + Stub-Mechanik)
    - t/06_binary_check.t (skip-if-absent-Testmuster)
  </read_first>
  <behavior>
    - _arch() bildet osArch (Slim::Utils::OSDetect::details()->{osArch}) via Regex-Dispatch (/^aarch64/i, /^arm/i, /^x86_64/i) auf { download, bindir } ab: aarch64→{arm64, aarch64-linux}, arm→{arm32, armhf-linux}, x86_64→{x86_64, x86_64-linux}
    - _downloadUrl() setzt die Spotify-Download-URL aus dem download-Arch-String zusammen
    - _cacheDir() liefert catdir(serverPrefs cachedir, 'spoton', 'soloist', bindir); keyPath() liefert catfile(cachedir, 'spoton', 'soloist', 'spak.key') (NICHT arch-spezifisch, D-10)
    - get() konstruiert den erwarteten Binary-Pfad direkt (catfile(_cacheDir, 'soloist')), prüft -f && -x, validiert Version, gibt (path, version) oder undef zurück — ohne Crash wenn absent
    - _versionCheck() nutzt Array-Form open(my $fh,'-|', $binary, VERSION_FLAG) (KEIN Shell-String/Backtick), vergleicht gegen SOLOIST_VERSION; bei Abweichung wird NICHT aktiviert (D-05/Pitfall 1)
    - libPath() liefert catdir(plugin basedir, 'Bin', bindir) für LD_LIBRARY_PATH (D-06)
    - Nicht-Linux (main::ISWINDOWS || main::ISMAC): get() liefert sofort undef
  </behavior>
  <action>Erstelle Soloist.pm mit Package `Plugins::SpotOn::Soloist`, Imports analog Helper.pm (use strict/warnings, File::Spec::Functions catdir/catfile, Slim::Utils::Log, Slim::Utils::Prefs) plus `my $serverPrefs = preferences('server')` für cachedir. Übernimm `_versionCompare` verbatim aus Helper.pm. Implementiere Arch-Map als %ARCH_MAP Regex-Dispatch mit Feldern download/bindir gemäß PATTERNS.md. Die Version-Flag-Konstante (VERSION_FLAG) und SOLOIST_VERSION-Wert kommen aus dem Checkpoint-Ergebnis. WICHTIG: NICHT `Slim::Utils::Misc::findbin`/`_findBin`/`addFindBinPaths` verwenden (Anti-Pattern RESEARCH — Soloist liegt im cachedir). Registriere Soloist.pm in t/05_perl_syntax.t @pm_files (nach Helper.pm). Erzeuge t/26_soloist_check.t nach dem skip-if-absent-Muster von t/06, das den Arch-Map (Mock osArch → erwartete download/bindir-Paare) und die _downloadUrl-Konstruktion rein unit-testet (ohne reales Binary). Halte einen `init()`/`setChange`-Invalidierungshaken für den `backend`-Pref bereit (Cache-Invalidierung wie Helper.pm auf 'binary').</action>
  <verify>
    <automated>perl -I/usr/share/squeezeboxserver -c Plugins/SpotOn/Soloist.pm 2>&1 | grep -q "syntax OK" &amp;&amp; prove -I. t/26_soloist_check.t</automated>
  </verify>
  <acceptance_criteria>
    - `prove -I. t/05_perl_syntax.t` listet und prüft Soloist.pm grün (Datei in @pm_files vorhanden)
    - t/26_soloist_check.t assertiert: osArch 'aarch64' → download 'arm64' + bindir 'aarch64-linux'; 'armv7l'/'arm' → 'arm32'/'armhf-linux'; 'x86_64' → 'x86_64'/'x86_64-linux'
    - t/26 assertiert die vollständige Download-URL für mindestens einen Arch (Host `soloist-builds.spotifycdn.com`, Dateiname `soloist_release_<download>.tar.gz`)
    - Quellcode enthält KEINEN Aufruf von findbin/addFindBinPaths (grep -v '^#' Plugins/SpotOn/Soloist.pm | grep -c 'findbin\|addFindBinPaths' == 0)
    - Version-Check verwendet open '-|' Array-Form, kein Backtick/Shell-String (grep bestätigt kein `` ` `` um das Binary)
  </acceptance_criteria>
  <done>Soloist.pm kompiliert, Arch-Map + URL-Konstruktion + libPath end-to-end unit-getestet und committed; get() liefert undef ohne Crash wenn kein Binary da ist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Download-and-cache — SimpleAsyncHTTP saveAs, tar-Extract, Post-Download-Validierung (D-03/D-04)</name>
  <files>Plugins/SpotOn/Soloist.pm, t/26_soloist_check.t</files>
  <read_first>
    - Plugins/SpotOn/Soloist.pm (Task 1 Stand)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Pattern 2 saveAs, Standard Stack tar array-form, Pitfall 2/3, Assumptions A3)
    - Plugins/SpotOn/API/Credentials.pm (make_path mode 0700 Muster, Zeile ~790)
  </read_first>
  <behavior>
    - ensureBinary(): wenn get() undef und Linux → Download anstoßen; nicht-blockierend
    - Download via Slim::Networking::SimpleAsyncHTTP->new(cb, ecb, { saveAs => $archivePath, timeout => 30 })->get(_downloadUrl())
    - make_path(_cacheDir, { mode => 0700 }) vor Download
    - onComplete: Array-Form system('tar','xzf',$archivePath,'-C',$destDir) (NIE Shell-String — Injection-Guard); danach das Binary im Extract-Dir per Namenssuche `soloist` finden (Fallback falls Archiv ein Unterverzeichnis enthält, A3), chmod 0755, Post-Download-_versionCheck; nur bei bestandenem Check als aktiv markieren, sonst warnen und NICHT aktivieren (Pitfall 1/2 fail-closed)
    - onError: Warnung loggen, kein Crash, get() bleibt undef
    - Bereits gecachtes Binary wird NICHT überschrieben außer bei Cache-Clear (D-04 struktureller Schutz gegen Pitfall 1)
  </behavior>
  <action>Erweitere Soloist.pm um ensureBinary()/downloadBinary() und die Callback-Paare _onSoloistDownloadDone/_onSoloistDownloadError gemäß RESEARCH Pattern 2. Nutze File::Temp für den Staging-Archivpfad im cachedir. Extraktion ausschließlich Array-Form system() (T-71-04). Nach Extraktion Binary per rekursiver Namenssuche `soloist` lokalisieren (nicht auf flache Struktur verlassen, A3), ausführbar machen, dann _versionCheck() gegen SOLOIST_VERSION; bei Mismatch Binary nicht aktivieren und Warnung mit maskiertem Kontext loggen. Ergänze t/26 um einen Test, der die Extraktions-Zielpfad-Logik und die "überschreibe kein vorhandenes Binary"-Regel prüft (mit File::Temp::tempdir als Fake-cachedir, ohne echten Netzwerk-Download).</action>
  <verify>
    <automated>perl -I/usr/share/squeezeboxserver -c Plugins/SpotOn/Soloist.pm 2>&1 | grep -q "syntax OK" &amp;&amp; prove -I. t/26_soloist_check.t</automated>
  </verify>
  <acceptance_criteria>
    - Extraktion nutzt Array-Form system('tar','xzf',...) — grep bestätigt kein interpolierter tar-Shell-String
    - t/26 assertiert: bei bereits vorhandenem Binary im Fake-cachedir wird kein Re-Download/Overwrite ausgelöst (D-04)
    - onError-Pfad hinterlässt get() == undef ohne Exception (Test oder Code-Assertion)
    - Post-Download-Aktivierung ist an bestandenen _versionCheck gekoppelt (Code-Assertion: Aktivierung nur im version-ok-Zweig)
  </acceptance_criteria>
  <done>Download-Pipeline (saveAs → tar → find → version-check → aktivieren-oder-verwerfen) implementiert, fail-closed bei Fehler/Version-Mismatch, Tests grün, committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: spak-Key Storage — storeKey/hasKey/keyPath, atomarer 0600-Write (D-10/D-11)</name>
  <files>Plugins/SpotOn/Soloist.pm, t/27_soloist_key.t</files>
  <read_first>
    - Plugins/SpotOn/Soloist.pm (Task 1/2 Stand)
    - Plugins/SpotOn/API/Credentials.pm (installPairedCredentials atomic-write Zeilen ~785-805: staging-tempfile → unlink target → rename → File::Copy::move Fallback → chmod 0600)
    - .planning/phases/71-soloist-foundation/71-PATTERNS.md (storeKey()-Direktübersetzung, Validation-before-write)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Security Domain V5/V6, Code Example storeKey)
  </read_first>
  <behavior>
    - keyPath() → catfile(_cacheDir-Root spoton/soloist, 'spak.key') (NICHT arch-spezifisch)
    - storeKey($key): make_path(dir, {mode=>0700}); File::Temp staging → print → close → unlink target if -f → rename (Fallback File::Copy::move) → chmod(0600, target); Rückgabe (1) bzw. (0,'write_failed')
    - hasKey(): -f keyPath()
    - clearKey(): unlink keyPath() falls vorhanden (für Settings-Clear)
    - Der rohe Key wird NIE geloggt (Masking-Disziplin analog _maskAccountId, T-71-02)
  </behavior>
  <action>Ergänze Soloist.pm um storeKey/hasKey/keyPath/clearKey exakt nach dem Credentials.pm-Atomic-Write-Muster (staging+rename+chmod 0600). Die eigentliche Format-Validierung des eingegebenen Keys (Länge/Zeichensatz, D-11) passiert im Settings-Handler (Plan 03) VOR dem Aufruf — storeKey selbst schreibt defensiv, loggt aber niemals den Key-Inhalt. Erzeuge t/27_soloist_key.t mit File::Temp::tempdir als Fake-cachedir: storeKey schreiben, dann assertieren (stat(keyPath))[2] & 0777 == 0600 und hasKey() == wahr; clearKey() → hasKey() falsch.</action>
  <verify>
    <automated>prove -I. t/27_soloist_key.t</automated>
  </verify>
  <acceptance_criteria>
    - t/27 assertiert Datei-Modus exakt 0600 nach storeKey ((stat)[2] & 0777)
    - t/27 assertiert atomaren Pfad: kein Fenster mit target in Default-Modus (Write geht über staging+rename — Code-Assertion, kein in-place open '>')
    - grep bestätigt: kein $log-Aufruf, der die Key-Variable interpoliert (T-71-02)
    - hasKey/clearKey verhalten sich wie spezifiziert (Test grün)
  </acceptance_criteria>
  <done>spak.key wird atomar mit mode 0600 geschrieben/erkannt/gelöscht; Key nie im Log; t/27 grün, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spotify CDN → LMS-Host (Download) | Ungeprüftes Binary-Archiv überquert die Grenze (kein Checksum/Signatur) |
| Settings-Form → Soloist.pm (spak-Key) | Attacker-kontrollierbarer Key-String erreicht Datei-Write |
| Dateisystem (spak.key at rest) | Lokaler Secret; LMS-Threat-Model nimmt lokalen FS-Zugriff = vertrauenswürdig an |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Tampering | Soloist-Download (unversioniert, kein Checksum) | high | mitigate | HTTPS-Transport + Post-Download-_versionCheck (fail-closed: Binary mit unerwarteter Version wird nicht aktiviert); Restrisiko dokumentiert (kein Signatur-Mechanismus existiert, Pitfall 2) — in TROUBLESHOOTING vermerken (Phase 75) |
| T-71-02 | Information Disclosure | spak-Key im Log | high | mitigate | Key niemals loggen; Masking-Disziplin analog _maskAccountId; storeKey interpoliert Key nie in Log-Strings |
| T-71-03 | Information Disclosure | spak.key world-readable | medium | mitigate | chmod(0600) unmittelbar nach atomarem staging+rename — kein Fenster mit Default-Modus 0644 |
| T-71-04 | Tampering | tar-Extraktion Shell-Injection | high | mitigate | Array-Form system('tar','xzf',$path,'-C',$dest); nie Shell-String/Backtick (Anti-Pattern RESEARCH) |
| T-71-SC | Tampering | Supply-Chain Soloist-Binary | high | mitigate | Keine npm/pip/cargo-Installs in dieser Phase (RESEARCH: Package Legitimacy N/A). Binary-Bezug ist die einzige Supply-Chain-Fläche, abgedeckt durch T-71-01. Keine blocking-human-Checkpoints nötig (kein Package-Manager-Install). |
</threat_model>

<verification>
- `prove -I. t/05_perl_syntax.t t/26_soloist_check.t t/27_soloist_key.t` grün
- `perl -I/usr/share/squeezeboxserver -c Plugins/SpotOn/Soloist.pm` → syntax OK
- Manuell (Dev-Maschine, einmalig): realer Download + Extract + Version-Flag bestätigt A2/A3/A5
</verification>

<success_criteria>
Soloist.pm existiert als eigenständiges Modul mit get/ensureBinary/download/_versionCheck/storeKey/hasKey/keyPath/clearKey/libPath, cachedir-basiert (kein findbin), fail-closed bei Download/Version-Fehlern, spak.key mode 0600 atomar. Alle drei Soloist-Tests grün. SOLOIST_VERSION vom User gepinnt.
</success_criteria>

## Artifacts this phase produces (Plan 01)

- `Plugins::SpotOn::Soloist` (neues Modul)
- Konstanten: `SOLOIST_VERSION`, `VERSION_FLAG`, `%ARCH_MAP`
- Methoden/Subs: `init`, `get`, `ensureBinary`/`downloadBinary`, `_onSoloistDownloadDone`, `_onSoloistDownloadError`, `_versionCheck`, `_versionCompare`, `_arch`, `_downloadUrl`, `_cacheDir`, `keyPath`, `storeKey`, `hasKey`, `clearKey`, `libPath`
- Dateipfade (Laufzeit, nicht im Repo): `cachedir/spoton/soloist/<bindir>/soloist`, `cachedir/spoton/soloist/spak.key`
- Tests: `t/26_soloist_check.t`, `t/27_soloist_key.t`; Registrierung in `t/05_perl_syntax.t`

<output>
Create `.planning/phases/71-soloist-foundation/71-01-SUMMARY.md` when done
</output>
