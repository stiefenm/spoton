---
phase: 71-soloist-foundation
plan: 03
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
autonomous: true
requirements: [SOLO-BYOK]
estimate:
  tokens: 70000
  raw_tokens: 44000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Die Settings-Global-Sektion zeigt ein Backend-Dropdown (librespot | Soloist) neben Bitrate/Format (D-08)"
    - "Waehlt der User 'Soloist', erscheint das spak-Key-Feld + Soloist-Status live per JS-onchange ohne Reload (D-08)"
    - "Beim Speichern wird der backend-Wert gegen die Enum {librespot,soloist} whitelisted und der spak-Key format-validiert (Laenge/Zeichensatz) bevor Soloist->storeKey ihn schreibt (D-11)"
    - "Bei fehlenden Voraussetzungen (Binary/Key/nicht-Linux) zeigt Settings Warnungen, was fehlt - Aktivierung bleibt moeglich (D-09)"
    - "Der Default des backend-Prefs ist 'librespot'; ein Backend-Wechsel ruft DaemonManager->scheduleInit()"
  artifacts:
    - Plugins/SpotOn/strings.txt
  key_links:
    - "Settings.pm handler() ruft Soloist->storeKey/hasKey/clearKey/get (aus Plan 01)"
    - "Backend-Wechsel -> DaemonManager->scheduleInit() -> startHelper() Backend-Zweig (Plan 02)"
    - "Plugin.pm initPlugin registriert backend-Pref-Default + ruft Soloist->init()"
---

<objective>
BYOK-Bedienoberflaeche und Persistenz verdrahten: Backend-Dropdown in der bestehenden Global-Sektion (D-08), conditional spak-Key-Feld mit Live-JS-Toggle (D-08 - neues UI-Muster fuer dieses Codebase, RESEARCH Pattern 5), serverseitige Whitelist-/Format-Validierung (D-11), Soloist-Status-Warnungen (D-09), backend-Pref-Default + Soloist-Init in Plugin.pm. Alle neuen Strings in allen 11 Sprachen (i18n-Regel).

Umgesetzte Decisions: D-08 (Dropdown in Global-Sektion + conditional Key-Feld), D-09 (Warnungen bei fehlenden Voraussetzungen), D-11 (Key-Eingabe + Format-Validierung).

Purpose: Ohne diese Schicht kann der User kein Backend waehlen und keinen Key hinterlegen - die sichtbare Haelfte der Phase.
Output: Plugin.pm-Pref-Default + Soloist-Init, Settings.pm handler-Erweiterung, basic.html Dropdown/Feld/JS, strings.txt (11 Sprachen).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-soloist-foundation/71-CONTEXT.md
@.planning/phases/71-soloist-foundation/71-RESEARCH.md
@.planning/phases/71-soloist-foundation/71-PATTERNS.md
@Plugins/SpotOn/Settings.pm
@Plugins/SpotOn/Plugin.pm
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend-Pref-Default + Soloist->init in Plugin.pm; Settings.pm Save/Status-Handler</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/Settings.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (prefs->init Block 108-123; Helper->init Aufruf 131-132)
    - Plugins/SpotOn/Settings.pm (prefs() 106-110; handler() 112-220: bitrate-Whitelist 136-139, scheduleInit 148-149, pref_clientId Charset-Guard 155-172, sp_dc masked-preview 185-207, Status-Params 115-129)
    - Plugins/SpotOn/Soloist.pm (get/hasKey/storeKey/clearKey aus Plan 01)
    - .planning/phases/71-soloist-foundation/71-PATTERNS.md (Settings.pm Whitelist/scheduleInit/Status-Params-Direktuebersetzung)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Security Domain V5, Pitfall 7)
  </read_first>
  <action>Plugin.pm: In prefs->init den Default fuer den backend-Pref auf 'librespot' ergaenzen. Nach dem Helper->init()-Aufruf ein require + Soloist->init() einfuegen (Cache-Invalidierung auf backend-Pref-Change). Settings.pm handler(): (1) Status-Params fuer D-09 setzen - soloistMissing (string PLUGIN_SPOTON_SOLOIST_BINARY_MISSING wenn Soloist->get() leer), soloistKeyMissing (wenn Soloist->hasKey() falsch), soloistUnsupportedOS ((ISWINDOWS||ISMAC)?1:0), soloistVersion (aus Soloist->get()), plus aktueller backend-Wert fuers Template. (2) Im saveSettings-Block: backend gegen ein Hash %valid_backends mit Schluesseln librespot und soloist whitelisten (Default librespot bei Ungueltigkeit) und via prefs->set persistieren; danach DaemonManager->scheduleInit() (bestehendes Muster). (3) spak-Key analog pref_clientId/sp_dc: pref_soloistKey trimmen, gegen erlaubten Zeichensatz filtern (alphanumerisch plus die realen spak-Key-Zeichen; reales Format per Dev-Maschine bestaetigen, RESEARCH A1/Pitfall 5), Laenge cappen, Newlines und Shell-Metazeichen ablehnen (fail-closed, V5); nur bei nicht-leerem, gueltigem Wert Soloist->storeKey aufrufen; leere Eingabe bei vorhandenem Key -> clearKey (analog sp_dc WR-03). Der rohe Key darf nie ins gerenderte HTML zurueck (nur maskierte Vorschau oder leeres Feld). Den backend-Schluessel NICHT in die prefs()-Liste aufnehmen (manuell gespeichert wie clientId), damit LMS ihn nicht mit Rohinput ueberschreibt.</action>
  <verify>
    <automated>perl -I/usr/share/squeezeboxserver -c Plugins/SpotOn/Settings.pm 2>&1 | grep -q "syntax OK" &amp;&amp; perl -I/usr/share/squeezeboxserver -c Plugins/SpotOn/Plugin.pm 2>&1 | grep -q "syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm prefs->init enthaelt den backend-Default 'librespot' und ruft Soloist->init()
    - Settings.pm whitelistet backend gegen genau die Werte librespot und soloist vor prefs->set (Code-Assertion: %valid_backends)
    - spak-Key wird vor storeKey format-validiert (trim + Charset-Filter + Length-Cap + Ablehnung von Newline); leere Eingabe -> clearKey
    - Backend-Wechsel loest DaemonManager->scheduleInit() aus
    - Roher spak-Key erscheint in keinem $paramRef-Wert, der ins Template geht
  </acceptance_criteria>
  <done>backend-Pref existiert mit Default; Save-Pfad validiert backend + spak-Key fail-closed und persistiert via Soloist->storeKey; Status-Params gesetzt; beide Module kompilieren.</done>
</task>

<task type="auto">
  <name>Task 2: basic.html - Backend-Dropdown, conditional spak-Key-Feld, JS-onchange Live-Toggle, Status-Warnungen</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (Bitrate-select 298-304; pkce-fallback conditional div 126; Script-Block 358-384 als Ort/Konvention fuer inline-JS; BINARY_STATUS 285)
    - .planning/phases/71-soloist-foundation/71-PATTERNS.md (Dropdown-Direktuebersetzung, server-rendered conditional div, JS-onchange Gap)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Pattern 5 - Live-Toggle ist neues Muster)
  </read_first>
  <action>Fuege in der Global-Sektion nahe dem Bitrate-WRAPPER (298-304) einen WRAPPER mit Titel PLUGIN_SPOTON_BACKEND ein, der ein select mit name pref_backend und id pref_backend sowie Optionen librespot und Soloist enthaelt (selected-Markierung via TT IF-Vergleich gegen prefs.pref_backend, analog Bitrate). Darunter ein div mit id soloist-fields, dessen initiale Sichtbarkeit server-seitig gerendert wird (style display:block wenn prefs.pref_backend gleich soloist sonst display:none, Muster von pkce-fallback Zeile 126). Darin: (a) ein Text-Input mit name pref_soloistKey und maskierter Vorschau falls ein Key gespeichert ist; (b) Status-Zeilen, die die Warn-Params aus Task 1 anzeigen - soloistUnsupportedOS, soloistMissing (Binary fehlt), soloistKeyMissing (Key fehlt), sonst soloistVersion (D-09). Ergaenze im bestehenden Script-Block (ab 358) einen onchange-Handler auf das pref_backend-Element, der die display-Eigenschaft von soloist-fields live auf block bzw none setzt - dies ist neues Client-JS fuer dieses Template (RESEARCH Pattern 5, kein bestehendes Analog). Nutze die vorhandene inline-Vanilla-JS-Konvention (wie spotonPkceStart).</action>
  <verify>
    <automated>grep -q 'name="pref_backend"' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html &amp;&amp; grep -q 'id="soloist-fields"' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html &amp;&amp; grep -q "getElementById('pref_backend')" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</automated>
  </verify>
  <acceptance_criteria>
    - Template enthaelt ein select name=pref_backend mit Optionen librespot und soloist, korrekt vorselektiert aus prefs.pref_backend
    - div id=soloist-fields ist initial server-seitig sichtbar genau dann wenn prefs.pref_backend gleich soloist
    - Ein onchange-Handler auf pref_backend toggelt soloist-fields ohne Page-Reload (Live-Toggle)
    - Status-Zeilen decken alle vier D-09-Zustaende ab (unsupported OS / Binary fehlt / Key fehlt / Version vorhanden)
    - Das Key-Feld zeigt nie den rohen gespeicherten Key, nur eine maskierte Vorschau oder leer
  </acceptance_criteria>
  <done>Backend-Dropdown + conditional soloist-fields + Live-JS-Toggle + Status-Warnungen im Template; alle referenzierten String-Keys existieren (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: strings.txt - neue Soloist-Settings-Keys in allen 11 Sprachen</name>
  <files>Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/strings.txt (Format eines bestehenden Multi-Language-Blocks, z.B. PLUGIN_SPOTON_BITRATE / PLUGIN_SPOTON_BINARY_MISSING - Sprachliste EN/DE/FR/IT/ES/NL/NO/PL/RU/SV/CS o.ae.)
    - .planning/phases/71-soloist-foundation/71-CONTEXT.md (Terminologie: BYOK, spak-Key, Soloist)
  </read_first>
  <action>Ergaenze strings.txt um die neuen Keys, die Task 1/2 referenzieren: PLUGIN_SPOTON_BACKEND, PLUGIN_SPOTON_BACKEND_DESC, PLUGIN_SPOTON_SOLOIST_KEY (spak-Key-Feld-Label), PLUGIN_SPOTON_SOLOIST_KEY_DESC, PLUGIN_SPOTON_SOLOIST_BINARY_MISSING, PLUGIN_SPOTON_SOLOIST_KEY_MISSING, PLUGIN_SPOTON_SOLOIST_UNSUPPORTED_OS, PLUGIN_SPOTON_SOLOIST_STATUS (Versions-/Bereit-Anzeige). Jeder Key MUSS fuer JEDE Sprache, die im File bereits gefuehrt wird, eine echte Uebersetzung erhalten (i18n-Regel: nie EN als Fallback in andere Sprachen kopieren). Terminus 'Soloist' und 'spak-Key' bleiben unuebersetzt als Produktnamen; die umgebenden Saetze werden uebersetzt. Reihe die neuen Keys thematisch beim naechsten verwandten Block ein (Backend/Bitrate/Binary-Status).</action>
  <verify>
    <automated>prove -I. t/02_strings.t</automated>
  </verify>
  <acceptance_criteria>
    - t/02_strings.t bleibt gruen (Struktur/Sprach-Vollstaendigkeit valide)
    - Alle acht neuen Keys existieren und haben fuer jede im File gefuehrte Sprache eine Zeile (Zaehlung: pro Key gleiche Sprachanzahl wie ein Bestandskey)
    - Keine Sprache faellt auf den englischen Text zurueck (echte Uebersetzungen)
    - Alle in basic.html/Settings.pm referenzierten PLUGIN_SPOTON_SOLOIST_*- und PLUGIN_SPOTON_BACKEND-Keys sind definiert
  </acceptance_criteria>
  <done>Alle neuen Soloist-Settings-Strings in allen gefuehrten Sprachen vorhanden; t/02_strings.t gruen; keine undefinierten Template-Keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Settings-Form -> Settings.pm | backend-Auswahl und spak-Key als attacker-kontrollierbarer POST |
| Settings.pm -> Soloist.pm storeKey | validierter Key erreicht Datei-Write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-05 | Tampering | backend-Dropdown-Wert aus POST | medium | mitigate | Whitelist gegen Enum {librespot,soloist} vor prefs->set (Muster wie bitrate-Whitelist), unbekannte Werte -> librespot |
| T-71-06 | Input Validation | spak-Key aus POST | high | mitigate | Fail-closed Format-Validierung (trim + Charset-Filter + Length-Cap + Ablehnung Newline/Shell-Metazeichen) vor storeKey (V5, defense in depth obwohl Key nie shell-interpoliert wird) |
| T-71-02 | Information Disclosure | spak-Key im gerenderten HTML | high | mitigate | Nur maskierte Vorschau ins Template; roher Key round-trippt nie in $paramRef -> HTML |
| T-71-08 | Tampering | CSRF auf saveSettings | low | accept | Settings-Save nutzt das bestehende LMS CSRF-geschuetzte saveSettings-Formular (protectURI in page()); keine neue ungeschuetzte Endpoint-Flaeche |
</threat_model>

<verification>
- perl -c fuer Plugin.pm und Settings.pm -> syntax OK
- prove -I. t/02_strings.t und t/09_settings.t gruen
- Manuell: Settings-Seite laden, Backend auf Soloist stellen -> spak-Key-Feld erscheint live; Save -> Warnungen bei fehlendem Binary/Key
</verification>

<success_criteria>
Backend-Dropdown + conditional spak-Key-Feld mit Live-Toggle in der Global-Sektion; Save validiert backend (Whitelist) und spak-Key (fail-closed) und persistiert via Soloist->storeKey; D-09-Warnungen sichtbar; alle Strings in 11 Sprachen; keine Regression in Settings-Tests.
</success_criteria>

## Artifacts this phase produces (Plan 03)

- Plugin.pm: neuer Pref-Default `backend => 'librespot'`; Aufruf `Plugins::SpotOn::Soloist->init()`
- Settings.pm: %valid_backends Whitelist; Save-Handling fuer pref_backend und pref_soloistKey; Status-Params soloistMissing / soloistKeyMissing / soloistUnsupportedOS / soloistVersion
- basic.html: select#pref_backend, div#soloist-fields, input pref_soloistKey, onchange-Live-Toggle
- strings.txt: PLUGIN_SPOTON_BACKEND, PLUGIN_SPOTON_BACKEND_DESC, PLUGIN_SPOTON_SOLOIST_KEY, PLUGIN_SPOTON_SOLOIST_KEY_DESC, PLUGIN_SPOTON_SOLOIST_BINARY_MISSING, PLUGIN_SPOTON_SOLOIST_KEY_MISSING, PLUGIN_SPOTON_SOLOIST_UNSUPPORTED_OS, PLUGIN_SPOTON_SOLOIST_STATUS (alle Sprachen)

<output>
Create `.planning/phases/71-soloist-foundation/71-03-SUMMARY.md` when done
</output>
