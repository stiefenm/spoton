---
phase: 71-soloist-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c
  - Plugins/SpotOn/Bin/fake-libpulse/Makefile
  - .github/workflows/build-fake-libpulse.yml
autonomous: true
requirements: [SOLO-LIBPULSE]
user_setup:
  - service: soloist-binary-symbols
    why: "Die benoetigte PulseAudio-Symbol-Liste wird aus einem realen Soloist-Binary abgeleitet (nm -D | grep pa_); braucht eine Linux-Dev-Maschine mit einem heruntergeladenen Binary."
estimate:
  tokens: 65000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "fake-libpulse.c implementiert die PulseAudio-Symbole, die das Soloist-Binary importiert (spike-validiert: ~47 PA-Funktionen inkl. pa_threaded_mainloop_*), wobei pa_stream_write den S32LE/44100/Stereo-PCM an einen Ausgabe-FD weiterreicht (D-06)"
    - "Die CI baut libpulse.so.0 fuer x86_64, aarch64 und armhf gegen glibc (NICHT musl) und legt sie unter Bin/<arch>/ ab (D-06, RESEARCH Pitfall 3)"
    - "file(1) auf jeder gebauten .so zeigt einen glibc-Interpreter/dynamic-linker, keinen musl (Verifikationsschritt gegen Pitfall 3)"
  artifacts:
    - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c
    - Plugins/SpotOn/Bin/fake-libpulse/Makefile
    - .github/workflows/build-fake-libpulse.yml
  key_links:
    - "Die gebauten libpulse.so.0 landen in denselben Bin/<arch>/-Dirs, die build-librespot.yml bereits fuellt; Soloist->libPath() (Plan 01) zeigt via LD_LIBRARY_PATH dorthin (konsumiert Phase 72)"
---

<objective>
Die eigenstaendige, spike-validierte Fake-libpulse-Audio-Bruecke bauen und ausliefern (D-06). Dies ist die einzige genuin neue Engineering-Flaeche der Phase: eine schlanke C-Shared-Library, die die PulseAudio-Client-API stubt, sodass Soloist ohne PulseAudio-Server laeuft und seinen PCM-Stream (S32LE/44100/Stereo) direkt per FD abgreifbar macht. Anders als die bestehende librespot-Pipeline (cross/musl/Rust) braucht dieser C-Build glibc-Cross-Toolchains - eine strukturell andere CI-Job-Familie im selben Repo, die in dieselben Bin/<arch>/-Verzeichnisse ausliefert (RESEARCH Pitfall 3).

Dieser Plan hat KEINE Laufzeit-Kopplung an Plan 01/02/03 (reiner Batch/Build-Artefakt) und laeuft daher in Wave 1 parallel zu Plan 01.

Umgesetzte Decision: D-06 (Fake-libpulse per CI cross-compile, 3 Architekturen, im Plugin-Zip unter Bin/<arch>/, Soloist setzt LD_LIBRARY_PATH).

Purpose: Ohne diese .so kann der Soloist-Daemon (Phase 72) keinen Ton ausgeben. Das Artefakt muss frueh entstehen, damit Phase 72 daran andocken kann.
Output: fake-libpulse.c + Makefile + GitHub-Actions-Workflow (glibc cross-gcc Matrix).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-soloist-foundation/71-CONTEXT.md
@.planning/phases/71-soloist-foundation/71-RESEARCH.md
@.planning/phases/71-soloist-foundation/71-PATTERNS.md
@.github/workflows/build-librespot.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: fake-libpulse.c + Makefile - PulseAudio-Stub-Bibliothek (S32LE-PCM via FD)</name>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Bin/fake-libpulse/Makefile</files>
  <precondition>Ein reales Soloist-Binary liegt auf der Dev-Maschine vor (aus Plan 01 Checkpoint-Download), sodass die tatsaechlich importierte PulseAudio-Symbolmenge via `nm -D soloist | grep ' U pa_'` (bzw. `objdump -T`) abgeleitet werden kann - die Symbolliste wird nicht geraten.</precondition>
  <read_first>
    - .planning/ROADMAP.md (Active/v4.0 Spike Results: Fake-libpulse.so.0 ~250 LOC C, 47 PA-Funktionen inkl. pa_threaded_mainloop_*, Soloist gibt S32LE/44100/Stereo via pa_stream_write aus)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Architecture Diagram CI-Zweig, Pitfall 3 glibc-vs-musl, Standard Stack tar/gcc)
    - .planning/phases/71-soloist-foundation/71-PATTERNS.md (build-fake-libpulse Analog: Shape von build-librespot, NICHT die Toolchain)
  </read_first>
  <action>Leite die zu implementierende PulseAudio-Client-Symbolmenge aus einem realen Soloist-Binary ab (nm -D / objdump -T, alle undefinierten pa_*-Symbole). Schreibe fake-libpulse.c, das genau diese Symbole als Stubs bereitstellt - der Spike belegt ~47 Funktionen inkl. der pa_threaded_mainloop_*-Familie, pa_context_* (new/connect/get_state/set_state_callback), pa_stream_* (new/connect_playback/write/get_state/set_write_callback) und pa_*_free-Paare. Die tragende Funktion pa_stream_write nimmt den vom Soloist gelieferten S32LE/44100Hz/Stereo-PCM-Puffer und schreibt ihn an einen Ausgabe-Deskriptor (Default via Umgebungsvariable konfigurierbarer Ziel-FD/-Pfad, damit Phase 72 den Stream an den LMS-StreamServer koppeln kann); alle uebrigen Stubs geben plausible Erfolgs-/State-Werte zurueck, damit Soloists Threaded-Mainloop und State-Machine sauber durchlaufen (Spike-Verhalten). Schreibe ein Makefile, das mit `$(CC) -shared -fPIC -o libpulse.so.0 fake-libpulse.c -lpthread` baut und CC/CFLAGS per Variable ueberschreibbar laesst (fuer die Cross-Toolchains in Task 2). Fuege einen kurzen Header-Kommentar mit Herkunft (spike-validiert, D-06) und dem PCM-Format hinzu. Keine Referenz auf externe/private Pfade im Code.</action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse &amp;&amp; file Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0 | grep -qi "shared object"</automated>
  </verify>
  <acceptance_criteria>
    - fake-libpulse.c definiert alle pa_*-Symbole, die das reale Soloist-Binary als undefiniert importiert (Abgleich: nm -D soloist zeigt kein von der .so unerfuelltes pa_-Symbol)
    - pa_stream_write leitet den empfangenen Puffer an einen Ausgabe-FD/-Pfad weiter (Code-Assertion + optionaler Lauf: Bytes erscheinen am Ziel)
    - Der native x86_64-Build via make erzeugt eine ladbare shared object (file(1) bestaetigt ELF shared object)
    - Makefile erlaubt CC-Override (Cross-Compile-Vorbereitung)
    - Keine externen/privaten Pfad-Referenzen im Quellcode
  </acceptance_criteria>
  <done>fake-libpulse.c + Makefile bauen nativ eine libpulse.so.0, die Soloists PA-Symbolbedarf deckt und PCM per FD ausgibt; committed.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub-Actions-Workflow - glibc cross-compile Matrix (x86_64/aarch64/armhf) nach Bin/<arch>/</name>
  <files>.github/workflows/build-fake-libpulse.yml</files>
  <read_first>
    - .github/workflows/build-librespot.yml (Matrix-Job-Shape 50-118, bin_dir-Namen x86_64-linux/aarch64-linux/armhf-linux, upload-artifact-Muster, Verify-Step mit file/sha256)
    - .planning/phases/71-soloist-foundation/71-RESEARCH.md (Installation: gcc-aarch64-linux-gnu, gcc-arm-linux-gnueabihf; Pitfall 3; Environment Availability Fallback)
    - Plugins/SpotOn/Bin/fake-libpulse/Makefile (CC-Override-Schnittstelle aus Task 1)
  </read_first>
  <action>Erzeuge .github/workflows/build-fake-libpulse.yml als eigenstaendigen Workflow (workflow_dispatch + push auf tags v*, analog build-librespot Trigger). Matrix mit drei Zielen, die auf die bestehenden bin_dir-Namen abbilden: x86_64-linux (natives gcc auf ubuntu-latest), aarch64-linux (CC=aarch64-linux-gnu-gcc), armhf-linux (CC=arm-linux-gnueabihf-gcc). Schritt 1: apt-get install -y gcc-aarch64-linux-gnu gcc-arm-linux-gnueabihf (nur fuer die Cross-Ziele noetig; x86_64 nutzt vorhandenes gcc). Schritt 2: make -C Plugins/SpotOn/Bin/fake-libpulse CC=<matrix-cc> -> libpulse.so.0. Schritt 3: nach Bin/<matrix.bin_dir>/libpulse.so.0 kopieren, chmod 755. Schritt 4 (Verify, Pitfall 3): file libpulse.so.0 ausgeben und assertieren, dass der Interpreter/Dynamic-Linker glibc ist (ld-linux), NICHT musl - der Job schlaegt fehl, wenn musl im file-Output erscheint. Schritt 5: upload-artifact je Arch (Muster wie build-librespot). WICHTIG: NICHT die cross/musl-Toolchain von build-librespot wiederverwenden (RESEARCH Pitfall 3 - glibc/musl-Mismatch bricht Soloists dynamischen Loader). Dokumentiere den Docker-Fallback aus RESEARCH als Kommentar, falls apt-Cross-gcc auf dem Runner fehlschlaegt.</action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/build-fake-libpulse.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Workflow-YAML ist valide und definiert eine 3-Ziel-Matrix mit bin_dir x86_64-linux / aarch64-linux / armhf-linux
    - Cross-Ziele nutzen CC=aarch64-linux-gnu-gcc bzw arm-linux-gnueabihf-gcc via Makefile-Override; keine cross/musl-Rust-Toolchain
    - Ein Verify-Step prueft mit file(1) auf glibc-Interpreter und schlaegt bei musl fehl (Pitfall 3)
    - Output landet unter Plugins/SpotOn/Bin/<bin_dir>/libpulse.so.0 und wird als Artefakt hochgeladen
    - apt-get install der glibc-Cross-gcc-Pakete ist enthalten
  </acceptance_criteria>
  <done>Eigenstaendiger CI-Workflow baut libpulse.so.0 fuer 3 Architekturen gegen glibc, verifiziert den Linker-Typ und liefert in Bin/<arch>/ aus; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Runner -> Plugin-Zip (Bin/<arch>/libpulse.so.0) | Gebautes Build-Artefakt wird mit dem Plugin ausgeliefert und spaeter in Soloists Prozess geladen |
| Umgebungsvariable -> pa_stream_write Ziel-FD | Steuert, wohin der PCM-Stream geschrieben wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-09 | Tampering | glibc/musl-Mismatch der .so bricht Loader oder laedt falsche lib | high | mitigate | Verify-Step mit file(1) erzwingt glibc-Interpreter; Job schlaegt bei musl fehl (Pitfall 3) |
| T-71-10 | Elevation of Privilege | fake-libpulse wird via LD_LIBRARY_PATH in Soloist geladen | medium | mitigate | Nur die projekteigene, aus Bin/<arch>/ ausgelieferte .so wird referenziert (Soloist->libPath, Plan 01); kein user-konfigurierbarer Pfad-Override (D-04/D-06); die .so implementiert ausschliesslich PA-Stubs, keine weitere Logik |
| T-71-11 | Information Disclosure | PCM-Ausgabe-FD/-Pfad aus Umgebung | low | accept | Ziel-FD wird in Phase 72 vom Plugin gesetzt (nicht user-kontrolliert); lokaler FS gilt im LMS-Threat-Model als vertrauenswuerdig |
| T-71-SC | Tampering | Keine npm/pip/cargo-Installs; nur OS-Pakete (cross-gcc) auf dem Runner | low | accept | Package-Legitimacy-Gate N/A (RESEARCH bestaetigt: keine Language-Ecosystem-Dependencies). apt-Cross-gcc sind Standard-Ubuntu-Pakete auf dem CI-Runner, kein Plugin-Dependency. |
</threat_model>

<verification>
- make -C Plugins/SpotOn/Bin/fake-libpulse -> native libpulse.so.0, file(1) = ELF shared object
- YAML-Lint des Workflows valide
- CI-Lauf (workflow_dispatch): 3 Arch-Artefakte gebaut, file(1)-Verify glibc gruen
- Manuell (Dev-Maschine): nm -D soloist zeigt keine von der .so unerfuellten pa_-Symbole
</verification>

<success_criteria>
Eine schlanke, spike-validierte fake-libpulse.c stubt Soloists PulseAudio-Bedarf und gibt S32LE-PCM per FD aus; ein eigenstaendiger CI-Workflow baut sie gegen glibc fuer x86_64/aarch64/armhf, verifiziert den Linker-Typ (kein musl) und liefert in dieselben Bin/<arch>/-Dirs wie librespot aus.
</success_criteria>

## Artifacts this phase produces (Plan 04)

- `Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c` (PulseAudio-Client-Stub, ~47 pa_*-Symbole, pa_stream_write -> FD)
- `Plugins/SpotOn/Bin/fake-libpulse/Makefile` (CC-override-faehiger Shared-Object-Build)
- `.github/workflows/build-fake-libpulse.yml` (glibc cross-gcc Matrix: x86_64-linux / aarch64-linux / armhf-linux -> Bin/<arch>/libpulse.so.0)

<output>
Create `.planning/phases/71-soloist-foundation/71-04-SUMMARY.md` when done
</output>
