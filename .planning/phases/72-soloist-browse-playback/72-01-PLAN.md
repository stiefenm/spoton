---
phase: 72-soloist-browse-playback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Soloist.pm
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/Unified/DaemonManager.pm
  - Plugins/SpotOn/custom-convert.conf
  - Plugins/SpotOn/custom-types.conf
  - t/03_convert_conf.t
  - t/04_types_conf.t
  - t/29_soloist_browse.t
  - t/30_soloist_launcher.t
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With backend=soloist, playing spoton://track:ID routes through the LMS transcoder (sol-flc-*-* or sol-pcm-*-* profile) instead of the daemon HTTP path (D-01, D-02, D-03)"
    - "The sol flc rule encodes 32-bit FLAC from Soloist's raw S32LE output; the sol pcm rule delivers raw PCM as fallback (D-04, D-05 baseline)"
    - "A failing Soloist single-track invocation is retried up to 2 times with a 2s delay by the launcher before LMS advances to the next track (D-06)"
    - "The spak api-key never appears on any process argv — it is read from the 0600 key file inside the generated launcher"
    - "With backend=librespot, contentType/getFormatForURL/canDirectStream/canSeek behave exactly as before this phase (no librespot regression)"
    - "Seeking is not offered on the soloist path — canSeek returns 0 when backend=soloist (Pitfall 3)"
  artifacts:
    - "Plugins/SpotOn/custom-convert.conf — sol flc and sol pcm rules in the LMS 3-line format"
    - "Plugins/SpotOn/custom-types.conf — sol content-type row"
    - "Plugins/SpotOn/Soloist.pm — dataDir(), isPaired(), launcherPath(), ensureLauncher()"
    - "Plugins/SpotOn/ProtocolHandler.pm — backend-aware contentType/getFormatForURL/formatOverride/canDirectStream/canSeek/new"
    - "t/29_soloist_browse.t and t/30_soloist_launcher.t — dispatch + launcher unit tests"
  key_links:
    - "convert rule token [spoton-soloist] <-> Slim::Utils::Misc::addFindBinPaths(Soloist::_rootDir()) registration — if the dir is not registered, LMS cannot resolve the launcher and every sol profile fails"
    - "contentType()/formatOverride() returning 'sol' <-> the sol-* rule headers in custom-convert.conf — a mismatch means no transcoder profile matches and playback silently fails"
    - "Launcher env SPOTON_SOLOIST_PCM_FD=1 <-> fake-libpulse.c FD resolution order — this is the entire audio egress contract"
    - "Launcher embedded data-dir <-> Soloist->dataDir() <-> the dir a manual `--pair` run writes credentials into — mismatch means every browse track fails with an auth error (RESEARCH A3)"
---

<objective>
Tracer for Phase 72: revive the pre-Phase-30 per-track transcoder model for the Soloist backend. One complete happy path: user plays `spoton://track:ID` with `backend=soloist` → ProtocolHandler reports content-type `sol` and refuses direct streaming → LMS TranscodingHelper matches a `sol-flc-*-*` (or `sol-pcm-*-*`) profile → spawns the generated launcher wrapper → wrapper execs `soloist --single-track <uri>` with `LD_LIBRARY_PATH` (fake-libpulse) and `SPOTON_SOLOIST_PCM_FD=1` → raw S32LE PCM flows to stdout → `flac --bps=32` → player.

Purpose: prove the whole architecture (dispatch + launcher + conf + egress) end-to-end before the Settings work builds on it. Implements D-01 through D-06.
Output: launcher generator in Soloist.pm, sol conf rules, backend-aware ProtocolHandler, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/72-soloist-browse-playback/72-CONTEXT.md
@.planning/phases/72-soloist-browse-playback/72-RESEARCH.md
@Plugins/SpotOn/Soloist.pm
@Plugins/SpotOn/ProtocolHandler.pm
@Plugins/SpotOn/custom-convert.conf
@Plugins/SpotOn/custom-types.conf
</context>

## Artifacts this phase produces

| Symbol / File | Kind | Created by |
|---------------|------|-----------|
| `Plugins::SpotOn::Soloist::LAUNCHER_NAME` (`'spoton-soloist'`) | constant | 72-01 Task 1 |
| `Plugins::SpotOn::Soloist::dataDir()` | sub — `catdir(_rootDir(), 'data')` | 72-01 Task 1 |
| `Plugins::SpotOn::Soloist::isPaired()` | sub — paired-credential heuristic | 72-01 Task 1 |
| `Plugins::SpotOn::Soloist::launcherPath()` | sub — `catfile(_rootDir(), LAUNCHER_NAME)` | 72-01 Task 1 |
| `Plugins::SpotOn::Soloist::ensureLauncher()` | sub — idempotent wrapper generator + findbin registration | 72-01 Task 1 |
| `sol` content-type row | custom-types.conf | 72-01 Task 1 |
| `sol flc * *`, `sol pcm * *` rules | custom-convert.conf | 72-01 Task 1 |
| `Plugins::SpotOn::ProtocolHandler::_useSoloist()` | private sub — backend pref check | 72-01 Task 1 |
| Backend-aware `contentType/getFormatForURL/formatOverride/canDirectStream` | edits | 72-01 Task 1 |
| Backend-aware `canSeek`, soloist-safe `new()`, PCM samplesize hinting | edits | 72-01 Task 2 |
| `t/29_soloist_browse.t`, `t/30_soloist_launcher.t` | tests | 72-01 Task 3 |
| `soloistPaired`/`soloistLauncherPath` template params, D-07 Settings reorg | Settings.pm / basic.html / strings.txt | 72-02 |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end soloist single-track transcoder path — one happy path</name>
  <files>Plugins/SpotOn/Soloist.pm, Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/ProtocolHandler.pm, Plugins/SpotOn/custom-convert.conf, Plugins/SpotOn/custom-types.conf</files>
  <read_first>Plugins/SpotOn/Soloist.pm (whole file — _rootDir/_cacheDir/keyPath/libPath/ensureBinary), Plugins/SpotOn/ProtocolHandler.pm lines 74-241 (contentType/getFormatForURL/formatOverride/canDirectStream), Plugins/SpotOn/Helper.pm lines 19-40 (addFindBinPaths precedent), Plugins/SpotOn/custom-convert.conf, Plugins/SpotOn/custom-types.conf, .planning/phases/72-soloist-browse-playback/72-RESEARCH.md (Pattern 1-3, Pitfalls 1-6, fake-libpulse contract), and `git show f93c9d2:Plugins/SpotOn/custom-convert.conf` (historical son rules — the template)</read_first>
  <precondition>The end-to-end audio check at the bottom of verify assumes a stored spak-key (Soloist->keyPath()) AND a completed `--pair` run against Soloist->dataDir() on this host. Code changes and unit/structural verifies do NOT depend on this; if pairing is absent, record the E2E audio check as a UAT item in the SUMMARY instead of failing the task.</precondition>
  <reversibility rating="reversible">Wrapper + static conf rules; deleting the sol rules and the launcher restores Phase-71 behavior. D-01 (keeping spoton:// URLs) is the migration-free choice and needs no URL rewrites.</reversibility>
  <action>
Wire ONE path: `spoton://track:ID` + `backend=soloist` → transcoder → launcher → soloist → S32LE stdout → flac. Per D-01 the URL scheme is untouched — all backend selection is a runtime pref read.

**1. Soloist.pm — launcher generator (RESEARCH Pattern 3):**
- Add `use constant LAUNCHER_NAME => 'spoton-soloist';`
- `sub dataDir { catdir(_rootDir(), 'data') }` — the persistent `--data-dir` that holds paired credentials (RESEARCH A3). This is THE single canonical data-dir; any future pair invocation must use it.
- `sub isPaired` — returns 1 if `dataDir()` exists and contains at least one non-dot entry (opendir/readdir). Mark with a comment that the exact credential filename Soloist writes is unverified (RESEARCH A3) so presence-of-any-file is the heuristic.
- `sub launcherPath { catfile(_rootDir(), LAUNCHER_NAME) }`
- `sub ensureLauncher` — idempotent, called on every LMS start (Pitfall 5: the launcher lives in cachedir and dies with a cache-clear; regeneration must be unconditional and cheap):
  - Guard: return if `main::ISWINDOWS || main::ISMAC` or `!_arch()` (A5: POSIX sh is safe, Soloist is Linux-only).
  - `require File::Path; File::Path::make_path` for `_rootDir()` and `dataDir()` with mode 0700.
  - Register the launcher dir for convert-rule token resolution: `Slim::Utils::Misc::addFindBinPaths(_rootDir())` (mirror Helper.pm's init() pattern). Update Soloist.pm's header comment: findbin is still never used for *binary discovery*; it IS now used so the static `[spoton-soloist]` convert token resolves.
  - Compute the binary path deterministically as `catfile(_cacheDir($archInfo), BINARY_NAME)` — NOT via `get()` — so the wrapper can be written before the first download completes (paths are stable; a missing binary just makes the wrapper exit non-zero, which the D-06 retry/skip path absorbs).
  - Write the wrapper atomically (File::Temp tempfile in `_rootDir()` + rename, mirroring storeKey()), then chmod 0755. Assemble the script via a NON-interpolating Perl heredoc/sprintf template: only four values are injected from Perl (libPath(), keyPath(), binary path, dataDir()); every shell variable (`$KEY`, `$@`, `$rc`, `$n`, `$start`, `$now`, `${LD_LIBRARY_PATH}`) stays literal.
  - Wrapper contract (POSIX sh, shebang `#!/bin/sh`):
    (a) export LD_LIBRARY_PATH with libPath() PREPENDED to any existing value — this is what makes fake-libpulse.so.0 load;
    (b) export SPOTON_SOLOIST_PCM_FD=1 — per fake-libpulse.c FD resolution, PCM goes to stdout, which LMS pipes into the encoder;
    (c) read the api-key into a shell variable via command substitution from keyPath(), `|| exit 1` — the key is passed as `-k "$KEY"` so it never appears in the convert-rule argv or /proc of the wrapper's parent (T-72-01);
    (d) invoke `"<binary>" -n "SpotOn" -k "$KEY" -D "<dataDir>" "$@"` — generic `"$@"` passthrough so the same wrapper serves `--single-track <uri>` (convert rule) and a manual `--pair` (pairing instructions in 72-02);
    (e) D-06 retry (RESEARCH Pitfall 4 — the HTTP 404-retry in handleDirectError never fires on the transcoder path, so retry lives here at pipe level): a loop of up to 2 quick-failure retries. Each attempt records epoch seconds before/after (`date +%s`); on exit 0 → exit 0; on non-zero exit with elapsed >= 5 seconds → exit with that code immediately (PCM was already flowing — a retry would splice duplicated/misaligned audio; LMS sees EOF and advances = skip); on fast failure (< 5s) → sleep 2, retry. After the 2 loop retries, the final attempt uses `exec` so LMS's process management sees soloist directly. Errors from soloist go to stderr only (never fd 1) — the convert rule routes stderr to $STDERRLOG$; this is the error-display answer for the discretion item (log-based, same as librespot transcode errors).
- Wire regeneration into `ensureBinary()`: as its first action after the OS guard, call `ensureLauncher()` — ensureBinary() is already invoked from the Settings save path whenever backend=soloist, which covers mid-run cache-clears without touching Settings.pm in this plan.

**2. Plugin.pm — boot wiring:** immediately after the existing `Plugins::SpotOn::Soloist->init();` call in initPlugin, add `Plugins::SpotOn::Soloist::ensureLauncher() if !main::SCANNER;` (the sub self-guards for OS/arch). This satisfies Pitfall 5 (regenerate on every boot) and registers the findbin path before TranscodingHelper ever resolves a profile.

**3. custom-types.conf — sol row (D-02):** append `sol	sol	audio/x-sb-spoton-soloist	audio` (TAB-separated, mirroring the soc row).

**4. custom-convert.conf — sol rules (D-02/D-04, Pitfall 6):** append two rules in the EXACT 3-line format (header line, TAB + capabilities comment, TAB + command — commit c2c9dc8 showed the parser breaks otherwise). Adapt the historical `son flc` rule from f93c9d2 with the RESEARCH Pattern-1 deltas: the launcher token replaces `[spoton]`, all librespot-only flags (bitrate/discovery/audio-cache) are dropped, no seek templating of any kind is carried over (Soloist's single-track mode has no seek flag — the RT capability and its substitution variable stay out), and the flac bit depth is declared as 32 (D-04: Soloist emits S32LE; flac cannot down-convert, and declaring 24 against 32-bit frames mis-frames every sample — Pitfall 2):

Rule 1 header `sol flc * *`; capabilities line `# R`; command: `[spoton-soloist] --single-track $URL$ 2>>"$STDERRLOG$" | [flac] -cs --channels=2 --sample-rate=44100 --bps=32 --endian=little --sign=signed --fast --totally-silent --ignore-chunk-sizes -`

Rule 2 header `sol pcm * *`; capabilities line `# R`; command: `[spoton-soloist] --single-track $URL$ 2>>"$STDERRLOG$"`

Leave the existing `soc pcm` and `son ogg` rules byte-identical. The `R` capability tells LMS the command consumes the remote URL itself ($URL$), so LMS spawns the pipe rather than opening the stream.

**5. ProtocolHandler.pm — minimal backend dispatch (D-01/D-02/D-03):**
- Add private `sub _useSoloist { ($prefs->get('backend') || 'librespot') eq 'soloist' }`.
- `contentType()`: replace the constant with the RESEARCH Pattern-2 branch — `'sol'` when _useSoloist(), else `'son'`.
- `getFormatForURL()`: new FIRST branch — `return 'sol' if _useSoloist() && $url && $url =~ m{^spoton://(?:track|episode):};` ahead of the existing soc/pcm branches (per RESEARCH: soloist branch before URL-shape branches).
- `formatOverride()`: new FIRST branch before the DaemonManager require — when _useSoloist() and the song's track URL matches `^spoton://(?:track|episode):`, return 'sol'. Without this, LMS Song.pm builds a `son-*`/`soc-*` profile key and no sol rule ever matches (the key_links item).
- `canDirectStream()`: directly after the `return 0 unless $client;` guard and BEFORE the streamingMode gate, add: if _useSoloist() and $url matches `^spoton://(?:track|episode):`, log (diagnosticMode warn, pattern of the neighboring gates: reason=soloist_transcoder_path) and return 0 (D-03 — no HTTP daemon exists; LMS falls back to the transcoder). Placement before the streamingMode block also keeps the unit test free of per-client pref stubs. Connect-URLs are deliberately untouched (Phase 73).
  </action>
  <verify>
    <automated>prove -l t/03_convert_conf.t t/04_types_conf.t t/05_perl_syntax.t && perl -e 'my $c = do { open my $f, "<", "Plugins/SpotOn/custom-convert.conf" or die; local $/; <$f> }; die "sol flc missing" unless $c =~ m{^sol flc \* \*\n\t# R\n\t\[spoton-soloist\]}m; die "sol pcm missing" unless $c =~ m{^sol pcm \* \*\n\t# R\n\t\[spoton-soloist\]}m; die "bps32 missing" unless $c =~ m{--bps=32}; print "conf ok\n"'</automated>
    <human-check>E2E audio (requires precondition: key + pairing on this host): run the generated wrapper as `&lt;cachedir&gt;/spoton/soloist/spoton-soloist --single-track spotify:track:&lt;id&gt; &gt; /tmp/pcm.raw 2&gt;/tmp/sol.log`, kill after ~10s. /tmp/sol.log must carry all log lines (A1 stdout hygiene) and /tmp/pcm.raw must be pure PCM (size ≈ seconds × 352800 bytes; `hexdump -C /tmp/pcm.raw | head` shows no ASCII banners). Then in LMS with backend=soloist play a Browse track and confirm audio. If Soloist turns out to log on fd 1, switch the wrapper to a dedicated FD ≥ 3 with SPOTON_SOLOIST_PCM_FD and shell fd redirection, per RESEARCH Alternatives row.</human-check>
  </verify>
  <acceptance_criteria>
- `prove -l t/03_convert_conf.t t/04_types_conf.t t/05_perl_syntax.t` passes (existing assertions — sol additions must not break soc/son asserts).
- The conf check above confirms: `sol flc`/`sol pcm` 3-line rules exist, reference the `[spoton-soloist]` token with `--single-track $URL$`, and the flc rule declares `--bps=32`.
- `grep -c 'spoton-soloist' Plugins/SpotOn/custom-convert.conf` returns 2 (one per rule).
- `grep -v '^#' Plugins/SpotOn/custom-types.conf | grep -c '^sol'` returns 1.
- Soloist.pm defines dataDir, isPaired, launcherPath, ensureLauncher and LAUNCHER_NAME: `grep -c 'sub dataDir\|sub isPaired\|sub launcherPath\|sub ensureLauncher' Plugins/SpotOn/Soloist.pm` returns 4.
- Plugin.pm calls ensureLauncher at init: `grep -c 'ensureLauncher' Plugins/SpotOn/Plugin.pm` returns 1, and `grep -c 'ensureLauncher' Plugins/SpotOn/Soloist.pm` >= 2 (definition + ensureBinary call site).
- ProtocolHandler dispatch: `grep -c '_useSoloist' Plugins/SpotOn/ProtocolHandler.pm` >= 5 (definition + contentType + getFormatForURL + formatOverride + canDirectStream).
- D-06 in wrapper template: `grep -c 'sleep 2' Plugins/SpotOn/Soloist.pm` >= 1 and the wrapper template contains `"$@"` and `SPOTON_SOLOIST_PCM_FD=1`.
  </acceptance_criteria>
  <done>Playing a Browse track with backend=soloist resolves to a sol-* transcoder profile that spawns the generated launcher; with backend=librespot every touched sub returns its pre-phase value. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProtocolHandler soloist edge completion — new(), seek gating, PCM hinting; DaemonManager message</name>
  <files>Plugins/SpotOn/ProtocolHandler.pm, Plugins/SpotOn/Unified/DaemonManager.pm</files>
  <read_first>Plugins/SpotOn/ProtocolHandler.pm lines 387-530 (new + getNextTrack) and 756-795 (isRepeatingStream/canSeek/canTranscodeSeek/getSeekData), Plugins/SpotOn/Unified/DaemonManager.pm lines 631-673 (startHelper soloist branch), and `git show f93c9d2:Plugins/SpotOn/ProtocolHandler.pm` sections for sub new / canSeek / canTranscodeSeek (the proven transcoder-model behavior to mirror)</read_first>
  <behavior>
- new() with backend=soloist and url spoton://track:X returns a defined stream object (falls through to Slim::Player::Protocols::HTTP->new) instead of undef — the (b2) daemon-proxy block must not run.
- new() with backend=librespot keeps the (b2) substitution/undef behavior unchanged.
- canSeek(client) returns 0 when backend=soloist; unchanged (LMS-version check) when librespot.
- canTranscodeSeek still returns 0 (already true) — comment now also covers the soloist rationale.
  </behavior>
  <action>
**1. new() soloist gate (D-03 consequence):** the current (b2) block substitutes a daemon HTTP URL for `spoton://track|episode` URLs and returns **undef** when no unified daemon is alive — under soloist there is never a daemon, so playback would die here whenever LMS instantiates the handler. Guard the (b2) block with `!_useSoloist()`: on the soloist path fall straight through to the final `Slim::Player::Protocols::HTTP->new($args)` with the original spoton:// URL — exactly what the pre-Phase-30 transcoder-model handler did (see f93c9d2 new(): browse URLs reached HTTP->new untouched; with the `R` capability the transcoder consumes $URL$ itself and this object is not used for audio data). Add a short comment citing D-03 and the historical precedent. Leave block (a) (D-08 mode-transition log) and the connect- block (b) untouched.

**2. Seek gating (Pitfall 3):** in canSeek(), before the LMS-version check, `return 0 if _useSoloist();` — soloist single-track mode has no seek/offset capability at all; advertising seekability would make the seek bar restart tracks. Extend the canTranscodeSeek() comment: it already returns 0 for the unified-daemon reason, and 0 is also the correct soloist answer for Phase 72 (Browse-seek revisits in Phase 74 raw-capture work). getSeekData/canDoAction need no change (canSeek=0 prevents seek entry).

**3. PCM fallback hinting (D-04 fallback correctness):** in getNextTrack(), in the Browse branch (after the duration cache lookup), when `_useSoloist()` and the URL matches `^spoton://(?:track|episode):`, set the stream parameters LMS uses to signal raw PCM to the player: guard each with `->can(...)` on `$song->track` — samplesize 32, samplerate 44100, channels 2. Without this the sol-pcm profile would be announced as 16-bit and produce garbage; the flc target is self-describing and unaffected. Keep it to three guarded setter lines with a D-04/Pitfall-2 comment.

**4. DaemonManager startHelper message:** in the soloist_ready branch, replace the Phase-71 "daemon spawn deferred to a future phase" info log + comment with an accurate Phase-72 statement: Browse playback is served per-track by the custom-convert.conf launcher (no persistent daemon needed — the discretion decision from RESEARCH: pure transcoder spawning, no DaemonManager involvement for Browse); the persistent WS daemon arrives with Phase 73 Connect. No behavior change — the branch still returns without starting anything.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t t/12_protocol_rename.t t/28_soloist_dispatch.t</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/05_perl_syntax.t t/12_protocol_rename.t t/28_soloist_dispatch.t` passes.
- new(): the (b2) block is conditional on the librespot path — `grep -n '_useSoloist' Plugins/SpotOn/ProtocolHandler.pm` shows a hit inside sub new.
- canSeek contains a `_useSoloist` early return; canTranscodeSeek body still returns 0.
- getNextTrack sets samplesize/samplerate/channels only under the soloist+browse guard: `grep -c 'samplesize' Plugins/SpotOn/ProtocolHandler.pm` >= 1.
- DaemonManager soloist_ready branch log text mentions the per-track transcoder and Phase 73: `grep -c 'Phase 73' Plugins/SpotOn/Unified/DaemonManager.pm` >= 1.
- Behavior assertions from the behavior block are covered by t/29 (Task 3) — Task 3's suite must pass against this task's code unmodified.
  </acceptance_criteria>
  <done>Soloist-path playback survives every ProtocolHandler entry point LMS may call (new, seek queries, PCM signaling); librespot behavior is bit-identical; DaemonManager documents the Phase-72 model.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 test coverage — conf assertions + dispatch + launcher unit tests</name>
  <files>t/03_convert_conf.t, t/04_types_conf.t, t/29_soloist_browse.t, t/30_soloist_launcher.t</files>
  <read_first>t/03_convert_conf.t, t/04_types_conf.t, t/28_soloist_dispatch.t (the stub-harness pattern to copy: write_stub, main:: constants, controllable prefs), t/26_soloist_check.t and t/27_soloist_key.t (Soloist.pm loading harness), Plugins/SpotOn/ProtocolHandler.pm lines 1-30 (load-time deps that need stubs), Plugins/SpotOn/Soloist.pm (ensureLauncher as written in Task 1)</read_first>
  <action>
Close the RESEARCH Validation-Architecture Wave-0 gaps:

**1. t/03_convert_conf.t:** add assertions mirroring the existing soc/son ones: `^sol flc \* \*` and `^sol pcm \* \*` headers exist; both sol commands reference `[spoton-soloist]` and `--single-track $URL$`; the flc command declares `--bps=32`; the content contains no `$START$` substitution variable anywhere (seek templating must never return — RESEARCH Anti-Pattern); each sol header is followed by a TAB-indented line (3-line format guard, Pitfall 6). Keep every existing assertion.

**2. t/04_types_conf.t:** add a block parallel to the son checks: a non-comment `sol` line exists, field 3 is `audio/x-sb-spoton-soloist`, field 4 is `audio`.

**3. t/29_soloist_browse.t (new):** isolated-require harness for ProtocolHandler.pm modeled on t/28's write_stub approach. Required stubs beyond t/28's set: Slim::Formats::RemoteStream (empty base package), Slim::Player::Protocols::HTTP (`sub new { bless {}, shift }`), Slim::Utils::Versions (`compareVersions` returns 1), Slim::Utils::Network, Slim::Utils::Misc, Slim::Music::Info, Slim::Schema::RemoteTrack as needed for load; a Prefs stub whose `get` reads from a controllable `%Slim::Utils::Prefs::FAKE` hash (so tests flip `backend`); the Plugin stub with SPOTON_CACHE_VERSION. Set `$::VERSION = '9.0.0'` for canSeek. Assertions with backend=soloist: `contentType` eq 'sol'; `getFormatForURL('spoton://track:abc123')` eq 'sol'; `getFormatForURL('spoton://episode:abc123')` eq 'sol'; `canDirectStream($clientStub, 'spoton://track:abc123')` == 0 (client stub: plain blessed hash — the soloist gate sits before any per-client pref access); `canSeek($clientStub)` == 0; `new({ url => 'spoton://track:abc123', client => undef })` is defined. With backend=librespot (and with backend unset): `contentType` eq 'son'; `getFormatForURL('spoton://track:abc123')` eq 'soc' (pre-phase behavior); canSeek truthy. If a stub for full new() coverage proves disproportionate, keep the librespot-side new() case out rather than weakening the soloist-side defined-object assertion.

**4. t/30_soloist_launcher.t (new):** load Soloist.pm with the t/26-style harness plus a server-Prefs stub returning a tempdir cachedir and a Slim::Utils::Misc stub recording addFindBinPaths calls; Plugin stub `_pluginDataFor` returns a tempdir basedir. Force the arch path (osArch stub 'x86_64'). Call `Plugins::SpotOn::Soloist::ensureLauncher()` and assert: launcher file exists at launcherPath() and is executable (-x); dataDir() was created; content starts with `#!/bin/sh`; contains `SPOTON_SOLOIST_PCM_FD=1`, an `LD_LIBRARY_PATH` export, a command substitution reading the keyPath() file, `-k "$KEY"` (key via shell variable, never a literal key value), `-D` followed by the dataDir() path, `"$@"` passthrough, `sleep 2` and an `exec ` line (D-06 retry shape); the raw string of keyPath()'s content never appears (write a dummy key file first and assert its value is absent from the script). Assert a second ensureLauncher() call succeeds (idempotency) and addFindBinPaths was invoked with _rootDir(). Assert isPaired() is false on the empty dataDir and true after touching a file inside it.
  </action>
  <verify>
    <automated>prove -l t/03_convert_conf.t t/04_types_conf.t t/29_soloist_browse.t t/30_soloist_launcher.t</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/03_convert_conf.t t/04_types_conf.t t/29_soloist_browse.t t/30_soloist_launcher.t` passes.
- `prove -l t/` passes (full suite — no regression in t/05/t/09/t/12/t/26-28).
- t/29 exercises BOTH backend values for contentType/getFormatForURL/canDirectStream/canSeek (D-01/D-02/D-03 + Pitfall 3 coverage).
- t/30 asserts the launcher security contract: key read from file inside the script, key value absent from script text, quoted `"$@"`.
  </acceptance_criteria>
  <done>All RESEARCH Wave-0 gaps closed; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS → spawned shell wrapper | Convert-rule command line assembled by LMS with $URL$ substitution |
| Wrapper → soloist binary | Env vars + argv cross into a third-party binary |
| Track URI (Spotify data) → shell | $URL$ originates from API responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Information Disclosure | spak-key on argv (/proc/pid/cmdline) | high | mitigate | Key read inside wrapper via command substitution from 0600 keyPath(); convert rule carries no key token (Task 1c, t/30 assertion) |
| T-72-02 | Tampering/Elevation | Shell injection via $URL$ in wrapper args | high | mitigate | URLs reach the rule only as `spoton://(track\|episode):[A-Za-z0-9]+` (regex-validated in ProtocolHandler/explodePlaylist); wrapper passes `"$@"` quoted, no eval/interpolation (Task 1d) |
| T-72-03 | Elevation | Arbitrary library load via LD_LIBRARY_PATH | medium | mitigate | Path fixed to Soloist->libPath() (plugin-shipped Bin/<arch>/), no user-configurable override (Task 1a) |
| T-72-04 | Tampering | Launcher script replaced by attacker | medium | mitigate | Written 0755 inside LMS-user-owned cachedir (0700 root dir), regenerated idempotently every boot (Task 1) |
| T-72-05 | Information Disclosure | PCM egress FD/path env abuse | low | mitigate | SPOTON_SOLOIST_PCM_FD hardcoded to 1 in the generated script; not user-controlled |
| T-72-06 | Repudiation/DoS | Retry loop masks persistent failures | low | accept | Bounded at 2 retries with fast-fail gate (D-06); stderr routed to $STDERRLOG$ for diagnosis |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `prove -l t/` fully green.
- `perl -c` clean via t/05 for Soloist.pm, Plugin.pm, ProtocolHandler.pm, DaemonManager.pm.
- Structural: sol rules + sol type row present; launcher generator + findbin registration present.
- Manual/UAT (spoton-uat skill, needs paired host): Browse playback with backend=soloist produces clean audio via sol-flc; switching back to librespot restores daemon streaming without restart artifacts.
</verification>

<success_criteria>
- D-01: spoton:// URLs unchanged; backend chosen at runtime per play.
- D-02: contentType/getFormatForURL/formatOverride report sol/son by pref; conf has rules for both families.
- D-03: canDirectStream=0 on soloist browse path; transcoder profile engages.
- D-04: sol→FLAC 32-bit standard rule + sol→PCM fallback rule; no OGG re-encoding.
- D-05: no raw-capture/passthrough code introduced; architecture untouched for Phase 74.
- D-06: launcher retries twice on fast failure then lets LMS skip; no librespot fallback.
- Full test suite green; librespot path regression-free.
</success_criteria>

<output>
Create `.planning/phases/72-soloist-browse-playback/72-01-SUMMARY.md` when done. Record the A1 stdout-hygiene result (or its deferral to UAT) explicitly — 72-02 and Phase 73 depend on knowing whether stdout is clean.
</output>
