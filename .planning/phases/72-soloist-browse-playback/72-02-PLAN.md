---
phase: 72-soloist-browse-playback
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - t/02_strings.t
autonomous: true
requirements: [D-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The backend selection is its own top-level section at the very top of the Settings setup flow, above the Account section (D-07)"
    - "With backend=soloist, librespot-specific settings (librespot binary status, bitrate, streaming mode, normalization, player playback-auth section) are hidden; with backend=librespot, soloist-specific fields (spak-key, soloist status, pairing state) are hidden — both server-rendered and live-toggled (D-07)"
    - "When soloist is selected but not yet paired, Settings shows a not-paired warning with the exact launcher --pair command to run (RESEARCH Open Question 2)"
    - "Every new UI string exists in all 11 languages (CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV) with real translations"
  artifacts:
    - "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html — Backend top-level section + librespot-fields/soloist-fields conditional groups"
    - "Plugins/SpotOn/Settings.pm — soloistPaired + soloistLauncherPath template params"
    - "Plugins/SpotOn/strings.txt — backend section + pairing strings in 11 languages"
  key_links:
    - "Settings.pm soloistPaired param <-> Soloist->isPaired() (from 72-01) — without it the template cannot render pairing state"
    - "Pairing instructions <-> Soloist->launcherPath() — the displayed command must be the SAME wrapper the convert rule spawns, so --pair writes into the SAME dataDir() the wrapper reads (RESEARCH A3)"
    - "JS backend toggle <-> #librespot-fields and #soloist-fields divs — a missed div id leaves stale fields visible after a dropdown change"
---

<objective>
Implement D-07: promote the backend choice to its own top-level Settings section at the top of the setup flow, and make every other section adapt conditionally to the chosen backend — librespot-specific fields disappear under Soloist and vice versa. Surface the Soloist pairing prerequisite (paired / not-paired + how to pair) so a user can get from "backend=soloist" to working Browse playback without reading source code.

Purpose: many settings only make sense for one backend (authorization, audio format, daemon status); D-07 makes the page coherent per backend and closes the pairing-visibility gap that would otherwise make every soloist Browse track fail silently.
Output: reorganized basic.html, extended Settings.pm handler params, new i18n strings in 11 languages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-soloist-browse-playback/72-CONTEXT.md
@.planning/phases/72-soloist-browse-playback/72-01-SUMMARY.md
@Plugins/SpotOn/Settings.pm
@Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / File | Kind | Created by |
|---------------|------|-----------|
| `$paramRef->{soloistPaired}` | Settings template param (Soloist->isPaired()) | Task 1 |
| `$paramRef->{soloistLauncherPath}` | Settings template param (Soloist->launcherPath()) | Task 1 |
| Top-level Backend section + `#librespot-fields` group | basic.html restructure | Task 1 |
| Pairing status block (paired / not-paired + command) | basic.html soloist-fields | Task 1 |
| `PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION`, `PLUGIN_SPOTON_SOLOIST_PAIRED`, `PLUGIN_SPOTON_SOLOIST_NOT_PAIRED`, `PLUGIN_SPOTON_SOLOIST_PAIR_HOWTO` | strings (11 languages) | Task 2 |

<tasks>

<task type="auto">
  <name>Task 1: D-07 Settings reorganisation — Backend top-level section + conditional groups + pairing state</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (whole file — section order, soloist-fields div, backend toggle JS at the bottom), Plugins/SpotOn/Settings.pm lines 122-260 (save handling — must NOT change) and 495-531 (template params), Plugins/SpotOn/Soloist.pm (isPaired/launcherPath from 72-01), .planning/phases/72-soloist-browse-playback/72-RESEARCH.md (Open Question 2, Runtime State Inventory pairing row)</read_first>
  <reversibility rating="reversible">Pure template/param reorganisation; the Phase-71 layout is one git revert away.</reversibility>
  <action>
**Settings.pm (params only — all save/sanitization logic from Phase 71 stays byte-identical):** in the template-param block next to the existing soloist* params, add `$paramRef->{soloistPaired} = Plugins::SpotOn::Soloist::isPaired() ? 1 : 0;` and `$paramRef->{soloistLauncherPath} = Plugins::SpotOn::Soloist::launcherPath() || '';` (guard with the existing `require Plugins::SpotOn::Soloist`). Also suppress the librespot binary-missing hint under soloist: the existing `$paramRef->{helperMissing}` assignment gets `unless ($prefs->get('backend') || 'librespot') eq 'soloist'` semantics so a soloist-only install does not show a red librespot warning (D-07: librespot-specific state hidden under Soloist).

**basic.html restructure (D-07):**
1. Create a new top-level section IMMEDIATELY after the player-settings info box and before the Account `<hr>`/`<h2>`: an `<hr>` + `<h2>[% 'PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION' | string %]</h2>`, then MOVE (not copy) the existing backend-dropdown WRAPPER block and the whole `#soloist-fields` div up into it. The Global section keeps its `<h2>` but loses the backend dropdown.
2. Extend `#soloist-fields` with a pairing-status block below the spak-key setting, rendered only when the binary+key states are green (mirror the existing status-cascade IF/ELSIF order: unsupported OS > binary missing > key missing > pairing): when `soloistPaired` — green dot + `PLUGIN_SPOTON_SOLOIST_PAIRED`; else — orange dot + `PLUGIN_SPOTON_SOLOIST_NOT_PAIRED`, a desc paragraph `PLUGIN_SPOTON_SOLOIST_PAIR_HOWTO`, and the exact command in a `<code>` block: `[% soloistLauncherPath | html %] --pair` (this is the same generated wrapper the convert rule uses, so pairing lands in the same data-dir — RESEARCH A3). The howto string must tell the user to run it on the LMS host as the LMS service user.
3. Wrap the librespot-specific blocks in a single `<div id="librespot-fields" style="[% IF backend == 'soloist' %]display:none[% ELSE %]display:block[% END %]">`: the Binary Status WRAPPER, the Bitrate WRAPPER (librespot OGG tiers — Soloist quality is server-assigned, RESEARCH CLI notes), the global Streaming Mode WRAPPER (daemon direct/proxy — meaningless without an HTTP daemon), the Normalization WRAPPER (librespot flag), AND the Player Auth ("playback authorization") WRAPPER incl. its `#spoton-player-auth` anchor (librespot credential derivation — Soloist pairs via `--pair` instead). The Account/PKCE/sp_dc/Pathfinder sections stay visible for both backends (the Web API powers Browse menus regardless of playback backend). The Diagnostics section stays visible for both.
4. Also gate the top-of-page `playbackAuthState == 'required'` banner with `&& backend != 'soloist'` (template-level IF) — it deep-links into the hidden Player Auth section.
5. Extend the existing Phase-71 backend-toggle JS: on dropdown change, `#soloist-fields` shows when value is 'soloist' and `#librespot-fields` shows when it is not (inverse pair). Keep server-rendered initial display for both divs so the page is correct without JS.
6. Do not touch the AJAX functions, the setup-guide block, or any name= attributes — the form must POST identical field names so the untouched Settings.pm save logic keeps working.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t t/09_settings.t && perl -e 'my $h = do { open my $f, "<", "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html" or die; local $/; <$f> }; die "backend section missing" unless $h =~ /PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION/; die "backend section not before account section" unless index($h, "PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION") < index($h, "PLUGIN_SPOTON_SETTINGS_ACCOUNT_SECTION"); die "librespot-fields div missing" unless $h =~ /id="librespot-fields"/; die "pairing block missing" unless $h =~ /soloistPaired/ && $h =~ /soloistLauncherPath/; my $c = () = $h =~ /pref_backend/g; die "backend dropdown duplicated or lost" unless $h =~ /name="pref_backend"/; print "template ok\n"'</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/05_perl_syntax.t t/09_settings.t` passes.
- Template check above passes: backend section string appears before the account section string; `#librespot-fields` exists; pairing block references soloistPaired + soloistLauncherPath; `pref_backend` select still present exactly once as a form field.
- `grep -c 'name="pref_backend"' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns 1 (moved, not duplicated).
- `grep -c 'soloistPaired\|soloistLauncherPath' Plugins/SpotOn/Settings.pm` >= 2 (both params assigned).
- Settings.pm save-handling region (pref_backend whitelist, pref_soloistKey handling, pref_spDc, removeAccount) is unchanged: `git diff Plugins/SpotOn/Settings.pm` shows edits only in the template-param area (plus the helperMissing gate).
- The backend-toggle JS references both `librespot-fields` and `soloist-fields`.
  </acceptance_criteria>
  <done>Settings page renders backend choice first; every remaining section is coherent for the selected backend; a soloist user can see pairing state and the exact command to pair.</done>
</task>

<task type="auto">
  <name>Task 2: i18n — new strings in all 11 languages + strings-test coverage</name>
  <files>Plugins/SpotOn/strings.txt, t/02_strings.t</files>
  <read_first>Plugins/SpotOn/strings.txt lines 80-200 (existing PLUGIN_SPOTON_SOLOIST_* block — placement + the 11-language format: CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV), t/02_strings.t (bilingual_keys list mechanism)</read_first>
  <action>
Add four new string keys adjacent to the existing SOLOIST block (nearest-thematic-sibling placement, per the Phase 53 convention), each with REAL translations in all 11 languages — never English fallback rows (project i18n rule):
- `PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION` — section heading, e.g. EN "Playback Backend", DE "Wiedergabe-Backend".
- `PLUGIN_SPOTON_SOLOIST_PAIRED` — short status, EN "Paired with Spotify", DE "Mit Spotify gekoppelt".
- `PLUGIN_SPOTON_SOLOIST_NOT_PAIRED` — short status, EN "Not paired yet — Browse playback will fail until pairing is done", DE "Noch nicht gekoppelt — Browse-Wiedergabe schlägt fehl, bis das Pairing abgeschlossen ist".
- `PLUGIN_SPOTON_SOLOIST_PAIR_HOWTO` — instruction paragraph: run the shown command on the LMS host as the LMS service user, then transfer playback to the "SpotOn" device in the Spotify app once; credentials are stored locally and pairing is one-time. EN reference text: "Run this command on the LMS host as the LMS service user, then select the 'SpotOn' device in your Spotify app once. Pairing is a one-time step; credentials are stored locally."
All UI strings and code comments in English where language-neutral (EN row is canonical); translate the other 10 rows genuinely (use the existing SOLOIST_KEY_DESC block as a style/terminology reference for each language).

Extend t/02_strings.t's `@bilingual_keys` list with the four new keys so EN+DE presence is regression-guarded.
  </action>
  <verify>
    <automated>prove -l t/02_strings.t && perl -e 'my $s = do { open my $f, "<:encoding(UTF-8)", "Plugins/SpotOn/strings.txt" or die; local $/; <$f> }; for my $k (qw(PLUGIN_SPOTON_SETTINGS_BACKEND_SECTION PLUGIN_SPOTON_SOLOIST_PAIRED PLUGIN_SPOTON_SOLOIST_NOT_PAIRED PLUGIN_SPOTON_SOLOIST_PAIR_HOWTO)) { die "$k missing" unless $s =~ /^\Q$k\E$/m; my ($block) = $s =~ /^\Q$k\E\n((?:\t.+\n)+)/m; for my $lang (qw(CS DA DE EN ES FR IT NL NO PL SV)) { die "$k missing $lang" unless $block =~ /^\t\Q$lang\E\t/m; } } print "strings ok\n"'</automated>
  </verify>
  <acceptance_criteria>
- The 11-language completeness check above passes for all four keys.
- `prove -l t/02_strings.t` passes with the four keys added to @bilingual_keys.
- No language row is an untranslated copy of the EN text in DE/FR/IT/ES/NL (spot-check: DE rows differ from EN rows for all four keys).
- `prove -l t/` fully green.
  </acceptance_criteria>
  <done>All new UI text renders localized in every supported language; strings test guards the keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Settings form POST | Untrusted form input (unchanged save handlers) |
| Template → rendered HTML | Server values interpolated into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-07 | Tampering | pref_backend / pref_soloistKey POST values | high | mitigate | No change to Phase-71 whitelist/charset save handlers (Task 1 acceptance: save region untouched — verified via git diff) |
| T-72-08 | Information Disclosure | spak-key echoed into template | high | mitigate | Existing masked-preview mechanism untouched; new params expose only a boolean (soloistPaired) and a path (soloistLauncherPath), both `\| html`-escaped |
| T-72-09 | Cross-site scripting | soloistLauncherPath in `<code>` block | low | mitigate | Value is LMS-derived (cachedir path, no user input) and rendered with the TT `\| html` filter |
| T-72-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `prove -l t/` fully green.
- Manual: with backend=librespot the page shows Global/Bitrate/StreamingMode/Normalization/PlayerAuth and hides spak-key/pairing; flip the dropdown (no save) — the groups toggle live; save with soloist — server-rendered state matches; not-paired warning shows the launcher command from 72-01.
</verification>

<success_criteria>
- D-07 fully implemented: backend is the first setup decision on the page; every other section conditionally adapts.
- Pairing prerequisite is visible and actionable (command shown), closing RESEARCH Open Question 2 for Phase 72.
- 11-language i18n complete; no regressions in save handling or existing tests.
</success_criteria>

<output>
Create `.planning/phases/72-soloist-browse-playback/72-02-SUMMARY.md` when done.
</output>
