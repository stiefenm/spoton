---
phase: 72-soloist-browse-playback
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Soloist.pm
  - t/30_soloist_launcher.t
autonomous: true
gap_closure: true
requirements: [D-01, D-03, D-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The soloist child process spawned by the generated launcher receives spotify:track:ID / spotify:episode:ID on --single-track even though LMS's $URL$ token hands the launcher spoton://track:ID — the launcher translates the scheme at the spawn boundary, so D-01 (LMS-side URLs stay spoton://), D-03 and D-04 now work end-to-end (closes verification gap #1 / CR-01)"
    - "t/30_soloist_launcher.t feeds a spoton://track:ID value through the ACTUAL generated wrapper (executed against a stub binary) and asserts the child argv contains the translated spotify: URI — the regression test the verifier demanded"
    - "The spak api-key stays off LMS's convert-rule command line, off the wrapper's own argv, out of the generated script text, and out of logs; the -k argv handoff to the soloist child is explicitly documented in the launcher template as an accepted trade-off because soloist 1.3.7.489 offers no env-var/stdin/key-file alternative (closes verification gap #2 / WR-01 via the documented-trade-off branch)"
    - "The full existing test suite (1101 tests, prove -l t/) passes with no regressions; every pre-existing t/30 assertion (including T-72-01: raw key never appears in the script text) still holds"
  artifacts:
    - "Plugins/SpotOn/Soloist.pm — _launcherScript template gains a POSIX positional-parameter rewrite loop (spoton:// -> spotify:) before the D-06 retry loop, and an explicit WR-01 accepted-risk comment above KEY=$(cat ...)"
    - "t/30_soloist_launcher.t — new behavioral subtest: generated wrapper executed against a stub binary, child argv captured to a file and asserted (track + episode + passthrough cases, -k key delivery)"
  key_links:
    - "$URL$ (song->track->url = spoton://track:ID) <-> launcher rewrite loop <-> soloist --single-track spotify:track:ID — the exact link 72-VERIFICATION.md marked NOT WIRED; after this plan it is wired inside the wrapper, keeping custom-convert.conf and ProtocolHandler.pm untouched (D-01)"
    - "KEY=$(cat keyPath) command substitution <-> -k <key> child argv <-> WR-01 trade-off comment — the behavioral test proves the 0600 file's content actually reaches the binary AND pins the documented mechanism"
---

<objective>
Close the two code-level gaps from 72-VERIFICATION.md (status: gaps_found, 8/11) with surgical fixes to the generated launcher — no re-implementation of the working dispatch/settings scaffolding.

Gap 1 (CR-01, BLOCKER): the `sol flc`/`sol pcm` convert rules pass LMS's `$URL$` token (`spoton://track:ID`) verbatim through the launcher's `"$@"` to `soloist --single-track`, which per the verified CLI reference expects a real Spotify URI. Fix: translate `spoton://track:ID` / `spoton://episode:ID` to `spotify:track:ID` / `spotify:episode:ID` inside the generated wrapper (`Soloist.pm::_launcherScript`) — the single choke-point between the convert rule and the binary. LMS-side URLs stay `spoton://` (D-01 untouched).

Gap 2 (WR-01): the spak-key is handed to the soloist child as a literal `-k "$KEY"` argv element, visible via `/proc/<pid>/cmdline`. Verified twice that soloist 1.3.7.489 supports NO alternative key input — the RESEARCH.md `--help` transcript lists only `-k, --api-key <key>`, and `strings` on the binary shows `Error: --api-key is required` with the only recognized env vars being `SPOTIFY_SSL_CERT_FILE`/`SPOTIFY_SSL_CERT_DIR`. Therefore take the verification gap's explicitly sanctioned fallback: document the accepted argv-visibility trade-off next to the `KEY=$(cat ...)` line and pin it with tests; the must-have scope above is updated accordingly.

Purpose: make the phase's core deliverable (Soloist Browse playback) actually functional end-to-end and close the security-review loop.
Output: patched `_launcherScript` template, behavioral argv-capture regression tests in t/30.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-soloist-browse-playback/72-VERIFICATION.md
@.planning/phases/72-soloist-browse-playback/72-REVIEW.md
@Plugins/SpotOn/Soloist.pm
@t/30_soloist_launcher.t
@Plugins/SpotOn/custom-convert.conf
</context>

<!-- planner-discipline-allow: spoton:// -->
<!-- The literal internal scheme is load-bearing shell/Perl source in these tasks;
     the negative greps in acceptance criteria target the RUNTIME argv-capture
     file produced by the stub binary, never the source or script text. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — translate spoton:// to spotify: inside the generated launcher, proven by an argv-capture test</name>
  <files>Plugins/SpotOn/Soloist.pm, t/30_soloist_launcher.t</files>
  <read_first>
    - Plugins/SpotOn/Soloist.pm lines 354-473 (`_launcherScript` heredoc template + `ensureLauncher`) — the template is a NON-interpolating single-quoted heredoc; all shell `$` stay literal, only the four `__LIB__`/`__KEY__`/`__BIN__`/`__DATA__` placeholders are substituted in Perl
    - t/30_soloist_launcher.t (whole file) — stub harness, `$DUMMY_KEY` setup via `storeKey()`, existing structural assertions that MUST keep passing
    - .planning/phases/72-soloist-browse-playback/72-VERIFICATION.md gap #1 (`missing` items — this task implements both)
    - Plugins/SpotOn/custom-convert.conf — confirm the rules stay untouched (`--single-track $URL$` remains as-is)
  </read_first>
  <behavior>
    - Test 1 (track): executing the generated wrapper with args `--single-track spoton://track:abc123DEF` against a stub binary results in the stub's captured argv containing the exact line `spotify:track:abc123DEF`, preceded by `--single-track`; wrapper exit code 0
    - Test 2 (episode): args `--single-track spoton://episode:xyz789` yield captured argv line `spotify:episode:xyz789`
    - Test 3 (negative): the captured argv file contains zero occurrences of the internal `spoton://` scheme prefix
    - Test 4 (passthrough + order): non-URL arguments are forwarded unchanged and in order — captured argv is exactly `-n`, `SpotOn`, `-k`, `<dummy key content>`, `-D`, `<dataDir>`, `--single-track`, `<translated uri>`
    - Test 5 (regression): every pre-existing t/30 assertion (shebang, PCM FD, LD_LIBRARY_PATH, KEY=$(cat ...), -k "$KEY", -D dataDir, "$@" passthrough, sleep 2, final exec, T-72-01 raw-key-not-in-script-text) still passes
  </behavior>
  <action>
    Fixes verification gap #1 / CR-01, restoring end-to-end D-01/D-03/D-04 behavior.

    PART A — Soloist.pm `_launcherScript` template edit (surgical; do NOT touch ProtocolHandler.pm, Plugin.pm, or custom-convert.conf — per D-01 the LMS-side URL scheme stays `spoton://` and translation happens only at this spawn boundary):

    Insert a translation block into the heredoc template between the `KEY=$(cat "__KEY__") || exit 1` line and the D-06 retry-loop comment. The block is a POSIX positional-parameter rotation (POSIX sh has no arrays; the launcher targets `#!/bin/sh` per RESEARCH A5):
    - Save the argument count in a new variable `argc` (assign from `$#`) and initialize `argi=0`.
    - Loop `while [ "$argi" -lt "$argc" ]`; in the body: assign `a=$1`, then `shift`, then a `case "$a"` statement whose single pattern matches values starting with the internal scheme (`spoton://*`) and rewrites `a` to the concatenation of the literal `spotify:` and the suffix expansion `${a#spoton://}` (this maps `spoton://track:ID` to `spotify:track:ID` and `spoton://episode:ID` to `spotify:episode:ID` with one rule); all other values fall through unchanged. Close the case, then re-append with `set -- "$@" "$a"` and increment `argi=$((argi + 1))`.
    - After the loop, `"$@"` holds the same arguments in original order with any spoton:// value translated — the three existing `"__BIN__" ... "$@"` invocation lines (two in the retry loop, one final exec) need NO changes.
    - Variable naming: use exactly `argc`, `argi`, `a` — the template already uses `n`, `rc`, `start`, `now`, `elapsed` for the retry loop; do not collide and do not rename existing variables.
    - Precede the block with a short English comment naming CR-01 and stating that LMS's $URL$ token delivers the plugin-internal spoton:// URL and soloist's --single-track requires a real spotify: URI (comments in English per project convention).
    - Keep `a` quoted (`"$a"`, `set -- "$@" "$a"`) everywhere — no new word-splitting/injection surface (relevant to threat T-72-03-02 below).
    - The heredoc is single-quoted (`<<'SCRIPT'`), so every `$` in the new block stays literal in Perl — no escaping needed. Verify none of the new lines contain the placeholder tokens `__LIB__`/`__KEY__`/`__BIN__`/`__DATA__` (the `s///g` substitutions must not touch the translation block).

    PART B — t/30_soloist_launcher.t behavioral subtest (append after the existing script-content test block, before the idempotency block or after it — position is free, but keep existing blocks byte-identical):

    - Wrap the whole new subtest in a `SKIP:` block that skips unless `$^O ne 'MSWin32' && -x '/bin/sh'` — the subtest executes POSIX shell; per project Windows-compat policy tests must degrade gracefully on Windows (the launcher itself is Linux-only, ensureLauncher early-returns on ISWINDOWS).
    - Create a stub "soloist binary": write a shell script into a fresh `tempdir(CLEANUP => 1)` whose body is `#!/bin/sh` followed by a `printf '%s\n' "$@" >` redirect into an argv-capture file path baked in via Perl interpolation at stub-write time (e.g. `$stubdir/argv.out`), then `exit 0`. chmod 0755.
    - Generate the wrapper under test by calling `Plugins::SpotOn::Soloist::_launcherScript($lib, $key, $stub_bin, $data)` DIRECTLY with: `$lib` = any existing temp dir (LD_LIBRARY_PATH export is harmless), `$key` = `Plugins::SpotOn::Soloist::keyPath()` (the dummy-key file written by `storeKey($DUMMY_KEY)` at the top of the test file already exists there), `$stub_bin` = the stub path, `$data` = `Plugins::SpotOn::Soloist::dataDir()`. Write the returned script text to a temp file, chmod 0755. Calling `_launcherScript` directly (rather than re-running `ensureLauncher`) is deliberate: it substitutes the stub for `__BIN__`, which ensureLauncher's deterministic binary path cannot.
    - Run 1 (track): `system($wrapper, '--single-track', 'spoton://track:abc123DEF')`; assert exit status 0 (stub exits 0, so the retry loop exits immediately with no sleep). Slurp the argv-capture file and assert, per the behavior list: exact line `spotify:track:abc123DEF`; line `--single-track`; the line sequence `-n`, `SpotOn`, `-k`, `$DUMMY_KEY`, `-D`, dataDir, `--single-track`, `spotify:track:abc123DEF` in order; and zero lines matching the internal scheme prefix (negative grep is against the CAPTURE FILE only — the wrapper script text legitimately contains the literal in its comment and case pattern).
    - Run 2 (episode): re-run with `spoton://episode:xyz789`, assert captured line `spotify:episode:xyz789`.
    - Do NOT weaken or delete any existing assertion; the `-k "$KEY"` and `"$@"` structural regexes still match the modified template (the invocation lines are unchanged).
  </action>
  <verify>
    <automated>prove -l t/30_soloist_launcher.t && prove -l t/</automated>
  </verify>
  <acceptance_criteria>
    - `Plugins/SpotOn/Soloist.pm` `_launcherScript` template contains the argc/argi/a rotation loop with the single `case` rewrite between the KEY line and the retry loop; the three `__BIN__` invocation lines are byte-identical to before
    - `custom-convert.conf`, `ProtocolHandler.pm`, `Plugin.pm` have no diff in this task
    - New t/30 subtest executes the generated wrapper against a stub binary; captured child argv contains `spotify:track:abc123DEF` for input `spoton://track:abc123DEF` and `spotify:episode:xyz789` for the episode input
    - Captured argv file has zero occurrences of the internal `spoton://` prefix (runtime capture file only)
    - Captured argv preserves order and non-URL args verbatim (`-n SpotOn -k <key> -D <dataDir> --single-track <uri>`)
    - Subtest is SKIP-guarded for `MSWin32` / missing `/bin/sh`
    - `prove -l t/30_soloist_launcher.t` green; `prove -l t/` full suite green with no count regression below the current 1101
  </acceptance_criteria>
  <done>Verification gap #1 truth holds: a soloist invocation reached through the real generated launcher receives a parseable `spotify:` URI, proven by an automated argv-capture regression test; all existing tests pass.</done>
  <reversibility rating="reversible">Pure template + test change; reverting the commit restores the prior wrapper.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — document the -k argv trade-off (soloist has no alternative key input) and pin it with tests</name>
  <files>Plugins/SpotOn/Soloist.pm, t/30_soloist_launcher.t</files>
  <read_first>
    - Plugins/SpotOn/Soloist.pm lines 354-403 (`_launcherScript` — the KEY comment block being expanded) and lines 476-530 (`storeKey` — the existing "raw key is NEVER logged" security-comment style to mirror)
    - .planning/phases/72-soloist-browse-playback/72-REVIEW.md WR-01 (the finding and its fix guidance)
    - .planning/phases/72-soloist-browse-playback/72-RESEARCH.md lines 266-286 (verified `soloist --help` CLI surface: `-k, --api-key <key>` is the only key mechanism; no env var, no stdin, no key-file flag)
    - t/30_soloist_launcher.t (existing key-handling assertions: `KEY=$(cat ...)`, `-k "$KEY"`, T-72-01)
  </read_first>
  <behavior>
    - Test 1: the generated launcher script text contains a `WR-01` marker comment documenting the accepted argv-visibility trade-off
    - Test 2: the Task-1 argv capture shows `-k` immediately followed by the exact `$DUMMY_KEY` content — the 0600 key file's bytes really reach the child (a broken keyPath/cat would fail this)
    - Test 3: T-72-01 still passes — the raw key value appears in the RUNTIME child argv (by design, documented) but never in the generated script TEXT
  </behavior>
  <action>
    Fixes verification gap #2 / WR-01 via the gap's explicitly sanctioned fallback ("If -k is the only supported mechanism, document the accepted trade-off"). Decision basis (pre-verified during planning, executor does not need the binary): the RESEARCH.md first-hand `--help` transcript of pinned soloist 1.3.7.489 lists only `-k, --api-key <key>` for the key, and `strings` on the distributed x86_64 binary shows `Error: --api-key is required` with the only recognized environment variables being `SPOTIFY_SSL_CERT_FILE`/`SPOTIFY_SSL_CERT_DIR` — no key env var, no stdin path, no key-file flag exists. Do NOT invent an env-var mechanism (e.g. SPOTON_API_KEY) — the binary would ignore it and still demand `-k`.

    PART A — Soloist.pm comment edits (no functional change):
    - In the `_launcherScript` heredoc, replace/extend the current comment above `KEY=$(cat "__KEY__") || exit 1` with an English comment block stating, in substance: the key is command-substituted from the 0600 spak.key file so it never appears in the convert rule, LMS config, this wrapper's own argv, or the script text; soloist 1.3.7.489 accepts the key ONLY via its -k/--api-key argument (verified against --help and the binary itself — no env-var or stdin alternative), so for the duration of each track transcode the key is visible in the soloist child's /proc/<pid>/cmdline to local users on the host; this is an ACCEPTED RISK (WR-01) — the 0700 root dir + 0600 key file remain the primary control; revisit if a future soloist release adds an env/stdin key path; on shared multi-user hosts, mounting /proc with hidepid=2 removes the exposure. Include the literal token `WR-01` in the comment (test anchor).
    - Above `sub _launcherScript` in the Perl code, add a one-line pointer noting the -k argv trade-off is documented and accepted per WR-01 / 72-VERIFICATION gap #2.

    PART B — t/30_soloist_launcher.t assertions:
    - In the existing script-content test block, next to the current `-k "$KEY"` assertion, add: `like($content, qr/WR-01/, ...)` asserting the accepted-trade-off marker is present in the generated script.
    - In the Task-1 behavioral subtest, add the ordered-argv assertion that `-k` is immediately followed by a line equal to `$DUMMY_KEY` (if Task 1's order assertion already covers this exactly, strengthen its description to name the WR-01 key-delivery contract instead of duplicating).
    - Leave the T-72-01 `unlike` assertion (raw key never in script text) untouched — it must still pass, proving text-level hygiene is preserved while the runtime argv handoff is the documented mechanism.
  </action>
  <verify>
    <automated>prove -l t/30_soloist_launcher.t && prove -l t/</automated>
  </verify>
  <acceptance_criteria>
    - Generated launcher script contains a WR-01 comment block covering: -k is soloist 1.3.7.489's only key mechanism, /proc/<pid>/cmdline visibility window, accepted-risk statement, hidepid=2 note for shared hosts
    - No functional line of the launcher changed in this task (comments only); `diff` of the template's non-comment lines vs. Task 1's result is empty
    - t/30 asserts the WR-01 marker in the generated script text
    - Behavioral capture asserts `-k` followed by the exact dummy-key file content in the child argv
    - T-72-01 assertion unchanged and green
    - `prove -l t/` fully green
  </acceptance_criteria>
  <done>Verification gap #2 is closed via its documented-trade-off branch: the key's only exposure surface is the soloist child's own argv, that exposure is explicitly documented and test-pinned, and every text/log/convert-rule surface remains key-free.</done>
  <reversibility rating="reversible">Comment + test additions only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS process → soloist child argv | spak-key crosses into a child process's `/proc/<pid>/cmdline`, readable by other local accounts |
| Spotify API response → convert-rule shell | track/episode IDs from API responses reach a shell-interpolated command line (`$URL$`) and now the wrapper's case/rewrite logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-03-01 | Information Disclosure | launcher `-k "$KEY"` argv to soloist child | medium | accept | Soloist 1.3.7.489 offers no env/stdin key input (verified via --help + binary strings). Exposure limited to per-track child lifetime; 0700 dir + 0600 key file remain primary control. Documented in-template as WR-01 accepted risk with hidepid=2 guidance (Task 2); revisit on soloist upgrade. |
| T-72-03-02 | Tampering | wrapper translation loop handling `$URL$`-derived values | low | mitigate | All new shell references fully quoted (`"$a"`, `set -- "$@" "$a"`); the rewrite is a fixed prefix substitution, no eval/expansion of attacker-shaped data. (Broader unquoted-`$URL$`-in-conf hardening is WR-03, explicitly out of this gap plan's scope — not a verification gap.) |
| T-72-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan (Perl + sh edits only); package-legitimacy gate not applicable. |
</threat_model>

<verification>
- `prove -l t/30_soloist_launcher.t` — new behavioral subtests (track translation, episode translation, negative scheme check, arg order, key delivery, WR-01 marker) plus all pre-existing structural assertions green
- `prove -l t/` — full suite green, no regression below the current 1101-test baseline
- `git diff` scope check: only `Plugins/SpotOn/Soloist.pm` and `t/30_soloist_launcher.t` modified; `custom-convert.conf`, `custom-types.conf`, `ProtocolHandler.pm`, `Plugin.pm`, `Settings.pm` untouched
- Manual re-verification hook (for the deferred human items in 72-VERIFICATION.md): after this plan, the "expected to fail per CR-01" caveat on human-verification item 1 (real Browse audio on a paired host) no longer applies — the E2E UAT can proceed
</verification>

<success_criteria>
- Both `gaps:` entries in 72-VERIFICATION.md are closed at the code level: (1) the launcher translates spoton:// to spotify: with an automated regression test through the actual translation path; (2) the key-argv exposure is resolved via the gap's sanctioned documented-trade-off branch with test-pinned documentation
- must_haves truths above all hold; re-running the verifier against this plan's must_haves yields no failed truths
- Zero regressions in the existing suite
</success_criteria>

<output>
## Artifacts this phase produces (gap plan 72-03)

- No new public Perl symbols. Modified internals:
  - `Plugins::SpotOn::Soloist::_launcherScript` — template body gains the `argc`/`argi`/`a` spoton://→spotify: rotation loop and the WR-01 accepted-risk comment block (function signature unchanged)
- `t/30_soloist_launcher.t` — new SKIP-guarded behavioral subtest(s): stub-binary argv capture (track/episode translation, arg order, key delivery) and WR-01 marker assertion

Create `.planning/phases/72-soloist-browse-playback/72-03-SUMMARY.md` when done.
</output>
