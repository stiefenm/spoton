---
phase: 73-soloist-connect-mode
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/Unified/SoloistWS.pm
  - Plugins/SpotOn/Unified/SoloistDaemon.pm
  - Plugins/SpotOn/Unified/DaemonManager.pm
  - t/31_soloist_ws.t
  - t/32_soloist_events.t
autonomous: true
requirements: [D-05, D-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "During a soloist Connect session, LMS-side pause/unpause/next/prev/seek/volume actions reach Soloist as WS commands and Spotify state follows (D-06 command direction)"
    - "When the WS connection is unreachable, LMS control actions fall back to the Spotify Web API (D-15 parity with librespot)"
    - "After a WS reconnect, LMS resyncs position/track/volume from a playback_state snapshot instead of drifting"
    - "Repeat mode is set with the correct two-command matrix: off=(context:false,track:false), context=(true,false), track=(false,true) (Pitfall 6 footnote, not the contradictory table)"
    - "An expired soloist build (exit code 10) stops the daemon permanently with a clear log message instead of crash-looping every 5-300s (Pitfall 7)"
    - "The remaining build lifetime parsed from soloist's own log line is cached per host for the Settings display (73-04 consumes it)"
  artifacts:
    - "Plugins/SpotOn/Connect.pm — backend dispatch in _sendControlCommand routing to SoloistWS"
    - "Plugins/SpotOn/Unified/SoloistWS.pm — full command map incl. sendRepeatMode/sendShuffle, get_state resync"
    - "t/31_soloist_ws.t — full WS parsing/serialization/reconnect coverage"
    - "t/32_soloist_events.t — event->spottyconnect mapping table incl. stop-collapse and repeat matrix"
  key_links:
    - "Connect.pm _onPause/_onVolume/_onSeek/_onPlaylistJump -> _sendControlCommand -> helperForClient isa SoloistDaemon -> ws->sendCommand — the single dispatch point; missing it sends HTTP /control/* to a port fake-libpulse serves PCM on"
    - "endpoint->command map (/control/pause->pause, /control/next->skip_next, ...) <-> soloist WS command vocabulary — a wrong verb returns {type:error} and the user action silently no-ops"
    - "exit-code-10 detection <-> CRASH_BACKOFF suppression — without the classification the backoff restarts a permanently dead binary forever"
---

<objective>
Complete the D-06 loop in the LMS→Soloist direction and harden the daemon lifecycle. After 73-01, Soloist drives LMS; now LMS drives Soloist: Connect.pm's existing forwarders route through a backend dispatch to WS commands (pause/play/skip_next/skip_prev/seek/set_volume), with the Web-API fallback preserved. Reconnect resync via get_state/playback_state. Repeat/shuffle helpers with the corrected two-command matrix. Build-expiry (exit code 10) escalation and expiry-day parsing for Settings. Full unit coverage in t/31 + t/32 (RESEARCH Wave-0 gaps for D-05/D-06).

Purpose: Connect is not bidirectional until LMS-side controls work; expiry hardening prevents the worst known failure mode of the pinned binary.
Output: backend-dispatched control path, resync logic, expiry handling, two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/73-soloist-connect-mode/73-CONTEXT.md
@.planning/phases/73-soloist-connect-mode/73-RESEARCH.md
@.planning/phases/73-soloist-connect-mode/73-01-SUMMARY.md
@Plugins/SpotOn/Connect.pm
@Plugins/SpotOn/Unified/SoloistWS.pm
@Plugins/SpotOn/Unified/SoloistDaemon.pm
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend dispatch for control commands + reconnect resync + repeat/shuffle helpers</name>
  <files>Plugins/SpotOn/Connect.pm, Plugins/SpotOn/Unified/SoloistWS.pm</files>
  <read_first>Plugins/SpotOn/Connect.pm lines 183-258 (_sendControlCommand + _sendControlFallback) and 317-506 (the four forwarders), Plugins/SpotOn/Unified/SoloistWS.pm (as built by 73-01), 73-RESEARCH.md Pattern 3 command table + Pitfall 6</read_first>
  <behavior>
- t/31: sendCommand serializations for the full map — pause, play, skip_next, skip_prev, seek+position_ms, set_volume+volume, add_to_queue+uri, get_state, get_queue, activate, deactivate — each produces `{"type":"command","command":...}` JSON with exactly the expected extra keys.
- t/31: `sendRepeatMode('off'|'context'|'track')` emits exactly TWO commands, set_repeat_context and set_repeat_track, with the enabled pairs (false,false)/(true,false)/(false,true).
- t/31: after a simulated reconnect (disconnect + connect with logged_in:true auth_state), the client sends get_state; a playback_state snapshot with a >3s position deviation emits one spottyconnect seek; a <=3s deviation emits none.
  </behavior>
  <action>
**1. Connect.pm — single dispatch point (keep the four forwarders and every guard untouched):** at the top of `_sendControlCommand`, resolve the helper: `require Plugins::SpotOn::Unified::DaemonManager; my $helper = Plugins::SpotOn::Unified::DaemonManager->helperForClient($client->id);`. If `$helper && $helper->isa('Plugins::SpotOn::Unified::SoloistDaemon')`: translate endpoint→WS command per the RESEARCH table — '/control/pause'→`pause`, '/control/play'→`play` (no uri: resume semantics), '/control/next'→`skip_next`, '/control/prev'→`skip_prev`, '/control/seek'→`seek` with `position_ms => $body->{position_ms}`, '/control/volume'→`set_volume` with `volume => int($body->{volume})`. Call `$helper->ws && $helper->ws->connected && $helper->ws->sendCommand(...)`; when the ws is absent/disconnected or sendCommand returns false, fall through to the existing `_sendControlFallback` (D-15 Web-API fallback works identically for soloist — same Spotify account, same player endpoints). Log the mapped command at the existing DIAG points (reuse the control_cmd_sent/control_cmd_fail diag vocabulary with a `backend=soloist` marker). The librespot HTTP path below stays byte-identical.

**2. SoloistWS — command surface completion:**
- Generic `sendCommand` exists from 73-01; add convenience wrappers used by Connect/Browse code: `sendRepeatMode($mode)` implementing the Pitfall-6 footnote matrix (two sequential commands; the official table's track-row is self-contradictory — follow the footnote: off=(context false, track false), context=(true,false), track=(false,true)); `sendShuffle($bool)` → `set_shuffle {enabled}`. `activate`/`deactivate` need no wrapper (generic sendCommand covers them; document them in the module POD/comment as the LMS-initiated-transfer escape hatch, unwired by design — D-07 transfer is app-driven).
- URI validation discipline: any command carrying a `uri` param (add_to_queue, play-with-uri) validates `^spotify:(?:track|episode):[A-Za-z0-9]+$` before sending and refuses otherwise (T-22-01; RESEARCH V5 — add_to_queue accepts ONLY track URIs).
- **Reconnect resync:** on every (re)connect completion, after `get_auth_state`, also send `get_state` when the stored auth state says logged_in. In the `playback_state` handler (73-01 has the skeleton): reconcile — update lastTrackId (emit `change` if a session is active and the track differs from lastTrackId), volume (emit `volume` if it differs), and position (emit `seek` only beyond the 3 s tolerance — same threshold constant as position_sync; extrapolate the local position from lastPositionMs + elapsed wallclock before comparing). This closes the drift window after WS drops during an active Connect session.
- `command_result` handler: debug-log command echo; `error` handler: warn including the failed context if the last sent command is tracked (keep a 1-deep lastCommand field — enough for log clarity, no queue).

**3. t/31 extension:** extend the 73-01 harness with the behavior-block cases: full serialization map, repeat matrix, reconnect→get_state, snapshot reconciliation with both tolerance outcomes, and the ws-down `sendCommand` false return (Connect.pm fallback contract).
  </action>
  <verify>
    <automated>prove -l t/31_soloist_ws.t</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/31_soloist_ws.t` passes with the new cases.
- `grep -c 'SoloistDaemon' Plugins/SpotOn/Connect.pm` >= 1 and `grep -c '_sendControlFallback' Plugins/SpotOn/Connect.pm` >= 2 (existing fallback + soloist fall-through reuse).
- `grep -c 'sub sendRepeatMode\|sub sendShuffle' Plugins/SpotOn/Unified/SoloistWS.pm` == 2.
- `grep -c 'set_repeat_context' Plugins/SpotOn/Unified/SoloistWS.pm` >= 1 and `grep -c 'set_repeat_track' Plugins/SpotOn/Unified/SoloistWS.pm` >= 1.
- `grep -c 'get_state' Plugins/SpotOn/Unified/SoloistWS.pm` >= 2 (reconnect send + handler).
  </acceptance_criteria>
  <done>Every LMS-side Connect control action reaches Soloist over WS with Web-API fallback; reconnects resync instead of drifting; repeat/shuffle helpers encode the corrected matrix. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Lifecycle hardening — build-expiry escalation, expiry-days parsing, backoff interplay</name>
  <files>Plugins/SpotOn/Unified/DaemonManager.pm, Plugins/SpotOn/Unified/SoloistDaemon.pm</files>
  <read_first>Plugins/SpotOn/Unified/DaemonManager.pm lines 379-503 (_streamAlivePoll incl. the 73-01 isa gates) and 522-574 (_handleCredentialCrash — the escalation template), Plugins/SpotOn/Unified/SoloistDaemon.pm (stderrTail + log file naming from 73-01), 73-RESEARCH.md Pitfall 7</read_first>
  <action>
**1. Exit-code-10 escalation (Pitfall 7):** in `_streamAlivePoll`'s crash branch for soloist helpers (the non-`Unified::Daemon` path from 73-01), before the generic backoff: obtain the exit code via the Proc::Background handle (`$helper->_proc->wait` on a dead process returns the wait status; exit code = status >> 8 — eval-guard the call). If the exit code is 10, escalate instead of restarting: warn with an actionable message naming the build-expiry cause and the pinned version (Soloist::SOLOIST_VERSION), set a plugin-cache flag `spoton_soloist_expired` => { version => SOLOIST_VERSION, at => time() } with 'never' TTL (cleared only when a different soloist version activates — add that clear to Soloist.pm's `_versionCheck` success path OR document it as manual-clear via cache flush; choose the _versionCheck clear, it is one line and self-healing), and `stopHelper` — mirroring `_handleCredentialCrash`'s stay-down discipline. Every other exit code takes the existing CRASH_BACKOFF path unchanged. Also short-circuit `startHelper`'s soloist branch: when the expired flag is set, log-skip like the other prereq states (new synthetic state string 'soloist_build_expired' folded into the same skip log) so the 60s watchdog cannot resurrect the daemon.

**2. Expiry-days parsing:** soloist logs `client expires in N days` on stdout at startup (live-verified). In SoloistDaemon, after the ws.port poll completes (daemon demonstrably up and its startup banner flushed), read the head of the per-player log file (bounded read, first 8 KiB) and match `client expires in (\d+) days`; on match write plugin-cache key `spoton_soloist_expiry_days` => { days => $1, checked_at => time() } (host-global — the binary is shared across players; last writer wins is fine). 73-04 renders it in Settings with a warning threshold. No timer, no re-poll — once per daemon start is enough granularity for a 90-day clock.

**3. Backoff interplay audit (small, mostly comments + asserts):** verify and document in code that (a) SoloistWS reconnect timers are killed by SoloistDaemon->stop (73-01 contract — add a defensive killTimers there if missing), (b) the staggered start path (initHelpers → pendingStarts) applies to soloist daemons unchanged (Pitfall 9 mDNS contention — confirm startHelper is the only spawn entry, add a one-line comment at the soloist branch citing STAGGER_DELAY), (c) `helperPids` includes soloist daemons so Plugin.pm's orphan cleanup never kills them (it iterates %helperInstances generically — verify, comment).
  </action>
  <verify>
    <automated>prove -l t/28_soloist_dispatch.t && perl -e 'my $c = do { open my $f, "<", "Plugins/SpotOn/Unified/DaemonManager.pm" or die; local $/; <$f> }; die "exit-10 escalation missing" unless $c =~ /spoton_soloist_expired/; die "startHelper gate missing" unless $c =~ /soloist_build_expired/; print "ok\n"'</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/28_soloist_dispatch.t` passes (no regression from the startHelper gate).
- `grep -c 'spoton_soloist_expired' Plugins/SpotOn/Unified/DaemonManager.pm` >= 2 (set + startHelper gate) and `grep -c 'spoton_soloist_expired' Plugins/SpotOn/Soloist.pm` >= 1 (version-change clear).
- `grep -c 'client expires in' Plugins/SpotOn/Unified/SoloistDaemon.pm` >= 1 and `grep -c 'spoton_soloist_expiry_days' Plugins/SpotOn/Unified/SoloistDaemon.pm` >= 1.
  </acceptance_criteria>
  <done>An expired binary parks the daemon with one clear message and a Settings-consumable flag; expiry countdown is captured; stagger/orphan/backoff mechanics are verified to cover soloist daemons. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: t/32 event-mapping table coverage</name>
  <files>t/32_soloist_events.t</files>
  <read_first>t/31_soloist_ws.t (harness to reuse — factor shared stubs by copy, not a shared helper module, matching the existing t/ convention), 73-RESEARCH.md Pattern 3 mapping table, Plugins/SpotOn/Unified/SoloistWS.pm</read_first>
  <behavior>
- Table-driven: each row = (event JSON fixture, expected spottyconnect emissions). Rows: track_changed first (start) vs subsequent (change with prev id); playback_changed paused→stop, stopped→stop, playing-after-pause→resume with position; volume_changed→volume passthrough 0 and 100 boundary values; position_sync 2s deviation→nothing, 5s deviation→seek with 3-decimal seconds; device_changed active→(start when track known)/inactive→stop; auth_state→no emission; context_changed/queue_changed/options_changed→no emission (this plan's contract); unknown type→no emission, no die.
- Repeat matrix rows: sendRepeatMode x3 asserting both commands and their enabled flags per mode.
- Gating rows: Connect toggle off → zero emissions for a full event burst; malformed JSON burst → zero emissions, zero deaths.
  </behavior>
  <action>
Create t/32_soloist_events.t as the D-06 mapping regression net: the isolated-require harness from t/31 (write_stub set, recorder client, JSON::PP delegation), then implement the behavior-block table with `is_deeply` on the recorded emission list after each fixture group, resetting recorder + SoloistWS session state between groups (fresh instance per group is simplest). Keep fixtures as compact single-line JSON strings mirroring the documented payloads (uri under item.uri etc. — matching what 73-01 implemented, corrected by any A4 findings recorded in 73-01-SUMMARY). This file is the contract 73-03 builds browse logic against — the mapping itself must not change silently.
  </action>
  <verify>
    <automated>prove -l t/32_soloist_events.t t/31_soloist_ws.t</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/32_soloist_events.t t/31_soloist_ws.t` passes.
- t/32 contains both stop-collapse fixtures and both position_sync tolerance rows: grep hits for 'paused' and 'stopped' and 'position_sync' in t/32_soloist_events.t each >= 1.
- `prove -l t/` fully green (no regression across the suite).
  </acceptance_criteria>
  <done>The complete event→spottyconnect mapping and command matrix are pinned by tests; RESEARCH Wave-0 gaps for D-05/D-06 are closed. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS user actions → soloist WS | Commands cross into the closed-source binary |
| soloist stdout log → Perl parser | Third-party text parsed for expiry/exit classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-07 | Tampering | uri params in WS commands | medium | mitigate | `^spotify:(track|episode):[A-Za-z0-9]+$` validation before send (Task 1) |
| T-73-08 | DoS | Crash-loop against an expired binary | medium | mitigate | Exit-code-10 permanent park + startHelper gate (Task 2) |
| T-73-09 | Repudiation | Silent command failures ({type:error} replies) | low | mitigate | lastCommand-context warn logging (Task 1) |
| T-73-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `prove -l t/` fully green including t/31 (extended) and t/32 (new).
- Manual/UAT: during an app-initiated Connect session, LMS pause/skip/seek/volume actions are mirrored in the Spotify app within ~1s; killing the soloist process mid-session triggers backoff restart; a simulated rc=10 (wrapper script test or documented reasoning) parks the daemon.
</verification>

<success_criteria>
- D-06: full bidirectional event/command set wired, with librespot-parity fallbacks and the corrected repeat matrix.
- D-05: WS client covers reconnect + resync; all parsing paths crash-safe and test-pinned.
- Pitfall 7 (expiry) and Pitfall 9 (stagger) mitigations in place.
</success_criteria>

<output>
Create `.planning/phases/73-soloist-connect-mode/73-02-SUMMARY.md` when done. Note any command verbs the live binary rejected (error replies) — 73-03's browse commands reuse this map.
</output>
