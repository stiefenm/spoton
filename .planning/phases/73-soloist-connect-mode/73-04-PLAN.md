---
phase: 73-soloist-connect-mode
plan: 04
type: execute
wave: 4
depends_on: ["73-03"]
files_modified:
  - Plugins/SpotOn/custom-convert.conf
  - Plugins/SpotOn/custom-types.conf
  - Plugins/SpotOn/Soloist.pm
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - t/03_convert_conf.t
  - t/04_types_conf.t
  - t/28_soloist_dispatch.t
  - t/30_soloist_launcher.t
  - CHANGELOG.md
autonomous: true
requirements: [D-01, D-02, D-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Phase-72 per-track machinery is fully retired: no sol convert rules, no sol content-type row, no generated launcher, no sox dependency anywhere in the plugin (D-03 completion, CONTEXT §specifics)"
    - "Settings shows the live Soloist state: per-player daemon running + paired status, WS auth state, build-expiry countdown with a warning, and an explicit hint when the LMS version lacks the WS library (< 9.1)"
    - "The pairing instructions describe the app-tap flow (start daemon, pick the device in the Spotify app once, per player) — the SSH --pair procedure is gone (D-07 consequence)"
    - "Sync groups are test-pinned: the soloist daemon runs on the sync master under the suffixed device name, and members receive proxied audio (Pattern 7)"
    - "All new UI strings exist in all 11 languages with real translations"
    - "librespot-backed installs see zero behavioral or UI regression"
  artifacts:
    - "Plugins/SpotOn/custom-convert.conf — soc/son rules only, byte-identical to their pre-phase content"
    - "Plugins/SpotOn/Settings.pm + basic.html — soloist daemon status block"
    - "Plugins/SpotOn/strings.txt — new/updated PLUGIN_SPOTON_SOLOIST_* keys, 11 languages"
    - "t/30_soloist_daemon.t (renamed from t/30_soloist_launcher.t) — daemon spawn-arg + per-player-dir + stale-file contract"
  key_links:
    - "Settings status block <-> cache keys spoton_soloist_ws_<mac> / spoton_soloist_expiry_days / spoton_soloist_expired written by 73-01/73-02 — renaming either side silently blanks the UI"
    - "isPairedForClient(per-player data dir) <-> the D-07 app-tap pairing that populates settings/Users/ — the heuristic is the only pairing signal Settings has"
    - "sol-rule deletion <-> 73-03's 'soc' content-type switch — deleting the rules is only safe because no code path selects a sol profile anymore"
---

<objective>
Finish the phase: retire the Phase-72 per-track path (launcher, sol rules, sox), pin sync-group behavior with tests, and surface the new daemon world in Settings (status, pairing howto, expiry warning, LMS-9.1 gate hint) in all 11 languages. CHANGELOG entry for the release notes.

Purpose: D-03 says the daemon REPLACES per-track spawning — leaving both paths alive reintroduces the two-process lock hazard the phase exists to kill; and without Settings visibility the new pairing flow is undiscoverable.
Output: clean conf/module surface, sync tests, Settings status block, i18n, CHANGELOG.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/73-soloist-connect-mode/73-CONTEXT.md
@.planning/phases/73-soloist-connect-mode/73-RESEARCH.md
@.planning/phases/73-soloist-connect-mode/73-03-SUMMARY.md
@Plugins/SpotOn/Soloist.pm
@Plugins/SpotOn/Settings.pm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the Phase-72 per-track path — launcher, sol rules, sol type, tests</name>
  <files>Plugins/SpotOn/custom-convert.conf, Plugins/SpotOn/custom-types.conf, Plugins/SpotOn/Soloist.pm, Plugins/SpotOn/Plugin.pm, t/03_convert_conf.t, t/04_types_conf.t, t/30_soloist_launcher.t</files>
  <read_first>Plugins/SpotOn/Soloist.pm (launcher block lines ~324-541), Plugins/SpotOn/Plugin.pm lines 135-160 (boot wiring), Plugins/SpotOn/custom-convert.conf, Plugins/SpotOn/custom-types.conf, t/03_convert_conf.t, t/04_types_conf.t, t/30_soloist_launcher.t</read_first>
  <reversibility rating="costly">Restoring the per-track path means reviving the launcher generator + conf rules from git history (D-03's documented rollback cost). Proceed only because 73-03's verification proved the daemon path.</reversibility>
  <action>
- **custom-convert.conf:** delete the `sol pcm * *` rule (all 3 lines). `soc pcm`/`son ogg` stay byte-identical. The external float32-conversion pipeline dependency disappears with the rule — after this deletion the plugin references no external audio converter anywhere. <!-- planner-discipline-allow: sox -->
- **custom-types.conf:** delete the `sol` row (73-03 removed every code path answering that content type).
- **Soloist.pm:** remove the entire Phase-72 launcher block: LAUNCHER_NAME constant, `dataDir()` (shared), `isPaired()` (shared-dir heuristic), `launcherPath()`, `_launcherScript()`, `ensureLauncher()` and the `ensureLauncher()` call inside `ensureBinary()`; remove the `Slim::Utils::Misc::addFindBinPaths` registration (nothing resolves the token anymore). Add `sub isPairedForClient { my ($mac) = @_; ... }` — the same non-dot-entry heuristic applied to `dataDirForClient($mac)` (comment: soloist stores the session under settings/Users/, presence-based heuristic per RESEARCH). Update the module header comment: findbin is (again) not used at all; per-player data dirs under players/<mac>/; the shared data/ dir from Phase 72 is left on disk untouched (a paired session there is orphaned but harmless — note it for TROUBLESHOOTING in the SUMMARY). Do not delete the generated launcher file from users' cachedirs at runtime — stale files are inert without the conf rules; mention in the SUMMARY.
- **Plugin.pm:** simplify the boot wiring — the WR-06 branch collapses to `Plugins::SpotOn::Soloist::ensureBinary() if backend eq 'soloist'` (no ensureLauncher else-branch; keep the SCANNER guard).
- **Tests:** t/03: replace the sol-rule assertions with their inverse (comment-filtered: `grep -v '^\s*#'` piped to a count of the launcher token must be 0) and keep every soc/son assertion; t/04: sol row assertions inverted the same way; t/30: rewrite as `t/30_soloist_daemon.t` (git mv) — reuse its Soloist.pm harness to cover what the launcher test used to guard, now for the daemon world: `dataDirForClient`/`cacheDirForClient` shapes (mac cleaning, players/ prefix), `isPairedForClient` false-on-empty/true-after-touch, `readKey` returns the file content and undef on absence, and (via the t/28-style SoloistDaemon require) `_spawnArgs` carrying -w 127.0.0.1:0 and both per-player dirs. Assert the retired symbols are gone from Soloist.pm (can('ensureLauncher') is false after require).
  </action>
  <verify>
    <automated>prove -l t/03_convert_conf.t t/04_types_conf.t t/30_soloist_daemon.t t/26_soloist_check.t t/27_soloist_key.t</automated>
  </verify>
  <acceptance_criteria>
- `prove -l t/03_convert_conf.t t/04_types_conf.t t/30_soloist_daemon.t t/26_soloist_check.t t/27_soloist_key.t` passes.
- `grep -v '^\s*#' Plugins/SpotOn/custom-convert.conf | grep -c 'spoton-soloist'` == 0 and `grep -v '^\s*#' Plugins/SpotOn/custom-convert.conf | grep -c 'sox'` == 0.
- `grep -v '^#' Plugins/SpotOn/custom-types.conf | grep -c '^sol\b'` == 0.
- `grep -c 'sub ensureLauncher\|sub _launcherScript\|sub launcherPath' Plugins/SpotOn/Soloist.pm` == 0.
- `grep -c 'sub isPairedForClient' Plugins/SpotOn/Soloist.pm` == 1.
- `prove -l t/` fully green (t/29/t/31/t/32 unaffected).
  </acceptance_criteria>
  <done>Exactly one soloist execution path exists (the persistent daemon); conf and module surface carry no per-track remnants; the retired guarantees are re-pinned against the daemon. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync-group pinning — tests + gap fixes</name>
  <files>t/28_soloist_dispatch.t</files>
  <read_first>Plugins/SpotOn/Unified/DaemonManager.pm (initHelpers slave delegation lines ~313-348, deviceNameForClient, sync subscription handler lines ~207-250), Plugins/SpotOn/Unified/SoloistDaemon.pm (name handling, stopForSync), t/28_soloist_dispatch.t, 73-RESEARCH.md Pattern 7</read_first>
  <behavior>
- t/28 additions: with a synced client-pair stub (slave with master), the soloist evaluation path delegates the daemon to the master's MAC and stops any slave-tracked helper (mirrors the librespot flow — assert via the recorded start/stop calls the harness already tracks or gains).
- deviceNameForClient with a synced non-group client appends the localized suffix within the 60-char cap — assert the soloist start path consumes exactly this name (the name passed into the SoloistDaemon constructor path/_spawnArgs).
- The sync-change handler's name-comparison restart also applies to a SoloistDaemon instance (helper->name vs deviceNameForClient mismatch → stopForSync when idle).
  </behavior>
  <action>
Pattern 7 says the librespot sync machinery transfers 1:1 — this task PROVES it and fixes any gap found while writing the assertions (expected gaps, if any, are of the shape "librespot-only isa/accessor assumption in the sync handler"; fix them in DaemonManager/SoloistDaemon with the same isa-gating style 73-01 used — if a fix touches those files, add them to the commit and note it in the SUMMARY). Extend t/28 with the behavior-block cases using its existing controllable client/prefs stubs (add a minimal Slim::Player::Sync stub with isSlave/slaves if the harness lacks one). The audio fan-out itself (new() proxy for members) is already pinned by t/29's synced cases — do not duplicate it here; reference it in a comment.
  </action>
  <verify>
    <automated>prove -l t/28_soloist_dispatch.t t/29_soloist_browse.t</automated>
    <human-check>Dev LMS: sync two squeezelite players, backend=soloist — exactly one soloist process (master MAC), device name carries the sync suffix in the Spotify app picker, both players output Browse audio; unsync → names/daemons re-evaluate without a crash loop.</human-check>
  </verify>
  <acceptance_criteria>
- `prove -l t/28_soloist_dispatch.t t/29_soloist_browse.t` passes with the new sync cases.
- t/28 contains assertions for slave delegation AND the sync-suffix name on the soloist path (grep hits for 'isSlave' and 'deviceNameForClient' in t/28_soloist_dispatch.t each >= 1).
  </acceptance_criteria>
  <done>Sync groups on the soloist backend are contract-tested; any discovered gap is fixed, not documented away. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Settings daemon status + pairing howto rewrite + i18n (11 languages) + CHANGELOG</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt, CHANGELOG.md</files>
  <read_first>Plugins/SpotOn/Settings.pm lines 495-530 (soloist template params from Phase 72), Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (soloist-fields block), Plugins/SpotOn/strings.txt (PLUGIN_SPOTON_SOLOIST_* block — the 11-language format), CHANGELOG.md head</read_first>
  <action>
**1. Settings.pm template params (replace/extend the Phase-72 set):**
- Remove the Phase-72 shared-dir pairing-status and launcher-path template params (both retired with the launcher); pairing becomes per-player — build `soloistPlayers` = arrayref over `Slim::Player::Client::clients()` of `{ name, mac, paired => Soloist::isPairedForClient($mac), daemonAlive => (DaemonManager->helperForClient($mac) && ->alive && ->isa SoloistDaemon) ? 1 : 0, wsState => cache 'spoton_soloist_ws_<macClean>' snapshot (logged_in/is_active) }`. Guard everything for backend ne soloist (params only computed when backend=soloist, matching the existing conditional pattern).
- Expiry: read `spoton_soloist_expiry_days` and `spoton_soloist_expired` → params `soloistExpiryDays` (undef when unknown), `soloistExpired`; warning threshold: flag `soloistExpiryWarn` when days <= 14.
- WS-library gate: param `soloistNeedsLms91` = 1 when `_backendPrereqState('soloist')` returns the wslib state (require DaemonManager; call the existing function rather than re-probing).

**2. basic.html:** inside the existing soloist-fields block, replace the pairing-status/launcher sub-block with: a per-player status table (player name, daemon running dot, paired yes/no, logged-in state), the expiry line (days remaining; warning styling when soloistExpiryWarn; hard error line when soloistExpired), and the LMS-9.1 hint when soloistNeedsLms91. Reuse the existing conditional-display conventions of the file (TT IF blocks, the two querySelectorAll-toggled groups from 72-02 — do not restructure them).

**3. strings.txt (all 11 languages: CS DA DE EN ES FR IT NL NO PL SV, real translations — never EN fallback):**
- REWRITE `PLUGIN_SPOTON_SOLOIST_PAIR_HOWTO`: app-tap flow — enable the Soloist backend, wait for the daemon, then select the player once in the Spotify app's device picker; pairing is per player and stored locally. No SSH, no command line.
- UPDATE `PLUGIN_SPOTON_SOLOIST_NOT_PAIRED` text to the new consequence wording (playback fails until the device is selected once in the app).
- NEW keys (thematically adjacent placement per the Phase-53 convention): `PLUGIN_SPOTON_SOLOIST_DAEMON_RUNNING`, `PLUGIN_SPOTON_SOLOIST_DAEMON_STOPPED`, `PLUGIN_SPOTON_SOLOIST_EXPIRY_DAYS` (takes a number — keep the sentence number-final or use a %s placeholder consistent with existing parametrized strings in the file), `PLUGIN_SPOTON_SOLOIST_EXPIRED` (build expired — update required), `PLUGIN_SPOTON_SOLOIST_NEEDS_LMS91` (Soloist Connect needs LMS 9.1+ — the bundled WebSocket library is missing on this server).
- Remove strings that referenced the launcher/SSH flow if they become unreferenced (verify with grep across Plugin/Settings/HTML before deleting; unreferenced only).

**4. CHANGELOG.md:** add an Unreleased/next-version entry (do NOT invent a version number — memory: Versionsnummern immer mit dem User abstimmen): persistent Soloist daemon per player, native Spotify Connect for the Soloist backend, app-tap pairing replaces SSH, Browse via daemon (gapless-capable, sox dependency removed), seek restored, build-expiry warning in Settings.
  </action>
  <verify>
    <automated>prove -l t/02_strings.t t/09_settings.t t/05_perl_syntax.t</automated>
    <human-check>Settings page with backend=soloist shows the per-player table with live daemon/pairing state and the expiry line; with backend=librespot none of it renders; on the dev 9.2 install the LMS-9.1 hint stays hidden.</human-check>
  </verify>
  <acceptance_criteria>
- `prove -l t/02_strings.t t/09_settings.t t/05_perl_syntax.t` passes (t/02 enforces the 11-language completeness for every new key).
- `grep -c 'PLUGIN_SPOTON_SOLOIST_NEEDS_LMS91' Plugins/SpotOn/strings.txt` == 1 and each new key block contains 11 language lines.
- `grep -c 'soloistPlayers' Plugins/SpotOn/Settings.pm` >= 1.
- `grep -c 'soloistLauncherPath' Plugins/SpotOn/Settings.pm` == 0.
- CHANGELOG.md contains the Soloist Connect entry without a self-invented version tag.
  </acceptance_criteria>
  <done>The daemon world is fully visible and explainable from Settings in 11 languages; release notes are staged. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings page → template params | Daemon/cache state rendered into the web UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-13 | Information Disclosure | Settings status block | low | mitigate | Render only booleans/day-counts/player names; never the spak-key, data-dir paths kept host-local (Task 3) |
| T-73-14 | Tampering | Stale launcher scripts left in user cachedirs | low | accept | Inert without conf rules; documented for TROUBLESHOOTING (Task 1) |
| T-73-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `prove -l t/` fully green across the suite.
- Structural: no launcher/sol/sox remnants (Task-1 gates); Settings params wired to the 73-01/73-02 cache keys.
- Manual/UAT: Settings render matrix (Task-3 human-check) + sync scenario (Task-2 human-check) via spoton-uat skill.
</verification>

<success_criteria>
- D-03 completed end to end: the per-track path no longer exists; CONTEXT §specifics (sol rules removable) honored.
- D-01/D-02 surfaced: per-player daemon + pairing state visible and correctly lifecycled under sync.
- i18n complete (11 languages), CHANGELOG staged, zero librespot regression.
</success_criteria>

<output>
Create `.planning/phases/73-soloist-connect-mode/73-04-SUMMARY.md` when done. Include the TROUBLESHOOTING notes to carry into docs (orphaned shared data-dir, stale launcher files, LMS-9.1 requirement) — the docs themselves are reviewed at the next /gsd-pause-work per workflow convention.
</output>
