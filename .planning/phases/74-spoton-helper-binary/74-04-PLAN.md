---
phase: 74-spoton-helper-binary
plan: 04
type: execute
wave: 3
depends_on: ["74-02", "74-03"]
files_modified:
  - .github/workflows/build-librespot.yml
  - Plugins/SpotOn/Soloist.pm
  - t/33_soloist_patch.t
  - CHANGELOG.md
autonomous: true
requirements: [D-03, D-09]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CI builds spoton-helper for x86_64-linux, aarch64-linux, armhf-linux and folds each into Plugins/SpotOn/Bin/<arch>/ in the same workflow run that assembles the plugin zip (D-09)"
    - "Soloist.pm auto-patches once after a successful download+activation: it runs `check` first and skips when already patched, otherwise runs `patch` (D-03, idempotent)"
    - "auto-patch failure is non-fatal — Soloist still runs unpatched, with a warning (fail-open for core playback)"
    - "the helper path is constructed from @ARCH_MAP bindir + Bin/<arch>/, never via findbin"
  artifacts:
    - ".github/workflows/build-librespot.yml (build-spoton-helper matrix job + release zip fold-in)"
    - "Plugins/SpotOn/Soloist.pm (_helperPath, _autoPatch, _runHelperJson, wired into _onSoloistDownloadDone)"
    - "t/33_soloist_patch.t (auto-patch idempotency + fail-open Perl test)"
  key_links:
    - "build-spoton-helper artifacts must be in the release job's needs: so download-artifact@v4 sees them (CR-02 same-workflow constraint)"
    - "helper artifacts use the `helper-<arch>` prefix (never `spoton-helper-`) to avoid the existing `release-artifacts/spoton-*/` librespot fold-in globs"
    - "_autoPatch runs AFTER _versionCheck activation in _onSoloistDownloadDone, array-form exec only (never a shell string)"
---

<objective>
Ship the helper and wire it in: add a `build-spoton-helper` CI job that cross-builds the three musl targets and folds the binaries into the plugin zip under `Bin/<arch>/` (D-09), then hook auto-patch into `Soloist.pm` so a freshly downloaded, version-matched Soloist binary is patched exactly once, idempotently, and fail-open (D-03).

Purpose: This closes the loop — the binary reaches users in the plugin zip and runs automatically at install time. It is the expansion task connecting the Rust engine (Waves 1-2) to the live LMS lifecycle.
Output: A CI job producing shipped binaries, Perl orchestration in Soloist.pm, a Perl idempotency test, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-spoton-helper-binary/74-CONTEXT.md
@.planning/phases/74-spoton-helper-binary/74-RESEARCH.md
@.github/workflows/build-librespot.yml
@Plugins/SpotOn/Soloist.pm
@Plugins/SpotOn/Helper.pm
@t/26_soloist_check.t
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI — build-spoton-helper job + plugin-zip fold-in</name>
  <files>.github/workflows/build-librespot.yml</files>
  <read_first>
    - .github/workflows/build-librespot.yml (build matrix bin_dir names, build-fake-libpulse CR-02 pattern, release job needs list and Assemble plugin zip step)
    - spoton-helper/Cross.toml (the three musl targets)
    - .planning/phases/74-spoton-helper-binary/74-RESEARCH.md (Pitfall 2 same-workflow artifacts; Pitfall 3 always build on tag; Pitfall 4 arch vocabulary; Open Question 1 CI implication for private-pattern injection)
  </read_first>
  <action>
Add a `build-spoton-helper` job to build-librespot.yml, modeled on the existing `build-fake-libpulse` job (CR-02: it must live in THIS workflow so the release job actions/download-artifact@v4 can see its artifacts). Matrix of three entries mapping Rust triple to bin_dir: x86_64-unknown-linux-musl to x86_64-linux, aarch64-unknown-linux-musl to aarch64-linux, armv7-unknown-linux-musleabihf to armhf-linux. Steps: checkout; install Rust toolchain with the matrix target; run cargo install cross; the conditional pattern-injection step described below; run cross build --release --target the-triple with working-directory spoton-helper; copy spoton-helper/target/the-triple/release/spoton-helper to Plugins/SpotOn/Bin/the-bin_dir/spoton-helper and chmod 755; verify with file plus sha256sum; upload artifact named `helper-the-bin_dir` (WITHOUT the `spoton-` prefix — the existing librespot loops glob `release-artifacts/spoton-*/`, so a `spoton-helper-*` name would collide and get mis-copied; use the `helper-` prefix, mirroring how `fake-libpulse-` is kept distinct). Run this job unconditionally on tag and workflow_dispatch — do NOT gate it behind detect-changes rust_changed, because the helper compiles in seconds and must never be stale (Pitfall 3).

Pattern-injection step (honors the Plan 02 Task 1 decision): add a step BEFORE cross build that, when a private patterns source is reachable by the runner (guarded on a secret/availability check that indicates the private table can be materialized), writes the real spoton-helper/src/patch/patterns.rs; when it is not reachable, the step is a no-op and the shipped binary keeps the public empty table so patch reports the unsupported no-op status. Add a comment naming this the compliance-boundary injection point from Plan 02.

Wire into the release job: add build-spoton-helper to its needs list, and in the Assemble plugin zip step add a fold-in loop (mirroring the fake-libpulse loop, and placed so it does NOT get swept up by the `spoton-*` librespot loop) that copies each `release-artifacts/helper-*/spoton-helper` into `Plugins/SpotOn/Bin/the-bin_dir/spoton-helper` — deriving bin_dir via `sed 's/^helper-//'` — and chmods 755, so the helper ships inside SpotOn-the-version.zip under the D-09 Bin/arch layout.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-librespot.yml')); j=d['jobs']; assert 'build-spoton-helper' in j; assert 'build-spoton-helper' in j['release']['needs']; print('ok')" && grep -q 'release-artifacts/helper-\*' .github/workflows/build-librespot.yml && ! grep -q 'release-artifacts/spoton-helper-' .github/workflows/build-librespot.yml && echo naming-ok</automated>
  </verify>
  <done>build-librespot.yml has a build-spoton-helper matrix job for the three musl targets, it is listed in the release job needs, and the release zip step folds the helper (via the collision-safe `helper-*` artifact prefix) into each Bin/arch directory.</done>
  <acceptance_criteria>
    - The workflow YAML parses and contains a `build-spoton-helper` job
    - `build-spoton-helper` appears in the `release` job `needs` list
    - The job matrix maps armv7-unknown-linux-musleabihf to armhf-linux
    - Helper artifacts are named `helper-<bin_dir>` (NOT `spoton-helper-<bin_dir>`) so they do not match the librespot `spoton-*` globs
    - The Assemble plugin zip step folds `release-artifacts/helper-*/` into Plugins/SpotOn/Bin/<arch>/ (bin_dir via `sed 's/^helper-//'`)
    - The job runs without a detect-changes rust_changed guard
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Soloist.pm auto-patch wiring + idempotency test</name>
  <files>Plugins/SpotOn/Soloist.pm, t/33_soloist_patch.t</files>
  <read_first>
    - Plugins/SpotOn/Soloist.pm (_onSoloistDownloadDone activation point, _versionCheck, @ARCH_MAP bindir, libPath fake-libpulse dir construction, array-form open discipline, SOLOIST_VERSION constant)
    - Plugins/SpotOn/Helper.pm (--check JSON parsing pattern for a binary capability manifest)
    - t/26_soloist_check.t (stub-module + skip_all Perl test harness pattern to mirror)
    - .planning/phases/74-spoton-helper-binary/74-RESEARCH.md (Perl integration sketch: _autoPatch, _helperPath, _runHelperJson; D-03 patch-once idempotent, fail-open)
  </read_first>
  <action>
Add three subs to Soloist.pm. `_helperPath()` constructs basedir/Bin/the-bindir/spoton-helper using the same @ARCH_MAP bindir value and Plugin _pluginDataFor('basedir') that libPath() uses for fake-libpulse; return undef when arch is unknown or the file is absent/non-executable. `_runHelperJson($helper, @args)` runs the helper via array-form open('-|', $helper, @args) (never an interpolated shell string — the project anti-pattern), slurps stdout, and returns the decoded JSON hashref via from_json (JSON::XS::VersionOneAndTwo, as Helper.pm uses) or undef on failure. `_autoPatch($soloistPath)`: resolve _helperPath (return quietly if absent); call _runHelperJson with 'check','--binary',$soloistPath and return if the result is defined and its `patched` field is true (D-03 idempotency probe); otherwise call _runHelperJson with 'patch','--version',SOLOIST_VERSION,'--binary',$soloistPath; when the result is missing or its `patched` field is not true, log a warning that auto-patch did not complete and Soloist runs unpatched (fail-open — never die, core playback must still work).

Wire the call: in _onSoloistDownloadDone, immediately AFTER the existing successful activation branch (where $binary is set following _versionCheck($canonical)), call _autoPatch($canonical). Patch only an activated, version-matched binary. The raw spak-key is never involved here; do not log binary contents.

Create t/33_soloist_patch.t mirroring t/26 (LMS stub modules, skip_all when Soloist.pm absent). Provide a fake helper executable (a tiny shell/perl script written into a tempdir) whose `check` output is switchable between {"patched":true} and {"patched":false}, and whose `patch` output is {"patched":true}. Test cases: (1) when check reports patched:true, _autoPatch does NOT invoke patch (idempotent skip); (2) when check reports patched:false, _autoPatch invokes patch and succeeds; (3) when the helper is missing (_helperPath undef) _autoPatch returns without dying (fail-open); (4) when patch returns patched:false, a warning is logged and no exception is thrown. Assert the helper is invoked in array form (the fake helper can record its argv to a file for inspection).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Soloist.pm 2>&1 | tail -2 && prove -l t/33_soloist_patch.t 2>&1 | tail -8</automated>
  </verify>
  <done>Soloist.pm gains _helperPath/_runHelperJson/_autoPatch, calls _autoPatch after activation, and t/33 proves idempotent skip, patch-on-unpatched, missing-helper fail-open, and patch-failure warning without dying.</done>
  <acceptance_criteria>
    - `perl -c Plugins/SpotOn/Soloist.pm` reports syntax OK
    - `prove -l t/33_soloist_patch.t` passes
    - Test asserts patch is NOT invoked when check reports patched:true
    - Test asserts _autoPatch does not die when _helperPath is undef
    - Test asserts the helper is invoked via array-form exec (argv captured, no shell interpolation)
    - _autoPatch is called after the _versionCheck activation branch in _onSoloistDownloadDone
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (existing entry format and the current top-of-file version section)
  </read_first>
  <action>
Add an unreleased/next CHANGELOG section (matching the existing header format, do not invent a version number — leave it as the current in-progress section or an Unreleased heading consistent with the file) describing Phase 74: new bundled spoton-helper binary providing patch (Lifetime timestamp + FLAC24 enum gates, version-locked to Soloist 1.3.7.489, fail-closed), check (D-08 patch/integrity/arch manifest), and protobuf (collection/v2 stdin/stdout conversion) subcommands; CI cross-build for x86_64/arm64/arm32 shipped under Bin/<arch>/; automatic one-time patch on Soloist download. Keep wording English (project convention) and do not include the concrete byte-patterns.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -qi 'spoton-helper' CHANGELOG.md && echo ok</automated>
  </verify>
  <done>CHANGELOG.md documents the Phase 74 spoton-helper additions in the existing format.</done>
  <acceptance_criteria>
    - CHANGELOG.md mentions spoton-helper and its patch/check/protobuf subcommands
    - The entry notes the 3-arch CI build and auto-patch on install
    - No concrete byte-pattern values appear in the entry
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (Plan 04) produces

- `.github/workflows/build-librespot.yml` (amended) — `build-spoton-helper` matrix job (3 musl targets, conditional pattern-injection step, `helper-<arch>` collision-safe artifact names) + `release` needs entry + `helper-*` zip fold-in loop
- `Plugins/SpotOn/Soloist.pm` (amended) — `_helperPath()`, `_runHelperJson($helper,@args)`, `_autoPatch($soloistPath)`; `_autoPatch` call after activation in `_onSoloistDownloadDone`
- `t/33_soloist_patch.t` — auto-patch idempotency + fail-open Perl test
- `CHANGELOG.md` (amended) — Phase 74 entry
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soloist.pm → spoton-helper | Perl invokes the helper as a subprocess with a constructed path |
| CI runner → private pattern source | release build optionally injects private patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-09 | Tampering | shell-string invocation of the helper from Perl | medium | mitigate | array-form open('-|', $helper, @args) only; never an interpolated shell string (project anti-pattern), asserted by t/33 argv capture |
| T-74-10 | Denial of Service | auto-patch failure blocking Soloist playback | medium | mitigate | fail-open: _autoPatch never dies; on any failure Soloist runs unpatched with a warning |
| T-74-11 | Elevation of Privilege | patching a wrong/tampered binary at install | high | mitigate | _autoPatch patches only an already version-matched activated binary; the helper re-gates on version + count internally (Plan 02) |
| T-74-SC | Tampering | private-pattern injection in CI | high | mitigate | injection is a guarded conditional step; absence yields a safe public no-op binary (Plan 02 compliance decision) |
</threat_model>

<verification>
- Workflow YAML parses; build-spoton-helper wired into release needs + zip
- `perl -c Soloist.pm` OK; `prove -l t/33_soloist_patch.t` green
- CHANGELOG documents the phase
</verification>

<success_criteria>
- Helper ships in the plugin zip under Bin/<arch>/ for 3 arches (D-09)
- Auto-patch runs once, idempotent, fail-open, array-form exec (D-03)
- CHANGELOG updated
</success_criteria>

<output>
Create `.planning/phases/74-spoton-helper-binary/74-04-SUMMARY.md` when done
</output>
