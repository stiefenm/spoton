---
phase: 75-api-unification-spclient-modell
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
autonomous: true
requirements: [D-06, D-07, D-08, D-09]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SpClient->getAlbum / getAlbumTracks return Web-API-shaped album/track-list responses from spclient metadata/4, with track names resolved via per-track metadata enrichment (S-04) through the capped pipeline"
    - "SpClient->getArtist / getArtistAlbums return Web-API-shaped artist and album-list responses from metadata/4/artist"
    - "SpClient->getShow / getShowEpisodes / getEpisode attempt metadata/4 and transparently fall back to Client.pm on any 4xx/5xx (D-07 covers the spike-unverified show/episode endpoints)"
    - "SpClient->search with type 'track' serves 20 results via context-resolve + lazy enrichment; any multi-type search delegates to Client.pm (S-05)"
    - "Track metadata enrichment reuses the 3600s per-track cache so repeated album/search views trigger no duplicate metadata calls (D-09 burst avoidance)"
  artifacts:
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "getAlbum, getAlbumTracks, getArtist, getArtistAlbums, getShow, getShowEpisodes, getEpisode, search router, _enrichTracks helper, _normalizeAlbum/_normalizeArtist"
    - path: "t/36_spclient.t"
      provides: "Fixture-driven tests for each new endpoint incl. enrichment, search routing, show fallback"
  key_links:
    - from: "SpClient::search (track type)"
      to: "SpClient::_enrichTracks"
      via: "context-resolve URI list enriched per requested slice via metadata/4/track through _request (cap 2)"
      pattern: "_enrichTracks"
    - from: "SpClient::search (multi-type)"
      to: "Plugins/SpotOn/API/Client.pm"
      via: "delegation for any type set other than exactly 'track' (S-05: context-resolve is track-only)"
      pattern: "Client->search"
---

<objective>
Expand the proven 75-01 skeleton horizontally across the JSON metadata family: albums, artists, shows/episodes, and the search router (D-08 scope). Every method keeps the exact Client.pm callback contract (cb->($result) / cb->(undef,$err)) and the D-06 capability + D-07 fallback semantics established by getTrack.

Purpose: These are the endpoints Browse hits most; delivering them Web-API-shaped means plan 75-06 can switch call sites without touching caller logic.
Output: Seven new facade methods + search router + shared enrichment helper in SpClient.pm, all fixture-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
@.planning/phases/75-api-unification-spclient-modell/75-RESEARCH.md
@.planning/spikes/009-spclient-mercury-browse/RESULTS.md
@.planning/phases/75-api-unification-spclient-modell/75-01-SUMMARY.md
</context>

## Artifacts this plan produces

- `Plugins::SpotOn::API::SpClient` additions — `getAlbum($class,$accountId,$albumId,$cb)`, `getAlbumTracks($class,$accountId,$albumId,$params,$cb)`, `getArtist($class,$accountId,$artistId,$cb)`, `getArtistAlbums($class,$accountId,$artistId,$params,$cb)`, `getShow`, `getShowEpisodes`, `getEpisode`, `search($class,$accountId,$params,$cb)`, `_enrichTracks($class,$accountId,\@trackIds,$cb)`, `_normalizeAlbum($meta)`, `_normalizeArtist($meta)`, `_spFacade(...)` (optional shared router helper if the per-method boilerplate repeats).

<tasks>

<task type="auto">
  <name>Task 1: Album + artist endpoints with track-name enrichment (S-04)</name>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm (current state after 75-01 — pipeline params, _normalizeTrack, router helpers)
    - Plugins/SpotOn/API/Client.pm lines 685-745 (getArtist/getArtistAlbums/getAlbum/getAlbumTracks signatures and paths — the response shapes callers expect)
    - Plugins/SpotOn/Plugin.pm `_albumFeed` and artist-albums feed subs (grep for getAlbumTracks/getArtistAlbums call sites) — confirm exactly which response fields are consumed before shaping
    - .planning/spikes/009-spclient-mercury-browse/RESULTS.md (metadata/4/album + artist rows, S-04)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Add to SpClient.pm, each wrapped in the D-06 capability check + D-07 fallback to the Client.pm method of the same name:
    - _enrichTracks($class, $accountId, \@trackIds, $cb): resolves N track ids to normalized track objects via GET /metadata/4/track/{hex} per id THROUGH _request (so the cap-2 concurrency gate and the 3600s response cache apply — D-09: bursts are serialized, repeats are cache hits). Preserves input order, tolerates individual failures by substituting undef and filtering, calls back once with the arrayref. This helper is reused by search here and by the collection/playlist plans (75-04/75-05).
    - getAlbum: GET /metadata/4/album/{idToHex} Accept json → _normalizeAlbum: id/uri via hexToId, name, artists from artist[], release date from date{year,month,day} → "YYYY-MM-DD" string, label and popularity (spclient delivers both — Dev-Mode-removed fields return to the UI, per CONTEXT specifics), images from cover_group as in _normalizeTrack, total_tracks from disc[].track[] counts, and tracks.items left EMPTY (track names are not in the album response, S-04 — getAlbumTracks owns that).
    - getAlbumTracks: album metadata → flatten disc[].track[] gids in disc/track order → slice per $params->{offset}/{limit} (defaults 0/50, matching Client.pm) → _enrichTracks on the slice → cb with { items => [...normalized tracks...], total => flattened count, offset, limit }. Only the requested slice is enriched (lazy, D-09).
    - getArtist: GET /metadata/4/artist/{hex} → _normalizeArtist: id/uri/name, popularity, images from portrait_group (fall back to cover-less), genres => [].
    - getArtistAlbums: artist metadata album_group/single_group/compilation_group arrays → collect album gids (each group entry wraps an album[] array — inspect the fixture/spike shape), normalize each entry to a minimal Web-API album stub {id, uri, name, images?} from the embedded data; where the group entries carry only gid+name, return those without extra metadata calls (callers render name+id lists; confirm against the Plugin.pm feed read in read_first, and only add per-album enrichment if a consumed field is otherwise missing). Respect $params->{offset}/{limit} slicing with total.
    Extend t/36_spclient.t: canned metadata/4/album fixture (2 discs, 3 tracks, cover_group) → getAlbumTracks asserts enrichment requests only for the sliced ids and Web-API items shape; getAlbum asserts label+popularity+date mapping; artist fixture → getArtistAlbums group flattening; enrichment-cache test: two getAlbumTracks calls for the same album issue metadata/4/track requests only once (response cache hit on second).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - t/36_spclient.t passes with the new album/artist tests
    - getAlbumTracks test asserts exactly limit-many enrichment requests for a first-page call against a larger album (lazy slice)
    - getAlbum normalized fixture output contains label and popularity keys (spclient value-add)
    - Every new method has a fallback test path or shares the asserted _spFacade fallback helper proven by a test (D-07)
  </acceptance_criteria>
  <done>Album and artist browsing data comes Web-API-shaped from spclient with order-preserving, cache-aware track enrichment.</done>
</task>

<task type="auto">
  <name>Task 2: Show + episode endpoints with D-07 safety net</name>
  <read_first>
    - Plugins/SpotOn/API/Client.pm lines 744-808 (getShow/getShowEpisodes/getEpisode paths, params, shapes)
    - Plugins/SpotOn/Plugin.pm `_showFeed` and episode item construction (grep getShowEpisodes/getEpisode) — fields consumed (resume_point? duration? images?)
    - Plugins/SpotOn/API/SpClient.pm (current state)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Add getShow, getShowEpisodes, getEpisode using GET /metadata/4/show/{hex} and /metadata/4/episode/{hex} with Accept json. These two metadata paths were NOT exercised in Spike 009 — that is acceptable because the D-07 router treats ANY 4xx/5xx as an automatic Client.pm delegation, so a non-existent endpoint degrades to today's Web-API behavior with one wasted request; do NOT pre-emptively hardcode them as unsupported. Normalize to the Web-API shapes the Plugin.pm show/episode feeds consume (read them first): show {id,uri,name,description,publisher?,images,total_episodes?}, episodes list {items:[{id,uri,name,duration_ms,release_date,images}],total,offset,limit} with slicing like getAlbumTracks, episode single object of the same item shape. Where a consumed field (e.g. resume_point) does not exist in spclient metadata, normalize it to undef — callers already guard optional fields; if the read_first pass shows a feed HARD-requires a field spclient cannot supply, route that method's spclient path through Client.pm directly and record the reason in the SUMMARY (that is D-07 routing, not scope reduction — the method still works).
    Extend t/36_spclient.t: show fixture → normalized shape; forced 404 on the show path → Client stub delegation recorded (explicit unverified-endpoint safety-net test); episode slice test.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - t/36_spclient.t passes with show/episode tests
    - The 404-on-show test asserts one spclient attempt then exactly one Client delegation (D-07)
    - Normalized episode items contain duration_ms and release_date keys
  </acceptance_criteria>
  <done>Show/episode browsing works on the spclient path where the endpoint exists and degrades invisibly to Client.pm where it does not.</done>
</task>

<task type="auto">
  <name>Task 3: Search router — context-resolve for track search, Client.pm for multi-type (S-05)</name>
  <read_first>
    - Plugins/SpotOn/API/Client.pm lines 456-472 (search signature, param contract: q/type/limit/offset)
    - Plugins/SpotOn/Plugin.pm lines 2620-2710 and 2990-3010 (search call sites: per-type $type values, offset usage) and Plugins/SpotOn/DontStopTheMusic.pm lines 140-170 (type 'track' with offset paging)
    - .planning/spikes/009-spclient-mercury-browse/RESULTS.md (context-resolve rows: 20 track URIs, no offset, track-only)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Implement SpClient::search with Client-identical signature. Routing rules:
    - $params->{type} equals exactly 'track' AND account is login5-capable: GET /context-resolve/v1/spotify:search:{uri_escape_utf8 query} → pages[0].tracks[].uri list (up to 20 — double the Dev-Mode Web-API cap, the reason this path exists) → cache the URI list 300s per (account, query) → slice by offset/limit → _enrichTracks the slice → cb with Web-API search shape { tracks => { items => [...], total => scalar(@uris), limit, offset } }. If offset >= the cached URI count, delegate the call to Client.pm->search instead (Web API supports deeper offset paging; context-resolve does not — S-05), so DSTM's paging loop keeps working past the 20.
    - Any other type value (album, artist, playlist, comma lists) OR a PKCE-only account: delegate to Client->search unchanged (S-05: context-resolve has no multi-type search; explicitly cited Web-API fallback in CONTEXT).
    - D-07: any context-resolve error → Client->search delegation.
    Extend t/36_spclient.t: (a) type 'track' offset 0 → context-resolve URL requested, enrichment for min(limit,20) ids, Web-API tracks.items shape; (b) type 'album' → Client stub delegation, zero spclient HTTP; (c) type 'track' offset 20 → Client delegation; (d) context-resolve 500 → Client delegation.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t && prove -l t/05_perl_syntax.t</automated>
  </verify>
  <acceptance_criteria>
    - All four routing tests pass
    - Track-search response shape nests items under a tracks key (Web-API compatible for _searchFeed consumers)
    - The context-resolve path never sends an offset query param to spclient (source assertion)
  </acceptance_criteria>
  <done>Search transparently serves 20-result track searches from spclient while multi-type and deep-offset searches keep the proven Web-API path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spclient JSON responses → normalizers | Untrusted structures fed into shape mapping consumed by OPML rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-07 | Denial of Service (self) | Enrichment bursts during playback (D-09, Rapid-Skip KE) | high | mitigate | All enrichment through _request cap-2 gate; lazy slice-only enrichment; 3600s per-track cache; search URI-list cache 300s |
| T-75-08 | Denial of Service | Normalizers dying on absent/unexpected JSON keys | medium | mitigate | Defensive undef-tolerant mapping, fixture tests for missing-field cases; _doRequest eval guards route dies to cb |
| T-75-09 | Information Disclosure | Query strings with user search terms in logs | low | accept | Same exposure as existing Client.pm path logging; path+method logging discipline retained |
</threat_model>

<verification>
- `prove -l t/36_spclient.t` green; `prove t/` full suite green
- Client.pm still byte-identical (`git diff --stat Plugins/SpotOn/API/Client.pm` empty)
- Live verification of the show/episode metadata endpoints happens in phase UAT (spike-unverified; D-07 covers failure)
</verification>

<success_criteria>
- All five must_haves truths demonstrated by named tests
- Every facade method preserves the cb->($result) / cb->(undef,$err) contract so 75-06 call-site switching needs no caller changes
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-02-SUMMARY.md` when done
</output>
