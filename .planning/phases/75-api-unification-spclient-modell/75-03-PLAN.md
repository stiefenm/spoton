---
phase: 75-api-unification-spclient-modell
plan: 03
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - spoton-helper/src/main.rs
  - spoton-helper/src/protobuf_cmd.rs
  - spoton-helper/build.rs
  - spoton-helper/Cargo.toml
  - spoton-helper/Cargo.lock
  - spoton-helper/proto/README.md
autonomous: true
requirements: [D-02]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "spoton-helper builds and its test suite is green with only the patch and check subcommands (D-02)"
    - "The protobuf/protobuf-codegen crates are gone from the dependency tree (cargo tree confirms)"
    - "`spoton-helper check` still emits the unchanged D-08 capability JSON (t/26 green)"
    - "The vendored .proto files remain in the repo as schema documentation with a README stating their status (D-02)"
  artifacts:
    - path: "spoton-helper/src/main.rs"
      provides: "clap dispatch with exactly Patch and Check commands"
    - path: "spoton-helper/proto/README.md"
      provides: "Documentation-only notice for the retained .proto schemas (Perl ProtobufLite is the runtime decoder now)"
  key_links:
    - from: "spoton-helper/Cargo.toml"
      to: "spoton-helper build"
      via: "no build-dependencies section remains, so no codegen runs at build time"
      pattern: "cargo build"
---

<objective>
Remove the protobuf subcommand from spoton-helper (D-02): ~505 LOC Rust, the codegen build script, and the protobuf/protobuf-codegen crates. Phase 75's Perl ProtobufLite.pm (75-01, with Rust-parity golden vectors already captured in t/34) is the only protobuf path from now on (D-01). The vendored .proto files stay as schema documentation.

Purpose: shrinks the Rust maintenance surface, speeds up the 3-arch cross builds, and removes two supply-chain dependencies.
Output: A leaner helper crate (patch + check only) with green cargo tests and unchanged Perl-side check integration.

D-02 reversibility rating: costly — restoring the subcommand later means re-establishing the codegen pipeline and crate dependencies. The decision was locked by the user in 75-CONTEXT.md; the golden vectors in t/34 preserve wire-format ground truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
@.planning/phases/75-api-unification-spclient-modell/75-RESEARCH.md
</context>

## Artifacts this plan produces

- Modified `spoton-helper/src/main.rs` — `Cmd` enum reduced to `Patch` and `Check` variants; `mod protobuf_cmd` removed; crate doc comment updated.
- Deleted `spoton-helper/src/protobuf_cmd.rs` and `spoton-helper/build.rs`.
- `spoton-helper/Cargo.toml` — `protobuf` dependency and `[build-dependencies]` section removed; crate description updated (no longer a "schema converter").
- New `spoton-helper/proto/README.md`.

<tasks>

<task type="auto">
  <name>Task 1: Delete the protobuf subcommand, codegen, and crates (D-02)</name>
  <reversibility rating="costly">Restoring the subcommand requires re-adding protobuf/protobuf-codegen crates and the build.rs codegen pipeline for 12 proto inputs; ProtobufLite.pm + t/34 golden vectors preserve the wire-format knowledge.</reversibility>
  <precondition>t/34_protobuf_lite.t exists in the working tree and passes (75-01 ported the Rust fixture semantics and captured encode-parity golden bytes before this deletion).</precondition>
  <read_first>
    - spoton-helper/src/main.rs (Cmd enum lines ~30-56, module list, crate doc comment)
    - spoton-helper/Cargo.toml (dependency + build-dependencies sections, description field)
    - spoton-helper/build.rs (confirm its ONLY job is protobuf codegen before deleting the whole file)
    - spoton-helper/tests/fixture.rs and spoton-helper/src/check.rs (confirm zero protobuf references — research verified none, re-verify before build)
  </read_first>
  <files>spoton-helper/src/main.rs, spoton-helper/src/protobuf_cmd.rs, spoton-helper/build.rs, spoton-helper/Cargo.toml, spoton-helper/Cargo.lock</files>
  <action>
    Delete spoton-helper/src/protobuf_cmd.rs entirely. In main.rs: remove the `mod protobuf_cmd;` line, the `Cmd::Protobuf` variant with its clap attributes, and its match arm; update the crate-level doc comment so it describes the helper as patcher + integrity checker only. Delete spoton-helper/build.rs (its sole content is the protobuf_codegen invocation — confirmed in read_first). In Cargo.toml: remove the `protobuf` line from [dependencies], remove the whole [build-dependencies] section, and change the package description to "SpotOn Soloist helper: binary patcher and integrity checker". Run `cargo build` to regenerate Cargo.lock (commit the shrunken lock). Do NOT touch spoton-helper/proto/ contents in this task.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/spoton-helper && cargo build --quiet && cargo test --quiet && ! cargo tree | grep -q "protobuf" && cargo run --quiet -- --help | grep -qi "patch" </automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` and `cargo test` succeed in spoton-helper/
    - `cargo tree` output contains no protobuf crate
    - `cargo run -- --help` lists patch and check and does not list a protobuf subcommand
    - src/protobuf_cmd.rs and build.rs no longer exist
  </acceptance_criteria>
  <done>The helper crate compiles and tests green with exactly two subcommands and no protobuf dependency chain.</done>
</task>

<task type="auto">
  <name>Task 2: Proto docs README + Perl-side integration re-verification</name>
  <read_first>
    - spoton-helper/proto/ directory listing (12 files incl. metadata/ subdir)
    - t/26_soloist_check.t and t/33_soloist_patch.t (confirm neither shells the removed subcommand)
    - .github/workflows/ build job for spoton-helper (grep build-spoton-helper; confirm no protobuf-specific step)
  </read_first>
  <files>spoton-helper/proto/README.md</files>
  <action>
    Create spoton-helper/proto/README.md (English, per project convention) stating: these vendored Spotify .proto schemas are retained as DOCUMENTATION ONLY per Phase 75 D-02 — the runtime decoder is Plugins::SpotOn::API::ProtobufLite (generic wire parser, D-01) with golden vectors in t/34_protobuf_lite.t; no Rust code consumes these files anymore; list the source (extracted from Spotify 1.2.52.442 / librespot upstream) and note collection2v2.proto as the schema for collection/v2/paging field numbers. Then re-run the Perl-side integration proof: `prove -l t/26_soloist_check.t t/33_soloist_patch.t` and the full `prove t/`; if the CI workflow references the removed subcommand anywhere (read_first grep), update it minimally — expected result is no change needed.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && test -f spoton-helper/proto/README.md && prove -l t/26_soloist_check.t t/33_soloist_patch.t && ls spoton-helper/proto/*.proto | wc -l | grep -qE '^1[01]$'</automated>
  </verify>
  <acceptance_criteria>
    - proto/README.md exists, names ProtobufLite.pm and t/34 as the successors, and marks the schemas documentation-only
    - t/26 and t/33 pass unchanged
    - All pre-existing .proto files still present (D-02: schemas stay)
    - Full `prove t/` green
  </acceptance_criteria>
  <done>Schema documentation is preserved and every Perl/CI consumer of the helper is verified unaffected by the rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo dependency tree | Supply-chain surface being REDUCED (two crates removed, none added) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-10 | Tampering | Cargo supply chain | low | mitigate | Pure removal — no new packages installed; Cargo.lock regenerated and committed for a reproducible shrunken tree |
| T-75-11 | Denial of Service | Accidental removal of a still-consumed code path | low | mitigate | Precondition gates on t/34 golden vectors; grep-verified zero Perl/CI consumers; cargo test + t/26 + t/33 re-run |
</threat_model>

<verification>
- `cargo build && cargo test` green in spoton-helper/
- `prove t/` green
- CI build-spoton-helper job needs no functional change (build.rs removal only makes it faster)
</verification>

<success_criteria>
- All four must_haves truths hold
- Net diff removes ~500+ LOC Rust and two crates while every remaining helper capability (patch, check) is test-proven intact
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-03-SUMMARY.md` when done
</output>
