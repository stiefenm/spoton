---
phase: 75-api-unification-spclient-modell
plan: 04
type: execute
wave: 3
depends_on: ["75-01", "75-02"]
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-09]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SpClient->getSavedAlbums / getFollowedArtists / getSavedShows serve library sets via collection/v2/paging with the exact Content-Type application/vnd.collection-v2.spotify.proto and the verified set names (S-06/S-07)"
    - "collection/v2 PageResponse decoding returns ALL repeated items across pagination pages (A1), cached as a URI list and served to callers as Web-API offset/limit pages"
    - "SpClient->getSavedTracks serves Liked Songs from context-resolve spotify:user:{username}:collection — the full list without Spotify-side paging — sliced and enriched per requested page"
    - "SpClient->getRecentlyPlayed decodes the protobuf-only recently-played/v3 response (S-09) into the item shape the Plugin.pm feed consumes"
    - "All collection paths use the credentials.json username from verifyCredentials, never the prefs spotifyUserId (RESEARCH username-source rule)"
  artifacts:
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "_collectionAll, _collectionPage, SET_MAP constant, getSavedAlbums, getFollowedArtists, getSavedShows, getSavedTracks, getRecentlyPlayed"
    - path: "t/36_spclient.t"
      provides: "Wire-level collection/v2 tests (CT header, PageRequest bytes, repeated-item pagination), liked-songs slicing, recently-played decode"
  key_links:
    - from: "SpClient::_collectionPage"
      to: "Plugins/SpotOn/API/ProtobufLite.pm"
      via: "PageRequest encode + PageResponse decode (fields per proto/collection2v2.proto)"
      pattern: "ProtobufLite"
    - from: "SpClient collection methods"
      to: "Plugins/SpotOn/API/Credentials.pm"
      via: "username sourced from verifyCredentials($accountId)->{username}"
      pattern: "verifyCredentials"
---

<objective>
Expand SpClient.pm across the protobuf-backed library family (D-08): collection/v2 sets (Saved Albums / Followed Artists / Saved Shows), Liked Songs via context-resolve, and Recently Played. This is where ProtobufLite (D-01) earns its keep at runtime and where the Web-API pagination contract (offset/limit) is bridged onto spclient's token/full-list models.

Purpose: the library views are the pages users open most; these are also the endpoints where spclient removes Dev-Mode pain (Liked Songs arrive complete, no 50-item paging churn).
Output: Five facade methods + collection plumbing, wire-level tested against the schema in proto/collection2v2.proto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
@.planning/phases/75-api-unification-spclient-modell/75-RESEARCH.md
@.planning/spikes/009-spclient-mercury-browse/RESULTS.md
@spoton-helper/proto/collection2v2.proto
</context>

## Artifacts this plan produces

- `Plugins::SpotOn::API::SpClient` additions — `SET_MAP` constant hash (`albums=>'collection'`, `artists=>'artist'`, `shows=>'show'`, `pinned_playlists=>'ylpin'`, `saved_episodes=>'listenlater'` — full verified S-07 mapping documented even where no facade consumes it yet), `_collectionPage($class,$accountId,$set,$pageToken,$limit,$cb)`, `_collectionAll($class,$accountId,$set,$cb)` (paginates until empty next_page_token, caches URI+added_at list 60s per account+set), `_sliceAsPage(\@list,$offset,$limit)`, `getSavedAlbums($class,$accountId,$params,$cb)`, `getFollowedArtists($class,$accountId,$params,$cb)`, `getSavedShows($class,$accountId,$params,$cb)`, `getSavedTracks($class,$accountId,$params,$cb)`, `getRecentlyPlayed($class,$accountId,$params,$cb)`.

<tasks>

<task type="auto">
  <name>Task 1: collection/v2 plumbing + getSavedAlbums (S-06/S-07, A1)</name>
  <read_first>
    - spoton-helper/proto/collection2v2.proto (PageRequest fields 1-4, PageResponse items=1/next_page_token=2/sync_token=3, CollectionItem uri=1/added_at=2/is_removed=3)
    - Plugins/SpotOn/API/SpClient.pm (current pipeline: _request _raw mode, _enrichTracks from 75-02)
    - Plugins/SpotOn/API/Client.pm lines 509-534 (getSavedAlbums/getFollowedArtists response shapes callers expect)
    - Plugins/SpotOn/Plugin.pm saved-albums / followed-artists feeds (grep getSavedAlbums/getFollowedArtists) — consumed fields
    - .planning/spikes/009-spclient-mercury-browse/RESULTS.md (Collection/v2 section — CT rule, set table, pagination, added_at caveat S-08)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Implement _collectionPage: POST https://{host}/collection/v2/paging with body = ProtobufLite PageRequest (field 1 username from verifyCredentials, field 2 set name, field 3 pagination_token — omit the field entirely when empty, field 4 varint limit 200), headers Content-Type AND Accept both exactly application/vnd.collection-v2.spotify.proto (S-06: any other value is a 400), through _request with _raw + _noCache. Decode the PageResponse with ProtobufLite: field 1 repeated CollectionItem (arrayref — the A1 case), each item parsed for uri (f1), added_at (f2 varint, relative-order-only per S-08 — never render as a date), is_removed (f3, filter out truthy); field 2 next_page_token.
    Implement _collectionAll: loop _collectionPage until next_page_token is empty, accumulate items, cache the accumulated list 60s under spoton_spclient_coll_${accountId}_${set} (library-tier TTL per CLAUDE.md), serve from cache when present. Implement _sliceAsPage for offset/limit slicing with total.
    Implement getSavedAlbums (D-06 capability + D-07 fallback to Client->getSavedAlbums as everywhere): _collectionAll set 'collection' (S-07: the Saved-Albums set is literally named collection — the intuitive name gives 403) → slice → per-album metadata via getAlbum's normalizer path THROUGH the cap-2 pipeline (only the slice, cache-backed) → cb Web-API shape { items => [{ added_at => item added_at, album => {...} }], total, offset, limit, next => (offset+limit < total ? truthy : undef) } — match what the Plugin.pm feed actually consumes (read_first).
    Extend t/36_spclient.t with wire-level tests: (a) captured POST asserts both headers carry the exact vnd.collection-v2 CT string and the body decodes back (via ProtobufLite) to username/set/limit; (b) two-page pagination fixture (page 1 with next_page_token, page 2 without; 2+ items per page) → all items present in order; (c) is_removed tombstones filtered; (d) second getSavedAlbums call within TTL issues zero collection POSTs (list cache).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - Wire test asserts the literal CT string application/vnd.collection-v2.spotify.proto on both headers
    - Pagination test proves >=4 items across 2 pages all surface (repeated-field + multi-page)
    - getSavedAlbums fixture output is Web-API-shaped with items[].album populated by the normalizer
    - Set name used for saved albums is 'collection' (source assertion via SET_MAP)
  </acceptance_criteria>
  <done>collection/v2 paging works end-to-end against the schema with full-list caching and Web-API page semantics.</done>
</task>

<task type="auto">
  <name>Task 2: getFollowedArtists + getSavedShows on collection sets</name>
  <read_first>
    - Plugins/SpotOn/API/Client.pm lines 521-549 (getFollowedArtists cursor shape {artists:{items,cursors}}, getSavedShows shape)
    - Plugins/SpotOn/Plugin.pm _fetchAllFollowedArtists and saved-shows feed (cursor 'after' usage, consumed fields)
    - Plugins/SpotOn/API/SpClient.pm (state after Task 1)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    getFollowedArtists: _collectionAll set 'artist' → since the Web-API method is cursor-based, emulate the cursor contract the Plugin.pm pagination helper consumes: treat $params->{after} as an index into the cached list (after = last returned artist id; resolve its position, continue from position+1; absent = start), enrich the slice via getArtist metadata (cap-2, cached) and return { artists => { items => [...], total => count, cursors => { after => last-id-or-undef } } }. Read _fetchAllFollowedArtists FIRST and match exactly what it consumes — if it only loops on cursors.after until absent, that emulation is sufficient.
    getSavedShows: _collectionAll set 'show' → slice per offset/limit → enrich via the getShow path (which itself falls back per D-07 if show metadata is unavailable — in that degenerate case route the whole method to Client->getSavedShows to avoid N fallback roundtrips: detect via the first enrichment result) → { items => [{ show => {...} }], total, offset, limit }.
    Extend t/36: cursor-emulation walk over a 3-artist fixture in two calls; saved-shows shape test; both methods delegate to Client for PKCE-only accounts (router regression).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - Cursor walk test retrieves all fixture artists across sequential after-calls with terminating cursors
    - Saved-shows items nest under a show key (Web-API shape)
    - Router regression: PKCE-only account → Client stub records the call, zero spclient HTTP
  </acceptance_criteria>
  <done>Followed artists and saved shows serve from collection sets with caller-compatible cursor/offset semantics.</done>
</task>

<task type="auto">
  <name>Task 3: getSavedTracks (Liked Songs, no paging) + getRecentlyPlayed (protobuf, S-09)</name>
  <read_first>
    - Plugins/SpotOn/Plugin.pm _savedTracksFeed (lines ~1631+) and _recentlyPlayedFeed (lines ~1466+) — the EXACT fields each consumes from items (track keys? played_at? added_at?)
    - Plugins/SpotOn/API/Client.pm lines 473-508 (getRecentlyPlayed/getSavedTracks signatures + shapes)
    - spoton-helper/proto/recently_played_backend.proto (Context uri/lastPlayedTime field numbers for ProtobufLite extraction)
    - .planning/spikes/009-spclient-mercury-browse/RESULTS.md (context-resolve collection row — ALL Liked Songs, 149KB, no paging; recently-played S-09)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    getSavedTracks: GET /context-resolve/v1/spotify:user:{username}:collection (username from verifyCredentials — NOT prefs) → pages[].tracks[].uri full Liked-Songs list (no Spotify paging — the headline spclient win) → cache URI list 60s per account → _sliceAsPage per offset/limit (Client-compatible defaults 0/50) → _enrichTracks the slice → cb { items => [{ track => {...} }], total => full count, offset, limit, next => ... } matching _savedTracksFeed consumption (read it first; note the play-all path requests large quantities — the URI list is already complete, only enrichment is sliced, so play-all can receive the full URI-derived items with enrichment batched through the cap-2 gate; if _savedTracksFeed's play-all only needs uri+id for playlist seeding, skip name enrichment for the play-all shape accordingly — decide from the caller's actual field usage and record the choice in the SUMMARY).
    getRecentlyPlayed: GET /recently-played/v3/user/{username}/recently-played through _request with _raw (protobuf-only endpoint, Accept json is ignored — S-09) → ProtobufLite parse using field numbers from recently_played_backend.proto (Context: uri + lastPlayedTime) → keep only spotify:track: URIs (the endpoint mixes tracks/albums/playlists contexts; _recentlyPlayedFeed renders tracks — confirm in read_first) → enrich → cb shape compatible with the Web-API recently-played consumer ({ items => [{ track => {...}, played_at => ISO-ish or epoch passthrough as consumed }] }). If lastPlayedTime semantics don't match what the feed formats, normalize to whatever the caller consumes and document it.
    Both methods: D-06 capability check, D-07 fallback to the Client method.
    Extend t/36: liked-songs fixture (25 URIs) with offset=10/limit=10 → exactly 10 enrichment calls, total 25; recently-played protobuf fixture built with ProtobufLite encode (2 track contexts + 1 playlist context) → 2 track items out; username-source test: prefs spotifyUserId stub differs from credentials username and the request URL must contain the credentials username.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - Liked-songs test: total equals full URI count while enrichment ran only for the slice
    - Recently-played test filters non-track contexts and decodes lastPlayedTime via multi-byte varint
    - Username-source test proves credentials.json username in the URL (A5 guard)
    - Full `prove t/` green
  </acceptance_criteria>
  <done>Liked Songs and Recently Played flow from spclient with complete-list semantics, sliced enrichment, and caller-identical shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spclient protobuf responses → ProtobufLite | Untrusted binary parsed in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-12 | Denial of Service | Malformed collection/recently-played protobuf | high | mitigate | ProtobufLite bounded parsing (75-01); undef result routes through D-07 fallback to Client.pm, never a crash |
| T-75-13 | Denial of Service (self) | Enrichment of large library slices during playback (D-09) | high | mitigate | Slice-only enrichment, cap-2 gate, 60s list cache + 3600s track cache; play-all shape decided from caller field usage to avoid needless enrichment |
| T-75-14 | Information Disclosure | Spotify canonical username in URLs/logs | medium | mitigate | Log path templates without the username where feasible; _maskUser for any username log line (Credentials discipline) |
| T-75-15 | Tampering | Cross-account collection cache bleed | medium | mitigate | accountId in coll/list cache keys |
</threat_model>

<verification>
- `prove -l t/36_spclient.t` + full `prove t/` green
- Wire-format assertions decode the captured request bytes back through ProtobufLite (self-consistent with the Rust-parity vectors from 75-01)
- Live collection/v2 POST against a real account (A3 final confirmation) is mandatory UAT via /gsd-verify-work with the 75-06 smoke script
</verification>

<success_criteria>
- All five must_haves truths demonstrated by named tests
- No collection path reads prefs spotifyUserId
- Client.pm still byte-identical
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-04-SUMMARY.md` when done
</output>
