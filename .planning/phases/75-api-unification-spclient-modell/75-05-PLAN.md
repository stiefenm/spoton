---
phase: 75-api-unification-spclient-modell
plan: 05
type: execute
wave: 4
depends_on: ["75-04"]
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-09]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SpClient->getUserPlaylists serves the user's playlist library from the protobuf-only rootlist endpoint (S-10), Web-API-shaped for the Plugin.pm playlists feed"
    - "SpClient->getPlaylistItems serves playlist tracks from playlist/v2 (JSON via Accept header) with sliced track enrichment, matching the _playlistFeed item shape"
    - "Both methods honor D-06 capability routing and D-07 fallback to Client.pm"
  artifacts:
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "getUserPlaylists (rootlist), getPlaylistItems (playlist/v2), _normalizePlaylistMeta"
    - path: "t/36_spclient.t"
      provides: "Rootlist protobuf decode test (nested folder/item/playlist), playlist/v2 shape + enrichment tests"
  key_links:
    - from: "SpClient::getUserPlaylists"
      to: "Plugins/SpotOn/API/ProtobufLite.pm"
      via: "rootlist Response→Folder→Item→Playlist nested decode using field numbers from proto/rootlist_request.proto"
      pattern: "ProtobufLite"
    - from: "SpClient::getPlaylistItems"
      to: "SpClient::_enrichTracks"
      via: "playlist/v2 contents item URIs enriched per requested page"
      pattern: "_enrichTracks"
---

<objective>
Complete the spclient endpoint surface (D-08) with the playlist family: the protobuf-only rootlist (user's playlist library, S-10) and playlist/v2 playlist contents. After this plan, every browse-data method that plan 75-06 switches call sites to exists on SpClient.pm.

Purpose: playlists are the last data family; rootlist is the deepest ProtobufLite exercise (3-level nesting) and proves the generic parser handles real-world nested messages (D-01).
Output: Two facade methods + nested-message normalization, fixture-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
@.planning/phases/75-api-unification-spclient-modell/75-RESEARCH.md
@.planning/spikes/009-spclient-mercury-browse/RESULTS.md
@spoton-helper/proto/rootlist_request.proto
</context>

## Artifacts this plan produces

- `Plugins::SpotOn::API::SpClient` additions — `getUserPlaylists($class,$accountId,$params,$cb)`, `getPlaylistItems($class,$accountId,$playlistId,$params,$cb)`, `_normalizePlaylistMeta`, rootlist decode helper `_parseRootlist($bytes)`.

<tasks>

<task type="auto">
  <name>Task 1: getUserPlaylists via rootlist (protobuf-only, S-10)</name>
  <read_first>
    - spoton-helper/proto/rootlist_request.proto AND its imports (playlist_playlist_state.proto, playlist_folder_state.proto) — field numbers for Response.root, Folder.item, Item.playlist/folder, Playlist row_id/attributes; trace where the playlist NAME lives (attributes message)
    - Plugins/SpotOn/API/Client.pm lines 638-667 (getUserPlaylists /me/playlists shape: items[].{id,name,uri,owner,images,tracks.total?})
    - Plugins/SpotOn/Plugin.pm _userPlaylistsFeed (grep getUserPlaylists) — consumed fields
    - Plugins/SpotOn/API/SpClient.pm (current state)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Implement getUserPlaylists: GET /playlist/v2/user/{username}/rootlist?decorate=revision,attributes,length,owner,timestamp through _request with _raw (rootlist answers protobuf regardless of Accept, S-10; username from verifyCredentials). Decode with _parseRootlist using ProtobufLite against the field numbers you read from rootlist_request.proto: Response → root Folder → repeated item; each Item holds either a playlist or folder — flatten folders recursively (bounded depth guard, e.g. 10 levels, V5) collecting playlist entries; extract per playlist: uri/row_id (spotify:playlist:{id} — derive id), and the decorated name from the attributes message (locate the exact nested field number in the proto; if the decorate response omits names in practice, fall back per-playlist to _normalizePlaylistMeta over GET /playlist/v2/playlist/{id} Accept json for the sliced page only — decide from the fixture/proto reality and record the choice in the SUMMARY). Normalize to the /me/playlists shape consumed by the feed: { items => [{ id, uri, name, owner?, images? }], total, offset, limit } with offset/limit slicing. D-06/D-07 as everywhere (fallback Client->getUserPlaylists).
    Extend t/36: build a rootlist fixture WITH ProtobufLite encode (root folder containing 2 playlist items and 1 nested folder containing 1 playlist) → all 3 playlists surface flattened with ids; malformed rootlist bytes → D-07 delegation to Client stub.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - Nested-folder fixture yields all 3 playlists in order with spotify:playlist: URIs
    - Malformed-bytes test delegates to the Client stub (no die)
    - Recursion depth guard exists (source assertion) — a self-referencing fixture cannot loop forever
  </acceptance_criteria>
  <done>The user's playlist library renders from rootlist protobuf with folder flattening and Web-API shape.</done>
</task>

<task type="auto">
  <name>Task 2: getPlaylistItems via playlist/v2 (JSON) with sliced enrichment</name>
  <read_first>
    - Plugins/SpotOn/API/Client.pm lines 778-808 (getPlaylistItems signature incl. opts hashref used by ProtocolHandler.pm:815 and the Web-API items shape)
    - Plugins/SpotOn/Plugin.pm _playlistFeed (grep _playlistFeed / getPlaylistItems) and Plugins/SpotOn/ProtocolHandler.pm line ~815 — consumed fields and pagination params
    - .planning/spikes/009-spclient-mercury-browse/RESULTS.md (playlist/v2 row: revision, length, attributes, contents with Accept json)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Implement getPlaylistItems preserving Client.pm's exact signature INCLUDING the opts/params hashref variant ProtocolHandler.pm uses: GET /playlist/v2/playlist/{playlistId} with Accept application/json (playlist/v2 honors JSON here, unlike rootlist) → response contents.items[] carry track URIs (+ attributes) but no track names → slice by offset/limit (default page 100 to match the existing _playlistFeed pageLimit) → _enrichTracks the slice → cb { items => [{ track => {...normalized...} }], total => response length field, offset, limit, next } matching what _playlistFeed and the ProtocolHandler resolution path consume (read both first; the ProtocolHandler path seeds playback and may need only uri/id — serve the same normalized items, they are a superset). Cache the playlist envelope 300s (playlist tier) keyed by account+playlist; enrichment rides the 3600s track cache. D-06/D-07 as everywhere.
    Extend t/36: playlist fixture (5 URIs, length 5) offset 0 limit 3 → 3 enrichment calls, total 5, items[].track shape; second call within TTL → no envelope refetch; PKCE-only account → Client delegation.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - Slice test: exactly 3 enrichment requests for limit=3 against a 5-track playlist, total reported 5
    - Envelope cache test passes (one playlist/v2 GET across two calls)
    - Full `prove t/` green
  </acceptance_criteria>
  <done>Playlist contents serve from playlist/v2 with caller-identical shapes for both the browse feed and the ProtocolHandler resolution path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rootlist/playlist responses → normalizers | Untrusted protobuf/JSON, includes user-generated playlist names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-16 | Denial of Service | Unbounded folder recursion in rootlist decode | medium | mitigate | Explicit depth guard in _parseRootlist + malformed-input test |
| T-75-17 | Denial of Service (self) | Playlist enrichment bursts (100-item pages) during playback (D-09) | high | mitigate | Slice-only enrichment through cap-2 gate; 3600s track cache; 300s envelope cache |
| T-75-18 | Tampering | Playlist names (user-generated content) into OPML | low | accept | Same exposure as the existing Web-API path; OPML layer already renders untrusted names |
</threat_model>

<verification>
- `prove -l t/36_spclient.t` + full `prove t/` green
- Client.pm byte-identical
- Live rootlist decorate-params behavior confirmed in phase UAT (smoke script covers a rootlist fetch)
</verification>

<success_criteria>
- All three must_haves truths demonstrated
- Every browse-data method plan 75-06 needs now exists on SpClient.pm: getTrack, getAlbum, getAlbumTracks, getArtist, getArtistAlbums, getShow, getShowEpisodes, getEpisode, search, getSavedTracks, getSavedAlbums, getFollowedArtists, getSavedShows, getRecentlyPlayed, getUserPlaylists, getPlaylistItems
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-05-SUMMARY.md` when done
</output>
