---
phase: 75-api-unification-spclient-modell
plan: 06
type: execute
wave: 5
depends_on: ["75-02", "75-05"]
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/DontStopTheMusic.pm
  - t/36_spclient.t
  - tools/spclient-smoke.pl
  - CHANGELOG.md
autonomous: true
requirements: [D-06, D-08]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Browse/Search/Library call in Plugin.pm, ProtocolHandler.pm, Connect.pm, and DontStopTheMusic.pm goes through the SpClient facade (D-08 full unification)"
    - "Player-control calls (playerPlay/playerPause/playerVolume/playerSeek) in Connect.pm still call Client.pm directly (D-08: Client.pm is player-control only)"
    - "Library write/contains operations (save/remove/check tracks+shows, addToPlaylist) reach the Web API via SpClient passthrough delegations (collection/v2/write is deferred to its own phase)"
    - "Plugin.pm initPlugin resets SpClient and Login5 state alongside the existing Client reset (plugin-reload safety)"
    - "A standalone smoke script exists for UAT: mints a login5 token from a credentials.json and fetches one track + the rootlist via spclient"
  artifacts:
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "Passthrough delegations: getLimit, getMe, getTopTracks, getPersonalMixes, saveTracks, removeTracks, checkTracks, saveShows, removeShows, checkShows, addToPlaylist, getWebPlayerPlaylistItems, pathfinderHome"
    - path: "tools/spclient-smoke.pl"
      provides: "LMS-free login5 + spclient roundtrip script (ProtobufLite + curl) for phase UAT"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry describing the spclient unification"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/API/SpClient.pm"
      via: "all browse/search/library call sites dispatch through the facade"
      pattern: "API::SpClient->"
    - from: "SpClient passthrough delegations"
      to: "Plugins/SpotOn/API/Client.pm"
      via: "one-line runtime-require delegations for Web-API-only operations"
      pattern: "Plugins::SpotOn::API::Client"
---

<!-- planner-discipline-allow: API::Client-> -->

<objective>
Flip the switch (D-08): route the ~70 browse/search/library call sites in Plugin.pm, ProtocolHandler.pm, Connect.pm and DontStopTheMusic.pm through the SpClient facade so login5-capable accounts get spclient data everywhere, while PKCE-only accounts and Web-API-only operations (writes, multi-type search, player control) keep their proven paths. Ship the UAT smoke script and the CHANGELOG entry.

Purpose: this is the phase goal made real — after this plan a stored-credentials account browses entirely over spclient with automatic fallback, and no caller knows the difference.
Output: Mechanically switched call sites, complete passthrough surface, smoke script, CHANGELOG.

D-08 reversibility rating: costly — undoing the unification means re-pointing ~70 call sites and re-verifying shapes; mitigated by the facade design (single import surface, Client.pm untouched underneath).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
@.planning/phases/75-api-unification-spclient-modell/75-RESEARCH.md
@.planning/phases/75-api-unification-spclient-modell/75-05-SUMMARY.md
</context>

## Artifacts this plan produces

- `Plugins::SpotOn::API::SpClient` passthrough delegations (each a one-line runtime-require + delegate): `getLimit`, `getMe`, `getTopTracks`, `getPersonalMixes`, `saveTracks`, `removeTracks`, `checkTracks`, `saveShows`, `removeShows`, `checkShows`, `addToPlaylist`, `getWebPlayerPlaylistItems`, `pathfinderHome`.
- Switched call sites in `Plugin.pm`, `ProtocolHandler.pm`, `Connect.pm` (getTrack only), `DontStopTheMusic.pm`.
- `tools/spclient-smoke.pl` — standalone UAT script.
- CHANGELOG.md Unreleased entry.

<tasks>

<task type="auto">
  <name>Task 1: Passthrough delegations on SpClient (Web-API-only operations)</name>
  <read_first>
    - Plugins/SpotOn/API/Client.pm method signatures for every delegated name (getLimit line 117, getMe 444, getTopTracks 485, getPersonalMixes 668, saveTracks/removeTracks/checkTracks 550-586, saveShows/removeShows/checkShows 597-637, addToPlaylist 652, getWebPlayerPlaylistItems 1149, pathfinderHome 809)
    - Plugins/SpotOn/Plugin.pm lines 885-1080 (_doLibraryAction dynamic method dispatch and %_LIBRARY_API_METHODS — the delegations must satisfy this dynamic-call pattern)
    - Plugins/SpotOn/API/SpClient.pm (current state)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    Add one-line passthrough delegations on SpClient for every method listed under "Artifacts" above: each does a runtime `require Plugins::SpotOn::API::Client` and forwards @_ unchanged (goto-style or explicit call — preserve class-method calling convention and full argument list, including hashref opts). Rationale to encode in a header comment: write/contains library ops stay on the Web API because collection/v2/write is untested and deferred (75-CONTEXT Deferred Ideas); getTopTracks/getPersonalMixes/pathfinderHome/getWebPlayerPlaylistItems have no spclient equivalent in the verified Spike-009 catalog; getLimit stays Client-backed because probe cleanup is deferred to Phase 76/77. This makes SpClient a COMPLETE drop-in for every non-player Client method used by the four browse consumers, so the Task 2 switch is a mechanical rename (CONTEXT: "Caller müssen idealerweise nur den Import ändern").
    Extend t/36: delegation matrix test — for each passthrough name, calling it on SpClient invokes the recorded Client stub with identical arguments; plus the dynamic-dispatch case: `my $m = 'saveTracks'; Plugins::SpotOn::API::SpClient->$m(...)` works (mirrors _doLibraryAction).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - Delegation matrix test covers all 13 passthrough methods and passes
    - Dynamic method-name dispatch test passes
    - Each delegation forwards the exact argument list (asserted via stub capture)
  </acceptance_criteria>
  <done>SpClient exposes the complete non-player Client surface; no caller needs to know which host serves a method.</done>
</task>

<task type="auto">
  <name>Task 2: Switch the browse consumers to the facade (D-08)</name>
  <reversibility rating="costly">Undo re-touches ~70 call sites across 4 files; the facade keeps shapes Client-identical so a revert is mechanical but wide.</reversibility>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (all `Plugins::SpotOn::API::Client->` occurrences — enumerate before editing; note initPlugin's Client->reset call and the probe/limitsProbed calls that STAY on Client)
    - Plugins/SpotOn/ProtocolHandler.pm, Plugins/SpotOn/Connect.pm, Plugins/SpotOn/DontStopTheMusic.pm (their occurrence lists)
    - Plugins/SpotOn/API/SpClient.pm reset()/Login5 reset() signatures
  </read_first>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/ProtocolHandler.pm, Plugins/SpotOn/Connect.pm, Plugins/SpotOn/DontStopTheMusic.pm</files>
  <action>
    Mechanically re-point call sites from the legacy client class to Plugins::SpotOn::API::SpClient, with these EXACT boundaries:
    - Plugin.pm: switch every data/limit/library call (search, getTrack, getAlbum, getAlbumTracks, getArtistAlbums, getShowEpisodes, getEpisode, getPersonalMixes, getPlaylistItems, getUserPlaylists, getSavedTracks, getSavedAlbums, getSavedShows, getFollowedArtists, getRecentlyPlayed, getTopTracks, getWebPlayerPlaylistItems, pathfinderHome, checkTracks, checkShows, addToPlaylist, all getLimit calls, and the _doLibraryAction dynamic-dispatch target class). KEEP on the legacy class: reset, probeEndpointLimits, limitsProbed (probe machinery is deferred cleanup, Phase 76/77) — and ADD SpClient->reset plus Login5->reset next to the existing reset call in initPlugin. Update any adjacent comments that name the old class for switched calls.
    - ProtocolHandler.pm: switch all 10 occurrences (getTrack x2, getAlbum, getAlbumTracks, getEpisode, getShowEpisodes, getPlaylistItems, getLimit x3).
    - Connect.pm: switch ONLY the two getTrack calls. The four player* calls stay on the legacy class — D-08 defines Client.pm as the player-control home.
    - DontStopTheMusic.pm: switch all 5 occurrences (search x2, getTopTracks, getLimit x2).
    - Settings.pm, Status.pm, TokenManager.pm: untouched (out of D-08 scope; probe/status stay Client-backed).
    Add/adjust `require` statements where the SpClient module is not yet loaded in a file. Run the full suite after the switch.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && [ "$(grep -oE 'API::Client->[a-zA-Z_]+' Plugins/SpotOn/Plugin.pm | sort -u | tr '\n' ' ')" = "API::Client->limitsProbed API::Client->probeEndpointLimits API::Client->reset " ] && [ "$(grep -oE 'API::Client->[a-zA-Z_]+' Plugins/SpotOn/Connect.pm | sort -u | tr '\n' ' ')" = "API::Client->playerPause API::Client->playerPlay API::Client->playerSeek API::Client->playerVolume " ] && [ -z "$(grep -oE 'API::Client->[a-zA-Z_]+' Plugins/SpotOn/DontStopTheMusic.pm)" ] && [ -z "$(grep -oE 'API::Client->[a-zA-Z_]+' Plugins/SpotOn/ProtocolHandler.pm)" ] && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm's remaining legacy-class method calls are exactly the probe trio (equality gate above)
    - Connect.pm's remaining legacy-class calls are exactly the four player-control methods
    - ProtocolHandler.pm and DontStopTheMusic.pm have zero legacy-class method calls
    - initPlugin resets Client, SpClient, and Login5 (source assertion)
    - Full `prove t/` green after the switch
  </acceptance_criteria>
  <done>All browse/search/library traffic flows through the facade; player control and probe machinery remain on Client.pm exactly as D-08 specifies.</done>
</task>

<task type="auto">
  <name>Task 3: UAT smoke script + CHANGELOG</name>
  <read_first>
    - Plugins/SpotOn/API/ProtobufLite.pm (pure-Perl API to reuse standalone)
    - Plugins/SpotOn/API/Login5.pm (wire-building constants/field layout to mirror — the script re-uses ProtobufLite directly, not Login5 which needs LMS)
    - CHANGELOG.md (Unreleased section format from recent releases)
  </read_first>
  <files>tools/spclient-smoke.pl, CHANGELOG.md</files>
  <action>
    Create tools/spclient-smoke.pl — a standalone dev/UAT script (English comments) that runs WITHOUT LMS: usage `perl tools/spclient-smoke.pl /path/to/credentials.json [trackId]`. It loads Plugins::SpotOn::API::ProtobufLite via `use lib` relative to the repo root, reads username/auth_data from the given credentials.json, builds the login5 LoginRequest (same field layout as Login5.pm, librespot CID constant), POSTs it via `curl --silent --fail --data-binary @- -H` (system curl keeps the script dependency-free; NEVER echo the token or auth_data — print only token length and expiry), then with the minted token GETs metadata/4/track/{hex of trackId, default a well-known track id passed on the CLI} printing name/artist, and GETs the rootlist printing the playlist count. Exit non-zero on any failure with a stage-labelled message (mint/metadata/rootlist). This is the Phase-73-precedent live-UAT vehicle for the whole login5+spclient chain (RESEARCH Wave-0 gap: live roundtrip is mandatory UAT, not automatable here — no paired account in this environment).
    Add a CHANGELOG.md Unreleased entry (English): Browse/Search/Library unified onto spclient.spotify.com via login5 (stored-credential accounts; automatic Web-API fallback), search returns up to 20 track results, popularity/label metadata restored, Liked Songs load without paging; spoton-helper protobuf subcommand removed (Perl decoder replaces it). Do NOT bump any version number and do NOT touch repo.xml (release procedure is user-approved separately — Kein Auto-Release).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c tools/spclient-smoke.pl && grep -qi "spclient" CHANGELOG.md && prove -l t/05_perl_syntax.t</automated>
  </verify>
  <acceptance_criteria>
    - `perl -c tools/spclient-smoke.pl` compiles clean (no LMS modules required)
    - Script prints token LENGTH only, never the token value (source assertion)
    - CHANGELOG Unreleased entry present; no version bump, repo.xml untouched
  </acceptance_criteria>
  <done>UAT has a one-command live-verification vehicle and the change is documented for the next release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools/spclient-smoke.pl → shell/curl | Handles credential material outside the LMS process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-19 | Information Disclosure | Smoke script printing/leaking token or auth_data | high | mitigate | Print token length only; pass body via stdin (--data-binary @-) so secrets never appear in argv/ps; script reads local 0600 credentials.json |
| T-75-20 | Denial of Service | A facade regression breaking all browse at once (wide switch) | medium | mitigate | Full prove t/ gate; PKCE-only accounts exercise pure delegation (behavior-identical); D-07 runtime fallback catches spclient-side breakage |
| T-75-21 | Elevation | Dynamic method dispatch (_doLibraryAction) reaching unintended subs | low | mitigate | %_LIBRARY_API_METHODS allowlist retained unchanged; delegation test covers the dynamic path |
</threat_model>

<verification>
- Full `prove t/` green; `perl -c` clean for all four switched files (covered by t/05)
- Equality gates prove the exact D-08 boundary (probe trio + player quartet on Client, everything else on the facade)
- Mandatory live UAT (via /gsd-verify-work on the dev machine with a paired account): run tools/spclient-smoke.pl, then browse Liked Songs / Saved Albums / search / a playlist in LMS while a Connect session plays (D-09 Rapid-Skip regression watch: no "error audio key 0 2" in daemon stderr)
</verification>

<success_criteria>
- All five must_haves truths hold
- A PKCE-only account shows zero behavioral change (delegation-only path)
- A stored-credentials account performs all browse reads against spclient hosts (observable in diagnostic logs by path names: metadata/4, context-resolve, collection/v2, playlist/v2)
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-06-SUMMARY.md` when done
</output>
