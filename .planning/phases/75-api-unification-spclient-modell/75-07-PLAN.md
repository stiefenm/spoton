---
phase: 75-api-unification-spclient-modell
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
autonomous: true
gap_closure: true
requirements: [D-07, D-08, D-09]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "getPlaylistItems reports a total that reflects ONLY spotify:track: URIs (never the raw envelope length, which counts non-track entries too), so a caller advancing its offset by the returned item count never overlaps or truncates a mixed-content playlist (CR-01)"
    - "Every paginated facade built on _enrichMeta/_enrichCollectionSlice (getAlbumTracks, getArtistAlbums-adjacent, getShowEpisodes, getSavedTracks, getSavedAlbums, getFollowedArtists, getSavedShows, getPlaylistItems, search) substitutes a minimal id/uri stub for a failed per-item metadata/4 enrichment instead of dropping it, so every requested window returns exactly as many items as were sliced (CR-01)"
    - "getAlbumTracks/getArtistAlbums/getShowEpisodes never die inside the async HTTP success callback on an empty/malformed metadata/4 response body; they fall back to Client.pm exactly like any other spclient-path error (WR-01)"
    - "getSavedShows honors params._noCache (bypassing the 60s collection/v2 list cache on demand, matching Plugin.pm:2088's call), and the four library write passthroughs (saveTracks/removeTracks/saveShows/removeShows) invalidate the corresponding spclient list cache so a Like/Follow is visible on the very next fetch (WR-02)"
    - "_collectionAll's pagination loop is bounded (a hard page cap) and detects a repeated next_page_token, so a malformed or adversarial spclient response can never loop forever (WR-03)"
    - "getPlaylistItems rejects a $playlistId that is not exactly 22 base62 characters before it is ever spliced into the spclient URL path or cache key (WR-04)"
  artifacts:
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "Filter-before-slice + recount in getPlaylistItems; stub-substituting _enrichMeta/_enrichCollectionSlice; guarded normalize closures in getAlbumTracks/getArtistAlbums/getShowEpisodes; _noCache-honoring + page-capped _collectionAll; write-path cache invalidation; validated playlistId"
    - path: "t/36_spclient.t"
      provides: "Regression tests for mixed-content windows, partial-enrichment-failure windows, empty-body normalize guards, unbounded-pagination guard, _noCache bypass, write-invalidation, and invalid playlistId rejection"
  key_links:
    - from: "SpClient::getPlaylistItems"
      to: "SpClient::_sliceAsPage"
      via: "track-URI-filtered list (not the raw envelope) feeds the slice, so total/offset arithmetic and the caller's offset-advance-by-returned-count invariant (Plugin.pm _fetchPages / _albumFeed play-all, ProtocolHandler.pm explodePlaylist) stay consistent"
      pattern: "filter-before-slice"
    - from: "SpClient::_enrichMeta / SpClient::_enrichCollectionSlice"
      to: "every paginated facade consuming them"
      via: "stub substitution on per-item failure instead of grep{defined} drop, guaranteeing scalar(@results) == scalar(@requested) always"
      pattern: "window-size-exact enrichment"
    - from: "Plugin.pm:2088 (getSavedShows _noCache=>1 call)"
      to: "SpClient::_collectionAll"
      via: "an optional noCache argument that bypasses (without disabling) the 60s list cache"
      pattern: "_noCache passthrough"
---

<objective>
Close all five verified gaps from 75-VERIFICATION.md / 75-REVIEW.md against the shipped `Plugins/SpotOn/API/SpClient.pm`: the CRITICAL pagination offset desync (CR-01) and the four unresolved warnings (WR-01..WR-04). None of these were fixed after 75-REVIEW.md was committed (`10285f9`); this plan applies the review's own proposed fixes verbatim (or the equivalent, more D-07-consistent variant noted per task) so phase 75 can pass re-verification.

Purpose: CR-01 breaks the D-08 behavioral-equivalence guarantee every caller (`Plugin.pm::_fetchPages`, `_albumFeed` play-all, `ProtocolHandler.pm::explodePlaylist`) depends on under exactly the conditions D-07/D-09's own threat model (T-75-08, T-75-13) designed the facade to tolerate — mixed-content playlists and individual `metadata/4` fetch failures under load. The four warnings compound risk (stale library lists, an unbounded pagination loop, a URL/cache-key injection vector) but are independently smaller in scope.

Output: `SpClient.pm`'s paginated facades preserve the requested-window-size invariant under mixed-content and partial-enrichment-failure conditions; three previously-unguarded normalize closures can no longer die on an empty spclient body; `_collectionAll` is bounded and `_noCache`-aware; library writes invalidate their list caches; `getPlaylistItems` validates its id before any URL/cache-key use. All existing tests plus new regression coverage stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-api-unification-spclient-modell/75-VERIFICATION.md
@.planning/phases/75-api-unification-spclient-modell/75-REVIEW.md
@.planning/phases/75-api-unification-spclient-modell/75-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — filter-before-slice + recount in getPlaylistItems, stub substitution in _enrichMeta/_enrichCollectionSlice</name>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm lines 623-666 (`_enrichMeta` — current `grep { defined } @results` drop), 1338-1384 (`_enrichCollectionSlice` — same drop pattern with `added_at` pairing), 2085-2135 (`getPlaylistItems` — `total` from unfiltered `$envelope->{length}` while `items` is filtered to track-only post-slice)
    - Plugins/SpotOn/API/SpClient.pm lines 1323-1336 (`_sliceAsPage` — already returns `(\@slice, $total)` where `$total` is the FULL length of whatever list it's given; feeding it a pre-filtered track-only list is sufficient, no signature change needed)
    - .planning/phases/75-api-unification-spclient-modell/75-REVIEW.md CR-01 section (proposed fix, both options: filter-before-slice+recount for the mixed-URI-type case, stub-substitution for the enrichment-failure case)
    - Plugins/SpotOn/Plugin.pm lines 1908-1940 (`_fetchPages`: `$requestOffset = $startOffset + scalar(@accumulated)`, T-25-01 empty-page guard) and ~3270-3280 (`_albumFeed` play-all: `$fetchPage->(scalar(@accumulated))`) — confirm the offset-advance-by-returned-count contract this fix restores
    - Plugins/SpotOn/ProtocolHandler.pm lines ~840-895 (`explodePlaylist`: `$fetchPage->($offset + scalar(@{ $data->{items} }))`) — same contract, second caller
    - t/36_spclient.t lines 1705-1785 (existing `getPlaylistItems` fixtures/tests) and 848-864 (`getAlbumTracks` slice test) — patterns to extend, `set_error_for`/`set_response_for` mock capabilities (lines 141-260)
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    In `getPlaylistItems`, filter `@contentItems`' URIs down to `spotify:track:`-prefixed entries BEFORE calling `_sliceAsPage` (not after, as today) — non-track playlist entries (episodes, local files) must never be sliced into the window or counted toward `total`. Replace the current `$total` derivation (`$envelope->{length}`, which counts all entries) with the `$total` that `_sliceAsPage` returns for the filtered track-only list, so window arithmetic and `total` always agree, restoring the offset-advance-by-returned-count contract for `_fetchPages`/`_albumFeed` play-all/`explodePlaylist`.

    In `_enrichMeta`, stop filtering the final callback array with `grep { defined }`. For every id — whether `idToHex` conversion failed OR the `metadata/4/$metaType` request errored — substitute a minimal stub `{ id => $id, uri => "spotify:$metaType:$id", name => undef }` into that result slot instead of leaving/dropping `undef`, so `scalar(@$cbResult) == scalar(@$ids)` always holds regardless of individual failures (429s/timeouts under load — exactly the D-09 degraded mode this facade is designed to tolerate).

    In `_enrichCollectionSlice`, apply the identical substitution: on a failed/undef normalization, still push `{ added_at => $entry->{added_at}, $wrapKey => stub }` (stub derived the same way from `$entry->{uri}`) instead of dropping the slot — `scalar(@results) == scalar(@$slice)` always holds. This automatically fixes `getSavedAlbums`, `getFollowedArtists`, and `getSavedShows`'s post-probe remainder (all consume `_enrichCollectionSlice`/`_enrichMeta` — no per-method changes needed there beyond this shared helper fix), plus `search` and `getSavedTracks`/`getAlbumTracks`/`getShowEpisodes` (all consume `_enrichTracks`/`_enrichMeta` directly).

    Do NOT touch `getRecentlyPlayed`'s own separate inline enrichment loop (lines ~1799-1829) — it is a hand-rolled duplicate of the same drop pattern, but it has no offset-based pagination caller (Plugin.pm:1480 calls it exactly once per menu open with a fixed `limit`, never accumulating across repeated calls), so it is a deliberate scope boundary for this gap closure, not an oversight — note this explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - New test: a 5-item playlist envelope with 2 non-track URIs mixed in among 3 track URIs, offset=0/limit=3 -> `total` reflects the 3 filtered track URIs (not 5), `items` count is exactly the track-filtered slice, and NOT the raw envelope-length value.
    - New test: simulate two sequential `getPlaylistItems` calls the way `explodePlaylist`/`_fetchPages` actually chain them (second call's offset = first call's `scalar(@{items})`) against that same mixed-content envelope -> the full track set is collected with zero duplicate tracks and zero premature termination.
    - New test: a 3-track playlist window with ONE of the three `metadata/4/track/{hex}` requests forced to error via `set_error_for` -> `items` array length is still 3 (stub substituted, not dropped); the stub's `track.name` is `undef` and `track.uri` matches the source URI.
    - New test: same partial-enrichment-failure pattern against `getSavedAlbums` (one `metadata/4/album/{hex}` forced to fail via `set_error_for`) -> `_enrichCollectionSlice` still returns the full requested count with a stub in the failed slot.
    - All pre-existing `t/36_spclient.t` assertions continue to pass (in particular the 5-track all-track envelope test at line ~1731, where filtered-total and raw-length total are numerically identical, so no behavior change is visible there).
    - Full `prove t/` green.
  </acceptance_criteria>
  <done>getPlaylistItems and every _enrichMeta/_enrichCollectionSlice-based facade preserve the requested-window-size invariant under mixed-content and partial-enrichment-failure conditions; CR-01 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — guard undef $meta in three normalize closures; WR-04 — validate playlistId before URL/cache-key use</name>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm lines 756-812 (`getAlbumTracks`), 892-947 (`getArtistAlbums`), 1012-1061 (`getShowEpisodes`) — the three normalize closures passed as `_spFacade`'s 3rd arg, each dereferencing `$meta->{...}` with no definedness/HASH-ref guard
    - Plugins/SpotOn/API/SpClient.pm lines 340-357 (`_doRequest`'s success handler: `$result` stays `undef` on an empty/whitespace-only body, with NO error raised) and 594-621 (`_spFacade`: dispatches `$normalize->($result, $cb)` on the success path with no guard of its own) — confirms how an empty 200 body reaches the normalize closure undetected
    - Plugins/SpotOn/API/SpClient.pm lines 685-715 (`getAlbum`) and 116-133 (`idToHex`) — the established id-validation placement pattern (immediately after the D-06 creds-check branch, before the facade request) that every other id-taking facade already follows
    - Plugins/SpotOn/API/SpClient.pm lines 2085-2096 (`getPlaylistItems` D-06 branch) and 2039-2071 (`_playlistEnvelope` — where `$playlistId` is spliced unvalidated into the URL path and the `spoton_spclient_plenv_*` cache key)
    - .planning/phases/75-api-unification-spclient-modell/75-REVIEW.md WR-01 and WR-04 sections
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <behavior>
    - getAlbumTracks/getArtistAlbums/getShowEpisodes, given an empty or non-HASH `$meta` (empty-body 200 response): fall back to Client.pm exactly like any other spclient-path error, instead of dying inside the async HTTP success callback.
    - getPlaylistItems, given a `$playlistId` that is not exactly 22 base62 (`[0-9A-Za-z]`) characters: returns `{ error => 'invalid_id' }` immediately, with ZERO spclient HTTP requests issued and no reach into `_playlistEnvelope`.
    - getPlaylistItems, given a valid 22-char base62 `$playlistId` (all pre-existing fixture ids in t/36 are exactly 22 chars): behavior is unchanged.
  </behavior>
  <action>
    WR-01: in each of `getAlbumTracks`/`getArtistAlbums`/`getShowEpisodes`, assign the fallback closure currently passed inline as `_spFacade`'s 4th positional arg to a named `my $fallback = sub { ... }` variable, and reuse that SAME variable both as the fallback argument and from inside the normalize closure: guard `unless ($meta && ref($meta) eq 'HASH') { $fallback->($fcb); return; }` as the very first line of each of the three normalize closures, before any `$meta->{...}` dereference. This routes an empty/malformed spclient success body through the identical Client.pm delegation every other spclient error already uses (full D-07 behavior — not merely a surfaced parse-error), instead of dying inside the SimpleAsyncHTTP success callback and hanging the request with `$cb` never invoked.

    WR-04: in `getPlaylistItems`, immediately after the existing D-06 creds-check branch (matching where `getAlbum`/`getArtist`/`getShow`/`getEpisode`/`getTrack` validate their ids via `idToHex`), add a charset/length guard: `unless (defined $playlistId && $playlistId =~ /^[0-9A-Za-z]{22}$/) { $cb->(undef, { error => 'invalid_id' }); return; }` — before `_playlistEnvelope` is ever called, so a malformed or user-influenced playlistId (containing `/`, `?`, `..`, or the wrong length — arriving via LMS favorites URLs, CLI `playlist play` args, or OPML passthrough params) can never be spliced into the spclient URL path or the `spoton_spclient_plenv_${accountId}_${playlistId}` cache key. Unlike track/album/artist/show/episode ids, playlist ids are used directly as base62 in the URL (no `idToHex` conversion needed here) — a plain charset/length regex is the correct, minimal guard.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t</automated>
  </verify>
  <acceptance_criteria>
    - New test per facade (getAlbumTracks, getArtistAlbums, getShowEpisodes): an empty-body 200 response (`$Slim::Networking::SimpleAsyncHTTP::auto_response_content = ''`) delegates to the corresponding Client.pm stub exactly once — no die, no hang, no double-callback (assert `$cb` fires exactly once via a call counter).
    - New test: `getPlaylistItems` called with a malformed playlistId (e.g. a string containing `/` or a 10-char string) returns `{ error => 'invalid_id' }` with ZERO spclient HTTP requests (`non_apresolve_requests()` is empty) and does NOT reach `Plugins::SpotOn::API::Client::getPlaylistItems_calls` (id validation happens before any network call or Client.pm delegation for this malformed-id path — distinct from the D-06/D-07 delegation paths, which stay unaffected).
    - Existing playlist tests using the 22-char fixture ids (`testplaylist00000000NN`) continue to pass unchanged.
    - Full `prove t/` green.
  </acceptance_criteria>
  <done>No normalize closure can die on an empty/malformed spclient body; getPlaylistItems never interpolates an unvalidated playlistId into a URL or cache key. WR-01 and WR-04 closed.</done>
</task>

<task type="auto">
  <name>Task 3: WR-02 — honor _noCache in getSavedShows + invalidate list caches on write; WR-03 — bound _collectionAll's pagination loop</name>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm lines 1278-1321 (`_collectionAll` — unconditional 60s cache read on entry, unbounded `next_page_token`-driven recursion with no page cap or repeated-token guard), 1517-1603 (`getSavedShows` — never reads `$params->{_noCache}`), 2191-2225 (`saveTracks`/`removeTracks`/`saveShows`/`removeShows` one-line passthroughs)
    - Plugins/SpotOn/API/Client.pm lines 550-620 (`saveTracks`/`removeTracks`/`saveShows`/`removeShows` signatures: `($class, $accountId, $uris, $cb)`) — confirms `$accountId` is `$_[0]` in the passthrough's `@_` after `my $class = shift;`
    - Plugins/SpotOn/Plugin.pm line 2088 (`Plugins::SpotOn::API::SpClient->getSavedShows($acct, { %$params, _noCache => 1 }, $cb);`) — the caller this WR-02 fix must satisfy
    - Plugins/SpotOn/API/SpClient.pm line 1619-1623 (`_likedSongsUris` cache key `spoton_spclient_liked_${accountId}`) and `SET_MAP` constant (lines 67-73, `shows => 'show'`) — the exact cache-key formats to invalidate
    - t/36_spclient.t lines 1124-1159 (existing 2-page `_collectionAll` pagination fixture, `encode_page_response` helper) and 1296-1326 (`getSavedShows` probe-fallback/D-06 tests) — patterns to extend
    - .planning/phases/75-api-unification-spclient-modell/75-REVIEW.md WR-02 and WR-03 sections
  </read_first>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <action>
    WR-03: add `use constant COLLECTION_MAX_PAGES => 100;` (100 * 200-item pages = 20k items, generous headroom over any real library). In `_collectionAll`'s `$fetchPage` closure, track a page counter incremented on every page attempt; if it exceeds `COLLECTION_MAX_PAGES`, `undef $fetchPage` and deliver `$cb->(undef, { error => 'parse_error' })` (fallback-classified — every `_collectionAll` caller already checks `_isFallbackError`, so this degrades invisibly to Client.pm). Also track the most-recently-requested page token; if the server echoes that SAME token back instead of an empty one, treat it as a normal end-of-list (NOT an error, since real accumulated data exists) — log a warning noting the repeated-token condition for diagnosability, then finish and cache what has been accumulated via the existing success path.

    WR-02: add an optional `$noCache` parameter to `_collectionAll($class, $accountId, $set, $cb, $noCache)` that skips ONLY the initial cache read when true (the fresh result is still written to the 60s cache afterward, refreshing it for other callers — this is a bypass-on-demand, not a cache-disable). Update `getSavedShows` to pass `$params->{_noCache}` through as that 5th argument. Add write-path cache invalidation to the four write passthroughs: `saveTracks`/`removeTracks` remove `"spoton_spclient_liked_${accountId}"` (peeking `$accountId` as `$_[0]` from the shifted `@_` without consuming the arg list still passed on to Client.pm) before delegating; `saveShows`/`removeShows` remove `"spoton_spclient_coll_${accountId}_" . SET_MAP->{shows}` the same way before delegating. `checkTracks`/`checkShows` are read-only contains-checks and stay untouched (no invalidation needed).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/36_spclient.t && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - New test: `_collectionAll` (via `getSavedAlbums` or a direct fixture) fed a `collection/v2/paging` response whose `next_page_token` always echoes the SAME non-empty value regardless of the request's own token -> the fetch loop terminates at or before `COLLECTION_MAX_PAGES` page requests (test completes without hanging) and still delivers the accumulated items via the normal success path (not an error).
    - New test: `getSavedShows` called twice for the same account with `_noCache => 1` on the second call -> exactly two separate `collection/v2/paging` POSTs are issued (cache bypassed on demand), matching Plugin.pm:2088's actual call shape; a THIRD call without `_noCache` immediately after -> zero additional POSTs (the bypass didn't disable the cache).
    - New test: `getSavedShows` (cache populated) -> `saveShows($accountId, [...], $cb)` -> `getSavedShows` again (no `_noCache`) -> issues a FRESH `collection/v2/paging` POST (cache was invalidated by the write), not a cache hit.
    - New test: same write-invalidation pattern for `saveTracks`/`removeTracks` against the Liked Songs cache key — verify via a direct `$cache->get('spoton_spclient_liked_' . $accountId)` check before (populated) and after (removed) the write call.
    - Full `prove t/` green (1648+ pre-existing tests plus all new tests from Tasks 1-3).
  </acceptance_criteria>
  <done>_collectionAll cannot loop forever on a malformed/adversarial next_page_token; getSavedShows honors _noCache; library writes invalidate the affected spclient list caches. WR-02 and WR-03 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spclient responses (playlist/v2 envelope, metadata/4/*, collection/v2 pages) -> paginated facade slicing/enrichment | Untrusted network JSON/protobuf whose item counts, content-type mix, and pagination tokens directly drive Perl offset arithmetic and cache-key construction consumed downstream by Plugin.pm/ProtocolHandler.pm |
| User-influenced playlistId (LMS favorites URLs, CLI playlist args, OPML passthrough params) -> spclient URL path + cache key | Untrusted input reaching URL/cache-key string interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-08 | Denial of Service | Normalizers dying on absent/unexpected JSON keys | medium | mitigate | Extends the phase's existing undef-guard discipline to the three remaining unguarded normalize closures (getAlbumTracks/getArtistAlbums/getShowEpisodes) that dereference an empty-body $meta directly — WR-01, Task 2 |
| T-75-13 | Denial of Service (self) | Enrichment failures under load silently shrinking paginated windows | high | mitigate | Stub substitution in _enrichMeta/_enrichCollectionSlice keeps every window exactly the requested size even when individual metadata/4 fetches fail under 429/timeout — closes the pagination-desync manifestation of this threat (CR-01, Task 1) |
| T-75-22 | Tampering | Unvalidated user-influenced $playlistId spliced into the spclient URL path and cache key | medium | mitigate | 22-char base62 charset/length validation before _playlistEnvelope is ever called (WR-04, Task 2) |
| T-75-23 | Denial of Service | _collectionAll pagination loop with no page cap or repeated-token guard | medium | mitigate | COLLECTION_MAX_PAGES cap + repeated-token detection, fallback-classified abort that still preserves already-accumulated data (WR-03, Task 3) |
</threat_model>

<verification>
- `prove -l t/36_spclient.t` green after each task, full `prove t/` green at the end (1648+ pre-existing tests plus all new regression tests from Tasks 1-3)
- `Plugins/SpotOn/API/Client.pm` remains byte-identical (`git diff --stat` empty) — this plan touches only `SpClient.pm` and its test file
- Direct code read confirming: getPlaylistItems total/items agree on a track-only filtered count; _enrichMeta/_enrichCollectionSlice never drop a slot; the three WR-01 normalize closures guard $meta; getSavedShows reads _noCache; the four write passthroughs invalidate their list cache key; _collectionAll has both a page cap and a repeated-token guard; getPlaylistItems validates playlistId before _playlistEnvelope
</verification>

<success_criteria>
- All six must_haves truths demonstrated by passing automated tests
- CR-01 (critical) and WR-01/WR-02/WR-03/WR-04 (warnings) all closed with no remaining unresolved finding from 75-REVIEW.md
- Re-running `/gsd-verify-phase 75` scores all previously-failed truths (getSavedTracks, getPlaylistItems, and the four "Degraded" truths sharing the same root cause) as VERIFIED
</success_criteria>

<output>
Create `.planning/phases/75-api-unification-spclient-modell/75-07-SUMMARY.md` when done
</output>
