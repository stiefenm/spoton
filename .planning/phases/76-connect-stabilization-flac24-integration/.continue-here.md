# Phase 76: Connect Stabilization + FLAC24 Integration — Session Handoff

**Paused:** 2026-08-30 ~14:00 UTC
**Phase Status:** Executed (8/8 plans), Code Review done (6/6 fixed), UAT blocked by audio stutter

## Current State

### Completed
- **Planning:** 8 Pläne in 4 Waves, 3 Plan-Checker-Iterationen (7→1→0)
- **Execution:** Alle 8 Pläne erfolgreich (Wave 1-4, parallel Worktrees)
- **Code Review:** 1 Critical + 5 Warnings gefunden und gefixt
- **Worktree-Fix:** `worktree.baseRef: "head"` in `.claude/settings.local.json` gesetzt (war von `origin/main` statt aktuellem Branch)

### Blocking Issues (UAT)

**1. Soloist Browse Audio Stutter (BLOCKER)**
- Symptom: Audio startet verzögert, stottert progressiv, ~5s Stille, wiederholt
- Isoliert: tritt auch im Direct-Stream-PCM-Pfad auf (kein Transcode)
- Root Cause: Ring-Buffer/HTTP-Layer in fake-libpulse, pre-Phase-76 (nie getestet)
- Debug-Handoff: `.planning/debug/soloist-browse-stutter.md`
- **Nächster Schritt:** `/gsd-debug soloist-browse-stutter --model fable`

**2. FLAC24 Transcode Architektur (DEFERRED)**
- `soc flc * *` Regel deaktiviert (custom-convert.conf)
- Problem: LMS Prefetch + single /stream Endpoint = Takeover-Konflikte
- Lösungsansätze in Debug-Handoff dokumentiert
- Wird nach Stutter-Fix adressiert

### Was funktioniert
- Connect-Playback (8s-Gap GEFIXT, sub-second Reconnect)
- Browse/UX Fixes (#161 Playlist-Typ, #94 Kontextmenü, Window 6 Offset-Guard)
- Rust Connect Fixes (#159 409-Status, #131 Buffer-Latency, #128 Position-Resync)
- Format-Resolver (`resolveSoloistFormat`, MP3-Pfad via `smp`)
- Connect-Lifecycle (Auto-Play-Gate, Power-Restore #151, Gruppen-Fix #158)
- Up Next (#135 getQueue on-demand)
- Settings (OGG-Dropdown bei Soloist versteckt, Save-Guard)
- Code-Review-Fixes (CR-01 samplesize, WR-01..WR-05)

### Human Actions Pending
- UAT-Durchlauf nach Stutter-Fix (76-UAT-CHECKLIST.md)
- librespot-Binary deployt (76-02/76-05 Rust-Änderungen), muss nach Debug-Fix erneut deployed werden
- `libpulse.so.0` muss nach C-Änderungen neu gebaut + deployed werden

## Critical Anti-Patterns

| # | Pattern | Severity | Description |
|---|---------|----------|-------------|
| 1 | LMS Prefetch + Single-Stream-Daemon | advisory | LMS prefetcht den nächsten Track via getNextTrack→startBrowseTrack, was den Daemon zum sofortigen Track-Wechsel zwingt. Jeder Prefetch öffnet eine neue /stream-Verbindung → Takeover. Die `soc flc` Transcode-Regel verschärft das, weil new() IMMER aufgerufen wird (flc beats pcm in Player-Capabilities). Deferred-Callback-Ansatz scheiterte an Pitfall-4-Loop. |
| 2 | Phase-73-Truths nie live verifiziert | advisory | Alle 9 Phase-73 PRESENT_BEHAVIOR_UNVERIFIED Truths. Browse-Playback-Stutter wäre bei Phase-73-UAT aufgefallen. |

## Resume Command

```
/gsd-debug soloist-browse-stutter --model fable
```

Nach dem Debug-Fix: `/gsd-verify-work 76` für das vollständige UAT.
