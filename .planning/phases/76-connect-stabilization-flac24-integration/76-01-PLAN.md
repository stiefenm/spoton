---
phase: 76-connect-stabilization-flac24-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c
  - Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0
  - Plugins/SpotOn/custom-convert.conf
  - Plugins/SpotOn/ProtocolHandler.pm
autonomous: true
requirements: [D-04, D-05, D-08]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "fake-libpulse's ring buffer carries S32LE (4 bytes/sample); FLOAT32LE input converts via clamped double-precision lrint to the full int32 range (D-04)"
    - "The C host test asserts f32-to-S32 conversion values including clamping and passes (make test green)"
    - "A `soc flc * *` convert rule exists and produces a valid 24-bit FLAC from raw S32LE input through a non-seekable pipe using the LMS-bundled sox (D-05)"
    - "Soloist browse tracks report samplesize 32 so $SAMPLESIZE$ substitutes 32; librespot soc paths keep the LMS default of 16 (D-08)"
  artifacts:
    - path: "Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c"
      provides: "S32LE ring + conversion + timing math + S32 host-test assertions"
      contains: "2147483647"
    - path: "Plugins/SpotOn/custom-convert.conf"
      provides: "soc flc transcode rule (sox raw-S32 to FLAC -b 24)"
      contains: "soc flc * *"
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "samplesize(32) hint on soloist stream paths"
      contains: "samplesize(32)"
  key_links:
    - from: "Plugins/SpotOn/custom-convert.conf"
      to: "Plugins/SpotOn/ProtocolHandler.pm"
      via: "$SAMPLESIZE$ substitution reads $track->samplesize (TranscodingHelper.pm:449, default 16)"
      pattern: "SAMPLESIZE"
    - from: "Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c RING_BYTES_PER_SEC"
      to: "timing math (_stream_refresh_timing)"
      via: "4 bytes/sample must be consistent in ring capacity, HTTP serving, and read/write index math"
---

<objective>
FLAC24 pipeline tracer (the architectural heart of Phase 76): upgrade fake-libpulse from destructive S16LE down-conversion to S32LE end-to-end, and wire the first complete high-res chain — Soloist float32 → S32LE ring → `/stream` → LMS `soc flc` sox rule → 24-bit FLAC to the player.

Purpose: The current `_convert_and_push()` throws away 8 bits (48 dB) of dynamic range. D-04 locks the fix; RESEARCH (empirically tested with bundled sox 14.4.3 + flac 1.3.4) locks S32LE as the output format and sox (never `flac --bps=32`, which bundled flac 1.3.4 rejects) as the transcoder.

Output: Rebuilt libpulse.so.0 with S32 ring, `soc flc * *` rule in custom-convert.conf, samplesize hints in ProtocolHandler.pm.

This is the phase's single `type="tracer"` slice: it touches every layer of the new audio path (C shim → HTTP → conf rule → Perl hint) with a real end-to-end verification. Remaining FLAC24 work (format resolver D-06, explicit prefs, dropdown D-07) expands from this slice in plans 76-04 and 76-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: fake-libpulse S32LE conversion — all five coupled sites + host tests + rebuild</name>
  <reversibility rating="reversible">Ring format is internal to the shim; reverting to S16 is a local diff plus rebuild (D-04 rated reversible in CONTEXT.md).</reversibility>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0</files>
  <read_first>
    - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c (full file — 2329 LOC; the five coupled sites are at approx. lines 475-526 conversion, 597-601 HTTP header, 1099-1100 RING_BYTES_PER_SEC, 1124-1136 timing math, 2044-2073 test harness)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md (section "fake-libpulse.c" — verified excerpts of all five sites)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Pitfall 2 — why all five sites must change together)
    - Plugins/SpotOn/Bin/fake-libpulse/Makefile (build + `test` target)
  </read_first>
  <action>
    Convert the ring from S16LE to S32LE per RESEARCH Pitfall 2 (D-04). All five coupled sites in fake-libpulse.c:

    1. `_convert_and_push()` (approx. 484-526): the FLOAT32LE branch changes `int16_t stackbuf[1024]` to `int32_t stackbuf[1024]` and the scaling to `(int32_t)lrint((double)f * 2147483647.0)` after the existing clamp to [-1.0, 1.0]. CRITICAL NUMERIC PITFALL: do NOT use `lrintf(f * 2147483647.0f)` — 2147483647 is not representable as a float (it rounds to 2^31), so f=1.0 would overflow int32. Promote to double before multiplying. The `PA_SAMPLE_S32LE` branch (currently a `>> 16` down-shift) becomes a plain `_ring_push` memcpy passthrough (S32 is now the native ring format). The `PA_SAMPLE_S16LE` branch (currently the passthrough) becomes an up-conversion: each int16 sample expands to int32 via `((int32_t)sample) << 16`. Update the comment block at approx. 475-483 to describe the new S32 ring.
    2. HTTP response header (approx. 597-601): replace the `audio/L16;rate=44100;channels=2` Content-Type with `application/octet-stream` plus a C comment documenting the payload as raw S32LE 44100 Hz stereo. Before changing, grep Plugins/SpotOn for "L16" to confirm no Perl consumer parses this header (LMS derives stream format from the strm frame / convert rules, not from this header); record the grep result in the commit message.
    3. `RING_BYTES_PER_SEC` (approx. 1099-1100): `(44100 * 2 * 2)` becomes `(44100 * 4 * 2)`; update the adjacent "always S16LE" comment. RING_CAPACITY derives from it — memory doubles to ~7 MB, acceptable per RESEARCH Pitfall 3.
    4. Timing math (approx. 1124-1136): ring bytes/sample is now 4. `input_bps` stays 4 for FLOAT32LE/S32LE and 2 for S16LE, but the fill scaling flips: `fill_input = fill * input_bps / 4` (was `/ 2`). For f32/s32 input this is identity; for s16 input the ring holds twice the input bytes.
    5. Host-test harness (`-DFAKE_LIBPULSE_TEST`, approx. 2044-2073): update the conversion assertion to S32 — pattern {1.0f, -1.0f, 0.5f, 2.0f, -3.0f, 0.0f} must produce int32 {2147483647, -2147483647, 1073741824, 2147483647, -2147483647, 0} (0.5 * 2147483647.0 = 1073741823.5, lrint rounds-to-even up to 1073741824); the read buffer grows from 12 to 24 bytes. Adjust the drop-oldest / writable_size / flush test constants for the doubled byte math so all 6 existing `ok:` checks still pass.

    Rebuild: `make -C Plugins/SpotOn/Bin/fake-libpulse all` (warning-clean with -Wall -Wextra) and commit the rebuilt libpulse.so.0 alongside the source — the deployed .so is runtime state that does not follow from the source change (RESEARCH Runtime State Inventory).
  </action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 6</automated>
  </verify>
  <done>All 6 host-test checks pass against the S32 ring; libpulse.so.0 rebuilt and committed; build is warning-clean.</done>
  <acceptance_criteria>
    - `make -C Plugins/SpotOn/Bin/fake-libpulse test` prints 6 `ok:` lines and exits 0
    - Source assertion: `grep -c '2147483647' Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c` >= 2 (conversion constant + test expectation)
    - Source assertion: `grep -c '44100 \* 4 \* 2' Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c` == 1 (RING_BYTES_PER_SEC)
    - Source assertion: `grep -c 'application/octet-stream' Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c` == 1 (HTTP header)
    - `git status` shows libpulse.so.0 modified and committed in the same task commit as fake-libpulse.c
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: soc flc convert rule + samplesize hints + end-to-end pipe verification</name>
  <files>Plugins/SpotOn/custom-convert.conf, Plugins/SpotOn/ProtocolHandler.pm</files>
  <read_first>
    - Plugins/SpotOn/custom-convert.conf (current content: only `soc pcm * *` and `son ogg * *`)
    - Plugins/SpotOn/ProtocolHandler.pm (approx. lines 74-160 contentType/formatOverride, 181-310 canDirectStream, 600-680 soloist new()/samplesize block at 631-650; grep `samplesize` for all hint sites)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md (sections "custom-convert.conf" and "ProtocolHandler.pm" — the exact tested rule text and the samplesize block excerpt)
    - /etc/squeezeboxserver/convert.conf (reference syntax for [sox]/flac live rules, -C 0 usage)
    - /usr/share/perl5/Slim/Player/TranscodingHelper.pm lines 440-460 ($SAMPLESIZE$ substitutes `$track->samplesize || 16` — verified this session)
  </read_first>
  <action>
    Wire the FLAC24 transcode rule and the bit-depth hints (D-05, D-08):

    1. Append to Plugins/SpotOn/custom-convert.conf the new rule `soc flc * *` with capability line `# IFT:{START=--skip=%t}U:{END=--until=%v}` and command `[sox] -q -t raw --encoding signed-integer --bits $SAMPLESIZE$ --endian little -r 44100 -c 2 - -t flac -C 0 -b 24 -`. Rationale to encode in a conf comment: `soc` is SHARED between soloist (S32LE after Task 1) and librespot Connect/browse PCM (S16LE); `$SAMPLESIZE$` substitutes from `$track->samplesize || 16` (TranscodingHelper.pm:449), so soloist streams (hinted 32 below) and librespot streams (default/hinted 16) both transcode correctly. `-b 24` output is constant: 32-bit input truncates float rounding noise (lossless in practice, source is <=24-bit), 16-bit input pads losslessly. Do NOT invoke the flac binary directly for raw input (bundled flac 1.3.4 rejects high-bps raw input — RESEARCH Anti-Patterns; sox performs the raw-to-FLAC conversion).
    2. ProtocolHandler.pm samplesize hints (D-08): in the soloist browse block (approx. 638-645) change the hint value 16 to 32 in the `samplesize` call and rewrite the obsolete comment block at approx. 631-637 (it still claims true 24-bit is deferred to Phase 74). Then locate the code path that creates/annotates the Track object for Connect URLs (spoton://connect-*) — grep `samplesize` and `connect-` across Plugins/SpotOn — and add the same hint there, backend-dispatched: 32 when `_useSoloist()`, 16 otherwise (librespot Connect PCM is S16LE). If librespot soc paths currently set no samplesize, the TranscodingHelper default of 16 already covers them — in that case add only the soloist-side 32 hints and note the default reliance in a comment referencing TranscodingHelper.pm:449.
    3. End-to-end pipe verification of the rule command (RESEARCH-validated): generate 1 second of S32LE stereo test signal (e.g. via perl pack or sox synth), pipe it through the exact rule command with 32 substituted for $SAMPLESIZE$ using the LMS-bundled sox at /usr/share/squeezeboxserver/Bin/x86_64-linux/sox, and verify the output is a decodable FLAC with 24-bit precision (`flac -t` exits 0 and `metaflac --show-bps` or soxi reports 24). Run this as a shell check inside the task, not as a committed script.
  </action>
  <verify>
    <automated>prove -l t/03_convert_conf.t t/04_types_conf.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'successful'</automated>
    <human-check>Live FLAC24 chain (covered by the consolidated Phase 76 UAT, D-11): play a Soloist track to squeezelite with the flc rule active and confirm LMS songinfo / squeezelite -d output shows FLAC 24-bit, with clean audio (no white noise, correct speed — RESEARCH Pitfall 2 warning signs).</human-check>
  </verify>
  <done>soc flc rule present and pipe-verified at 24-bit; samplesize hints set (soloist 32, librespot 16/default); conf + syntax tests green; full `prove -l t/` green.</done>
  <acceptance_criteria>
    - `grep -c 'soc flc \* \*' Plugins/SpotOn/custom-convert.conf` == 1
    - `grep -c 'SAMPLESIZE' Plugins/SpotOn/custom-convert.conf` >= 1 (rule is bit-depth-driven, not hardcoded to one backend)
    - `grep -c 'samplesize(32)' Plugins/SpotOn/ProtocolHandler.pm` >= 1
    - The end-to-end pipe check ran during the task: bundled sox consumed raw S32LE from a pipe and emitted FLAC that `flac -t` decodes with 24 bits per sample (paste command + output into the SUMMARY)
    - `prove -l t/` fully green (no regression in the 36-file suite)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `fake-libpulse.c`: S32LE ring format (`RING_BYTES_PER_SEC = 44100*4*2`), FLOAT32LE→S32 double-lrint conversion, S32LE passthrough branch, S16LE→S32 up-conversion branch, `application/octet-stream` /stream Content-Type, S32 host-test assertions
- `libpulse.so.0`: rebuilt binary artifact (x86_64 dev build; CI `build-fake-libpulse.yml` covers the 3 release arches on next tag)
- `custom-convert.conf`: new rule `soc flc * *` ($SAMPLESIZE$-driven sox raw→FLAC -C 0 -b 24)
- `ProtocolHandler.pm`: `samplesize(32)` hints on soloist browse + connect track objects (librespot paths stay at the LMS default 16 or get an explicit 16 hint)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /stream HTTP (localhost) | LMS/player reads raw PCM from the shim's local HTTP server |
| convert rule execution | LMS spawns sox with substituted placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-01 | Tampering/DoS | S32 ring buffer bounds | medium | mitigate | All ring writes go through `_ring_push` bounds-checked byte counting; host tests re-assert capacity/writable_size semantics after the 2x byte-math change |
| T-76-02 | Tampering | soc flc rule command | low | mitigate | Only the `[sox]` tool token and LMS-substituted `$SAMPLESIZE$`/`%t`/`%v` placeholders — no user-controlled string interpolation (RESEARCH Security V5) |
| T-76-03 | DoS | Doubled RING_CAPACITY (~7 MB) | low | accept | Fixed-size allocation, bounded; negligible on all target platforms |
</threat_model>

<verification>
- `make -C Plugins/SpotOn/Bin/fake-libpulse test` — 6/6 checks green against S32 ring
- `prove -l t/` — full suite green
- End-to-end pipe check: raw S32LE → rule command → 24-bit FLAC verified with bundled tools
</verification>

<success_criteria>
- No S16 down-conversion remains on the soloist float32 input path; ring, header, capacity, and timing math are consistently 4 bytes/sample
- `soc flc * *` rule is live and safe for BOTH backends via $SAMPLESIZE$ (soloist 32 / librespot 16)
- Committed .so matches committed source
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-01-SUMMARY.md` when done.
</output>
