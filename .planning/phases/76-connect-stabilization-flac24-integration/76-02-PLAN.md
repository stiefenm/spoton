---
phase: 76-connect-stabilization-flac24-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/unified.rs
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/Unified/Daemon.pm
autonomous: true
requirements: [GH-159, GH-131, GH-128]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A /control/{play,pause,next,prev} command that Spirc drops because the device is Not Active returns HTTP 409, not 204 (GH #159)"
    - "Connect.pm distinguishes 409 from transport errors: on 409 it stops/ejects the stale LMS stream and does NOT fire the Web API fallback (which would hijack the user's actually-active device)"
    - "Sync-group master daemons spawn with a raised --buffer-latency-ms so LMS drift-correction can be replenished faster than 1x real-time (GH #131)"
    - "After a mid-song Connect handoff, LMS receives a fresh position notification tied to the moment the /stream relay actually starts serving, not only the transfer-time position (GH #128)"
  artifacts:
    - path: "librespot-spoton/src/unified.rs"
      provides: "409 mapping for inactive-Spirc control commands + relay-start position resync + Rust unit test"
      contains: "CONFLICT"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "409-aware _sendControlCommand error handling (stop/eject, no Web API fallback)"
    - path: "Plugins/SpotOn/Unified/Daemon.pm"
      provides: "--buffer-latency-ms arg push for synced players"
      contains: "buffer-latency-ms"
  key_links:
    - from: "librespot-spoton/src/unified.rs /control status"
      to: "Plugins/SpotOn/Connect.pm _sendControlCommand error callback"
      via: "SimpleAsyncHTTP treats non-2xx as error; callback must branch on $http->code == 409"
      pattern: "409"
---

<objective>
Fix three librespot-backend Connect bugs rooted in the Rust control/stream layer: #159 (player stuck in BUFFERING forever after Connect-deselect while paused — /control returns success for silently-dropped commands), #131 (sync-group stutter — 1x-real-time rate limiter cannot replenish drift corrections), #128 (progress bar lags ~5s after mid-song handoff — position notify fires before the /stream relay is ready).

Purpose: These are the top user-facing Connect stability items blocking a usable v4.0 (D-01 full scope). Root causes are localized with HIGH confidence in 76-RESEARCH.md (Pitfall 5, phase_requirements table) and the issue bodies.

Output: unified.rs status fix + relay-start resync + Rust test; Connect.pm 409 handling; Daemon.pm buffer-latency arg.

Note: the Rust changes ship via CI binary rebuild at the next tag; for the live UAT (76-08 checklist) a local `cargo build` deploy to the dev LMS is used.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: unified.rs — 409 for inactive-Spirc control commands (#159) + Rust test</name>
  <reversibility rating="reversible">The status contract is internal between our own Rust binary and our own Perl module, shipped together in one plugin zip — changing it back is a two-file local diff.</reversibility>
  <files>librespot-spoton/src/unified.rs</files>
  <read_first>
    - librespot-spoton/src/unified.rs (the /control handler status match at approx. lines 1210-1235, AND the pre-existing 409 CONFLICT usage on /stream supersession at approx. lines 995-1090 (H11) — a DIFFERENT endpoint; do not conflate the two)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md (section "unified.rs" — verified excerpt of the status match with the 422 arm as the pattern for a distinct status)
    - GH issue #159 body (root-cause analysis: `SpircCommand::Play will be ignored while Not Active` while LMS logs `control_cmd_ok`)
  </read_first>
  <action>
    Per GH #159: change the `(false, "pause") | (false, "play") | (false, "next") | (false, "prev")` arm of the /control status match from `StatusCode::NO_CONTENT` to `StatusCode::CONFLICT` (409), with a comment naming GH #159 and the semantic: "Spirc dropped the command (device is not the active Connect target) — a distinct status so the Perl side can eject the stale stream instead of buffering forever." Before changing, verify in the surrounding code what makes `result` None for these four commands — per the issue analysis it is the Spirc-inactive path (body-parse failures are only handled by the separate volume/seek 422 arm); if a second None-producing path exists for these commands, document it in the comment and confirm 409 is still the correct signal for it.

    Add a Rust unit test (in unified.rs's test module or the crate's existing test layout — RESEARCH Validation Architecture flags this as a Wave-0 gap) that exercises the status-mapping function/match for the four command names with result-absent and asserts 409, and with result-present asserts 204. If the match is inline in the handler closure and not directly testable, extract it into a named fn `control_status(result_present: bool, cmd: &str) -> StatusCode` first, then test that.

    Verify no OTHER caller of /control depends on 204-on-inactive: grep the Perl tree for "/control/" consumers (Connect.pm is the only caller today — confirm).
  </action>
  <verify>
    <automated>cd librespot-spoton && cargo test 2>&1 | tail -3 | grep -q 'test result: ok'</automated>
  </verify>
  <done>Inactive-Spirc play/pause/next/prev return 409; active commands still 204; cargo test green including the new status-mapping test.</done>
  <acceptance_criteria>
    - `grep -c 'CONFLICT' librespot-spoton/src/unified.rs` count increased by at least 1 vs HEAD (new usage in the /control match, distinct from the H11 /stream usage)
    - `cargo test` passes with a test asserting the (false, "play") → 409 mapping
    - `grep -rn '/control/' Plugins/SpotOn/ | grep -v Connect.pm` returns nothing (Connect.pm is the sole consumer)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Connect.pm — 409-aware control error handling (#159 Perl side)</name>
  <files>Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - Plugins/SpotOn/Connect.pm (sub _sendControlCommand approx. lines 251-360 — the librespot SimpleAsyncHTTP block: success cb logs OK without inspecting status; error cb unconditionally calls _sendControlFallback; also read _sendControlFallback itself)
    - GH issue #159 body ("Expected: LMS either refuses to resume with a clear state/error, or is told the device is no longer an active Connect target so it can stop/eject the stale track")
  </read_first>
  <action>
    In `_sendControlCommand`'s librespot-path error callback (SimpleAsyncHTTP invokes the error callback for ALL non-2xx responses, so the new 409 lands here — verify by reading how SimpleAsyncHTTP exposes the response code, typically `$http->code`): branch on code 409 BEFORE the existing Web API fallback. On 409:
    - Log at warn level: endpoint, mac, and the reason "device is not the active Connect target (GH #159)". Emit the diagnosticMode [DIAG] line as `control_cmd_rejected` (new marker, so logs distinguish it from control_cmd_fail).
    - Do NOT call `_sendControlFallback` — the Web API PUT would act on the user's actually-active device, which is exactly wrong.
    - Eject the stale stream: dispatch a `stop` request on the client with `source(__PACKAGE__)` (the established pattern at Connect.pm approx. 1031-1033, see PATTERNS "Slim::Control::Request Command-Dispatch"), so LMS leaves BUFFERING-STREAMING instead of hanging until the device is re-selected.
    All other error codes keep the existing fallback behavior byte-for-byte. The soloist WS branch above the librespot block is untouched.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Live #159 repro (consolidated Phase 76 UAT): start Connect playback, pause, deselect the device in the Spotify app, press Play from Material Skin — LMS must stop/eject within ~5s instead of hanging in BUFFERING; server.log shows the control_cmd_rejected marker; re-selecting the device restores normal control.</human-check>
  </verify>
  <done>409 handled as terminal stop/eject with no Web API fallback; every other error path unchanged; syntax + full test suite green.</done>
  <acceptance_criteria>
    - Source assertion: `grep -c 'control_cmd_rejected' Plugins/SpotOn/Connect.pm` >= 1
    - Source assertion: within the 409 branch there is a `Slim::Control::Request` stop dispatch with `source(__PACKAGE__)` (grep for the stop dispatch near the 409 branch)
    - Source assertion: the 409 branch contains no `_sendControlFallback` call (verify by reading the branch — the fallback call remains only in the non-409 path)
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Daemon.pm --buffer-latency-ms for synced players (#131) + relay-start position resync (#128)</name>
  <files>Plugins/SpotOn/Unified/Daemon.pm, librespot-spoton/src/unified.rs, Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - Plugins/SpotOn/Unified/Daemon.pm (helperArgs push block approx. lines 119-165 — the conditional arg-push idiom per PATTERNS "Daemon.pm")
    - librespot-spoton/src/main.rs (line approx. 196: existing --buffer-latency-ms CLI flag, default 2000)
    - librespot-spoton/src/unified.rs (the /stream relay: the "relay starting" log point (grep "relay starting") and the OGG-header-wait block)
    - librespot-spoton/src/connect.rs (lines approx. 56-268 — Phase 58's needs_position_sync mechanism: the atomic bool flag declared at line ~56, cloned at ~105, and consumed on the event path — it already pushes a corrected position to LMS via the event channel)
    - GH issue #128 body (log excerpt: multi-second gap between `endpoint: transfer` and `relay starting`; theory: position notification fires from transfer position before the relay serves audio)
    - GH issue #131 body (root-cause: CON-14 wall-clock rate limiter at 2000ms headroom vs _CheckSync/skipAhead/_Rebuffer; fix options)
  </read_first>
  <action>
    #131 (GH-131): in Daemon.pm's spawn-arg block, push `'--buffer-latency-ms', 5000` when `$client->isSynced() && $client->model ne 'group'` (the sync-detection idiom from DaemonManager.pm's deviceNameForClient, PATTERNS "Daemon.pm"). 5000 is the low end of the issue's suggested 5000-10000 range — it satisfies the hardcoded 5s rebuffer threshold in LMS Player::rebuffer while keeping Spotify-app transport latency acceptable. Comment the trade-off and the spawn-time limitation: a player that joins a sync group AFTER daemon spawn keeps 2000 until the next daemon restart — deliberately NOT restart-on-sync-change, per the Phase 63 GH #143 decision (sync-membership changes must not bounce daemons).

    #128 (GH-128): implement a relay-start position resync so the first position LMS trusts corresponds to when audio actually flows. Primary candidate (verify feasibility while reading unified.rs, then implement): at the /stream "relay starting" point, set the Phase 58 `needs_position_sync` flag (or directly emit the same position-carrying event that mechanism uses) so the next position notification pushed to LMS carries Spirc's CURRENT position at relay start, not the stale transfer-time position. If the relay code cannot reach the Spirc/event state cleanly, the fallback implementation is to emit a dedicated lightweight event at relay start that Connect.pm's existing `change`-handler position-resync path (post-#126) consumes. Either way: the mechanism must be a real committed fix, exercised by `cargo test` where the logic is testable, and its live effect (Spotify-app vs LMS bar divergence <= ~2s on a mid-song handoff) is verified in the consolidated UAT.
  </action>
  <verify>
    <automated>(cd librespot-spoton && cargo build -q) && prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Live #131: Connect playback on the dev sync group (two squeezelite instances) plays continuously for 3+ minutes without the 1s-play/10-20s-pause stutter cycle. Live #128: start a track in the Spotify app, transfer mid-song to the SpotOn player; after buffer fill, the Spotify app playhead and the LMS UI diverge by <= ~2s.</human-check>
  </verify>
  <done>--buffer-latency-ms 5000 pushed for synced non-group players; relay-start resync mechanism implemented in Rust (+Perl consumer if the fallback shape was needed); cargo build + full Perl suite green.</done>
  <acceptance_criteria>
    - `grep -c 'buffer-latency-ms' Plugins/SpotOn/Unified/Daemon.pm` >= 1, guarded by an isSynced check on the same or adjacent line
    - `grep -c 'relay starting' librespot-spoton/src/unified.rs` >= 1 and the resync trigger (needs_position_sync set or new event emission) is within that relay-start code path (cite line numbers in the SUMMARY)
    - `cargo build` and `cargo test` both green; `prove -l t/` fully green
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `unified.rs`: `StatusCode::CONFLICT` mapping for dropped play/pause/next/prev; extracted/testable `control_status` mapping (if extraction was needed); relay-start position-resync trigger; new Rust unit test for the /control status mapping
- `Connect.pm`: 409 branch in `_sendControlCommand` error callback with `control_cmd_rejected` [DIAG] marker and stop/eject dispatch
- `Daemon.pm`: conditional spawn arg `--buffer-latency-ms 5000` for synced non-group players

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS Perl → daemon /control (localhost HTTP) | control commands + status codes |
| daemon → LMS event channel | position/state notifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-04 | Spoofing | 409 response handling | low | accept | /control binds to 127.0.0.1 only (existing design); no new network surface |
| T-76-05 | DoS | stop-dispatch on 409 | low | mitigate | stop request carries source(__PACKAGE__) so SpotOn's own subscriber ignores the echo — no command loop |
| T-76-06 | Tampering | larger sync buffer (5000ms) | low | accept | Bounded constant; worst case is higher transport latency, no integrity impact |
</threat_model>

<verification>
- `cargo test` green in librespot-spoton (includes new 409 mapping test)
- `prove -l t/` green
- Live behaviors (#159 eject, #131 stutter-free sync, #128 <=2s divergence) covered by the consolidated Phase 76 UAT checklist (76-08)
</verification>

<success_criteria>
- The three issue-numbered behaviors are code-complete with automated gates green and precise live-UAT entries queued
- No behavior change for active-Spirc commands, unsynced players, or non-handoff playback
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-02-SUMMARY.md` when done.
</output>
