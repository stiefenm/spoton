---
phase: 76-connect-stabilization-flac24-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
  - .planning/WINDOWS.md
autonomous: true
requirements: [GH-161, GH-94, WIN-6]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Recently Played, Liked Songs, and Top Tracks menu entries expose Play All / Add to Queue hover actions in Material Skin (type playlist, GH #161); Made For You stays type link"
    - "SpClient search() compares the pagination offset against the track-FILTERED count, closing the Window 6 off-by-scope guard (WINDOWS.md #6)"
    - "Pressing More on a track reached via SpotOn's OPML browse menu shows the same context items as the TrackInfo menu from Now Playing (GH #94)"
  artifacts:
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "type playlist on flat-track-list entries + TrackInfo-parity context wiring"
    - path: "Plugins/SpotOn/API/SpClient.pm"
      provides: "track-filtered offset guard in search()"
    - path: "t/36_spclient.t"
      provides: "regression test pinning the filtered-count guard"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm _trackItem context actions"
      to: "Slim::Menu::TrackInfo"
      via: "browse More menu resolves through the TrackInfo framework so registered providers + LMS standard items appear in both menus"
---

<objective>
Browse/UX fixes that need no daemon and no Rust: GH #161 (flat track lists lack playlist-style hover actions), Window 6 (SpClient search() offset guard uses the unfiltered URI count — residual from Phase 75 CR-01), GH #94 (OPML browse context menu shows fewer items than TrackInfo).

Purpose: Cluster 4 of the phase (D-01 full scope). All three have exact fix locations verified in 76-PATTERNS.md and the issue bodies.

Output: Plugin.pm menu-type + context-parity changes, SpClient.pm guard fix with regression test, WINDOWS.md #6 marked fixed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Window 6 — search() offset guard against the track-filtered count</name>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t, .planning/WINDOWS.md</files>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm (sub search — the two offset guards at approx. lines 1193-1197 and 1224-1228, and the track-filter + slice block at approx. 1230-1237; verified excerpts in 76-PATTERNS.md section "SpClient.pm")
    - t/36_spclient.t (the Phase 75-07 CR-01 test style — mixed-content fixtures with per-hex response rules)
    - .planning/WINDOWS.md (entry #6 — the exact deviation description)
  </read_first>
  <behavior>
    - Test 1: context-resolve returns 5 URIs of which 3 are spotify:track: — a search call with offset 3 (>= filtered count 3, < raw count 5) delegates to Client.pm instead of returning an empty/short window
    - Test 2: offset 2 with 3 filtered tracks still serves from context-resolve (guard must not over-delegate)
  </behavior>
  <action>
    Fix the WINDOWS.md #6 off-by-scope guard (Phase 75 residual): in SpClient.pm `search()`, the delegation guard at approx. line 1224 compares `$offset` against `scalar(@uris)` (raw context-resolve URIs) but the slice at 1230-1237 operates on `@trackIds` (URIs filtered to spotify:track:). Restructure so the filter runs BEFORE the count-based guard and the guard compares `$offset` against the filtered track count — same shape as 75-07's filter-before-slice fix in getPlaylistItems. Keep the first guard (offset >= 20 context-resolve ceiling, S-05) unchanged. Delegation still routes through the existing `$fallbackToClient` closure with the existing S-05 log wording.

    Add the two behavior tests above to t/36_spclient.t using the file's established stub/fixture harness. Then mark the ledger entry fixed: run `node $HOME/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 6` (updates .planning/WINDOWS.md).
  </action>
  <verify>
    <automated>prove -l t/36_spclient.t 2>&1 | tail -1 | grep -q 'successful'</automated>
  </verify>
  <done>Guard compares against the filtered count; both regression tests pass; WINDOWS.md #6 status is fixed; full suite green.</done>
  <acceptance_criteria>
    - t/36_spclient.t contains a test where offset >= filtered-track-count but < raw-URI-count triggers exactly one Client.pm delegation
    - `grep -A2 '"id": 6' .planning/WINDOWS.md | grep -c fixed` >= 1 (ledger entry resolved)
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GH #161 — type playlist on flat-track-list menu entries</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (the three items: Recently Played at approx. line 1405, Top Tracks at approx. 1441, Liked Songs at approx. 1609; existing `type => 'playlist'` precedents at approx. 1341, 1380, 2167; Made For You feed `_madeForYouFeed` for the negative case)
    - GH issue #161 body (Spotty's convention: playlist type ONLY for entries leading DIRECTLY to a flat track list)
  </read_first>
  <action>
    Per GH #161: change `type => 'link'` to `type => 'playlist'` on exactly the menu entries whose feed renders a flat track list directly: Recently Played (`_recentlyPlayedFeed`), Liked Songs (`_savedTracksFeed`), and Top Tracks (`_topTracksFeed`) — verify each feed is flat before changing (read the feed sub headers; `_savedTracksFeed` already carries server-side Play All detection, confirming the shape). Made For You (`_madeForYouFeed`) renders a list of playlists (intermediate level) and MUST keep `type => 'link'` (its two push sites per the Phase 57 decision). If any of the three items appears at more than one push site (e.g. also in `_homeFeed`), apply the same type change at every push site of that item so Material Skin behavior is consistent between home rows and the browse tree.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'successful'</automated>
    <human-check>Material Skin: hover Recently Played / Liked Songs / Top Tracks — Play All and Add to Queue actions appear; Made For You still navigates as a plain link.</human-check>
  </verify>
  <done>All three flat-list entries (every push site) are type playlist; Made For You unchanged; suite green.</done>
  <acceptance_criteria>
    - Each of the three feeds' menu items carries `type => 'playlist'` at every push site (cite line numbers in the SUMMARY)
    - The Made For You item(s) still carry `type => 'link'`
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GH #94 — browse context menu parity with TrackInfo</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (sub _trackItem at approx. line 1149, sub _albumTrackItem at approx. 3404, the existing registerInfoProvider block at approx. 268-283 (Phase 37/GH-93), and the itemActions usage at approx. 2775)
    - /usr/share/perl5/Slim/Menu/TrackInfo.pm (how menu() assembles standard LMS items + registered providers, and how the `trackinfo items` CLI/JSON dispatch reaches it for a url parameter)
    - GH issue #94 body (root cause: two separate menu systems — OPML itemActions never invoke the TrackInfo framework)
    - Plugins/SpotOn/ProtocolHandler.pm (getMetadataFor — TrackInfo on a remote spoton:// url resolves metadata through this)
  </read_first>
  <action>
    Per GH #94: route the OPML browse More menu through the TrackInfo framework instead of hand-maintained itemActions, so ONE menu source feeds both navigation paths (RESEARCH Don't-Hand-Roll row "Kontextmenü-Parität"). Implementation direction: give _trackItem / _albumTrackItem items an `itemActions` `info` action that dispatches the LMS `trackinfo items` command with the item's spoton:// url as fixedParams (investigate the exact param shape by reading how Slim::Menu::TrackInfo's CLI handler extracts the url and how an existing plugin item invokes it — the LMS core XMLBrowser + TrackInfo sources in /usr/share/perl5 are the authority). The result must include BOTH the LMS standard items (Add to Favorites, More Info) AND SpotOn's registered providers (spotonTrackInfo from the approx. line 271 registration: Artist View, Album View, Like, Add to Playlist). Apply the same wiring to episode items (the shows/episodes discrepancy is called out in the issue). Preserve any browse-only actions currently in itemActions that TrackInfo does not cover — verify by diffing the current browse More items against the TrackInfo menu items before removing anything; do not drop actions.

    If investigation shows the `info` itemAction cannot reach Slim::Menu::TrackInfo for remote OPML items on LMS 8.x (the plugin floor), implement the parity instead by extending SpotOn's registered TrackInfo provider set and mirroring the missing standard actions into itemActions — but only take this branch with evidence (name the LMS-core limitation and line in the SUMMARY), since duplication is the documented anti-pattern.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t t/14_context_menu.t 2>&1 | tail -1 | grep -q 'successful'</automated>
    <human-check>Browse to any album track via SpotOn's OPML menu, press More: the item set matches the TrackInfo menu opened from Now Playing for the same track (LMS standard items + Artist View + Album View + Like + Add to Playlist). Repeat for a podcast episode.</human-check>
  </verify>
  <done>Browse More menu and TrackInfo menu render the same item set for tracks and episodes; no existing action lost; suite green.</done>
  <acceptance_criteria>
    - _trackItem and _albumTrackItem (and the episode item builder) carry the TrackInfo-routed info action, or (evidence-gated fallback branch) the provider/itemActions parity set — one mechanism, applied consistently
    - t/14_context_menu.t still passes (existing TrackInfo provider coverage unbroken)
    - The SUMMARY lists the before/after More-menu item sets proving parity
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `Plugin.pm`: `type => 'playlist'` on Recently Played / Liked Songs / Top Tracks items (all push sites); TrackInfo-routed `info` itemAction on `_trackItem` / `_albumTrackItem` / episode items
- `SpClient.pm`: filter-before-guard restructure in `search()` (offset compared against filtered track count)
- `t/36_spclient.t`: 2 new Window-6 regression tests
- `.planning/WINDOWS.md`: entry #6 status fixed

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spotify context-resolve response → search slicing | untrusted API data drives offset math |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-07 | Tampering/DoS | search() offset math on API data | low | mitigate | Guard now derives from the filtered list actually sliced; regression tests pin the window invariant (same class as 75-07 CR-01) |
| T-76-08 | Info Disclosure | TrackInfo url params | low | accept | spoton:// urls carry public track ids only |
</threat_model>

<verification>
- `prove -l t/` green (incl. new t/36 tests, t/14 context menu)
- Live checks (Material Skin hover actions, More-menu parity) via consolidated Phase 76 UAT
</verification>

<success_criteria>
- All three items ship without behavior regressions in existing menus, play-all, or search pagination
- WINDOWS.md open_count decreases by 1 (entry #6)
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-03-SUMMARY.md` when done.
</output>
