---
phase: 76-connect-stabilization-flac24-integration
plan: 05
type: execute
wave: 2
depends_on: ["76-02"]
files_modified:
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/Unified/SoloistWS.pm
  - t/32_soloist_events.t
autonomous: true
requirements: [GH-151, GH-158, CONNECT-AUTOPLAY]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After an LMS restart with a dormant/paused Connect session, playback does NOT start by itself; a fresh transfer from the Spotify app still starts playback normally (ROADMAP item: Auto-Play nach Restart)"
    - "A player that was powered OFF before a Connect session activated it is powered off again when the Connect session ends; a player that was already on stays on (GH #151)"
    - "The pause-skip-play sequence on a sync group no longer loops the song restart (GH #158) — or the investigation produced a committed fix for the actual root cause found in the diag log"
  artifacts:
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "restart-autoplay gate in the start handler + power save/restore on session start/end + GH #158 fix"
  key_links:
    - from: "Connect.pm start handler"
      to: "$client->pluginData connect-session state"
      via: "pluginData flags (established Connect session-state store) carry prev-power and session-provenance across events"
---

<objective>
Connect session-lifecycle fixes in Connect.pm: suppress unwanted auto-play after an LMS restart, restore the pre-Connect power state when a session ends (GH #151), and fix the group-player pause→skip→play restart loop (GH #158).

Purpose: Cluster 1 items that live above the transport layer. Runs after 76-02 (same file; and #158's symptom may be altered by 76-02's #159/#131 fixes — reproduce against the fixed baseline).

Output: gated start handler, power save/restore, #158 root-cause fix; regression tests where the harness reaches.

Note on verification depth: Connect.pm has never had an isolated unit harness (Phase 73-03 D4). Automated gates here are syntax + suite + SoloistWS-level tests; the live behaviors are pinned as human-checks harvested into the consolidated Phase 76 UAT (D-11).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-play-after-restart gate (investigate, then fix)</name>
  <precondition>Dev LMS instance (systemd unit lyrionmusicserver) is running locally and a SpotOn daemon can hold a Connect session — needed to reproduce the restart scenario live.</precondition>
  <files>Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - Plugins/SpotOn/Connect.pm (start handler at approx. lines 1018-1060 — the unconditional playlist-play excerpted in 76-PATTERNS.md "Connect.pm"; the pluginData session-state flags connectStartTime/newTrack/pendingConnect; the second start branch at approx. 767)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Pattern 4 — autoplay suppression approach: gate on fresh user trigger vs restored session)
    - Plugins/SpotOn/Unified/DaemonManager.pm (daemon spawn timing/registry — a spawn-adjacent timestamp is the candidate provenance signal)
  </read_first>
  <action>
    Two steps, evidence first (ROADMAP item "Auto-Play im Connect-Mode nach LMS-Restart ohne User-Aktion"):

    1. REPRODUCE on the dev box: establish a Connect session to a dev player (Spotify Desktop, spotify-control skill), pause it, then `sudo systemctl restart lyrionmusicserver` (sudoers-permitted). Capture server.log + the per-player daemon log across the restart and identify EXACTLY which inbound event re-triggers the start handler's playlist-play without any user action (daemon respawn re-announcing an existing Spirc session is the working hypothesis). Record the observed event sequence in the SUMMARY.
    2. FIX: gate the start handler's playlist-play on session provenance using the evidence: the established shape is a grace check — if the start event arrives within a short window of the daemon's (re)spawn AND there was no fresh user-side trigger (fresh transfer/device_changed or an explicit play command in the same burst — use whatever discriminator step 1 revealed), suppress the playlist-play (log why, keep metadata/state handling so the session stays visible and manually resumable) instead of starting audio. Store any needed state in $client->pluginData per the established pattern. The gate must NOT break: a genuine app-initiated transfer right after an LMS restart, the skipInitiated playlist-play path from QT 260827-of9, or the 76-02 flows. Do not confuse this with the per-player pref_enableAutoplay (DSTM autoplay — unrelated).
    3. Re-run the restart scenario and confirm: paused session stays paused after restart; a fresh app transfer immediately after restart still plays.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Consolidated Phase 76 UAT: (a) pause a Connect session, restart LMS — no audio starts by itself; (b) after restart, tap the device in the Spotify app — playback starts normally within the usual transfer time.</human-check>
  </verify>
  <done>Root-cause event sequence documented; gate implemented and live-verified in the dev repro (both directions: suppressed restart autoplay, preserved fresh transfer); full suite green.</done>
  <acceptance_criteria>
    - SUMMARY contains the captured restart event sequence (log excerpts with timestamps) naming the triggering event
    - The start handler contains a provenance gate referencing this ROADMAP item in its comment, with the suppress branch logging at info level
    - Live repro run on the dev box shows: paused-session restart produces no `playlist play spoton://connect-` dispatch (server.log evidence), fresh transfer does
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GH #151 — power state save/restore across the Connect session</name>
  <files>Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - Plugins/SpotOn/Connect.pm (start handler approx. 1018-1060; session-end stop handler at approx. 1169; the track-transition stop branch at approx. 1007 — do NOT hook that one; the Slim::Control::Request dispatch idiom at 1031-1033)
    - GH issue #151 body (ShairTunes prior art; edge cases: manual power-on during session, sync groups, multiple transfers)
  </read_first>
  <action>
    Per GH #151: on Connect session START (the approx. 1018 handler, before/around the point the play request would power the player on), read the player's current power state via `$client->power()` and store it as pluginData `connectPrevPower` — only on a genuine NEW session (not per-track start; guard so repeated start events within one session do not overwrite the saved state with the now-on value). On Connect session END (the approx. 1169 stop/session-end handler — verify it is the session-end path, not the track-transition stop at 1007): if `connectPrevPower` was saved as 0/off, dispatch a power-off request via Slim::Control::Request with `source(__PACKAGE__)`; always clear the flag afterward. Edge handling (issue notes): if at session end the player is already playing something ELSE (user started local playback during/after the session), skip the power-off and log; for sync groups apply the logic per player receiving the event (each member saved/restored individually). Repeated transfers away/back within a session: the first save wins until the flag is cleared at a real session end.
  </action>
  <verify>
    <automated>prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Consolidated Phase 76 UAT: power a player OFF, start a Connect session to it from the Spotify app (it powers on and plays), end the session (transfer away or disconnect) — the player powers off again. Repeat with a player that was already ON — it stays on.</human-check>
  </verify>
  <done>Save-on-start / restore-on-end implemented with the documented guards; suite green.</done>
  <acceptance_criteria>
    - `grep -c 'connectPrevPower' Plugins/SpotOn/Connect.pm` >= 2 (save site + restore site)
    - The restore dispatch uses Slim::Control::Request with source(__PACKAGE__) (source assertion)
    - The restore site is in the session-end handler, not the newTrack track-transition stop branch (cite line numbers in the SUMMARY)
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GH #158 — group-player pause-skip-play restart loop (investigate, then fix)</name>
  <files>Plugins/SpotOn/Connect.pm, Plugins/SpotOn/Unified/SoloistWS.pm, t/32_soloist_events.t</files>
  <read_first>
    - GH issue #158 (download the attached diag log: fetch the user-attachments URL from `gh issue view 158 --json body` with curl to the scratchpad and analyze it — repro steps: sync group, pause via app, skip via app, play via app → song restarts over and over)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Pitfall 4 — hypothesis: QT-12 skipInitiated playlist-play collides with the sync-group _Rebuffer whole-group pause on the new() proxy path)
    - .planning/quick/260827-of9-soloist-skip-stream-reconnect-fake-libpu/260827-of9-SUMMARY.md (what skipInitiated does and when it fires)
    - Plugins/SpotOn/Unified/SoloistWS.pm (skipInitiated flag set/consume sites) and Plugins/SpotOn/Connect.pm (change handler consuming it; the start/change/stop handlers around 1018-1200)
    - /usr/share/perl5/Slim/Player/StreamingController.pm (_Rebuffer / _CheckSync — how a group pause interacts with a fresh playlist play)
  </read_first>
  <action>
    Evidence-first fix for GH #158 (note: this player runs the LIBRESPOT backend per the issue — PCM/squeezelite/Docker — so analyze the librespot event path, not only the soloist skipInitiated path; RESEARCH Pitfall 4's soloist hypothesis is one candidate, not the conclusion):

    1. Analyze the attached diag log for the pause→skip→play window: which handler issues the repeated restarts (look for repeated `playlist play spoton://connect-` dispatches, repeated `start` events from the daemon, or LMS _Rebuffer cycles re-triggering the change handler).
    2. Reproduce on the dev box AFTER the 76-02 fixes are in the local build (two squeezelite instances synced; pause, skip, play from the Spotify app) — 76-02's #159 409-eject and #131 buffer change may already alter the symptom; record whether it still reproduces.
    3. Fix the identified loop at its root: the fix must break the restart cycle (e.g. de-duplicate the play dispatch when the same connect track is already the current streaming song on the group master, or gate the handler correctly for group members vs master) WITHOUT breaking: single-player skip recovery (QT-12 behavior, live-verified at ~1.16s), normal group playback, and the 76-02 changes. Add a regression test at whatever level the harness reaches (t/32_soloist_events.t for SoloistWS flag logic if the fix lands there; otherwise source assertions + the live UAT entry).
    4. If, after serious analysis, the root cause turns out to sit in LMS core group logic beyond SpotOn's reach, implement the best SpotOn-side mitigation found and document the residual precisely (issue comment draft in the SUMMARY — do not post it; user posts per project convention).
  </action>
  <verify>
    <automated>prove -l t/32_soloist_events.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Consolidated Phase 76 UAT: on the dev sync group — start Connect playback, pause in the app, skip, press play: music resumes on the new track within a few seconds, no repeated restarts in server.log.</human-check>
  </verify>
  <done>Diag log analyzed with the loop mechanism named; fix committed and dev-repro verified (or mitigation + precisely-scoped residual documented); suite green.</done>
  <acceptance_criteria>
    - SUMMARY names the concrete loop mechanism with diag-log line evidence
    - Dev repro result recorded both before-fix (or "no longer reproduces after 76-02", with evidence) and after-fix
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `Connect.pm`: restart-provenance gate in the start handler (suppress branch + info log); `connectPrevPower` pluginData save/restore with session-end power-off dispatch; GH #158 loop fix
- `SoloistWS.pm`: only if the #158 fix touches the skipInitiated logic (flag guards)
- `t/32_soloist_events.t`: regression coverage for any SoloistWS-level change

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon events → Connect handlers | daemon-originated events drive player power/playback commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-11 | Elevation/DoS | power-off dispatch at session end | low | mitigate | Only fires when a saved off-state exists AND the player is not playing something else; source(__PACKAGE__) prevents event echo |
| T-76-12 | DoS | autoplay suppress gate | medium | mitigate | Gate is provenance-scoped (restart window only) and live-verified in both directions (suppression + fresh transfer) before the task closes |
</threat_model>

<verification>
- `prove -l t/` green
- Dev-box live repros executed inside the tasks (restart scenario, #158 group scenario) with log evidence in the SUMMARY
- Final re-check of all three behaviors in the consolidated Phase 76 UAT (76-08 checklist)
</verification>

<success_criteria>
- No self-starting audio after LMS restarts; power etiquette matches ShairTunes prior art; group pause-skip-play recovers cleanly
- QT-12 single-player skip behavior and 76-02 fixes unregressed
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-05-SUMMARY.md` when done.
</output>
