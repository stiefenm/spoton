---
phase: 76-connect-stabilization-flac24-integration
plan: 07
type: execute
wave: 3
depends_on: ["76-01", "76-05"]
files_modified:
  - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c
  - Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0
  - Plugins/SpotOn/Connect.pm
  - TROUBLESHOOTING.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [D-12, D-13, WIN-5]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The ~8s window between flush-disconnect and the new /stream client attaching is measured with timestamps at every hop: pa_stream_flush → flush-disconnect → LMS playlist-play dispatch → first new GET /stream → first ring drain (D-12)"
    - "Either the gap is fixed (audible skip-to-audio well under 8s, target <=3s) OR the root cause + best-effort mitigation + workaround are documented as a Known Issue and WINDOWS.md #5 is updated accordingly (D-13 soft blocker)"
  artifacts:
    - path: "Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c"
      provides: "timestamped reconnect instrumentation (client attach/detach, first drain after flush)"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "timestamped play-dispatch instrumentation on the skip path"
  key_links:
    - from: "Connect.pm skipInitiated playlist-play timestamp"
      to: "fake-libpulse first-GET timestamp"
      via: "correlated log timeline localizes which hop owns the 8 seconds"
---

<objective>
Debug and fix the ~8s audio reconnect gap after a Spotify-app skip in Soloist Connect mode (WINDOWS.md #5, quick task 260827-of9 residual): LMS metadata switches in ~1.16s, but the ring goes undrained for ~8.0s (read_index frozen, write_index climbing) between flush-disconnect and a new HTTP client attaching.

Purpose: D-12 orders a serious debug + fix attempt; D-13 makes it a soft blocker — if the root cause resists a reasonable effort, document a Known Issue with workaround and move on (v4.0 can live with it).

Output: instrumented timeline, fix (or documented known issue), updated WINDOWS.md #5.

Runs in Wave 3 so measurements happen against the final S32 shim (76-01) and the final Connect.pm skip/session logic (76-02/76-05) — measuring the shipping pipeline, not an intermediate one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md
@.planning/quick/260827-of9-soloist-skip-stream-reconnect-fake-libpu/260827-of9-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument every hop and capture a live timeline of the reconnect gap</name>
  <precondition>Dev LMS running locally with a paired Soloist daemon (spak.key present, backend=soloist) and Spotify Desktop available for CDP control (start it with --remote-debugging-port and --remote-allow-origins=* — a prior instance without the flag must be killed with kill -9, RESEARCH Pitfall 7).</precondition>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0, Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - .planning/quick/260827-of9-soloist-skip-stream-reconnect-fake-libpu/260827-of9-SUMMARY.md (full context: what QT-12 fixed, the measured 8.000s read_index freeze, the CDP environment notes)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Pitfall 1 — hypothesis A2: LMS-side stream-open latency vs stale-ring drain; the instrumentation plan)
    - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c (post-76-01 state: _http_thread_fn accept/close paths, g_flush_disconnect consumption, _ring_flush, the existing [fakepulse] trace lines)
    - Plugins/SpotOn/Connect.pm (post-76-05 state: the change-handler skipInitiated playlist-play dispatch site)
    - ~/.claude/skills/spotify-control/SKILL.md (CDP driving; note QT-12 fell back to an ad-hoc python websocket-client script because tools/spotify-cdp.js was absent — reuse that fallback)
  </read_first>
  <action>
    Instrument, deploy locally, and measure (D-12):

    1. fake-libpulse.c: add millisecond-timestamped trace lines (gated on the existing g_debug_trace) at: HTTP client accept (new GET /stream), client close (both normal and flush-disconnect), and the first _ring_pop after a flush-disconnect. Rebuild (`make -C Plugins/SpotOn/Bin/fake-libpulse all`).
    2. Connect.pm: add a millisecond-timestamped DIAG log line immediately around the skip path's `playlist play spoton://connect-` dispatch (diagnosticMode-gated, matching the existing [DIAG] idiom).
    3. Deploy to the local dev LMS per the established local-deploy convention (sudoers permits the deploy + service restart; Pi deploys are out of scope per project rules), enable diagnosticMode + shim trace.
    4. Reproduce: transfer playback to the dev player via CDP, trigger Skip Next in the Spotify app (the QT-12 procedure), and capture the correlated timeline from the daemon log + server.log: t0 pa_stream_flush → t1 flush-disconnect close → t2 LMS playlist-play dispatch → t3 new GET /stream accept → t4 first ring drain. Repeat 3 times for stability. Additionally verify with squeezelite debug output (the spoton-uat skill's log-based audio verification approach) when the player actually resumes audible output.
    5. Localize the gap: which hop owns the ~8s? Candidate mechanisms to check against the data: (a) LMS delays opening the new stream (old-song teardown/buffer drain before connect — inspect Slim::Player source around direct-stream open if t2→t3 is the gap), (b) the shim refuses/misses the reconnect (t3 arrives fast but t4 lags — ring/client state bug), (c) the play dispatch itself is late (t1→t2 gap — Perl-side ordering). Record the verdict with numbers in the SUMMARY.
  </action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 6</automated>
  </verify>
  <done>Three reproducible timelines captured with all five timestamps; the owning hop is identified with numbers; host tests still green.</done>
  <acceptance_criteria>
    - SUMMARY contains a timeline table (t0-t4, 3 runs) with the gap attributed to a specific hop
    - New trace lines are debug/diagnostic-gated (no unconditional log spam in production)
    - `make -C Plugins/SpotOn/Bin/fake-libpulse test` green; `prove -l t/` green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix the owning hop — or document the Known Issue (D-13 soft blocker)</name>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Connect.pm, TROUBLESHOOTING.md, .planning/WINDOWS.md</files>
  <read_first>
    - The Task 1 timeline verdict (this plan's working notes/SUMMARY draft)
    - TROUBLESHOOTING.md (existing Known Issue entry format)
    - .planning/WINDOWS.md (entry #5)
  </read_first>
  <action>
    Based on the Task 1 verdict, implement the smallest correct fix at the owning hop and re-measure (D-12):
    - If (a) LMS-side stream-open latency: pursue the SpotOn-reachable levers first — e.g. dispatch the playlist-play with flags/ordering that skip the old song's drain, or flush/stop the old LMS stream explicitly before the play dispatch so the new direct-stream connect is not queued behind teardown. Only conclude "LMS core, out of reach" with the specific core code path cited.
    - If (b) shim-side: fix the client/ring state machine so a reconnect is accepted and drains immediately after flush-disconnect; extend the host test with a reconnect-after-flush check.
    - If (c) Perl-side dispatch latency: reorder/unblock the dispatch path.

    Re-run the CDP skip scenario 3 times: success = audible-audio switch (squeezelite-log verified) in <=3s consistently.

    D-13 branch — only if the root cause resists this serious attempt: (1) add a Known Issue entry to TROUBLESHOOTING.md (symptom, measured timeline, root-cause status, workaround: e.g. skip lands after ~8s / pause-unpause recovers immediately if that proved true in testing — verify the workaround live before writing it, per the test-before-post rule); (2) annotate WINDOWS.md #5 with the findings (keep it open); (3) draft (do not post) a short English status note for the issue tracker in the SUMMARY. If FIXED instead: mark WINDOWS.md #5 fixed via `node $HOME/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 5` and remove/downgrade any now-obsolete instrumentation that would spam logs (keep gated trace lines that remain useful).
  </action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | awk '{exit ($1>=6)?0:1}'</automated>
    <human-check>Consolidated Phase 76 UAT re-check: Spotify-app Skip Next during a Soloist Connect session — audible audio switches within ~3s (if fixed) or matches the documented Known Issue behavior + workaround (if D-13 branch taken).</human-check>
  </verify>
  <done>Either the measured skip-to-audio time is <=3s across 3 runs (WINDOWS.md #5 fixed), or the D-13 documentation set (TROUBLESHOOTING + WINDOWS annotation + draft note) is complete; suite + host tests green.</done>
  <acceptance_criteria>
    - SUMMARY states the outcome explicitly as FIXED (with 3 post-fix measurements) or KNOWN ISSUE (with the D-13 documentation artifacts listed)
    - If fixed: WINDOWS.md #5 status is fixed; if not: WINDOWS.md #5 carries the new findings in its description/annotation
    - `prove -l t/` and the C host test fully green; rebuilt libpulse.so.0 committed if the .c changed
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `fake-libpulse.c`: g_debug_trace-gated reconnect instrumentation (accept/close/first-drain timestamps); possible reconnect-after-flush fix + host-test check
- `Connect.pm`: [DIAG]-gated timestamp at the skip-path play dispatch; possible dispatch-ordering fix
- `TROUBLESHOOTING.md`: Known Issue entry (only in the D-13 branch)
- `.planning/WINDOWS.md`: entry #5 resolved or annotated

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDP to Spotify Desktop (test rig only) | --remote-allow-origins=* opens the debug websocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-15 | Elevation | CDP --remote-allow-origins=* | medium | mitigate | Test-session-only flag on the dev box, never in production/docs as a default (RESEARCH Security table); instance killed after the session |
| T-76-16 | Info Disclosure | new trace lines | low | mitigate | Trace/DIAG-gated; log content is timestamps + indices, never credentials |
</threat_model>

<verification>
- C host test 6/6 green; `prove -l t/` green
- Three-run measured timeline before and (if fixed) after the change in the SUMMARY
</verification>

<success_criteria>
- The 8s gap is no longer an unexplained number: it is either gone (<=3s) or precisely attributed, mitigated best-effort, and documented per D-13
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-07-SUMMARY.md` when done.
</output>
