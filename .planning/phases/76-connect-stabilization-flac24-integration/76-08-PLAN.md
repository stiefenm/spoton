---
phase: 76-connect-stabilization-flac24-integration
plan: 08
type: execute
wave: 4
depends_on: ["76-04", "76-06", "76-07"]
files_modified:
  - Plugins/SpotOn/Settings/Player.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/player.html
  - .planning/ROADMAP.md
  - .planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md
  - CHANGELOG.md
autonomous: true
requirements: [D-03, D-07, D-09, D-10, D-11, D-14, GH-149, GH-150]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With backend=soloist the per-player Stream Format dropdown offers Auto/PCM/FLAC/MP3 and hides OGG; with backend=librespot all five options render (D-07)"
    - "A single consolidated UAT checklist exists covering BOTH backends in one pass: Phase 73 Windows 1-4, Phase 75 SpClient smoke, GH #149/#150 verification, the librespot regression matrix (Browse/Connect/format switching), and every Phase 76 fix (D-09/D-11/D-14)"
    - "ROADMAP Phase 76 description reflects reality: #149/#150 marked code-fixed, remaining items listed, spoton-helper patch status (public no-op, private patterns absent) documented (D-03)"
  artifacts:
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/player.html"
      provides: "backend-conditional OGG option (server-rendered TT conditional)"
    - path: ".planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md"
      provides: "consolidated D-11 live-UAT script, both backends"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] entries for all Phase 76 changes"
  key_links:
    - from: "Plugins/SpotOn/Settings/Player.pm handler"
      to: "player.html template conditional"
      via: "backend template param drives the OGG option visibility"
      pattern: "backend"
---

<objective>
Close the phase's integration surface: the D-07 format dropdown (OGG hidden under Soloist), the D-03 ROADMAP cleanup incl. spoton-helper patch-status documentation, and the D-09/D-10/D-11/D-14 consolidated UAT checklist plus CHANGELOG.

Purpose: D-10 gave the planner the UAT-timing call — decision: ONE live UAT pass at end-of-phase, after all code plans, covering both backends in a single run (matches D-11 exactly and avoids repeated environment setup). This plan produces the master checklist that /gsd-verify-work and the harvested per-plan human-checks execute against. It is named 76-UAT-CHECKLIST.md deliberately, so it does not collide with the verifier-generated 76-UAT.md.

Patch-wiring scope clarification (RESEARCH Pitfall 6, planner-adopted): the ROADMAP item "spoton-helper patch tatsaechlich verdrahten" is already implemented (74-04: _autoPatch wired into download activation, fail-open, t/33-covered). What Phase 76 owes is the honest status: public builds carry an empty pattern table (CI secrets SPOTON_PRIVATE_PATTERNS_TOKEN/REPO do not exist), so the FLAC24-ENUM effect is unverifiable and is excluded from Phase 76 success criteria (Phase 77 UAT concern). The fake-libpulse S32 chain delivers 24-bit container quality independently of the enum patch.

Output: dropdown conditional, updated ROADMAP, UAT checklist, CHANGELOG.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-CONTEXT.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md
@.planning/phases/73-soloist-connect-mode/73-VERIFICATION.md
@.planning/phases/76-connect-stabilization-flac24-integration/76-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 — backend-conditional Stream Format dropdown</name>
  <files>Plugins/SpotOn/Settings/Player.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/player.html</files>
  <read_first>
    - Plugins/SpotOn/Settings/Player.pm (full file — the saveSettings branch validates pref_streamFormat against auto|ogg|pcm|flac|mp3, and the param block passes streamFormat to the template)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/player.html (the dropdown at approx. lines 42-50)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-PATTERNS.md (section "player.html" — recommends the server-rendered TT conditional over JS, since player.html has no backend dropdown to live-toggle)
  </read_first>
  <action>
    Implement D-07:
    1. Settings/Player.pm handler: add `$paramRef->{backend} = $prefs->get('backend') || 'librespot';` to the template-param block (the global backend pref, PATTERNS Shared Pattern "Backend-Erkennung").
    2. player.html: wrap the `<option value="ogg" ...>` line in a server-side TT conditional so it renders only when backend is not soloist (OGG is librespot-exclusive raw passthrough, D-07). No JS needed — the page has no live backend toggle.
    3. Stored-pref edge: when backend=soloist and the stored streamFormat is 'ogg', the template must show Auto as selected (the 76-04 resolver already treats ogg as auto at runtime); implement by normalizing the streamFormat template param to 'auto' in Player.pm when backend is soloist and the stored value is 'ogg' (display-only — do not rewrite the stored pref, so switching back to librespot restores the user's OGG choice).
    4. Also update the desc string handling only if the existing PLUGIN_SPOTON_STREAM_FORMAT_DESC text names OGG as always available — read it first; if it needs a change, that change must be translated in all 11 languages (CS/DA/DE/EN/ES/FR/IT/NL/NO/PL/SV, real translations). If the existing text is backend-neutral, touch nothing (preferred).
  </action>
  <verify>
    <automated>prove -l t/09_settings.t t/05_perl_syntax.t t/02_strings.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <human-check>Consolidated Phase 76 UAT: open the player settings page with backend=soloist — dropdown shows Auto/PCM/FLAC/MP3 (no OGG); switch backend to librespot, reload — all five options render, a previously stored OGG pref is selected again.</human-check>
  </verify>
  <done>OGG option backend-conditional; ogg-pref display normalization in place without pref rewriting; suite green.</done>
  <acceptance_criteria>
    - `grep -c 'backend' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/player.html` >= 1 (TT conditional present around the ogg option)
    - Settings/Player.pm passes the backend param and normalizes the displayed streamFormat for the soloist+ogg case without a $prefs->set call in that path (source assertion)
    - `prove -l t/` fully green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-03 — ROADMAP cleanup + patch-status documentation + CHANGELOG</name>
  <files>.planning/ROADMAP.md, CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 76 entry under "## Active — v4.0 Soloist Integration" — the long four-cluster description written before QT 260817-ana)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Pitfall 6 — verified patch no-op status and the exact secret names)
    - Plugins/SpotOn/Soloist.pm (approx. lines 385-465 — _autoPatch fail-open wiring, to confirm the wiring claim before documenting it)
    - CHANGELOG.md ([Unreleased] section format)
  </read_first>
  <action>
    D-03 ROADMAP cleanup — EDIT the existing Phase 76 entry in .planning/ROADMAP.md scoped-only (never rewrite the file): (1) mark #149 (idle-guard) and #150 (audio-key timeout) as code-fixed in QT 260817-ana with live verification pending in the Phase 76 UAT; (2) restate the remaining item list to match what this phase's plans actually deliver (the 8 plans); (3) replace the "spoton-helper patch tatsaechlich verdrahten" wording with the verified status: wiring complete since 74-04 (_autoPatch, fail-open, t/33), public builds ship the empty pattern table because the CI secrets SPOTON_PRIVATE_PATTERNS_TOKEN / SPOTON_PRIVATE_PATTERNS_REPO are not configured, therefore the FLAC24-enum patch effect is explicitly OUT of Phase 76 success criteria and deferred to Phase 77 UAT once a private pattern source exists (RESEARCH Pitfall 6 / Open Question 4); the fake-libpulse S32 chain delivers 24-bit container quality independently. Confirm the wiring claim first via `prove -l t/33_soloist_patch.t` and a read of _autoPatch.

    CHANGELOG.md: add [Unreleased] entries summarizing the phase's user-visible changes (FLAC24 pipeline for Soloist incl. new soc flc/smp rules and what they mean for librespot users, #159 BUFFERING fix, #131 sync buffer, #128 handoff position, #151 power restore, restart-autoplay suppression, #158 fix, #161 playlist actions, #94 menu parity, #135 Up Next, Window-5/6 outcomes). English, user-facing wording, one line per item, referencing issue numbers. No version number is assigned here (release numbering is user-approved only, per project rules).
  </action>
  <verify>
    <automated>prove -l t/33_soloist_patch.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
  </verify>
  <done>ROADMAP Phase 76 entry reflects the delivered plan set and the honest patch status; CHANGELOG carries all phase entries; no version bump.</done>
  <acceptance_criteria>
    - `grep -c '260817-ana' .planning/ROADMAP.md` >= 1 (the #149/#150 code-fixed attribution)
    - `grep -c 'SPOTON_PRIVATE_PATTERNS' .planning/ROADMAP.md` >= 1 (patch-status documentation names the missing secrets)
    - CHANGELOG [Unreleased] mentions at minimum: FLAC, 159, 131, 151, 135, 161 (grep each)
    - The rest of ROADMAP.md outside the Phase 76 entry is untouched (`git diff --stat` shows only the expected files)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-09/D-11/D-14 — consolidated live-UAT checklist (both backends, one pass)</name>
  <files>.planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/73-soloist-connect-mode/73-VERIFICATION.md (the frontmatter human_verification block + the 6 numbered Human Verification scenarios — Windows 1-4 source material)
    - .planning/WINDOWS.md (current open entries and their post-phase-76 states)
    - .planning/phases/76-connect-stabilization-flac24-integration/76-RESEARCH.md (Validation Architecture — the phase-gate definition; Pitfall 7 — the CDP flag requirement; Environment Availability table)
    - tools/spclient-smoke.pl (the Phase 75 LMS-free smoke script — its stages define the SpClient smoke section)
    - The SUMMARYs of plans 76-01..76-07 (each plan's human-check items feed the checklist)
  </read_first>
  <action>
    Create `.planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md` — the D-11 master script for ONE manual UAT pass on the dev setup (LMS + squeezelite + Spotify app; D-09 manual, no automated rig). Structure it as an ordered run minimizing environment switches, with per-item: setup, action, expected result, evidence to capture. Sections:

    1. **Environment prep**: local deploy state (which binaries are dev-built: libpulse.so.0, librespot-spoton if 76-02 is to be live-tested, soloist paired), Spotify Desktop CDP flags (--remote-debugging-port + --remote-allow-origins=*, kill -9 for stale instances — Pitfall 7), diagnosticMode on.
    2. **Soloist backend — Phase 73 Windows 1-4** (adapted verbatim from 73-VERIFICATION.md's 6 human-verification scenarios): transfer via device picker (D-07/73), bidirectional control loop + WS-down Web-API fallback, build-expiry rc=10 parking (best-effort — only if forceable), Wave-0 spike observations (track-end/autoplay/queue-echo/takeover gap), live Browse + sync-group UAT (3-track advance, pause/unpause position, seek, mixed playlist, synced pair), Settings page visual render (soloist vs librespot).
    3. **Soloist backend — Phase 76 items**: FLAC24 chain (auto→FLAC on squeezelite, songinfo/squeezelite -d evidence of 24-bit; pcm→direct raw; mp3→lame pipeline), skip reconnect gap re-check (76-07 outcome), restart-autoplay suppression, power restore (#151), Up Next (#135).
    4. **SpClient smoke** (Phase 75): run tools/spclient-smoke.pl stages + in-LMS Browse/Search/Library spot checks over the spclient path (one item per family: track metadata, search, liked songs, playlists, recently played).
    5. **librespot backend regression matrix (D-14)**: switch backend=librespot; Browse playback, Connect transfer + control loop, format switching auto/ogg/pcm/flac/mp3 (note the NEW behavior: explicit flac now really transcodes via soc flc; verify no white noise/speed artifacts — RESEARCH Pitfall 2 signs), #149 idle-guard + #150 audio-key-timeout verification (AP-drop scenario per QT 260817-ana: no daemon restart mid-playback, playback survives an AP reconnect), #159 eject scenario, #131 sync-group stutter check, #128 handoff position check, #158 group pause-skip-play.
    6. **Result ledger**: pass/fail table mirroring WINDOWS.md entries 1-5 with the commands to close them (`gsd-tools windows fixed <id>`) once verified.

    Every item that came from a plan's `<verify><human-check>` must appear (cross-check plans 76-01..76-07). Keep the doc self-contained enough that /gsd-verify-work can walk it conversationally.
  </action>
  <verify>
    <automated>test -s .planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md && grep -c '## ' .planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md | awk '{exit ($1>=6)?0:1}'</automated>
    <human-check>The checklist itself is then executed as THE Phase 76 UAT session (D-09/D-11): one pass, both backends, results recorded in the ledger section and fed into /gsd-verify-work.</human-check>
  </verify>
  <done>Checklist exists with all 6 sections, covers Windows 1-4 + SpClient smoke + D-14 regression + every plan human-check + #149/#150; ready for the single UAT pass.</done>
  <acceptance_criteria>
    - The checklist file contains sections for: environment prep, Windows 1-4, Phase 76 soloist items, SpClient smoke, librespot regression (D-14), result ledger (grep section headings)
    - `grep -c '149\|150' .planning/phases/76-connect-stabilization-flac24-integration/76-UAT-CHECKLIST.md` >= 1 and `grep -c 'spclient-smoke' ...76-UAT-CHECKLIST.md` >= 1
    - Every `<human-check>` item from plans 76-01..76-07 maps to a checklist entry (list the mapping in the SUMMARY)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- `Settings/Player.pm`: `backend` template param + soloist/ogg display normalization
- `player.html`: TT-conditional OGG option
- `.planning/ROADMAP.md`: corrected Phase 76 entry (#149/#150 code-fixed, patch-status truth, delivered plan list)
- `76-UAT-CHECKLIST.md`: the D-11 consolidated live-UAT script (new phase artifact)
- `CHANGELOG.md`: [Unreleased] entries for all Phase 76 changes

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings web UI | template renders pref-derived values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-17 | Tampering | streamFormat save path | low | accept | Existing whitelist regex in Player.pm already constrains values; this plan only changes display |
| T-76-18 | Elevation | CDP flag in the checklist | low | mitigate | Checklist marks --remote-allow-origins=* as test-session-only with a teardown step |
</threat_model>

<verification>
- `prove -l t/` green (t/09, t/02, t/33 explicitly)
- Checklist completeness cross-check against all plan human-checks
- ROADMAP diff limited to the Phase 76 entry
</verification>

<success_criteria>
- D-07 dropdown correct per backend; D-03 documentation truthful; the single D-11 UAT pass is fully scripted and ready to execute after wave 3 completes
</success_criteria>

<output>
Create `.planning/phases/76-connect-stabilization-flac24-integration/76-08-SUMMARY.md` when done.
</output>
