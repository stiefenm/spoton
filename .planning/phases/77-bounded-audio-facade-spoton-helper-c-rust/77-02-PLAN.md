---
phase: 77-bounded-audio-facade-spoton-helper-c-rust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Unified/SoloistWS.pm
  - Plugins/SpotOn/Unified/SoloistDaemon.pm
  - t/31_soloist_ws.t
  - t/30_soloist_daemon.t
autonomous: true
requirements: [CR-S1, CR-S3]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Seek-drift detection behaves byte-for-byte identically in _onPositionSync and _onPlaybackState after the _detectSeek extraction (CR-S1, per D-01)"
    - "_onPositionSync still does NOT require sessionActive; _onPlaybackState still DOES — the guard divergence stays at the call sites, never inside the helper (PATTERNS-verified difference)"
    - "A slow-but-eventual HTTP port announcement does NOT stop the daemon; a failed WS port poll still DOES call stop() — CR-S3's divergent failure semantics survive the scaffold extraction (RESEARCH Pitfall 4)"
  artifacts:
    - "Plugins/SpotOn/Unified/SoloistWS.pm: sub _detectSeek, called from both _onPositionSync and _onPlaybackState"
    - "Plugins/SpotOn/Unified/SoloistDaemon.pm: sub _pollPortScaffold shared by _pollWsPort and _pollHttpPort"
    - "t/30_soloist_daemon.t: new test rows covering the previously-untested poll functions (genuine coverage gap per PATTERNS)"
  key_links:
    - "_detectSeek reads lastPositionMs/lastPositionTs/sessionPaused and emits 'seek' via $self->_emit — the soloistBrowseActive gate inside _emit must remain the sole suppression point"
    - "_pollPortScaffold owns only attempts-counter + procAlive + reschedule; success/failure bodies stay as distinct closures per caller"
---

<objective>
Wave-1 Perl pre-cleanup (D-01): CR-S1 (extract the duplicated seek-drift detection in SoloistWS.pm into `_detectSeek`) and CR-S3 (extract the shared polling scaffold from `_pollWsPort`/`_pollHttpPort` in SoloistDaemon.pm without unifying their divergent failure semantics).

Correction honored from PATTERNS (overriding 77-CONTEXT.md's canonical_refs literal text): the CR-S1 duplicated code lives in **SoloistWS.pm** (drift blocks at lines 805-810 and 873-878), NOT Connect.pm. The two call sites are NOT quite identical — `_onPlaybackState` guards on `sessionActive` (line 869), `_onPositionSync` does not — so the extraction must keep guards at the call sites.

CR-S3 has **zero existing test coverage** (t/30_soloist_daemon.t only tests `_spawnArgs`, lines 308-318) — this plan closes that gap with new rows before and alongside the refactor.

Purpose: clean foundation before Wave-2 hardening touches the same modules (D-01: "Clean foundation before hardening").
Output: refactored SoloistWS.pm + SoloistDaemon.pm with t/31 and t/30 green (t/31 grows equivalence rows; t/30 grows poll-function coverage).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-CONTEXT.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md
</context>

## Artifacts this phase produces

Full phase symbol list lives in 77-01-PLAN.md ("Artifacts this phase produces"). This plan creates:
- Perl sub `Plugins::SpotOn::Unified::SoloistWS::_detectSeek($self, $posMs, $now)` (CR-S1)
- Perl sub `Plugins::SpotOn::Unified::SoloistDaemon::_pollPortScaffold` (CR-S3, exact name at executor's discretion but MUST be a single shared private helper; record chosen name in SUMMARY)
- New t/31 rows: `_detectSeek` equivalence assertions for both call sites
- New t/30 rows: `_pollWsPort` failure→stop(), `_pollHttpPort` failure→no stop(), scaffold reschedule behavior

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-S1 — extract _detectSeek from the two duplicated drift blocks in SoloistWS.pm</name>
  <files>Plugins/SpotOn/Unified/SoloistWS.pm, t/31_soloist_ws.t</files>
  <read_first>
    - Plugins/SpotOn/Unified/SoloistWS.pm lines 40-45 (SEEK_THRESHOLD constant), lines 784-815 (_onPositionSync — first duplicate, drift block 805-810, guard: defined lastPositionMs && defined lastPositionTs, NO sessionActive check), lines 827-895 (_onPlaybackState — second duplicate, drift block 873-878, guard at 869: sessionActive && defined lastPositionMs && defined lastPositionTs)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md section "CR-S1 _detectSeek() extraction" (the exact 6-line duplicated pattern and the guard-divergence warning)
    - t/31_soloist_ws.t (full file — 143 existing tests; find the rows exercising position_sync and playback_state seek emission to model new equivalence rows on)
  </read_first>
  <behavior>
    - Test 1: a position_sync message whose position deviates from the extrapolated expectation by more than SEEK_THRESHOLD seconds still emits 'seek' with the sprintf('%.3f') formatted position — identical to pre-refactor behavior
    - Test 2: the same deviation arriving via playback_state with sessionActive set still emits 'seek'
    - Test 3: a playback_state drift with sessionActive FALSE does not emit (call-site guard preserved); a position_sync drift without sessionActive DOES emit (no guard added)
    - Test 4: sessionPaused true → elapsedMs contribution is 0 (frozen baseline preserved inside the helper)
  </behavior>
  <action>
    Extract the duplicated drift computation (the `$elapsedMs`/`$expectedMs`/`$deltaSec` block against `SEEK_THRESHOLD`, identical at lines 805-810 and 873-878) into a single private method `_detectSeek($self, $posMs, $now)` that computes drift from `$self->sessionPaused`, `$self->lastPositionTs`, `$self->lastPositionMs` and, when `$deltaSec > SEEK_THRESHOLD`, emits via `$self->_emit('seek', sprintf('%.3f', $posMs / 1000))` — exactly the current emission, no format or threshold change. Replace both inline blocks with a `_detectSeek` call. CRITICAL (PATTERNS-verified divergence): the surrounding guards stay at the call sites — `_onPositionSync` keeps `defined lastPositionMs && defined lastPositionTs` only; `_onPlaybackState` keeps its additional `$self->sessionActive` requirement (line 869). Do NOT move `sessionActive` into the helper. Preserve the existing explanatory comments about the frozen-while-paused baseline (move or reference them at the helper). Add the four `<behavior>` rows to t/31_soloist_ws.t following its existing mock/dispatch style; all 143 pre-existing rows must pass unmodified (this is a pure refactor per CR-S1 — any pre-existing t/31 assertion change means the refactor altered behavior and must be reworked).
  </action>
  <verify>
    <automated>prove -l t/31_soloist_ws.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit, or the final prove line lacks "Result: PASS" (any failed/died subtest)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '_detectSeek' Plugins/SpotOn/Unified/SoloistWS.pm` returns exactly 3 (one sub definition + two call sites)
    - t/31_soloist_ws.t passes with >= 147 tests (143 pre-existing, all unmodified, + >= 4 new equivalence rows)
    - Source assertion: `_onPlaybackState`'s drift entry still requires `sessionActive`; `_onPositionSync`'s does not (verified by the Test 3 behavior row, not by eyeball)
  </acceptance_criteria>
  <done>Both call sites delegate to one `_detectSeek`; t/31 green at >= 147 tests with zero pre-existing assertion edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-S3 — extract the shared poll scaffold in SoloistDaemon.pm, add first-ever poll coverage to t/30</name>
  <files>Plugins/SpotOn/Unified/SoloistDaemon.pm, t/30_soloist_daemon.t</files>
  <read_first>
    - Plugins/SpotOn/Unified/SoloistDaemon.pm lines 270-420 (_pollWsPort and _pollHttpPort in full — identify precisely: the shared scaffold (attempts counter increment, procAlive check, reschedule-if-alive-and-under-max), the WS-only mtime-vs-_spawnTime stale-file guard, the WS-only $self->stop() on failure, the HTTP-only tmpfile unlink on success, the HTTP-only fall-through-without-stop failure path and its WR-07 comment)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-RESEARCH.md "Pitfall 4" (the exact divergences a naive merge would destroy, and why unifying the failure branch would make a slow HTTP port announcement kill an idle daemon)
    - t/30_soloist_daemon.t (full file, 331 lines — existing stub infrastructure incl. its SimpleAsyncHTTP stub, and the _spawnArgs test rows at 308-318 as the only current coverage)
  </read_first>
  <behavior>
    - Test 1: _pollWsPort exhausting max attempts with the process alive calls $self->stop() (failure semantics preserved)
    - Test 2: _pollHttpPort exhausting max attempts does NOT call stop() and leaves the daemon runnable (WR-07 idle-daemon protection preserved)
    - Test 3: a port file appearing on attempt N (< max) resolves the poll successfully for both variants (scaffold reschedule works)
    - Test 4: _pollWsPort ignores a port file with mtime older than _spawnTime (stale-file guard stays WS-only)
  </behavior>
  <action>
    Per D-01 CR-S3 ("careful, different error handling") and RESEARCH Pitfall 4: extract ONLY the truly identical scaffold — attempts-counter increment, `procAlive` check, and the reschedule-if-alive-and-under-max-attempts timer loop — into one shared private helper (suggested name `_pollPortScaffold`; it takes the per-variant behavior as callback closures, e.g. a read-and-validate closure and an on-exhausted closure). Keep as distinct per-caller code: the WS mtime-vs-`_spawnTime` staleness guard, the WS `$self->stop()` on exhaustion, the HTTP tmpfile unlink on success, and the HTTP no-stop fall-through on exhaustion (preserve the WR-07 comment where it lives today). Do NOT unify the failure branches — that is the exact regression Pitfall 4 names.

    Write the four `<behavior>` test rows FIRST in t/30_soloist_daemon.t (this file has never covered these functions — genuine gap per PATTERNS "Open Items Resolved"), using its existing write_stub infrastructure to fake the port files, timers, and procAlive; confirm they pass against the CURRENT unrefactored code (they pin behavior), then perform the extraction and confirm they still pass. Existing _spawnArgs rows must pass unmodified.
  </action>
  <verify>
    <automated>prove -l t/30_soloist_daemon.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit, or the final prove line lacks "Result: PASS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SoloistDaemon.pm contains exactly one shared poll-scaffold helper sub, called by both `_pollWsPort` and `_pollHttpPort`
    - t/30_soloist_daemon.t contains >= 4 new rows exercising `_pollWsPort`/`_pollHttpPort` semantics (Tests 1-4 above) and passes
    - Behavior assertion (Test 2): an exhausted HTTP-port poll leaves the daemon un-stopped — the WR-07 protection demonstrably survives
    - Behavior assertion (Test 1): an exhausted WS-port poll calls stop()
  </acceptance_criteria>
  <done>Scaffold extracted with divergent semantics pinned by the first-ever poll tests; t/30 + t/05 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon port files (tmpdir) → SoloistDaemon.pm | Filesystem-announced ports read by the poll functions; local-filesystem trust, mtime-guarded (WS variant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-04 | Spoofing | port-file read in _pollWsPort | low | mitigate | Preserve (do not weaken) the existing mtime-vs-_spawnTime stale-file guard during the CR-S3 extraction — Task 2 Test 4 pins it |
| T-77-05 | Denial of Service | _pollHttpPort failure path | medium | mitigate | Task 2 Test 2 pins the no-stop() exhaustion semantics so a slow HTTP announcement cannot kill an otherwise-healthy daemon (the exact regression Pitfall 4 warns about) |
| T-77-SC | Tampering | package installs | low | accept | N/A — zero new packages this phase |
</threat_model>

<verification>
- `prove -l t/31_soloist_ws.t 2>&1 | tail -1 | grep -q 'Result: PASS'` (>= 147 tests)
- `prove -l t/30_soloist_daemon.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'`
- `prove -l t/32_soloist_events.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'` — SoloistWS.pm event-emission regression guard (Pitfall 2: the same-day soloistBrowseActive fix lives in _emit; the CR-S1 refactor routes through _emit and must not disturb the gate)
</verification>

<success_criteria>
- CR-S1 and CR-S3 applied as pure refactors: zero pre-existing test assertions modified, new coverage added where none existed
- The two PATTERNS-documented divergences (sessionActive guard placement; poll failure semantics) provably preserved by new test rows
</success_criteria>

<output>
Create `.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-02-SUMMARY.md` when done. Record the chosen scaffold-helper name and final t/30/t/31 test counts.
</output>
