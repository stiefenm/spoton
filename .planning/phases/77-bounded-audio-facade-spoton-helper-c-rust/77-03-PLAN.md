---
phase: 77-bounded-audio-facade-spoton-helper-c-rust
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/SpClient.pm
  - t/36_spclient.t
autonomous: true
requirements: [CR-S2, CR-S4]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getAlbumTracks and getShowEpisodes paginate via the canonical _sliceAsPage helper with byte-identical results — including the pre-enrichment slicing order t/36's 'exactly limit-many enrichment calls' assertion depends on (CR-S4)"
    - "All 16 _hasLogin5Creds guard sites still early-return and delegate to Client.pm with unchanged arguments; the 15 uniform sites delegate via the wrapper, the compound guard (~:1180) stays as-is as a documented exception; the early return stays in the caller's scope (CR-S2, counts corrected per 77-REVIEWS.md)"
  artifacts:
    - "Plugins/SpotOn/API/SpClient.pm: getAlbumTracks and getShowEpisodes call _sliceAsPage (inline duplicates removed)"
    - "Plugins/SpotOn/API/SpClient.pm: sub _delegateToClient absorbing the 15 uniform log/require/delegate stanzas (16 guard sites total; the :1180 compound guard is a documented exception)"
  key_links:
    - "_sliceAsPage must slice BEFORE _enrichTracks is called (t/36 line ~881 asserts exactly limit-many enrichment calls)"
    - "call sites become: return $class->_delegateToClient(<method>, <original args>) unless $class->_hasLogin5Creds($accountId); — the unless/return shape stays at each site"
---

<objective>
Wave-1 Perl pre-cleanup (D-01) in SpClient.pm: CR-S4 (converge the two remaining inline pagination duplicates onto the existing `_sliceAsPage` helper) and CR-S2 (resolve the "evaluate wrapper" decision affirmatively with a `_delegateToClient` helper that absorbs the uniform log/require/delegate stanza while keeping each site's early return in the caller).

CR-S2 counts, corrected per 77-REVIEWS.md (the earlier "17 sites" figure was wrong): `grep -n '_hasLogin5Creds' SpClient.pm` yields 18 hits = 1 doc comment (:209) + 1 `sub` definition (:212) + **16 `unless` guards** (:476, 699, 779, 844, 925, 992, 1051, 1103, 1180, 1485, 1540, 1622, 1769, 1821, 2106, 2190). Of those 16, line :1180 is a NON-uniform compound guard — `unless ($type eq 'track' && $class->_hasLogin5Creds($accountId))` with a `$fallbackToClient` closure body, not the 5-line stanza — so the wrapper target is the **15 uniform sites**; :1180 stays unconverted as a documented exception.

CR-S2 discretion note (per D-01, "evaluate wrapper, low priority"): the evaluation outcome is IMPLEMENT, because the one obstacle CONTEXT names ("jede Methode braucht den early-return") is fully answered by keeping `return ... unless _hasLogin5Creds(...)` at the call site and moving only the 4-line body into the helper — Perl's dynamic method dispatch (`Plugins::SpotOn::API::Client->$method(@args)`) makes the body generic. The reviewer flagged CR-S2 as the phase's least valuable work and offered dropping it; the deliberate decision is to KEEP it: D-01 locks all five CR items into this phase's Wave 0, t/36's Client.pm stub recorders pin delegation behavior so the mechanical risk is fenced, and the corrected counts remove the phantom-site chase the reviewer warned about. If during execution any FURTHER call site turns out not to match the uniform 5-line shape PATTERNS documents (guard + INFOLOG + require + delegate-with-same-args + return), leave that site unconverted too and record it in the SUMMARY rather than forcing it.

Purpose: clean foundation before hardening (D-01); zero behavior change, existing t/36 assertions are the contract.
Output: SpClient.pm with converged pagination + delegation; t/36 green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-CONTEXT.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md
</context>

## Artifacts this phase produces

Full phase symbol list lives in 77-01-PLAN.md ("Artifacts this phase produces"). This plan creates:
- `_sliceAsPage` adoption in `getAlbumTracks` (SpClient.pm ~813-819) and `getShowEpisodes` (~1045+) — CR-S4
- Perl sub `Plugins::SpotOn::API::SpClient::_delegateToClient` — CR-S2

## Review incorporation (77-REVIEWS.md)

- **MEDIUM (CR-S2 numbers wrong + mild scope creep):** INCORPORATED — counts corrected to the reviewer-verified reality: 16 guard sites (not 17), of which 15 are uniform and :1180 is a compound guard the wrapper cannot absorb (now a named, mandatory exception rather than a discovered one). The reviewer's alternative — dropping CR-S2 entirely — was REJECTED deliberately: D-01 locks all five CR items into this phase, t/36's stub recorders fence the mechanical risk, and the corrected numbers remove the phantom-site chase that made the original acceptance criterion unattainable. Rationale recorded in the objective.

<tasks>

<task type="auto">
  <name>Task 1: CR-S4 — replace the two inline slice duplicates with _sliceAsPage, preserving pre-enrichment slicing order</name>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm lines 1404-1420 (_sliceAsPage — the canonical helper and its ($sliceRef, $total) return contract), lines 799-833 (getAlbumTracks — inline duplicate at 813-819 slicing @gids to @sliceGids BEFORE hexToId mapping and enrichment), lines 1040-1110 (getShowEpisodes — the structurally identical inline duplicate)
    - t/36_spclient.t lines 860-910 (the getAlbumTracks 'lazy slice -- exactly limit-many (2 of 3) enrichment calls' assertion at ~881 and 'D-09 cache reuse' rows — these MUST survive unmodified) and lines 1030-1080 (getShowEpisodes slice + per-episode enrichment coverage at ~1034)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md section "CR-S4 getAlbumTracks → _sliceAsPage" (exact replacement text)
  </read_first>
  <action>
    In `getAlbumTracks`, replace the inline block at lines 813-819 (`$total`/`$end` computation + `@sliceGids` array-slice expression) with a `_sliceAsPage(\@gids, $offset, $limit)` call returning `($sliceGids, $total)`, then keep the existing `map { $class->hexToId($_) } @$sliceGids` for `@sliceIds` — the slice MUST still happen before `_enrichTracks` is invoked so exactly limit-many enrichment calls occur (t/36 ~881 pins this). Apply the same substitution to the structurally identical block in `getShowEpisodes` (~1045+), preserving its pre-enrichment slicing order likewise. Both functions' downstream use of `$total` (paging metadata in the callback payload) must consume `_sliceAsPage`'s second return value unchanged. No t/36 assertion may be modified — if any existing row goes red, the substitution is wrong, not the test.
  </action>
  <verify>
    <automated>prove -l t/36_spclient.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit, or the final prove line lacks "Result: PASS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '_sliceAsPage' Plugins/SpotOn/API/SpClient.pm` shows call sites inside both getAlbumTracks and getShowEpisodes (in addition to the pre-existing callers)
    - The manual `$end = $total - 1 if $end > $total - 1` idiom no longer appears inside getAlbumTracks or getShowEpisodes (it lives only in _sliceAsPage)
    - t/36_spclient.t passes with zero modified pre-existing assertions, including the exactly-limit-many enrichment-call row (~line 881)
  </acceptance_criteria>
  <done>Both facades paginate via _sliceAsPage; t/36 green unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: CR-S2 — _delegateToClient wrapper absorbing the 15 uniform guard stanzas (of 16 total)</name>
  <files>Plugins/SpotOn/API/SpClient.pm, t/36_spclient.t</files>
  <read_first>
    - Plugins/SpotOn/API/SpClient.pm lines 205-225 (_hasLogin5Creds doc comment at :209 and sub at :212) and the 16 guard call sites at :476, 699, 779, 844, 925, 992, 1051, 1103, 1180, 1485, 1540, 1622, 1769, 1821, 2106, 2190 (77-REVIEWS.md-verified; the PATTERNS anchor list omitted :476 — re-locate each by grepping the file for the guard call, since Task 1's edits shift line numbers)
    - Plugins/SpotOn/API/SpClient.pm ~:1176-1186 — the compound guard `unless ($type eq 'track' && $class->_hasLogin5Creds($accountId))` whose body calls a `$fallbackToClient` closure: this site is NOT the uniform stanza and stays unconverted (documented exception)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md section "CR-S2 _hasLogin5Creds guard" (the uniform 5-line stanza shape: guard + INFOLOG with method name and D-06 tag + require Client.pm + delegate-with-same-args + return)
    - t/36_spclient.t lines 300-500 (the Client.pm stub with per-method call recorders like @getShowEpisodes_calls — delegation behavior is already observable through these)
  </read_first>
  <action>
    Add `sub _delegateToClient { my ($class, $method, @args) = @_; ... }` to SpClient.pm: emit the existing INFOLOG line shape (interpolating $method, keeping the '(D-06)' tag so log-greppability is preserved), `require Plugins::SpotOn::API::Client;`, then `return Plugins::SpotOn::API::Client->$method(@args);` via dynamic method dispatch. Convert each of the 15 UNIFORM guard sites from the 5-line stanza to the single-line form `return $class->_delegateToClient('<methodName>', <exact original delegate args>) unless $class->_hasLogin5Creds($accountId);` — argument lists copied verbatim per site (they differ per method; a transposition here is the main defect risk, so convert one site at a time and re-run t/36 after every few). The early return remains in the caller (the CONTEXT-flagged constraint). The compound guard at ~:1180 (`unless ($type eq 'track' && ...)` with its `$fallbackToClient` closure) is a KNOWN exception — leave it entirely untouched and note it in the SUMMARY; do not attempt to absorb it. Any further site deviating from the uniform shape stays unconverted too and gets recorded in the SUMMARY. Do not alter _hasLogin5Creds itself.
  </action>
  <verify>
    <automated>prove -l t/36_spclient.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit, or the final prove line lacks "Result: PASS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SpClient.pm contains sub _delegateToClient exactly once; `grep -c '_delegateToClient' Plugins/SpotOn/API/SpClient.pm` returns 16 (1 definition + 15 uniform call sites) — or definition + N call sites with (15 - N) further documented exceptions in the SUMMARY
    - `grep -c '_hasLogin5Creds(' Plugins/SpotOn/API/SpClient.pm` still returns 17 (1 doc comment at :209 + 16 guard call sites; the `sub` definition line carries no paren) — guards themselves untouched, only their bodies moved
    - The compound guard at ~:1180 is byte-identical to before this task (source inspection)
    - t/36_spclient.t passes: every delegation-path row (Client.pm stub recorders) still records the same method + argument shapes
  </acceptance_criteria>
  <done>Wrapper live at all 15 uniform sites, delegation behavior pinned green by t/36, the :1180 compound guard and any further exceptions documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spotify Web API → SpClient.pm | Untrusted remote payloads; this plan changes only local slicing/delegation plumbing, no new parse surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-06 | Elevation of Privilege | _delegateToClient dynamic dispatch | low | mitigate | $method is always a hardcoded string literal at each call site (never derived from input); the action mandates literal method names — no caller-controlled dispatch |
| T-77-07 | Information Disclosure | INFOLOG in the wrapper | low | accept | Same log content as the 15 absorbed stanzas (method name + D-06 tag); no credentials or tokens logged |
| T-77-SC | Tampering | package installs | low | accept | N/A — zero new packages this phase |
</threat_model>

<verification>
- `prove -l t/36_spclient.t 2>&1 | tail -1 | grep -q 'Result: PASS'`
- `prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'`
</verification>

<success_criteria>
- CR-S4: two inline duplicates gone, pagination behavior byte-identical (t/36 unmodified and green)
- CR-S2: evaluation resolved with a working wrapper (or documented per-site exceptions), delegation behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-03-SUMMARY.md` when done. Record: number of guard sites converted (target 15 uniform of 16 total; the :1180 compound guard is a known exception), any further exceptions and why.
</output>
