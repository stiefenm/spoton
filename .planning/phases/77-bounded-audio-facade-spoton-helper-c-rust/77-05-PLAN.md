---
phase: 77-bounded-audio-facade-spoton-helper-c-rust
plan: 05
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c
  - Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-12]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Rapid-skip: when two POST /boundary markers are planted back-to-back with no intervening drain, only the LAST watermark applies (D-04 last-wins — behavior already implemented, now pinned by a test)"
    - "A fresh client taking over DURING the armed window (arm landed, flush not yet) receives zero pre-flush bytes — only post-flush audio (D-05/D-02 'no pre-seek audio', the Pitfall 3 gap a lifecycle-only test would miss)"
    - "Two arms followed by two flushes keep the client attached through BOTH flushes (counter semantics validated — RESEARCH Open Question 1 characterized before live UAT)"
    - "Unarmed flush still disconnects (D-12) and a reconnecting client drains immediately (D-06) — pre-existing tests stay green untouched"
  artifacts:
    - "Three new host-test cases in fake-libpulse.c main(): 'ok: rapid-skip last boundary wins', 'ok: takeover during armed window serves no pre-flush bytes', 'ok: double seek-arm double flush keeps client attached'"
    - "Host-test suite prints 14 'ok:' lines (11 after Plan 77-01 + 3 here)"
    - "Rebuilt Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0 committed alongside any source change"
  key_links:
    - "All three cases drive state through real pa_stream_write/pa_stream_flush calls and loopback HTTP clients (harness convention) — no global-poking"
    - "The takeover test exercises the Plan 77-01 drain-loop write gate against the existing GET /stream takeover logic (fake-libpulse.c:858-905)"
---

<objective>
Wave-2 C hardening plan (D-07: "C host-tests for all new edge cases"): pin the rapid-skip last-wins behavior (D-04 — no C code change expected, only a missing test), prove the armed-window takeover path serves no stale bytes (the D-05 mismatch scenario, which is C-identical to seek: a new client attaches while an arm is pending), and characterize double-arm/double-flush (RESEARCH Open Question 1) before live UAT depends on it.

Expected outcome per RESEARCH: D-04 and D-06 require zero production-code changes (Pattern 2: last-wins is the existing overwrite behavior; D-06's disconnect path is already tested). If a test exposes a real defect (e.g. the drain gate leaks a chunk to a taking-over client, or the second flush of a double-seek disconnects), FIX it in fake-libpulse.c within this plan — that is the point of testing before UAT — and record the fix in the SUMMARY. The known ~46ms mid-chunk boundary overshoot is an ACCEPTED pre-existing limitation (RESEARCH Pattern 2) — do not attempt to shrink it here.

Purpose: every deliberate-flush edge case is machine-verified before the phase's single human checkpoint (Plan 77-06).
Output: host-test suite at 14 `ok:` lines; rebuilt .so.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-CONTEXT.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-RESEARCH.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-PATTERNS.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-01-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase symbol list lives in 77-01-PLAN.md ("Artifacts this phase produces"). This plan creates the three Wave-2 host-test cases named in must_haves (and any defect fixes they force).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-04 rapid-skip last-wins host test</name>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0</files>
  <precondition>Plan 77-01 committed: `make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:'` currently prints 11</precondition>
  <read_first>
    - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c lines 829-856 (POST /boundary plant — note the unconditional overwrite of g_boundary_at_pushed, which IS the last-wins mechanism), lines 940-1006 (boundary consumption in the drain loop, incl. the documented mid-chunk overshoot comment at ~940-955), lines 2855-2899 (the existing boundary-EOF test to model on, incl. its byte-exact close assertion)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-RESEARCH.md "Pattern 2" (why D-04 needs only tests: 'no C code change needed for D-04, only new host tests exercising back-to-back POST /boundary calls with no intervening drain')
  </read_first>
  <behavior>
    - Test: attach a client, push tracks' worth of known pattern bytes, send POST /boundary (watermark W1), push more bytes, send a second POST /boundary (watermark W2 > W1) BEFORE the drain crosses W1, then drain: the client's connection must close at W2's byte count, not W1's (assert total received bytes correspond to W2, via byte counting against total_pushed at the second plant)
  </behavior>
  <action>
    Add host-test case `ok: rapid-skip last boundary wins` implementing the behavior row, using `_test_connect_loopback` for both the streaming client and the two control POSTs, real `pa_stream_write` pushes, and an exact received-byte-count assertion (the D-04 contract is positional: the SECOND marker's watermark governs the EOF). Production code is expected to already satisfy this (overwrite semantics at the plant site); if the test fails, fix the plant/consume logic minimally and document why in the SUMMARY. Rebuild libpulse.so.0 via `make -C Plugins/SpotOn/Bin/fake-libpulse all` if any non-test source changed.
  </action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 12</automated>
    <fails_when>non-zero exit from the pipeline, an ok-count other than 12, or any "FAIL:" line in the make output</fails_when>
  </verify>
  <acceptance_criteria>
    - Output contains the exact line `ok: rapid-skip last boundary wins`
    - The test asserts on a received-byte COUNT tied to the second watermark (source inspection), not just "connection closed"
    - All 11 prior `ok:` lines still print
  </acceptance_criteria>
  <done>Last-wins pinned by a byte-count host test; suite at 12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Armed-window takeover (D-05) + double-arm/double-flush (Open Question 1) host tests</name>
  <files>Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c, Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0</files>
  <read_first>
    - Plugins/SpotOn/Bin/fake-libpulse/fake-libpulse.c lines 858-905 (GET /stream takeover: old client_fd closed, pending promoted — the path the armed-window test collides with), the Plan 77-01 additions (seek-arm handler, drain-loop gate, pa_stream_flush conditional — locate by grepping for the arm flag symbol), lines 738-773 (flush-disconnect consumption)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-RESEARCH.md "Pitfall 3" (why the takeover test MUST assert byte content: a lifecycle-only assertion passes even when stale bytes leak) and "Open Questions" #1 (the double-seek scenario to characterize)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-01-SUMMARY.md (final arm-counter semantics as actually implemented)
  </read_first>
  <behavior>
    - Test A (takeover mid-armed-window, D-05/D-02): client 1 attached and draining pattern P1; POST /seek-arm lands; a NEW client 2 connects with a fresh GET (takeover closes client 1); push more P1 bytes (pre-flush) — client 2 must receive NONE of them (drain gate holds through takeover); pa_stream_flush fires (armed — client 2 stays attached); push pattern P2 — client 2's first received bytes memcmp-equal P2 with zero P1 interleaved
    - Test B (double arm + double flush): two POST /seek-arm requests land back-to-back, then pa_stream_flush fires twice in succession; the attached client must survive BOTH flushes (no EOF), and audio pushed after the second flush must reach it; a THIRD, un-armed flush afterwards must disconnect (counter fully consumed, D-12 restored)
  </behavior>
  <action>
    Add host-test cases `ok: takeover during armed window serves no pre-flush bytes` (Test A) and `ok: double seek-arm double flush keeps client attached` (Test B), following the harness conventions (real API calls, loopback sockets, memcmp on content for Test A, EOF/no-EOF socket observations plus a post-second-flush content read for Test B). Test B's third-flush disconnect assertion is the guard proving the counter cannot leak into permanently suppressing D-12 skip semantics. If either test exposes a defect in the Plan 77-01 mechanism (e.g. the takeover promotes a client while armed and hands it a stale chunk before the gate check runs, or the counter mis-consumes), fix fake-libpulse.c minimally within this task — the arm/gate design allows the fix without protocol changes (the endpoint and counter contract must NOT change, since Plan 77-04's Perl dispatch already consumes them). Rebuild libpulse.so.0 and include it in the commit if any non-test source changed.
  </action>
  <verify>
    <automated>make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 14</automated>
    <fails_when>non-zero exit from the pipeline, an ok-count other than 14, or any "FAIL:" line in the make output</fails_when>
  </verify>
  <acceptance_criteria>
    - Output contains the exact lines `ok: takeover during armed window serves no pre-flush bytes` and `ok: double seek-arm double flush keeps client attached`
    - Test A asserts served byte content via memcmp against the post-flush pattern (source inspection — Pitfall 3 requirement)
    - Test B asserts the third (un-armed) flush disconnects — D-12 behavior restored after counter drain
    - Suite prints 14 `ok:` lines total; the pre-existing `ok: reconnect after flush-disconnect attaches and drains immediately` (D-06) among them
  </acceptance_criteria>
  <done>The two remaining deliberate-flush edge cases machine-verified; suite at 14; any mechanism fixes documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN → fake-libpulse HTTP port | Same accepted wildcard-bind posture as Plan 77-01; this plan adds test coverage, no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-11 | Denial of Service | armed-window drain gate | medium | mitigate | Test B's third-flush assertion proves arm-counter exhaustion restores disconnect semantics — a wound-up counter cannot permanently suppress D-12 (complements 77-01's saturation cap, T-77-03) |
| T-77-12 | Tampering | boundary overwrite (D-04) | low | accept | Last-wins overwrite by any LAN host is the same accepted /boundary posture (T-77-01 class); test pins intended semantics only |
| T-77-SC | Tampering | package installs | low | accept | N/A — zero new packages this phase |
</threat_model>

<verification>
- `make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 14`
- `prove -l t/*.t 2>&1 | tail -1 | grep -q 'Result: PASS'` — full-suite wave gate (no Perl edits expected here; guards accidental cross-module damage)
</verification>

<success_criteria>
- D-04/D-05/D-06 each covered by at least one green, content-asserting host test (D-07's host-test mandate for every new edge case)
- Open Question 1 characterized with a definitive automated answer before the live-UAT checkpoint
- No change to the seek-arm endpoint/counter contract Plan 77-04 consumes
</success_criteria>

<output>
Create `.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-05-SUMMARY.md` when done. Record: whether D-04 needed any production-code fix (expected: no), and the double-flush characterization result (feeds the 77-06 UAT briefing).
</output>
