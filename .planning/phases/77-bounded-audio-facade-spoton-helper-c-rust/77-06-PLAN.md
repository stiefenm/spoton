---
phase: 77-bounded-audio-facade-spoton-helper-c-rust
plan: 06
type: execute
wave: 3
depends_on: [77-04, 77-05]
files_modified:
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/Bin/fake-libpulse/libpulse.so.0
autonomous: false

requirements: [D-07, D-08, D-09]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "78-UAT test 10 (Browse seek) passes live: seek mid-track repositions audio and playback CONTINUES on the same HTTP connection — no skip-to-next (D-02/D-03, the phase's headline fix)"
    - "Killing the daemon mid-stream produces a clean EOF at squeezelite and LMS's normal retry takes over — no special recovery code needed (D-08, manual-only by design)"
    - "CI rebuild green on all 3 architectures (x86_64, aarch64, armv7) with each matrix job's binary verification step passing (D-09 — prerequisite for Phase 78 live testing)"
    - "The superseded debounced browse-seek code is deleted only AFTER the live UAT confirms the new path (RESEARCH 'State of the Art — Superseded' sequencing)"
  artifacts:
    - "Connect.pm without the superseded browse-seek sub (post-UAT deletion)"
    - "A green build-fake-libpulse.yml workflow run (3 matrix jobs) recorded by run URL in the SUMMARY"
  key_links:
    - "UAT validates RESEARCH assumptions A2 (seek-arm design vs the real closed-source Soloist flush sequencing) and A3 (getNextTrack runs on LMS seek-restart) — the two things host tests cannot prove"
---

<objective>
Wave-3 gate plan: the phase's single human checkpoint (D-07 live UAT with real audio — explicitly no fuzzing/stress), the post-UAT deletion of the superseded Connect.pm browse-seek path, and the D-09 CI binary rebuild for all 3 architectures.

This is where RESEARCH assumptions A2/A3/A4 meet reality: host tests (14 green after Wave 2) prove the C mechanism against simulated flushes, but only the real closed-source Soloist binary can prove its actual seek/flush call sequencing, and only live LMS can prove the seek-restart invokes getNextTrack. If UAT fails on A3, the documented fallback is relocating the arm+seek dispatch to canDirectStreamSong (see 77-04-PLAN objective) — that would be a gap-closure follow-up (`/gsd-plan-phase 77 --gaps`), not an improvised edit.

Deliberate exclusion (visible, not dropped): 78-UAT tests 11/15 (Connect transfer resume with position `0.000` as trackId) are NOT re-tested here — they are a Phase 78 Perl resume-parameter bug outside D-01..D-09, deferred to Phase 78 gap closure. Only test 10 and this phase's new edge cases are in scope.

Purpose: human-confirm the headline fix, then finish the phase cleanly (dead code removed, binaries rebuilt).
Output: UAT verdict recorded, Connect.pm cleaned, 3-arch CI artifacts green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-CONTEXT.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-04-SUMMARY.md
@.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-05-SUMMARY.md
@.planning/phases/78-browse-connect-reintegration-perl/78-UAT.md
</context>

## Artifacts this phase produces

Full phase symbol list lives in 77-01-PLAN.md ("Artifacts this phase produces"). This plan produces: the live-UAT verdict for test 10 + D-08, the deletion of the superseded browse-seek sub, and the dispatched 3-arch CI run.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live UAT — Browse seek, rapid-skip, daemon-crash EOF (D-07 + D-08)</name>
  <what-built>
    Phase 77 hardening is code-complete: the C layer suppresses flush-disconnect for armed (seek/mismatch) flushes and withholds pre-flush bytes (14/14 host tests), and ProtocolHandler dispatches arm-then-seek/arm-then-play deterministically from getNextTrack (t/29 pins ordering). The local x86_64 `libpulse.so.0` in Plugins/SpotOn/Bin/fake-libpulse/ was rebuilt from the changed source in the Wave 1/2 commits. What no machine test can verify: the real Soloist binary's flush sequencing (RESEARCH A2), LMS's seek-restart actually invoking getNextTrack (A3), and real-audio continuity.
  </what-built>
  <how-to-verify>
    Deploy the current tree to the live LMS instance (the `spoton-uat` project skill automates deployment, playback driving, and squeezelite-debug-log audio verification — use it if the LMS test environment is reachable, otherwise verify manually):
    1. **Browse seek (78-UAT test 10 — the headline):** Play an album track via SpotOn Browse; seek to mid-track in the LMS UI. EXPECTED: audio resumes at the new position within ~1s and the track KEEPS PLAYING to its natural end (watch server.log for the arm POST + WS seek; a skip-to-next-track = FAIL, the pre-phase symptom).
    2. **Repeated seek:** Seek twice in quick succession (drag the bar twice). EXPECTED: lands at the second position, no skip, no stall (validates the arm counter live — host Test B analog).
    3. **Rapid-skip (D-04):** Skip forward across 3+ tracks quickly. EXPECTED: playback lands on the last-selected track with no stall and no stale audio from skipped tracks.
    4. **Daemon crash (D-08, manual-only per VALIDATION):** `kill -9` the Soloist daemon process mid-stream. EXPECTED: squeezelite sees a clean EOF (playback stops, no hang, no white noise); starting playback again respawns the daemon and works.
    5. **Regression:** One natural track-advance at EOF still works (Browse auto-advance, 78-UAT test 9 behavior).
  </how-to-verify>
  <resume-signal>Reply with pass/fail per numbered item (1-5). On any FAIL: include the server.log excerpt around the failure — execution stops and the failure routes to gap closure, Tasks 2/3 do not run.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post-UAT deletion of the superseded browse-seek path in Connect.pm</name>
  <files>Plugins/SpotOn/Connect.pm</files>
  <precondition>Task 1's checkpoint passed items 1-5 (the new seek path is live-confirmed; the rollback insurance is no longer needed)</precondition>
  <read_first>
    - Plugins/SpotOn/Connect.pm (the region formerly holding _onSeek's browse branch — now a pointer comment from Plan 77-04 — plus the uncalled sub at ~855-871 and any SEEK_DEBOUNCE/_soloistBrowseWs usages, to determine what is now dead vs still shared)
    - .planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-04-SUMMARY.md (what Task 2 of Plan 04 left in place and why)
  </read_first>
  <action>
    <!-- planner-discipline-allow: _bufferedBrowseSeek -->
    Delete the now-uncalled `sub _bufferedBrowseSeek` from Connect.pm in full (sub body plus its leading doc-comment block). Before deleting, confirm via grep that its only remaining references are its own definition/comment; do NOT delete `_soloistBrowseWs` or `SEEK_DEBOUNCE` if any other caller remains (e.g. `_onPause` browse forwarding uses `_soloistBrowseWs`; `_bufferedSeek` uses SEEK_DEBOUNCE) — verify each symbol's remaining callers individually and remove only what became dead. Leave no comments naming the deleted sub behind; the Plan-77-04 pointer comment at the _onSeek removal site should reference ProtocolHandler::getNextTrack ownership without naming the deleted sub.
  </action>
  <verify>
    <automated>prove -l t/29_soloist_browse.t t/32_soloist_events.t t/37_connect_lifecycle.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit, or the final prove line lacks "Result: PASS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '_bufferedBrowseSeek' Plugins/SpotOn/Connect.pm` returns 0
    - `grep -rc '_bufferedBrowseSeek' Plugins/SpotOn/ | grep -v ':0'` returns nothing (no references anywhere in the plugin tree)
    - Connect-mode seek path (`_bufferedSeek`) still present and unchanged
    - Targeted suites + t/05 green; then full `prove -l t/*.t` green
  </acceptance_criteria>
  <done>Superseded path fully removed with shared helpers preserved; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: D-09 — dispatch and verify the 3-architecture CI binary rebuild</name>
  <files>(no repo files — CI dispatch and verification)</files>
  <precondition>`gh auth status` exits 0 with access to this repository (gh CLI authenticated); fallback if not: manual workflow_dispatch via the GitHub Actions UI, with the run URL pasted back</precondition>
  <read_first>
    - .github/workflows/build-fake-libpulse.yml (workflow_dispatch trigger at line 20; 3-job matrix "Build ${{ matrix.bin_dir }}"; the "Verify binary (glibc, not musl -- Pitfall 3)" step at ~line 80 whose per-job success is the D-09 acceptance signal; native-only host-test step at ~line 68)
  </read_first>
  <action>
    Dispatch the rebuild: `gh workflow run build-fake-libpulse.yml` on the current branch, then poll to completion with `gh run watch` (or `gh run list --workflow=build-fake-libpulse.yml --limit 1` + `gh run view <id>`). Confirm all 3 matrix jobs (x86_64, aarch64, armv7 — the bin_dir matrix) succeed, specifically that each job's "Verify binary" step passed (glibc NEEDED-entry check) and the native job's host-test step ran the 14-case suite. `build-librespot.yml` is NOT dispatched — this phase changed no Rust source (RESEARCH Environment Availability: librespot-spoton untouched), so only fake-libpulse binaries need rebuilding for Phase 78 live testing. If any cross-compile job fails on the C11 stdatomic change (RESEARCH assumption A1), apply the Pitfall 5 remedy — add `-std=gnu11` (never `-std=c11`) as a targeted CFLAGS addition in the Makefile — commit, and re-dispatch. Record the final green run URL in the SUMMARY.
  </action>
  <verify>
    <automated>gh run list --workflow=build-fake-libpulse.yml --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
    <fails_when>non-zero exit, or a printed conclusion other than "success" (failure/cancelled/empty)</fails_when>
  </verify>
  <acceptance_criteria>
    - Latest build-fake-libpulse.yml run conclusion is "success" with all 3 matrix jobs green
    - Each matrix job's binary-verification step passed (inspect via `gh run view <id> --log` or the Actions UI)
    - Run URL recorded in the SUMMARY (Phase 78 live testing depends on these artifacts per D-09)
  </acceptance_criteria>
  <done>3-arch rebuild green and recorded; A1 either confirmed or remedied via the targeted CFLAGS fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → release binaries | CI toolchain builds the shipped .so for 3 architectures; existing workflow with its own binary-verification step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-13 | Tampering | CI-built binaries | medium | mitigate | Rely on the workflow's existing "Verify binary" step (glibc/musl NEEDED-entry verification) as the per-arch integrity gate — Task 3 explicitly asserts it passed per job |
| T-77-14 | Denial of Service | dead-code deletion | low | mitigate | Task 2 deletes only after the blocking human checkpoint confirms the replacement path live, and grep-verifies zero remaining references |
| T-77-SC | Tampering | package installs | low | accept | N/A — zero new packages this phase; CI installs only the pre-existing apt cross-toolchains already pinned in the workflow |
</threat_model>

<verification>
- Checkpoint verdict recorded for UAT items 1-5 (test 10 pass is the phase gate per VALIDATION "Phase gate")
- `prove -l t/*.t 2>&1 | tail -1 | grep -q 'Result: PASS'` — full-suite final gate
- `make -C Plugins/SpotOn/Bin/fake-libpulse test 2>&1 | grep -c '^ok:' | grep -qx 14` — C suite final gate
- CI run conclusion "success" (Task 3 verify)
</verification>

<success_criteria>
- Browse seek confirmed working live (78-UAT test 10 closed); D-08 crash-EOF confirmed manually; D-04 rapid-skip confirmed live
- Superseded code removed post-confirmation; full Perl + C suites green
- 3-arch binaries rebuilt and verified (Phase 78 prerequisite satisfied)
- 78-UAT tests 11/15 explicitly recorded as deferred to Phase 78 gap closure in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/77-bounded-audio-facade-spoton-helper-c-rust/77-06-SUMMARY.md` when done. Record: UAT verdicts per item, the CI run URL, whether A1/A2/A3 held, and the explicit deferral of 78-UAT tests 11/15.
</output>
