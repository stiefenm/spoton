---
phase: 78-browse-connect-reintegration-perl
plan: 03
type: execute
wave: 3
depends_on: [78-02]
files_modified:
  - Plugins/SpotOn/Unified/SoloistWS.pm
  - t/29_soloist_browse.t
  - t/31_soloist_ws.t
  - t/32_soloist_events.t
autonomous: true
requirements: [D-06, D-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The browseSession state machine (~470 LOC) is gone from SoloistWS.pm; no browse-state accessor, constant, method, or timer remains (D-06 second commit)"
    - "_signalBoundary, lastTrackId maintenance, skipInitiated/sessionPaused/deactivating logic, sendCommand uri validation, and the enableSpotifyConnect pref check in _emitAllowed all survive unchanged"
    - "After removal, every track_changed maintains lastTrackId (the browse early-route is gone) — the getNextTrack no-play comparison from 78-01 works for all events"
    - "t/31's browse state-machine test blocks are deleted alongside the code; the full suite is green (D-07)"
  artifacts:
    - path: "Plugins/SpotOn/Unified/SoloistWS.pm"
      provides: "Lean event translator: WS events → boundary signals + Connect emission, no browse SM"
    - path: "t/31_soloist_ws.t"
      provides: "Bounded-model test set (browse-SM blocks removed, D-02 boundary tests from 78-01 retained)"
  key_links:
    - from: "Plugins/SpotOn/Unified/SoloistWS.pm _onTrackChanged"
      to: "lastTrackId accessor"
      via: "unconditional dedup + lastTrackId update for every valid track_changed"
      pattern: "lastTrackId"
---

<objective>
Execute the D-06 removal commit: delete the browseSession state machine from
SoloistWS.pm per the line-precise RESEARCH Removal Inventory, and delete the
browse-SM test blocks (D-07). Plan 78-01 proved the bounded model works and plan
78-02 made it live-safe — the compensatory Phase-72/73/76 browse code is now
provably dead (browseSession can never become 1: its only setter was the
startBrowseTrack call removed from getNextTrack in 78-01).

Purpose: ~470 LOC of state-machine code whose gates no longer fire is a standing
hazard (silent misroutes, misleading greps, stale comments). The architecture
review's quality bar is <= ~2 remaining special cases — every surviving flag must
justify itself.

Output: Lean SoloistWS.pm + matching test set; full suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-browse-connect-reintegration-perl/78-CONTEXT.md
@.planning/phases/78-browse-connect-reintegration-perl/78-RESEARCH.md
@.planning/phases/78-browse-connect-reintegration-perl/78-PATTERNS.md
</context>

## Artifacts this phase produces (plan 03 contribution)

No new symbols — this plan removes:
- SoloistWS accessors: the seven browse* fields from the mk_accessor list + init lines
- SoloistWS constants: the browse seed-lead / ready-timeout / ready-confirm trio
- SoloistWS methods: the browse track-changed router, seed/advance helpers, session
  start/end pair, readiness-gate quartet (wait/resolve/clear/timers), and the
  browse-session line in `_emitAllowed`
- t/31 browse state-machine test blocks; obsolete t/29 remnants

Surviving contract (unchanged exports/behavior): `_signalBoundary`, `lastTrackId`,
`sendCommand` (uri regex gate), `skipInitiated`/`sessionPaused`/`deactivating`,
`_onPlaybackState` reconciliation, `_emitAllowed` pref check.

<tasks>

<task type="auto">
  <name>Task 1: Remove the browseSession state machine from SoloistWS.pm (line-precise inventory)</name>
  <reversibility rating="reversible">Pure deletion of provably-dead code (D-06); git revert restores it wholesale.</reversibility>
  <files>Plugins/SpotOn/Unified/SoloistWS.pm</files>
  <read_first>
    - .planning/phases/78-browse-connect-reintegration-perl/78-RESEARCH.md — "Removal Inventory" § SoloistWS.pm (the authoritative line-by-line table; line numbers reference the pre-78-01 working tree, so locate each item by symbol name, not raw line)
    - Plugins/SpotOn/Unified/SoloistWS.pm (entire file — current post-78-01 state)
  </read_first>
  <action>
    Delete per the RESEARCH inventory, locating by symbol (line numbers have shifted
    since 78-01/78-02):
    1. The seven browse* accessors from the `mk_accessor(rw => ...)` list and their
       two init lines in the constructor.
    2. The three BROWSE_* constants and their comment blocks.
    3. `_onClosed`: the browse-ready resolve call.
    4. `_onDeviceChanged`: the browse-handover block.
    5. `_onTrackChanged`: the browse early-route conditional (the two-line `if` that
       returned into the browse router) — afterwards every valid event flows through
       the existing dedup + `lastTrackId` update + skipInitiated + emit path.
    6. `_onBrowseTrackChanged` — whole sub.
    7. `_onPlaybackChanged`: the whole browse-session block (Stage-B confirm timers +
       stopped-no-seed advance, including the WR-06/CR-01 logic inside it). The D-02
       `_signalBoundary if $status eq 'stopped'` line added in 78-01 sits ABOVE this
       block and MUST survive.
    8. `_onPositionSync`: the seed call + Stage-B resolve block.
    9. `_maybeSeedBrowseQueue`, `_hasNextPlaylistEntry`, `_nextBrowseSpotifyUri`,
       `_clientCurrentSpotifyUri` — whole subs (grep confirms only browse paths call them).
    10. `startBrowseTrack`, the `%_BROWSE_END_SKIP_PAUSE` hash, `endBrowseSession` — whole.
    11. `waitForBrowseReady`, `_resolveBrowseReady`, `_clearBrowseReadyTimers`,
        `_browseReadyTimeoutTimer`, `_browseReadyConfirmTimer` — whole.
    12. `_emitAllowed`: only the browse-session early-return line; the
        `enableSpotifyConnect` pref check STAYS.
    KEEP untouched: `_signalBoundary` (both call sites), `skipInitiated`/`sessionPaused`/
    `deactivating` logic, reconnect/handshake, `sendCommand` uri validation,
    `_onPlaybackState` reconciliation. Also purge comment paragraphs that only
    describe the removed machinery (e.g. the Model-B header notes) — but do NOT add
    new comments naming the removed identifiers.
  </action>
  <verify>
    <automated>perl -c Plugins/SpotOn/Unified/SoloistWS.pm 2>&1 | grep -q 'syntax OK' && ! grep -qiE 'browseSession|browseCurrentUri|browseSeededUri|browseAdvance|browseReady|browseTrackConfirmed|_onBrowseTrackChanged|_maybeSeedBrowseQueue|BROWSE_SEED|BROWSE_READY|_BROWSE_END' Plugins/SpotOn/Unified/SoloistWS.pm && grep -q '_signalBoundary if ' Plugins/SpotOn/Unified/SoloistWS.pm && prove -l t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS'</automated>
    <fails_when>non-zero exit: SoloistWS.pm fails perl -c, any browse-SM identifier still appears (code or comment), the D-02 conditional boundary line is missing, or prove prints "Result: FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: browseSession -->
    <!-- planner-discipline-allow: browseCurrentUri -->
    <!-- planner-discipline-allow: browseSeededUri -->
    <!-- planner-discipline-allow: browseAdvance -->
    <!-- planner-discipline-allow: browseReady -->
    <!-- planner-discipline-allow: browseTrackConfirmed -->
    <!-- planner-discipline-allow: _onBrowseTrackChanged -->
    <!-- planner-discipline-allow: _maybeSeedBrowseQueue -->
    <!-- planner-discipline-allow: BROWSE_SEED -->
    <!-- planner-discipline-allow: BROWSE_READY -->
    <!-- planner-discipline-allow: _BROWSE_END -->
    - Case-insensitive grep for every inventoried browse-SM identifier over SoloistWS.pm reports 0 matches
    - `grep -c '_signalBoundary' Plugins/SpotOn/Unified/SoloistWS.pm` >= 3 (sub + track_changed call + 'stopped' call all survive)
    - `grep -c 'enableSpotifyConnect' Plugins/SpotOn/Unified/SoloistWS.pm` >= 1 (pref check in _emitAllowed survives)
    - `git diff --stat` for this task shows SoloistWS.pm net-removing roughly 400+ lines and no other file changed
  </acceptance_criteria>
  <done>SoloistWS.pm compiles as a lean event translator; the only remaining Soloist special-case state is skipInitiated/sessionPaused/deactivating (within the architecture review's <= ~2-special-cases bar for new flags: zero new flags added).</done>
</task>

<task type="auto">
  <name>Task 2: Delete browse-SM test blocks and add cross-file leftover gates</name>
  <reversibility rating="reversible">Test-only (D-07).</reversibility>
  <files>t/31_soloist_ws.t, t/29_soloist_browse.t, t/32_soloist_events.t</files>
  <read_first>
    - t/31_soloist_ws.t (entire file — identify the emit-gate browse test, the D-17 readiness recorder, and the browse-SM sections vs. the D-02 boundary tests added in 78-01 which must stay)
    - t/29_soloist_browse.t (post-78-01 state — confirm no lingering references to removed SoloistWS symbols in stubs/comments)
    - t/32_soloist_events.t (structure only — event→spottyconnect mapping pins)
  </read_first>
  <action>
    Per D-07: in t/31 delete every test block that exercises the removed browse state
    machine (emit-gate browse case, readiness-gate recorder + Stage-B tests, seed/advance
    sequences); retain and re-verify the D-02 boundary matrix tests and all
    non-browse WS tests (reconnect, handshake, sendCommand validation, playback-state
    reconciliation, emission translation). Fix any stub setup that assigned removed
    accessors. In t/29, remove any remaining stub fields or comments referencing
    removed SoloistWS symbols. In t/32, add one case in the t/31-harness style
    asserting a track_changed event on a non-Connect Soloist player still translates
    to a spottyconnect emission (emission is no longer gated by any browse state —
    the echo discrimination now lives in Connect.pm, plan 78-02). Keep test counts/
    plans (`Test::More` plan numbers) consistent with the deletions.
  </action>
  <verify>
    <automated>prove -l t/29_soloist_browse.t t/31_soloist_ws.t t/32_soloist_events.t t/05_perl_syntax.t 2>&1 | tail -1 | grep -q 'Result: PASS' && ! grep -rqE 'startBrowseTrack|waitForBrowseReady|browseSession' t/29_soloist_browse.t t/31_soloist_ws.t t/32_soloist_events.t</automated>
    <fails_when>non-zero exit: prove prints "Result: FAIL", or any of the three retired browse-SM identifiers still appears in the three test files</fails_when>
  </verify>
  <acceptance_criteria>
    - `prove -l t/ 2>&1 | tail -1` prints "Result: PASS" (full suite green — the D-06 removal wave ends with everything passing)
    - Zero references to removed browse-SM identifiers across t/29, t/31, t/32
    - t/31 still contains the D-02 boundary matrix assertions from 78-01
    - t/32 contains the new ungated-emission case
  </acceptance_criteria>
  <done>Test set matches the bounded model exactly; no test exercises or names deleted machinery; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon WS events → SoloistWS.pm | All daemon events now flow through one translation path (no browse routing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-03 | DoS | SoloistWS WS frame handling | low | mitigate | eval-guarded from_json, WR-08 non-HASH item guard, and sendCommand uri regex all explicitly on the KEEP list — verified surviving by Task 1 acceptance criteria. |
| T-78-06 | Tampering | Removal collateral (accidentally deleting a shared guard) | medium | mitigate | Symbol-precise inventory + positive keep-gates (_signalBoundary count, enableSpotifyConnect presence) + full suite green as the merge gate. |
</threat_model>

<verification>
- `prove -l t/ 2>&1 | tail -1` prints "Result: PASS"
- `grep -rn 'browseSession' Plugins/ t/` returns nothing
- librespot regression: no librespot-path file was touched by this plan (git diff scope = SoloistWS.pm + 3 test files)
</verification>

<success_criteria>
- browseSession machinery fully removed from code AND tests in the same wave
- Surviving contract (boundary signaling, lastTrackId, validation, pref gate) intact
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/78-browse-connect-reintegration-perl/78-03-SUMMARY.md` when done
</output>
